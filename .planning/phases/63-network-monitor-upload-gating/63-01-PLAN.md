---
phase: 63-network-monitor-upload-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseNetworkMonitor.swift
  - GooseSwift/GooseAppModel.swift
  - GooseSwift.xcodeproj/project.pbxproj
autonomous: true
requirements: [NET-MON-01]

must_haves:
  truths:
    - "GooseNetworkMonitor wraps NWPathMonitor and reports reachability via a callback"
    - "GooseAppModel exposes a private(set) isNetworkReachable: Bool reflecting current connectivity"
    - "The monitor starts on app launch and updates isNetworkReachable on every path change"
  artifacts:
    - path: "GooseSwift/GooseNetworkMonitor.swift"
      provides: "NWPathMonitor wrapper with isReachable state + onReachabilityChange callback + start/stop"
      contains: "NWPathMonitor"
      min_lines: 30
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "isNetworkReachable observable property wired to the monitor"
      contains: "isNetworkReachable"
  key_links:
    - from: "GooseSwift/GooseAppModel.swift"
      to: "GooseNetworkMonitor"
      via: "onReachabilityChange callback assigned in init() updating isNetworkReachable"
      pattern: "onReachabilityChange"
    - from: "GooseSwift/GooseNetworkMonitor.swift"
      to: "Network.framework"
      via: "NWPathMonitor.pathUpdateHandler"
      pattern: "pathUpdateHandler"
---

<objective>
Create GooseNetworkMonitor — a dedicated wrapper around NWPathMonitor that publishes reachability — and wire it into GooseAppModel as an observable `isNetworkReachable: Bool` property started on launch.

Purpose: Establishes the connectivity signal (NET-MON-01, success criterion 1) that Wave 2 will consume to gate uploads. Matches WHOOP's WHPNetworkMonitor pattern.
Output: GooseNetworkMonitor.swift (new), isNetworkReachable property on GooseAppModel, monitor started in init, new file registered in project.pbxproj.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-network-monitor-upload-gating/63-CONTEXT.md

# Analog: dedicated subsystem monitor class with a callback (final class, callback not Combine)
@GooseSwift/GooseBLEBondingManager.swift
# Integration target: where the observable property goes + init() wiring pattern
@GooseSwift/GooseAppModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GooseNetworkMonitor wrapping NWPathMonitor</name>
  <files>GooseSwift/GooseNetworkMonitor.swift</files>
  <action>
    Create a new file with `import Foundation` and `import Network`. Define `final class GooseNetworkMonitor`.
    Hold a private `NWPathMonitor` instance and a dedicated `DispatchQueue` labeled `"com.goose.swift.network-monitor"` (reverse-DNS label convention).
    Expose `private(set) var isReachable: Bool` (initialise to true so launch is not falsely blocked before the first path update — NWPathMonitor delivers the first update asynchronously).
    Expose `var onReachabilityChange: ((Bool) -> Void)?` — callback pattern consistent with GooseBLEBondingManager.onBondingStateChange and GooseBLEClient.onConnectionStateChange (NOT Combine, per 63-CONTEXT established patterns).
    In `start()`: set `monitor.pathUpdateHandler` to compute `path.status == .satisfied`, store it into `isReachable` only when it changes, and on change dispatch the callback to the main queue (`DispatchQueue.main.async`) — the same main-thread delivery contract GooseBLEBondingManager uses for its callback. Call `monitor.start(queue:)` with the dedicated queue.
    Provide `stop()` that calls `monitor.cancel()`. Use runtime state only — no UserDefaults (per 63-CONTEXT: "no UserDefaults needed here").
    Guard `start()` against double-start with a private `isStarted` flag so repeated calls are no-ops.
  </action>
  <verify>
    <automated>grep -q "NWPathMonitor" GooseSwift/GooseNetworkMonitor.swift && grep -q "pathUpdateHandler" GooseSwift/GooseNetworkMonitor.swift && grep -q "onReachabilityChange" GooseSwift/GooseNetworkMonitor.swift && echo OK</automated>
  </verify>
  <done>GooseNetworkMonitor.swift exists with NWPathMonitor, isReachable, onReachabilityChange callback delivered on main, and start()/stop().</done>
</task>

<task type="auto">
  <name>Task 2: Register file in project.pbxproj and wire into GooseAppModel</name>
  <files>GooseSwift.xcodeproj/project.pbxproj, GooseSwift/GooseAppModel.swift</files>
  <action>
    Register the new source file in `GooseSwift.xcodeproj/project.pbxproj` following the exact existing pattern used for GooseUploadWatermark.swift (build file id `E100000000000000000000A`, file ref `E200000000000000000000A`). Use the next free ids `E100000000000000000000B` (PBXBuildFile) and `E200000000000000000000B` (PBXFileReference). Add four entries mirroring GooseUploadWatermark.swift:
    (1) a PBXBuildFile line in the build-file section (near line 174),
    (2) a PBXFileReference line (near line 382),
    (3) a children entry in the group that lists GooseUploadWatermark.swift (near line 608),
    (4) a Sources build-phase entry (near line 966).
    Keep the `lastKnownFileType = sourcecode.swift; path = GooseNetworkMonitor.swift; sourceTree = "<group>";` shape for the file reference.

    In `GooseSwift/GooseAppModel.swift`: add `let networkMonitor = GooseNetworkMonitor()` alongside the other `let` subsystem properties (near `uploadService` declaration, ~line 85). Add an observable stored property `private(set) var isNetworkReachable: Bool = true` near `serverReachable` (~line 30) so SwiftUI views invalidate on change (@Observable tracks it).
    In `init()` (after the existing `configureUploadService()` call, ~line 338): assign `networkMonitor.onReachabilityChange = { [weak self] reachable in self?.isNetworkReachable = reachable }` (callback already arrives on main thread per GooseNetworkMonitor contract; if the project requires it, wrap in `Task { @MainActor in }` to satisfy actor isolation). Then call `networkMonitor.start()`. Add a `ble.record(source: "app.network", title: "monitor.start")` log line for parity with the existing init logging style.
  </action>
  <verify>
    <automated>grep -q "GooseNetworkMonitor.swift" GooseSwift.xcodeproj/project.pbxproj && grep -q "isNetworkReachable" GooseSwift/GooseAppModel.swift && grep -q "networkMonitor.start()" GooseSwift/GooseAppModel.swift && echo OK</automated>
  </verify>
  <done>project.pbxproj references GooseNetworkMonitor.swift in all four required sections; GooseAppModel has isNetworkReachable wired to the monitor and starts it in init.</done>
</task>

<task type="auto">
  <name>Task 3: Build for simulator</name>
  <files>GooseSwift.xcodeproj/project.pbxproj</files>
  <action>
    Build the GooseSwift scheme for an iOS simulator to confirm the new file compiles and is correctly linked into the target. Use XcodeBuildMCP build_sim (or `xcodebuild build` for the GooseSwift scheme on a generic iOS Simulator destination). Fix any compile errors surfaced (most likely actor-isolation on the callback assignment or a missing import).
  </action>
  <verify>
    <automated>xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Simulator build succeeds with GooseNetworkMonitor compiled into the GooseSwift target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network.framework → app | OS-reported path status is trusted system state; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Information disclosure | NWPathMonitor path details | accept | Only a boolean reachability flag is read; no interface names, addresses, or DNS are persisted or logged |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — Network.framework is a first-party OS framework (no new dependency per 63-CONTEXT) |
</threat_model>

<verification>
- GooseNetworkMonitor.swift compiles and links into the GooseSwift target (simulator build succeeds).
- isNetworkReachable is observable and updated only via the monitor callback.
- No new external dependency added (Network.framework is OS-provided).
</verification>

<success_criteria>
- A GooseNetworkMonitor wraps NWPathMonitor and publishes isReachable via callback (NET-MON-01 criterion 1).
- GooseAppModel exposes isNetworkReachable, wired to the monitor and started on launch.
- Simulator build succeeds.
</success_criteria>

<output>
Create `.planning/phases/63-network-monitor-upload-gating/63-01-SUMMARY.md` when done.
</output>
