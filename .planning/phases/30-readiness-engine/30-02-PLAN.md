---
phase: 30-readiness-engine
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - Rust/core/src/bridge.rs
autonomous: true
requirements: [RDY-01, RDY-02, RDY-03]

must_haves:
  truths:
    - "\"metrics.goose_readiness_v1\" appears in BRIDGE_METHODS sorted array"
    - "dispatch arm for \"metrics.goose_readiness_v1\" in bridge.rs match block calls goose_readiness_v1 and returns JSON"
    - "Round-trip integration test: send ReadinessInput JSON through bridge_handle_json, receive ReadinessOutput JSON with correct level field"
    - "cargo test -p goose_core -- readiness passes (unit + bridge integration)"
  artifacts:
    - path: "Rust/core/src/bridge.rs"
      provides: "metrics.goose_readiness_v1 dispatch arm and BRIDGE_METHODS entry"
      contains: "metrics.goose_readiness_v1"
  key_links:
    - from: "bridge.rs dispatch match arm"
      to: "goose_readiness_v1() in metrics.rs"
      via: "request_args::<ReadinessInput> + metric_result_to_value or direct serde_json::to_value"
      pattern: "goose_readiness_v1"
---

<objective>
Wire goose_readiness_v1 into the bridge dispatch layer (RDY-01, RDY-02, RDY-03).

Purpose: Expose the readiness algorithm to the Swift side via the existing JSON-over-FFI bridge so HealthDataStore can call metrics.goose_readiness_v1 without reading any SQLite state (the function is stateless — caller supplies the trailing strain window).

Output: BRIDGE_METHODS entry + dispatch arm in bridge.rs + integration test confirming JSON round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/francisco/Documents/goose/.planning/ROADMAP.md
@/Users/francisco/Documents/goose/.planning/phases/30-readiness-engine/30-CONTEXT.md
@/Users/francisco/Documents/goose/Rust/core/src/bridge.rs
@/Users/francisco/Documents/goose/.planning/phases/30-readiness-engine/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire bridge dispatch arm and BRIDGE_METHODS entry for metrics.goose_readiness_v1</name>
  <files>Rust/core/src/bridge.rs</files>
  <behavior>
    - Integration test 1: JSON request {"schema":"goose.bridge.request.v1","request_id":"r1","method":"metrics.goose_readiness_v1","args":{"daily_strain":[]}} → response ok=true, result.insufficient_data=true, result.level="unknown"
    - Integration test 2: Request with 28 equal-strain pairs (all strain=10.0) → level="primed", acwr≈1.0, acwr_zone="optimal", monotony=null (std=0)
    - Integration test 3: Request with 27 pairs → insufficient_data=true, level="unknown"
    - Integration test 4: Request with 28 pairs where last 7 have strain=21.0 and first 21 have strain=5.0 → acwr > 1.5, level="rundown"
  </behavior>
  <action>
    In bridge.rs, make three edits:

    1. Import: Add ReadinessInput and goose_readiness_v1 to the existing metrics import line (around line 103-104 where goose_recovery_v1 etc. are imported).

    2. BRIDGE_METHODS array: Insert "metrics.goose_readiness_v1" in alphabetical order — it falls between "metrics.goose_hrv_v0" and "metrics.goose_recovery_v0". The array is sorted; inserting out of order will break the binary-search registration check.

    3. Dispatch match arm: Add after the "metrics.goose_stress_v0" arm (keep arms in the same order as BRIDGE_METHODS for readability):

       "metrics.goose_readiness_v1" => request_args::<ReadinessInput>(&request)
           .and_then(|input| {
               serde_json::to_value(goose_readiness_v1(&input))
                   .map_err(|e| GooseError::message(format!("cannot serialize readiness_v1 output: {e}")))
           })
           .map(|value| bridge_ok(&request.request_id, value))
           .unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error)),

    Note: goose_readiness_v1 returns ReadinessOutput directly (not AlgorithmRunResult<T>), so use serde_json::to_value directly rather than metric_result_to_value — same pattern as goose_recovery_v1_bridge. No database_path required (stateless function).

    Write the four integration tests (behavior block above) as #[test] functions inside the existing bridge.rs #[cfg(test)] mod tests block (search for "fn goose_recovery_v1_bridge" and add nearby). Use the bridge_handle_json test helper that already exists in bridge.rs (grep: "fn call_bridge" or similar). Match the existing test style exactly.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose_core -- readiness 2>&1 | tail -30</automated>
  </verify>
  <done>cargo test passes; grep confirms "metrics.goose_readiness_v1" appears in both BRIDGE_METHODS and the dispatch match; four integration tests all pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift caller → bridge JSON | daily_strain array may contain unexpected types or be missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Tampering | bridge JSON deserialization of daily_strain | mitigate | serde type enforcement; invalid JSON returns bridge_error with "method_error"; no panic path |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies; existing serde_json used |
</threat_model>

<verification>
cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose_core -- readiness 2>&1 | tail -30
</verification>

<success_criteria>
- "metrics.goose_readiness_v1" present in BRIDGE_METHODS sorted array in bridge.rs
- Dispatch arm in match block calls goose_readiness_v1 and serialises result to JSON
- Four integration tests pass: empty input (unknown), 28 equal strains (primed), 27 strains (unknown), high-ACWR 28 strains (rundown)
- cargo test -p goose_core -- readiness exits 0 with all tests passing
</success_criteria>

<output>
Create `.planning/phases/30-readiness-engine/30-02-SUMMARY.md` when done
</output>
