---
phase: 09-ble-stability-data-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/Cargo.toml
  - Rust/core/src/bridge.rs
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements: [FIX-04, FIX-05]
must_haves:
  truths:
    - "A Rust panic inside FFI dispatch returns a JSON error object instead of aborting the process"
    - "The bridge exposes storage.compact_raw_evidence which returns a compaction report"
    - "Compaction is a no-op (compacted_rows = 0) when the database is already below the byte limit"
    - "A test-only test.panic bridge method (compiled only under #[cfg(test)]) deterministically triggers the panic path"
  artifacts:
    - path: "Rust/core/Cargo.toml"
      provides: "panic = \"unwind\" in [profile.release]"
      contains: "panic = \"unwind\""
    - path: "Rust/core/src/bridge.rs"
      provides: "catch_unwind wrap on goose_bridge_handle_json + storage.compact_raw_evidence method + #[cfg(test)] test.panic arm"
      contains: "catch_unwind"
    - path: "Rust/core/tests/bridge_tests.rs"
      provides: "panic-catch (via test.panic) and compaction tests"
      contains: "panic"
  key_links:
    - from: "goose_bridge_handle_json"
      to: "handle_bridge_request_json"
      via: "std::panic::catch_unwind(AssertUnwindSafe(...))"
      pattern: "catch_unwind"
    - from: "storage.compact_raw_evidence match arm"
      to: "store.compact_raw_evidence_payloads_to_limit"
      via: "storage_compact_raw_evidence_bridge"
      pattern: "compact_raw_evidence_payloads_to_limit"
    - from: "test.panic match arm (#[cfg(test)])"
      to: "explicit panic!() inside dispatch"
      via: "deterministic test-only panic trigger"
      pattern: "test.panic"
---

<objective>
Make the Rust FFI boundary panic-safe (FIX-04) and expose the already-implemented storage compaction algorithm as a bridge method (FIX-05, Rust side).

Purpose: A malformed bridge request that triggers a Rust panic currently aborts the entire iOS app process. Wrapping the FFI entry point in `catch_unwind` (after switching the release profile to `panic = "unwind"`) turns any panic into a structured JSON error. Separately, the 24 MB storage retention cap requires a bridge method that Swift can call; the compaction algorithm already exists in `store.rs` and only needs wiring.

Output: `panic = "unwind"` in the release profile, a `catch_unwind` wrap on `goose_bridge_handle_json`, a new `storage.compact_raw_evidence` bridge method, a test-only `test.panic` bridge method for deterministic panic-path coverage, and Rust tests proving both behaviors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ble-stability-data-integrity/09-CONTEXT.md
@.planning/phases/09-ble-stability-data-integrity/09-RESEARCH.md
@.planning/phases/09-ble-stability-data-integrity/09-PATTERNS.md
@.planning/phases/09-ble-stability-data-integrity/09-VALIDATION.md
</context>

## Artifacts this phase produces

This plan creates the following new symbols (exclude from drift verification):
- `StorageCompactRawEvidenceArgs` (struct, `Rust/core/src/bridge.rs`) — `database_path: String`, `limit_bytes: i64`
- `storage_compact_raw_evidence_bridge` (fn, `Rust/core/src/bridge.rs`)
- `"storage.compact_raw_evidence"` (bridge method name / match arm)
- `"test.panic"` (test-only bridge method name / match arm, gated behind `#[cfg(test)]`)
- New `[profile.release]` key `panic = "unwind"` in `Rust/core/Cargo.toml`
- New tests in `Rust/core/tests/bridge_tests.rs` covering panic-catch and compaction

Pre-existing symbols this plan consumes (already in codebase, do NOT flag):
- `goose_bridge_handle_json`, `goose_bridge_free_string`, `handle_bridge_request_json`, `string_to_c_string`, `response_to_c_string`, `bridge_error`, `bridge_ok`, `request_args`, `open_bridge_store` (`Rust/core/src/bridge.rs`)
- `GooseStore::compact_raw_evidence_payloads_to_limit`, `RawEvidencePayloadRetentionReport` (`Rust/core/src/store.rs` lines 4641–4695)

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Add failing tests for panic-catch (via test.panic) and storage compaction</name>
  <files>Rust/core/tests/bridge_tests.rs</files>
  <read_first>
    - Rust/core/tests/bridge_tests.rs (existing test patterns: how a bridge request is built, sent through goose_bridge_handle_json or handle_bridge_request_json, and how the JSON response is parsed)
    - Rust/core/src/store.rs lines 4629–4695 (compact_raw_evidence_payloads_to_limit and raw_evidence_payload_bytes — report field names: before_bytes, after_bytes, compacted_rows, freed_bytes)
    - Rust/core/src/bridge.rs lines 2104–2117 (head of the dispatch match block in handle_bridge_request_json — where the test-only "test.panic" arm is added in Task 3)
    - Rust/core/src/bridge.rs lines 2685–2706 (goose_bridge_handle_json FFI entry) and the goose_bridge_free_string signature (the free helper to call on the returned pointer)
    - .planning/phases/09-ble-stability-data-integrity/09-VALIDATION.md (Wave 0 Requirements section — exact test expectations)
  </read_first>
  <action>
    Add two tests to bridge_tests.rs.
    Test 1 (panic catch, FIX-04 — REVIEW HIGH/codex: deterministic trigger): the panic MUST be triggered by the dedicated test-only bridge method `test.panic` that Task 3 adds behind `#[cfg(test)]`, NOT by a malformed-args/unwrap path (which is brittle and may fail to panic or panic in the wrong frame). Build a bridge request `{"method":"test.panic","request_id":"panic-test","args":{}}` and route it through the public FFI entry `goose_bridge_handle_json` so catch_unwind is exercised end to end: construct a CString, pass its `*const c_char` to the function, copy the returned C string into a Rust String, then free the returned pointer via the existing free helper `goose_bridge_free_string` to avoid a leak in the test (REVIEW suggestion). Parse the returned String and assert `{"ok": false, "error": {"code": "panic", ...}}`. The test continuing to run (process did not abort) is itself the FIX-04 proof under panic = "unwind".
    In the SAME test add a normal-request regression assertion (REVIEW suggestion): send a benign request `{"method":"core.version","request_id":"ok-test","args":{}}` through `goose_bridge_handle_json`, free its returned pointer, and assert it parses to `ok: true` — proving the catch_unwind wrap does not break non-panicking calls.
    Name the test so it matches the `cargo test --test bridge_tests -- panic` filter (include "panic" in the test fn name).
    Test 2 (compaction, FIX-05): seed a temp database (use the existing tempfile pattern from sibling tests) with several raw_evidence rows whose aggregate payload_hex exceeds a small chosen limit_bytes. Send a `storage.compact_raw_evidence` request with `database_path` and `limit_bytes`. Assert the response is `ok: true`, the result contains `before_bytes`, `after_bytes`, `compacted_rows`, `freed_bytes`, and that `compacted_rows > 0` and `after_bytes <= limit_bytes`. Add a second assertion: calling the same method again (now under the limit) returns `compacted_rows == 0` (no-op). Name the test to match `cargo test --test bridge_tests -- compact`.
    These tests will FAIL to compile/pass until Tasks 2 and 3 land (test.panic and storage.compact_raw_evidence do not exist yet) — that is the intended RED state.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --test bridge_tests -- panic compact 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - bridge_tests.rs contains a test whose name includes "panic" that calls the `test.panic` method through goose_bridge_handle_json, asserts error.code == "panic", AND asserts a benign core.version call still returns ok: true
    - The panic test frees both returned C string pointers via goose_bridge_free_string
    - bridge_tests.rs contains a test whose name includes "compact" asserting before_bytes/after_bytes/compacted_rows/freed_bytes fields and a no-op second pass
    - Before Tasks 2/3: the tests fail or fail to compile (RED). This is expected and acceptable for this Wave 0 task.
  </acceptance_criteria>
  <done>Two new tests exist that encode the FIX-04 (via deterministic test.panic) and FIX-05 expected behavior; the panic test also proves normal requests still succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (FIX-04): Wrap FFI entry in catch_unwind and switch release profile to panic = unwind</name>
  <files>Rust/core/Cargo.toml, Rust/core/src/bridge.rs</files>
  <behavior>
    - A panic raised inside handle_bridge_request_json is caught at the FFI boundary and converted to a JSON response with ok=false and error.code="panic".
    - The panic message is extracted from the payload (downcast &str then String fallback, else "unknown panic payload").
    - Normal (non-panicking) requests are unaffected and return their existing response.
    - All Rust work that may panic lives INSIDE the caught closure; the only thing returned from the Err arm is a C string built from a bridge_error (REVIEW MEDIUM: unwinding must never cross the FFI boundary).
  </behavior>
  <read_first>
    - Rust/core/src/bridge.rs lines 2685–2706 (current goose_bridge_handle_json body: null check, CStr::from_ptr UTF-8 conversion, the existing string_to_c_string(handle_bridge_request_json(request)) call, response_to_c_string and bridge_error signatures)
    - Rust/core/Cargo.toml lines 155–165 ([profile.release] stanza containing panic = "abort" at line 161; confirm [profile.dev] has no explicit panic key)
    - .planning/phases/09-ble-stability-data-integrity/09-RESEARCH.md Pattern 2 and Pitfalls 1 and 2 (why panic=unwind is a prerequisite; why AssertUnwindSafe is sound here)
  </read_first>
  <action>
    In Rust/core/Cargo.toml, change ONLY the `[profile.release]` key `panic = "abort"` to `panic = "unwind"` at line 161. Do NOT add or modify any `panic` key under `[profile.dev]` or any other profile.
    In Rust/core/src/bridge.rs goose_bridge_handle_json: keep the existing null-pointer guard and the existing CStr::from_ptr UTF-8 conversion unchanged. Replace the single call `string_to_c_string(handle_bridge_request_json(request))` with a `std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| string_to_c_string(handle_bridge_request_json(request))))`. The closure must contain ALL panic-prone work (the dispatch AND the C-string allocation) so unwinding is fully contained and cannot cross the FFI frame (REVIEW MEDIUM). On `Ok(ptr)` return the pointer. On `Err(payload)` extract the message via `payload.downcast_ref::<&str>()` mapped to String, else `payload.downcast_ref::<String>().cloned()`, else the literal "unknown panic payload"; then return `response_to_c_string(&bridge_error("unknown", "panic", message))` matching the request_id/source argument order used by the existing null-request and invalid-utf8 error returns in this same function. Note: the request_id is not recovered here because the panic may occur before/within parsing; "unknown" is the documented, accepted request_id for the panic response (REVIEW MEDIUM acknowledged).
    Use the import path `std::panic::{AssertUnwindSafe, catch_unwind}` (either fully-qualified inline or a `use` at the top of the file, matching the file's existing import style).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --test bridge_tests -- panic 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'panic = "unwind"' Rust/core/Cargo.toml` returns the line inside [profile.release]
    - `grep -v '^#' Rust/core/Cargo.toml | grep -c 'panic = "abort"'` returns 0 (filtering comments so a future commented reference cannot mask a real abort setting)
    - `grep -n 'catch_unwind' Rust/core/src/bridge.rs` shows the wrap inside goose_bridge_handle_json
    - The panic test from Task 1 passes (JSON error.code == "panic", process does not abort) AND the core.version regression assertion in that test passes
    - [profile.dev] still has no explicit panic key (grep confirms only the release stanza changed)
  </acceptance_criteria>
  <done>A panic in dispatch returns {"ok":false,"error":{"code":"panic",...}} instead of aborting; the panic test is green and normal requests still succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (FIX-05 + test.panic): Expose storage.compact_raw_evidence and a #[cfg(test)] test.panic bridge method</name>
  <files>Rust/core/src/bridge.rs</files>
  <behavior>
    - A request {"method":"storage.compact_raw_evidence","args":{"database_path":...,"limit_bytes":25165824}} returns ok=true with a result containing before_bytes, after_bytes, compacted_rows, freed_bytes.
    - When the database is already at or below limit_bytes, compacted_rows is 0 and the call is a fast no-op.
    - A request {"method":"test.panic",...} unconditionally panics inside the dispatch closure — but ONLY in test builds (the arm is gated behind #[cfg(test)] and is never compiled into release).
  </behavior>
  <read_first>
    - Rust/core/src/store.rs lines 4629–4695 (compact_raw_evidence_payloads_to_limit signature and the RawEvidencePayloadRetentionReport struct field names/serde — these are the result JSON shape)
    - Rust/core/src/bridge.rs lines 2104–2117 (head of the dispatch match block in handle_bridge_request_json — where the new arms are added; observe the existing arm shape that builds bridge_ok/bridge_error with request.request_id)
    - Rust/core/src/bridge.rs around line 2407 (the existing capture.import_frame_batch match arm — the canonical request_args + and_then + map(bridge_ok) + unwrap_or_else(bridge_error) arm pattern to replicate)
    - Rust/core/src/bridge.rs around lines 5775–5794 (capture_import_frame_batch_bridge — pattern for a bridge fn: open_bridge_store, call store method, serde_json::to_value with a GooseError::message on failure)
    - Rust/core/src/bridge.rs near open_bridge_store and request_args definitions (helper signatures)
  </read_first>
  <action>
    Add a deserialize args struct `StorageCompactRawEvidenceArgs { database_path: String, limit_bytes: i64 }` near the other bridge args structs (e.g., alongside CaptureImportFrameBatchArgs around line 1279).
    Add a bridge fn `storage_compact_raw_evidence_bridge(args: StorageCompactRawEvidenceArgs) -> GooseResult<serde_json::Value>` that calls `open_bridge_store(&args.database_path)?`, then `store.compact_raw_evidence_payloads_to_limit(args.limit_bytes)?`, then `serde_json::to_value(report)` mapping the error to `GooseError::message(format!("cannot serialize compaction report: {e}"))`. Model it exactly on capture_import_frame_batch_bridge.
    Add a match arm `"storage.compact_raw_evidence" => request_args::<StorageCompactRawEvidenceArgs>(&request).and_then(storage_compact_raw_evidence_bridge).map(|value| bridge_ok(&request.request_id, value)).unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error)),` in the same match block that holds the capture.import_frame_batch arm. Match the exact bridge_ok/bridge_error argument shape used by sibling arms in that block.
    REVIEW HIGH (deterministic panic trigger): add a test-only dispatch arm in the SAME match block, gated so it is never compiled into release. Put `#[cfg(test)]` directly on the match arm: `#[cfg(test)] "test.panic" => panic!("test.panic: intentional panic for FFI catch_unwind coverage"),`. This arm unconditionally panics inside the dispatch closure, giving Task 1's test a stable, non-brittle way to exercise the catch_unwind path without relying on incidental unwraps. Do NOT add any production code path that reaches this arm; it exists solely for tests.
    Do not modify compact_raw_evidence_payloads_to_limit in store.rs — it is already correct.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --test bridge_tests -- panic compact 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'storage.compact_raw_evidence' Rust/core/src/bridge.rs` shows the new match arm
    - `grep -n 'storage_compact_raw_evidence_bridge\|StorageCompactRawEvidenceArgs' Rust/core/src/bridge.rs` shows the fn and struct
    - `grep -n 'test.panic' Rust/core/src/bridge.rs` shows the test-only arm, and the line immediately above it (or an attribute on the same arm) is `#[cfg(test)]`
    - The panic test from Task 1 passes (error.code == "panic" via test.panic) and the compaction test passes (compacted_rows > 0 over limit, == 0 on no-op second pass)
    - `cargo test 2>&1 | tail -5` shows the full suite still green (no regressions)
    - `cargo build --release 2>&1 | tail -5` succeeds and the test.panic arm is NOT present in the release build (it is #[cfg(test)] only)
  </acceptance_criteria>
  <done>storage.compact_raw_evidence is callable via the bridge and returns the compaction report; a #[cfg(test)] test.panic arm gives the panic test a deterministic trigger; both tests are green and release excludes test.panic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust FFI (goose_bridge_handle_json) | Untrusted JSON request string crosses from the app into the Rust core |
| App → SQLite (raw_evidence) | Captured BLE payloads accumulate on-device storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | goose_bridge_handle_json | mitigate | FIX-04: catch_unwind converts a panic into a JSON error instead of an app-process abort; release profile set to panic = "unwind" so unwinding is possible; all panic-prone work confined inside the caught closure so unwinding never crosses the FFI frame |
| T-09-02 | Denial of Service | raw_evidence table growth | mitigate | FIX-05: storage.compact_raw_evidence bounds payload_hex bytes to the 24 MB limit, preventing storage-exhaustion DoS during large history syncs |
| T-09-03 | Tampering (data integrity) | compaction limit_bytes arg | accept | limit_bytes is validated non-negative inside compact_raw_evidence_payloads_to_limit (existing validate_non_negative); a hostile negative value is rejected by the store, not this layer |
| T-09-04 | Elevation (test hook reaching prod) | test.panic dispatch arm | mitigate | The test.panic arm is gated behind #[cfg(test)] and is never compiled into the release library; release build acceptance criterion confirms its absence |
</threat_model>

<verification>
- `cd Rust/core && cargo test --test bridge_tests 2>&1 | tail -20` green (panic + compact tests pass)
- `cd Rust/core && cargo test 2>&1 | tail -30` full suite green (no regressions)
- `grep -v '^#' Rust/core/Cargo.toml | grep -c 'panic = "abort"'` returns 0
- `cd Rust/core && cargo build --release 2>&1 | tail -5` succeeds (test.panic excluded from release)
</verification>

<success_criteria>
- A Rust panic at the FFI boundary returns a structured JSON error (ROADMAP success criterion 5), proven deterministically via the #[cfg(test)] test.panic method
- Normal bridge requests still succeed after the catch_unwind wrap (regression assertion)
- Raw evidence retention is bounded via the storage.compact_raw_evidence bridge method (ROADMAP success criterion 6, Rust side)
- Full Rust test suite passes; release build excludes the test-only panic arm
</success_criteria>

<output>
Create `.planning/phases/09-ble-stability-data-integrity/09-01-SUMMARY.md` when done
</output>
