---
phase: "119"
plan: "119-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseStealthMode.swift
  - GooseSwiftTests/GooseStealthModeTests.swift
  - GooseSwift/CoachLocalToolContext.swift
  - GooseSwift/CoachChatModel.swift
  - GooseSwift.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - STEALTH-01
  - STEALTH-02
must_haves:
  truths:
    - "GooseStealthMode.isHidden(metric:) returns false when the UserDefaults key is absent (safe default)"
    - "GooseStealthMode.isHidden(metric:) returns true when UserDefaults.standard.bool(forKey: StealthStorage.<key>) is true"
    - "StealthMask.none has an empty hidden set; isHidden always returns false"
    - "CoachLocalToolContext.build() accepts mask: StealthMask = .none without breaking existing callers"
    - "All 6 metric positions in the Coach JSON emit 'hidden_by_user' (not the real value) when the corresponding StealthMask entry is set"
    - "GooseStealthMode.swift is registered in project.pbxproj at exactly 4 locations so the project builds"
    - "GooseStealthModeTests passes all unit tests covering STEALTH-01 and STEALTH-02"
  artifacts:
    - GooseSwift/GooseStealthMode.swift
    - GooseSwiftTests/GooseStealthModeTests.swift
  key_links:
    - "StealthStorage key suffix ('recovery_score') → GooseStealthMode.isHidden → StealthMask.hidden stores Coach-JSON-key form ('recovery') — translation at mask-construction time in CoachChatModel"
    - "CoachLocalToolContext.vitals() masks hrv_rmssd and resting_hr by post-processing the rows array BEFORE the live-HR insert at index 0"
    - "project.pbxproj: PBXBuildFile + PBXFileReference + PBXGroup children + PBXSourcesBuildPhase — all 4 must reference GooseStealthMode.swift"
---

<objective>
Deliver the Stealth Mode data/logic layer (STEALTH-01, STEALTH-02): three pure-Swift types in
one new file and targeted masking in CoachLocalToolContext. No UI; Phase 122 adds the toggle UI.

Purpose: Allow users to suppress individual biometric metrics from Coach context by replacing
their values with "hidden_by_user" sentinel — preserving key presence so the Coach JSON
structure remains valid for LLM consumption.

Output:
- GooseSwift/GooseStealthMode.swift (StealthStorage + GooseStealthMode + StealthMask)
- GooseSwiftTests/GooseStealthModeTests.swift (unit tests)
- GooseSwift/CoachLocalToolContext.swift (mask param + masking in loadStats scores + vitals)
- GooseSwift/CoachChatModel.swift (mask construction + pass to both build() call sites)
- GooseSwift.xcodeproj/project.pbxproj (4-location registration of new Swift file)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-stealth-mode-rust-swift-core/119-CONTEXT.md
@.planning/phases/119-stealth-mode-rust-swift-core/119-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GooseStealthMode.swift and unit tests (STEALTH-01, per D-01–D-04)</name>
  <files>GooseSwift/GooseStealthMode.swift, GooseSwiftTests/GooseStealthModeTests.swift, GooseSwift.xcodeproj/project.pbxproj</files>
  <behavior>
    - StealthStorage.recoveryScore == "goose.swift.stealth.recovery_score"
    - StealthStorage.strainScore == "goose.swift.stealth.strain_score"
    - StealthStorage.hrvRmssd == "goose.swift.stealth.hrv_rmssd"
    - StealthStorage.restingHr == "goose.swift.stealth.resting_hr"
    - StealthStorage.sleepPerf == "goose.swift.stealth.sleep_performance"
    - StealthStorage.stressScore == "goose.swift.stealth.stress_score"
    - GooseStealthMode.isHidden(metric: "nonexistent_key") == false
    - GooseStealthMode.isHidden(metric: "recovery_score") == true when UserDefaults.standard.bool(forKey: StealthStorage.recoveryScore) == true; false otherwise
    - StealthMask.none.isHidden("recovery") == false
    - StealthMask(hidden: ["recovery", "strain"]).isHidden("recovery") == true
    - StealthMask(hidden: ["recovery", "strain"]).isHidden("sleep") == false
    - StealthMask(hidden: ["recovery"]).isHidden("totally_unknown_key") == false
  </behavior>
  <action>
Write GooseSwiftTests/GooseStealthModeTests.swift first (RED). Then write
GooseSwift/GooseStealthMode.swift to make tests pass (GREEN). Then register in project.pbxproj.

FILE: GooseSwiftTests/GooseStealthModeTests.swift

Structure: import XCTest + @testable import GooseSwift. final class GooseStealthModeTests: XCTestCase.
Test methods (see behavior block above for all assertions). In the UserDefaults integration test,
set the key before asserting, then call UserDefaults.standard.removeObject(forKey:) in a defer
block to clean up.

FILE: GooseSwift/GooseStealthMode.swift

Follow the file structure of GooseHealthKitExporter.swift: file-level comment, then the three
types in this order.

StealthStorage (per D-02): caseless enum. Six static let string constants using
"goose.swift.stealth.{suffix}" pattern matching existing keys in RemoteServerPersistence.swift
and CoreBluetoothBLETransport.swift. Suffixes: recovery_score, strain_score, hrv_rmssd,
resting_hr, sleep_performance, stress_score.

GooseStealthMode (per D-03): struct with a single static func isHidden(metric: String) -> Bool.
Uses a private static func keyFor(metric: String) -> String that maps the metric suffix to its
StealthStorage constant (switch/if-else over all 6 suffixes; default returns an empty string so
UserDefaults.bool(forKey: "") returns false for unknown inputs). Reads
UserDefaults.standard.bool(forKey: keyFor(metric: metric)).

StealthMask (per D-04): struct with let hidden: Set<String>. func isHidden(_ metric: String) ->
Bool returns hidden.contains(metric). static let none = StealthMask(hidden: []).

No imports beyond Foundation. No instance state on any type. No @Published, no ObservableObject.

FILE: GooseSwift.xcodeproj/project.pbxproj

Register GooseStealthMode.swift in exactly 4 locations (per RESEARCH.md Pattern 6):
1. PBXBuildFile stanza — new UUID referencing the file UUID
2. PBXFileReference stanza — new UUID, lastKnownFileType = sourcecode.swift, path = GooseStealthMode.swift
3. PBXGroup children list — insert the file UUID into the GooseSwift sources group alongside GooseHealthKitExporter.swift
4. PBXSourcesBuildPhase files list — insert the build file UUID

Use GooseHealthKitExporter.swift as the UUID anchor to derive adjacent positions. Generate two
fresh UUIDs (24 hex chars each) that do not collide with existing UUIDs in the file.

After editing, verify: grep -c 'GooseStealthMode.swift' GooseSwift.xcodeproj/project.pbxproj
must equal 4.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c 'GooseStealthMode.swift' GooseSwift.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>
    - GooseSwift/GooseStealthMode.swift exists with StealthStorage (6 keys), GooseStealthMode.isHidden, StealthMask
    - GooseSwiftTests/GooseStealthModeTests.swift exists with all behavior-block tests
    - grep -c 'GooseStealthMode.swift' project.pbxproj returns 4
    - All three types compile (no Swift errors at the new file)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Patch CoachLocalToolContext.swift — mask param + masking logic (STEALTH-02, per D-05, D-06)</name>
  <files>GooseSwift/CoachLocalToolContext.swift</files>
  <behavior>
    - build() signature gains mask: StealthMask = .none as the last parameter
    - Calling build() without the mask argument still compiles and behaves identically to before (default = .none = no masking)
    - When mask.isHidden("recovery") is true, the "recovery" value in the "scores" dict equals "hidden_by_user"
    - When mask.isHidden("strain") is true, the "strain" value equals "hidden_by_user"
    - When mask.isHidden("sleep") is true, the "sleep" value equals "hidden_by_user"
    - When mask.isHidden("stress") is true, the "stress" value equals "hidden_by_user"
    - When mask.isHidden("hrv_rmssd") is true, the vitals row with id "health-monitor" has value "hidden_by_user"
    - When mask.isHidden("resting_hr") is true, the vitals row with id "resting-hr" has value "hidden_by_user"
    - Live heart-rate row (inserted at index 0) is never masked
    - Masking in vitals() is applied to rows BEFORE the live-HR insert at index 0
  </behavior>
  <action>
Read CoachLocalToolContext.swift fully before editing. Map out: the build() function signature,
the loadStats() helper, the vitals() helper, the snapshot() serialisation helper.

STEP 1 — build() signature (per D-05):

Add mask: StealthMask = .none as the last parameter in build(). Pass mask down to loadStats()
and vitals() (whichever internal helpers emit the 6 metric values).

STEP 2 — scores masking (per D-05, D-06):

In the "scores" dict inside loadStats() (or wherever "recovery", "strain", "sleep", "stress"
are emitted as dict values), apply conditional substitution for each of the 4 score keys:

  "recovery": mask.isHidden("recovery") ? "hidden_by_user" : <original expression>,
  "strain":   mask.isHidden("strain")   ? "hidden_by_user" : <original expression>,
  "sleep":    mask.isHidden("sleep")    ? "hidden_by_user" : <original expression>,
  "stress":   mask.isHidden("stress")   ? "hidden_by_user" : <original expression>,

The metric key string is always preserved in the dict — only the value is replaced.

STEP 3 — vitals masking (per D-05, D-06 + RESEARCH.md Pitfall 3):

Pass mask into the vitals() private static helper. Inside vitals(), after building the rows
array from healthMonitorSnapshots().map(snapshot), apply masking BEFORE the live-HR row insert
at index 0:

  if mask.isHidden("hrv_rmssd"),
     let i = rows.firstIndex(where: { ($0["id"] as? String) == "health-monitor" }) {
    rows[i]["value"] = "hidden_by_user"
  }
  if mask.isHidden("resting_hr"),
     let i = rows.firstIndex(where: { ($0["id"] as? String) == "resting-hr" }) {
    rows[i]["value"] = "hidden_by_user"
  }
  // live-HR insert happens after masking

Do NOT pass mask into the snapshot() helper — keep snapshot() generic (per RESEARCH.md anti-pattern).

Snapshot IDs confirmed in HealthDataStore+Snapshots.swift: "health-monitor" (line 234), "resting-hr"
(line 284). If either firstIndex search returns nil (snapshot absent), the masking block is a
no-op — no crash, no guard needed.

The rows array elements are [String: Any] dicts, so rows[i]["value"] = "hidden_by_user" is a
direct mutation on a value-type copy (var rows). Confirm rows is declared as var before the
index mutations.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -n 'mask: StealthMask' GooseSwift/CoachLocalToolContext.swift</automated>
  </verify>
  <done>
    - build() signature includes mask: StealthMask = .none
    - The 4 score keys apply ternary masking with "hidden_by_user" sentinel
    - vitals() applies masking for "health-monitor" and "resting-hr" before the live-HR insert
    - File compiles without Swift errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CoachChatModel.swift call sites — build mask + pass to build() (STEALTH-02, per D-05)</name>
  <files>GooseSwift/CoachChatModel.swift</files>
  <action>
Read CoachChatModel.swift fully before editing. Locate the two call sites (research confirmed at
lines 116 and 163):

  Call site 1 (line ~116): inside the toolContextProvider closure for ChatGPTCoachProvider
  Call site 2 (line ~163): inside buildSystemPrompt()

MASK CONSTRUCTION PATTERN (per RESEARCH.md Pitfall 1 — metric name mismatch):

Build the mask from GooseStealthMode before each build() call site. The mask must use the
Coach-JSON-key form in StealthMask.hidden, not the storage-suffix form. The translation is:

  var hidden = Set<String>()
  if GooseStealthMode.isHidden(metric: "recovery_score")    { hidden.insert("recovery") }
  if GooseStealthMode.isHidden(metric: "strain_score")      { hidden.insert("strain") }
  if GooseStealthMode.isHidden(metric: "sleep_performance") { hidden.insert("sleep") }
  if GooseStealthMode.isHidden(metric: "stress_score")      { hidden.insert("stress") }
  if GooseStealthMode.isHidden(metric: "hrv_rmssd")         { hidden.insert("hrv_rmssd") }
  if GooseStealthMode.isHidden(metric: "resting_hr")        { hidden.insert("resting_hr") }
  let mask = StealthMask(hidden: hidden)

Apply this block once per call site, immediately before the CoachLocalToolContext.build(...)
invocation. StealthMask is a cheap value type; constructing it at each site is acceptable
(per Claude's Discretion in CONTEXT.md).

CALL SITE 1 — add mask construction before the return statement, then pass mask: mask to build().

CALL SITE 2 — add mask construction before let context = ..., then pass mask: mask to build().

Do not add any @Published properties, Observable state, or reactive bindings. The mask is built
on-demand at call time, reading UserDefaults.standard synchronously. Both call sites are already
on @MainActor (confirmed in RESEARCH.md); UserDefaults.bool(forKey:) is safe there.

Do not modify any other call site — research confirmed these are the only two.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c 'mask: mask' GooseSwift/CoachChatModel.swift</automated>
  </verify>
  <done>
    - Both CoachLocalToolContext.build() calls in CoachChatModel.swift pass mask: mask
    - mask is constructed from GooseStealthMode using the storage-suffix → Coach-JSON-key translation
    - grep -c 'mask: mask' CoachChatModel.swift returns 2
    - File compiles without Swift errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserDefaults → GooseStealthMode.isHidden | App-local preference read; no network, no user-supplied string reaches the read path |
| StealthMask → CoachLocalToolContext | Value type passed by the caller; the mask's hidden set is constructed from controlled GooseStealthMode calls only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-01 | Tampering | GooseStealthMode.isHidden — unknown metric key | low | accept | keyFor() returns "" for unknown inputs; UserDefaults.bool(forKey: "") returns false — safe default, no crash path |
| T-119-02 | Tampering | StealthStorage UserDefaults key namespace | low | mitigate | Keys namespaced under "goose.swift.stealth.*"; no collision with existing "goose.swift.*" or "goose.remote.*" key spaces confirmed by RESEARCH.md grep |
| T-119-03 | Information Disclosure | Coach JSON with mask bypassed | low | accept | Mask is constructed at call time from UserDefaults; there is no server-side enforcement — this is a client-side UX feature, not a security control. Documented as such. |
</threat_model>

<verification>
Build verification (full suite):

  cd /Users/francisco/Documents/goose
  xcodebuild test -project GooseSwift.xcodeproj \
    -scheme GooseSwift \
    -destination 'platform=iOS Simulator,name=iPhone 16' \
    -only-testing GooseSwiftTests/GooseStealthModeTests \
    CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E 'passed|failed|error:'

Per-task spot checks:
  grep -c 'GooseStealthMode.swift' GooseSwift.xcodeproj/project.pbxproj        # must be 4
  grep -n 'mask: StealthMask' GooseSwift/CoachLocalToolContext.swift             # must appear in signature
  grep -c 'mask: mask' GooseSwift/CoachChatModel.swift                           # must be 2
  grep -c 'hidden_by_user' GooseSwift/CoachLocalToolContext.swift                # must be >= 6

Confirm sentinel applied at all masking sites:
  grep -n 'hidden_by_user' GooseSwift/CoachLocalToolContext.swift
</verification>

<success_criteria>
STEALTH-01:
- GooseSwift/GooseStealthMode.swift exists and compiles
- StealthStorage enum has exactly 6 static let string constants with "goose.swift.stealth.*" keys
- GooseStealthMode.isHidden(metric:) reads UserDefaults.standard.bool(forKey:); returns false for absent keys and unknown metric names
- All STEALTH-01 unit tests in GooseStealthModeTests pass

STEALTH-02:
- StealthMask value type with hidden: Set<String> and isHidden(_ metric: String) -> Bool
- StealthMask.none is a static constant with an empty set
- CoachLocalToolContext.build() accepts mask: StealthMask = .none without breaking existing callers
- All 6 metric positions in the Coach JSON emit "hidden_by_user" when the corresponding mask entry is set
- HRV and resting HR vitals rows are masked before the live-HR row insert (no index drift)
- Both CoachChatModel.swift call sites construct a mask from GooseStealthMode and pass it to build()
- GooseSwiftTests/GooseStealthModeTests.swift passes: 7 tests, 0 failures
</success_criteria>

<output>
Create .planning/phases/119-stealth-mode-rust-swift-core/119-01-SUMMARY.md when done.
</output>
