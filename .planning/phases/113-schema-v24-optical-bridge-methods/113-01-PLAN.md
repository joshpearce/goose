---
phase: 113-schema-v24-optical-bridge-methods
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/store/mod.rs
  - Rust/core/src/bridge/mod.rs
  - Rust/core/src/bridge/metrics.rs
  - Rust/core/src/bridge/capabilities.rs
  - Rust/core/tests/optical_channel_bridge_tests.rs
  - Rust/core/tests/feature_flags_bridge_tests.rs
autonomous: true
requirements:
  - OPT-03
  - FF-03
  - BODY-01
  - PIP-02

must_haves:
  truths:
    - "CURRENT_SCHEMA_VERSION equals 24 in store/mod.rs; all prior migrations still apply on fresh open_in_memory()"
    - "optical_channel_samples table exists with UNIQUE(device_id, ts, packet_k, channel_index) and idx_optical_channel_samples_device_ts index"
    - "device_feature_flags table exists WITHOUT ROWID with PRIMARY KEY(device_id, flag_index)"
    - "body_composition_history table exists with CHECK(source IN ('manual','healthkit','scale')) and UNIQUE(source, date)"
    - "realtime_frames table exists with idx_realtime_frames_device_captured index"
    - "biometrics.insert_v20v21_batch, biometrics.insert_v26_batch, biometrics.optical_between are registered in BRIDGE_METHODS and dispatch via metrics::dispatch_metrics"
    - "capabilities.get_feature_flags and capabilities.upsert_feature_flags are registered in BRIDGE_METHODS and dispatch via new capabilities::dispatch_capabilities"
    - "bridge_methods_must_be_sorted and bridge_methods_constant_matches_dispatcher tests pass"
    - "optical_channel_bridge_tests and feature_flags_bridge_tests round-trip tests pass"
    - "cd Rust/core && cargo test --locked exits 0 with no failures"
  artifacts:
    - path: "Rust/core/src/store/mod.rs"
      provides: "CURRENT_SCHEMA_VERSION = 24; migration block with 4 new tables"
      contains: "CURRENT_SCHEMA_VERSION: i64 = 24"
    - path: "Rust/core/src/bridge/capabilities.rs"
      provides: "dispatch_capabilities function; get/upsert feature flags bridge arms"
      exports: ["dispatch_capabilities"]
    - path: "Rust/core/src/bridge/metrics.rs"
      provides: "3 new optical dispatch arms + args structs"
      contains: "biometrics.insert_v20v21_batch"
    - path: "Rust/core/tests/optical_channel_bridge_tests.rs"
      provides: "round-trip integration tests for all 3 optical methods"
    - path: "Rust/core/tests/feature_flags_bridge_tests.rs"
      provides: "round-trip integration tests for get/upsert feature flags"
  key_links:
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/bridge/capabilities.rs"
      via: "mod capabilities; + starts_with(capabilities.) routing arm"
      pattern: "capabilities::dispatch_capabilities"
    - from: "Rust/core/src/bridge/metrics.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "acquire_bridge_conn(&args.database_path) → optical_channel_samples table"
      pattern: "optical_channel_samples"
    - from: "Rust/core/src/bridge/capabilities.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "acquire_bridge_conn(&args.database_path) → device_feature_flags table"
      pattern: "device_feature_flags"
---

<objective>
Bump the SQLite schema from v23 to v24 in a single migration transaction that creates four new
tables (optical_channel_samples, device_feature_flags, body_composition_history, realtime_frames),
register five new bridge methods across metrics.rs (optical) and a new capabilities.rs (feature
flags), and gate the phase on a clean `cargo test --locked` run.

Purpose: This is the unlock phase — once the schema and bridge methods exist, Phases 114–122 can
consume the new tables from Swift/Kotlin without touching Rust schema again. capabilities.upsert_feature_flags
is included proactively so Phase 115 is purely Swift.

Output:
- store/mod.rs: CURRENT_SCHEMA_VERSION = 24 + 4-table migration block (per D-01, D-03–D-09)
- bridge/metrics.rs: biometrics.insert_v20v21_batch, biometrics.insert_v26_batch, biometrics.optical_between (per D-10, D-11, D-12)
- bridge/capabilities.rs (new): capabilities.get_feature_flags, capabilities.upsert_feature_flags (per D-13, D-14)
- bridge/mod.rs: BRIDGE_METHODS updated + mod capabilities; + routing arm (per D-15)
- tests/optical_channel_bridge_tests.rs + tests/feature_flags_bridge_tests.rs (per D-16)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-schema-v24-optical-bridge-methods/113-CONTEXT.md
@.planning/phases/113-schema-v24-optical-bridge-methods/RESEARCH.md
@.planning/phases/112-optical-protocol-decode-v20-v21-v26/112-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration v23 → v24 (4 tables, single transaction) per D-01 through D-09</name>
  <files>Rust/core/src/store/mod.rs</files>

  <read_first>
    - Rust/core/src/store/mod.rs lines 1–30 — locate CURRENT_SCHEMA_VERSION constant (line 23)
    - Rust/core/src/store/mod.rs lines 1860–1885 — locate migration 23 block end; find exact insertion point (after "PRAGMA user_version = 23;" before closing "#,)
    - Rust/core/src/store/mod.rs lines 1650–1700 — spo2_samples column style reference
    - Rust/core/tests/store_schema_version_tests.rs — confirm no literal "23"; verify tests use CURRENT_SCHEMA_VERSION constant only
  </read_first>

  <behavior>
    - Test 1 (existing, auto-updated): store_schema_version_tests.rs open_existing_current_rejects_stale_schema_version asserts version = CURRENT_SCHEMA_VERSION - 1 is rejected — passes once constant is bumped to 24
    - Test 2 (existing, auto-updated): in-store tests at lines 4003, 4644, 4792 assert_eq!(version, CURRENT_SCHEMA_VERSION) — pass automatically after bump
    - Test 3 (manual verify): GooseStore::open_in_memory() on a freshly migrated store returns Ok; querying sqlite_master for the 4 table names returns 4 rows
  </behavior>

  <action>
    Change 1 — bump version constant (per D-01):
    In store/mod.rs line 23, change:
      pub const CURRENT_SCHEMA_VERSION: i64 = 23;
    to:
      pub const CURRENT_SCHEMA_VERSION: i64 = 24;

    Change 2 — append migration 24 block (per D-01, D-03–D-09):
    Locate the exact end of the migration 23 block inside the execute_batch raw string. It ends with:
      INSERT OR IGNORE INTO goose_schema_migrations(version) VALUES (23);
      PRAGMA user_version = 23;
    The next line is the closing "#, delimiter.

    Insert the following SQL block between "PRAGMA user_version = 23;" and the closing "#,:

            CREATE TABLE IF NOT EXISTS optical_channel_samples (
                device_id TEXT NOT NULL,
                ts REAL NOT NULL,
                packet_k INTEGER NOT NULL,
                version INTEGER NOT NULL,
                channel_index INTEGER NOT NULL,
                samples_json TEXT NOT NULL,
                captured_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')),
                UNIQUE(device_id, ts, packet_k, channel_index)
            );
            CREATE INDEX IF NOT EXISTS idx_optical_channel_samples_device_ts
                ON optical_channel_samples(device_id, ts);

            CREATE TABLE IF NOT EXISTS device_feature_flags (
                device_id TEXT NOT NULL,
                flag_index INTEGER NOT NULL,
                flag_value INTEGER NOT NULL,
                discovered_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')),
                PRIMARY KEY(device_id, flag_index)
            ) WITHOUT ROWID;

            CREATE TABLE IF NOT EXISTS body_composition_history (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                date TEXT NOT NULL,
                weight_kg REAL,
                bmi REAL,
                body_fat_pct REAL,
                muscle_mass_kg REAL,
                water_pct REAL,
                source TEXT NOT NULL CHECK(source IN ('manual','healthkit','scale')),
                created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')),
                UNIQUE(source, date)
            );

            CREATE TABLE IF NOT EXISTS realtime_frames (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                device_uuid TEXT NOT NULL,
                frame_hex TEXT NOT NULL,
                captured_at TEXT NOT NULL DEFAULT 'realtime_pip',
                synced INTEGER NOT NULL DEFAULT 0
            );
            CREATE INDEX IF NOT EXISTS idx_realtime_frames_device_captured
                ON realtime_frames(device_uuid, captured_at);

            INSERT OR IGNORE INTO goose_schema_migrations(version) VALUES (24);
            PRAGMA user_version = 24;

    Critical constraint: Do NOT add AUTOINCREMENT to device_feature_flags — that table uses WITHOUT ROWID and has no id column (per D-07; SQLite disallows AUTOINCREMENT on WITHOUT ROWID tables).
    Critical constraint: The body_composition_history CHECK clause must read exactly CHECK(source IN ('manual','healthkit','scale')) — no other values permitted (per D-08).
    Do NOT alter any existing migration blocks; append only.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked store_schema_version 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "CURRENT_SCHEMA_VERSION" Rust/core/src/store/mod.rs | head -3` shows "= 24"
    - `cargo test --locked store_schema_version` passes (all store_schema_version_tests.rs tests green)
    - `cargo test --locked 2>&1 | grep -E "^(test result|FAILED)"` shows no FAILED lines and "test result: ok"
    - No literal string "23" added to any test file (verify with: grep -rn '"23"' Rust/core/tests/)
  </acceptance_criteria>

  <done>CURRENT_SCHEMA_VERSION = 24; four tables created in a single migration block; all existing schema version tests green; cargo test --locked passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store methods for optical_channel_samples and device_feature_flags per D-03–D-07</name>
  <files>Rust/core/src/store/mod.rs</files>

  <read_first>
    - Rust/core/src/store/mod.rs — grep for "fn insert_spo2" and surrounding lines to understand INSERT OR IGNORE pattern and return type convention
    - Rust/core/src/store/mod.rs — grep for "fn.*between" to see range query return type convention (tuple vs named struct)
    - Rust/core/src/bridge/metrics.rs lines 1402–1413 — InsertV24BatchArgs struct; how bridge calls the store method and maps the result
    - Rust/core/src/bridge/metrics.rs lines 381–397 — dispatch_metrics function signature to understand what store return types bridge already handles
  </read_first>

  <behavior>
    - Test 1: insert_optical_channel_samples inserts a row; a second insert with same (device_id, ts, packet_k, channel_index) is silently ignored (INSERT OR IGNORE idempotency per D-05)
    - Test 2: query_optical_channel_samples_between returns only rows within [start_ts, end_ts] for the given device_id and packet_k; rows outside the range are not returned
    - Test 3: upsert_feature_flags inserts rows; a second upsert with the same (device_id, flag_index) replaces the flag_value (INSERT OR REPLACE per D-06)
    - Test 4: get_feature_flags returns all flags for device_id; flags from a different device_id are not included
  </behavior>

  <action>
    Add four store methods to GooseStore in store/mod.rs. Follow the existing method placement conventions
    (group new methods near other biometric insert/query methods). All methods take &self and a rusqlite
    Connection or use the store's existing connection accessor pattern — check how existing store methods
    obtain the connection before implementing.

    Method 1 — insert_optical_channel_samples:
      Signature: pub fn insert_optical_channel_samples(&self, device_id: &str, ts: f64, packet_k: u8, version: u8, channel_index: u8, samples_json: &str) -> rusqlite::Result<usize>
      SQL: INSERT OR IGNORE INTO optical_channel_samples (device_id, ts, packet_k, version, channel_index, samples_json) VALUES (?1, ?2, ?3, ?4, ?5, ?6)
      Returns: rows_affected count (0 if duplicate, 1 if inserted)

    Method 2 — query_optical_channel_samples_between:
      Define a named struct for the return type:
        pub struct OpticalSampleRow {
            pub ts: f64,
            pub packet_k: i64,
            pub version: i64,
            pub channel_index: i64,
            pub samples_json: String,
        }
      Signature: pub fn query_optical_channel_samples_between(&self, device_id: &str, packet_k: u8, start_ts: f64, end_ts: f64) -> rusqlite::Result<Vec<OpticalSampleRow>>
      SQL: SELECT ts, packet_k, version, channel_index, samples_json FROM optical_channel_samples WHERE device_id = ?1 AND packet_k = ?2 AND ts >= ?3 AND ts <= ?4 ORDER BY ts ASC
      Map rows using the named struct — NOT tuple destructuring.

    Method 3 — upsert_feature_flags:
      Signature: pub fn upsert_feature_flags(&self, device_id: &str, flag_index: u8, flag_value: u8) -> rusqlite::Result<usize>
      SQL: INSERT OR REPLACE INTO device_feature_flags (device_id, flag_index, flag_value) VALUES (?1, ?2, ?3)
      Returns: rows_affected count

    Method 4 — get_feature_flags:
      Define a named struct:
        pub struct FeatureFlagRow {
            pub flag_index: i64,
            pub flag_value: i64,
            pub discovered_at: String,
        }
      Signature: pub fn get_feature_flags(&self, device_id: &str) -> rusqlite::Result<Vec<FeatureFlagRow>>
      SQL: SELECT flag_index, flag_value, discovered_at FROM device_feature_flags WHERE device_id = ?1 ORDER BY flag_index ASC
      Map rows using the named struct — NOT tuple destructuring.

    Naming: OpticalSampleRow and FeatureFlagRow are pub structs so bridge files can reference them.
    Derive: add #[derive(Debug)] on both row structs.
    Critical: Use struct field access in the bridge map lambdas (|r| r.ts, r.packet_k, ...) — do NOT use tuple destructuring. This is enforced by the project skill and will cause E0308 compile errors if violated.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo build --locked 2>&1 | grep -E "^error" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo build --locked` exits 0 (no compile errors from new store methods)
    - OpticalSampleRow and FeatureFlagRow structs are pub and accessible from bridge/ files
    - No tuple destructuring in the row-mapping closures — each field accessed as r.field_name
    - insert_optical_channel_samples uses INSERT OR IGNORE; upsert_feature_flags uses INSERT OR REPLACE
  </acceptance_criteria>

  <done>Four store methods compile clean; row types are pub structs with named fields; cargo build --locked exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bridge methods — optical arms in metrics.rs + new capabilities.rs per D-10–D-14</name>
  <files>Rust/core/src/bridge/metrics.rs, Rust/core/src/bridge/capabilities.rs</files>

  <read_first>
    - Rust/core/src/bridge/metrics.rs lines 375–430 — dispatch_metrics function; existing match arms boilerplate pattern to copy
    - Rust/core/src/bridge/metrics.rs lines 1395–1430 — InsertV24BatchArgs struct and insert_v24_biometric_batch_bridge function; understand acquire_bridge_conn usage
    - Rust/core/src/bridge/capture.rs — full file header; use imports; how acquire_bridge_conn is imported to replicate in capabilities.rs
    - Rust/core/src/bridge/mod.rs lines 1–50 — module-level use imports and pub(crate) re-exports to understand what capabilities.rs needs to import
  </read_first>

  <behavior>
    - Test 1: biometrics.insert_v20v21_batch JSON call with a device_id and two packets (each with two channels of 3 samples each) returns {"inserted": N} where N > 0
    - Test 2: biometrics.insert_v26_batch JSON call with ppg=[1,2,3] stores as channel_index=0; biometrics.optical_between then returns that row with channel_index=0
    - Test 3: biometrics.optical_between returns only rows within the time range for the given packet_k; out-of-range rows are absent
    - Test 4: capabilities.upsert_feature_flags with flags=[{index:0,value:1},{index:1,value:255}] returns {"upserted": 2}; capabilities.get_feature_flags returns both flags for that device_id
    - Test 5: capabilities.upsert_feature_flags called twice with the same flag_index replaces the value (not duplicates)
  </behavior>

  <action>
    === Part A: metrics.rs optical arms (per D-10, D-11, D-12) ===

    Add args structs in metrics.rs (above or near InsertV24BatchArgs; keep structs grouped):

      #[derive(Debug, Deserialize)]
      struct OpticalChannelArg {
          index: u8,
          samples: Vec<i64>,
      }

      #[derive(Debug, Deserialize)]
      struct OpticalPacketArg {
          ts: f64,
          packet_k: u8,
          version: u8,
          channels: Vec<OpticalChannelArg>,
      }

      #[derive(Debug, Deserialize)]
      struct InsertV20V21BatchArgs {
          database_path: String,
          device_id: String,
          packets: Vec<OpticalPacketArg>,
      }

      #[derive(Debug, Deserialize)]
      struct V26PacketArg {
          ts: f64,
          packet_k: u8,
          version: u8,
          ppg: Vec<i64>,
          num_channels: u8,
      }

      #[derive(Debug, Deserialize)]
      struct InsertV26BatchArgs {
          database_path: String,
          device_id: String,
          packets: Vec<V26PacketArg>,
      }

      #[derive(Debug, Deserialize)]
      struct OpticalBetweenArgs {
          database_path: String,
          device_id: String,
          packet_k: u8,
          start_ts: f64,
          end_ts: f64,
      }

    Add three bridge implementation functions in metrics.rs:

      fn insert_v20v21_batch_bridge(args: InsertV20V21BatchArgs) -> Result<serde_json::Value, String>:
        For each packet in args.packets, for each channel in packet.channels:
          - Serialize channel.samples to JSON string: serde_json::to_string(&channel.samples).map_err(|e| e.to_string())?
          - Call store.insert_optical_channel_samples(&args.device_id, packet.ts, packet.packet_k, packet.version, channel.index, &samples_json)
          - Accumulate inserted count (add rows_affected per call)
        Return serde_json::json!({"inserted": total_inserted})

      fn insert_v26_batch_bridge(args: InsertV26BatchArgs) -> Result<serde_json::Value, String>:
        For each packet in args.packets:
          - Serialize packet.ppg to JSON string
          - Call store.insert_optical_channel_samples(&args.device_id, packet.ts, packet.packet_k, packet.version, 0_u8, &samples_json)  ← channel_index=0 per D-11
          - Accumulate inserted count
        Return serde_json::json!({"inserted": total_inserted})

      fn optical_between_bridge(args: OpticalBetweenArgs) -> Result<serde_json::Value, String>:
        Call store.query_optical_channel_samples_between(&args.device_id, args.packet_k, args.start_ts, args.end_ts)
        Map result using struct field access (NOT tuple destructuring):
          rows.iter().map(|r| serde_json::json!({
              "ts": r.ts,
              "packet_k": r.packet_k,
              "version": r.version,
              "channel_index": r.channel_index,
              "samples_json": r.samples_json,
          })).collect::<Vec<_>>()
        Return serde_json::json!(rows_json)

    Add three dispatch arms to dispatch_metrics match block (alphabetical within existing arms):
      "biometrics.insert_v20v21_batch" arm — before "biometrics.insert_v24_batch"
      "biometrics.insert_v26_batch" arm — after "biometrics.insert_v24_batch"
      "biometrics.optical_between" arm — after "biometrics.insert_v26_batch"
    Each arm follows the exact boilerplate: request_args::<T>(request).and_then(fn).map(|value| bridge_ok(&request.request_id, value)).unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error))

    === Part B: new bridge/capabilities.rs (per D-13, D-14) ===

    Create Rust/core/src/bridge/capabilities.rs as a new file with this structure:

    Imports section: mirror the imports from capture.rs or metrics.rs —
      use super::{BridgeRequest, BridgeResponse, bridge_ok, bridge_error, request_args, acquire_bridge_conn};
      use serde::Deserialize;
      (add any additional imports needed for serde_json, store types)

    Args structs:
      #[derive(Debug, Deserialize)]
      struct GetFeatureFlagsArgs {
          database_path: String,
          device_id: String,
      }

      #[derive(Debug, Deserialize)]
      struct FeatureFlagArg {
          index: u8,
          value: u8,
      }

      #[derive(Debug, Deserialize)]
      struct UpsertFeatureFlagsArgs {
          database_path: String,
          device_id: String,
          flags: Vec<FeatureFlagArg>,
      }

    dispatch_capabilities function:
      pub(crate) fn dispatch_capabilities(request: &BridgeRequest) -> BridgeResponse {
          match request.method.as_str() {
              "capabilities.get_feature_flags" => request_args::<GetFeatureFlagsArgs>(request)
                  .and_then(get_feature_flags_bridge)
                  .map(|value| bridge_ok(&request.request_id, value))
                  .unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error)),
              "capabilities.upsert_feature_flags" => request_args::<UpsertFeatureFlagsArgs>(request)
                  .and_then(upsert_feature_flags_bridge)
                  .map(|value| bridge_ok(&request.request_id, value))
                  .unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error)),
              _ => unreachable!(
                  "dispatch_capabilities called with non-capabilities method: {}",
                  request.method
              ),
          }
      }

    Implementation functions:
      fn get_feature_flags_bridge(args: GetFeatureFlagsArgs) -> Result<serde_json::Value, String>:
        let store = acquire_bridge_conn(&args.database_path)?;
        let rows = store.get_feature_flags(&args.device_id).map_err(|e| e.to_string())?;
        Map using struct field access (|r| serde_json::json!({"flag_index": r.flag_index, "flag_value": r.flag_value, "discovered_at": r.discovered_at}))
        Return serde_json::json!(rows_json)

      fn upsert_feature_flags_bridge(args: UpsertFeatureFlagsArgs) -> Result<serde_json::Value, String>:
        let store = acquire_bridge_conn(&args.database_path)?;
        let mut upserted = 0usize;
        For each flag in args.flags:
          upserted += store.upsert_feature_flags(&args.device_id, flag.index, flag.value).map_err(|e| e.to_string())?;
        Return serde_json::json!({"upserted": upserted})

    No public symbols other than dispatch_capabilities — all other items are private.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo build --locked 2>&1 | grep -E "^error" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - Rust/core/src/bridge/capabilities.rs exists and is non-empty
    - `cargo build --locked` exits 0 with no errors from metrics.rs or capabilities.rs
    - dispatch_capabilities is pub(crate) and contains exactly two match arms (get_feature_flags, upsert_feature_flags)
    - Optical bridge functions use struct field access for row mapping (grep for "r\.ts\|r\.packet_k" in metrics.rs)
    - V26 insert stores channel_index = 0 (grep for "0_u8" or "channel_index.*0" in insert_v26_batch_bridge)
  </acceptance_criteria>

  <done>metrics.rs has 3 new optical dispatch arms; capabilities.rs created with 2 capability arms; cargo build --locked exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire BRIDGE_METHODS + mod declarations + routing arm per D-15, then integration tests per D-16</name>
  <files>Rust/core/src/bridge/mod.rs, Rust/core/tests/optical_channel_bridge_tests.rs, Rust/core/tests/feature_flags_bridge_tests.rs</files>

  <read_first>
    - Rust/core/src/bridge/mod.rs lines 47–210 — full BRIDGE_METHODS array; find exact insertion points for new biometrics.* entries and capabilities.* entries
    - Rust/core/src/bridge/mod.rs lines 510–580 — dispatch block; find the routing arms and where to insert starts_with("capabilities.") arm; find where mod declarations live
    - Rust/core/tests/v24_biometric_bridge_tests.rs — full file; understand JSON call pattern via call_bridge_method helper or direct GooseRustBridge invocation; understand how assertions are written
    - Rust/core/tests/sync_telemetry_round_trip.rs — open_in_memory() usage and test structure
  </read_first>

  <behavior>
    - Test (BRIDGE_METHODS compile-time): bridge_methods_must_be_sorted passes — array is sorted lexicographically
    - Test (dispatch compile-time): bridge_methods_constant_matches_dispatcher passes — every entry in BRIDGE_METHODS has a dispatch arm; capabilities.* arm routes to dispatch_capabilities
    - Test (optical round-trip): insert_v20v21_batch with 2 packets × 2 channels inserts 4 rows; optical_between returns all 4 rows in range
    - Test (optical round-trip): insert_v26_batch with 1 packet stores channel_index=0; optical_between with packet_k=26 returns 1 row with channel_index=0
    - Test (optical idempotency): second insert_v20v21_batch call with same data inserts 0 additional rows (INSERT OR IGNORE)
    - Test (flags round-trip): upsert_feature_flags with 3 flags returns {"upserted":3}; get_feature_flags returns all 3
    - Test (flags replace): upsert_feature_flags with same flag_index but new value; get_feature_flags returns updated value, still 1 row for that flag_index
  </behavior>

  <action>
    === Part A: bridge/mod.rs — BRIDGE_METHODS + module + routing (per D-15) ===

    Step 1 — add mod declaration:
      Add `mod capabilities;` alongside the existing domain module declarations in mod.rs.
      Find the block where `mod metrics;`, `mod capture;`, etc. are declared and insert `mod capabilities;`
      in alphabetical order (capabilities comes before capture).

    Step 2 — BRIDGE_METHODS insertions (alphabetical within each group, per D-15):
      In the biometrics section, the current entries are:
        "biometrics.insert_v24_batch",
        "biometrics.spo2_from_raw",
        "biometrics.v24_between",
      Change to:
        "biometrics.insert_v20v21_batch",
        "biometrics.insert_v24_batch",
        "biometrics.insert_v26_batch",
        "biometrics.optical_between",
        "biometrics.spo2_from_raw",
        "biometrics.v24_between",

      For the capabilities group, insert between calibration.* and capture.* entries:
        "capabilities.get_feature_flags",
        "capabilities.upsert_feature_flags",

      Verify alphabetical order after insertion — "biometrics" < "calibration" < "capabilities" < "capture" is correct.

    Step 3 — routing arm in the dispatch block:
      In the dispatch block (lines 510–580), find where method namespace routing is done with starts_with() checks.
      Add the following arm BEFORE the capture.* check (capabilities < capture alphabetically):
        if method.starts_with("capabilities.") {
            return capabilities::dispatch_capabilities(&request);
        }
      Ensure the arm references `capabilities::dispatch_capabilities` matching the pub(crate) function name exactly.

    === Part B: integration test files (per D-16) ===

    Create Rust/core/tests/optical_channel_bridge_tests.rs:

      Follow v24_biometric_bridge_tests.rs structure exactly. Use GooseStore::open_in_memory() for the
      test database (open_in_memory calls migrate internally — no separate migrate() call needed, per RESEARCH.md).
      Use the same bridge call helper pattern as v24_biometric_bridge_tests.rs (read it first to get the exact invocation pattern).

      Tests to include:
        test_insert_v20v21_batch_round_trip:
          Build a JSON call for biometrics.insert_v20v21_batch with device_id="test-device", two packets
          (packet_k=20, version=20, two channels each with samples=[100,200,300]).
          Assert result contains "inserted" > 0.
          Call biometrics.optical_between for the same device_id, packet_k=20, time range spanning both packets.
          Assert returned array length equals number of (packet × channel) rows inserted.

        test_insert_v26_batch_channel_index_zero:
          Build a JSON call for biometrics.insert_v26_batch with ppg=[1,2,3,4,5], packet_k=26, version=26.
          Call biometrics.optical_between for packet_k=26.
          Assert the returned row has channel_index=0.

        test_insert_v20v21_batch_idempotent:
          Insert the same packet twice via biometrics.insert_v20v21_batch.
          Assert the second call returns {"inserted": 0} (INSERT OR IGNORE idempotency).

        test_optical_between_range_filter:
          Insert packets at ts=1000.0, ts=2000.0, ts=3000.0.
          Call optical_between with start_ts=1500.0, end_ts=2500.0.
          Assert only the ts=2000.0 row is returned.

    Create Rust/core/tests/feature_flags_bridge_tests.rs:

      Tests to include:
        test_upsert_and_get_feature_flags:
          Call capabilities.upsert_feature_flags with flags=[{index:0,value:1},{index:1,value:255}].
          Assert result contains "upserted":2 (or >=1).
          Call capabilities.get_feature_flags for the same device_id.
          Assert returned array has length 2; flag_index 0 has flag_value 1; flag_index 1 has flag_value 255.

        test_upsert_feature_flags_replaces_value:
          Upsert flag_index=0 with value=1.
          Upsert flag_index=0 with value=99.
          Call get_feature_flags.
          Assert only one row for flag_index=0; flag_value=99 (INSERT OR REPLACE, latest wins).

        test_get_feature_flags_device_isolation:
          Upsert flags for device_id="device-A".
          Call get_feature_flags for device_id="device-B".
          Assert returned array is empty (no cross-device leakage).

    File naming: use the exact filenames optical_channel_bridge_tests.rs and feature_flags_bridge_tests.rs
    (Cargo.toml auto-discovers all .rs files in tests/ — no registration needed).
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test --locked bridge_methods_must_be_sorted` passes (array is sorted)
    - `cargo test --locked bridge_methods_constant_matches_dispatcher` passes (all 5 new methods have dispatch arms)
    - `cargo test --locked --test optical_channel_bridge_tests` passes (all 4 optical tests green)
    - `cargo test --locked --test feature_flags_bridge_tests` passes (all 3 flags tests green)
    - `cd Rust/core && cargo test --locked 2>&1 | grep -c FAILED` returns 0
    - `grep -c "capabilities.get_feature_flags" Rust/core/src/bridge/mod.rs` returns >= 1
    - `grep -c "capabilities.upsert_feature_flags" Rust/core/src/bridge/mod.rs` returns >= 1
    - `grep -c "biometrics.insert_v20v21_batch" Rust/core/src/bridge/mod.rs` returns >= 1
  </acceptance_criteria>

  <done>BRIDGE_METHODS sorted and complete; capabilities routing arm active; all 7 integration tests pass; full cargo test --locked suite exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift FFI → Rust bridge | JSON args passed from Swift into goose_bridge_handle_json; untrusted field values cross here |
| Rust bridge → SQLite | Parameterized SQL queries; all user-controlled values go through rusqlite bind parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | optical_channel_samples INSERT | mitigate | INSERT OR IGNORE prevents duplicate injection; rusqlite parameterized binds prevent SQL injection on all 6 columns |
| T-113-02 | Tampering | device_feature_flags INSERT OR REPLACE | mitigate | Parameterized binds on all 3 columns; WITHOUT ROWID + PRIMARY KEY enforces one row per (device_id, flag_index) — no phantom rows possible |
| T-113-03 | Spoofing | device_id argument in all bridge methods | accept | Single-user local device app; device_id originates from trusted CoreBluetooth peripheral identifier; no auth boundary at FFI layer |
| T-113-04 | Information Disclosure | samples_json TEXT column | accept | Data stored opaque as TEXT; serde_json::to_string serializes integers only; no eval path; local SQLite with no network exposure |
| T-113-05 | Denial of Service | Large samples_json payload | mitigate | samples_json is a JSON array of integers deserialized by serde through typed Vec<i64> — malformed JSON returns a deserialize error before reaching SQLite; extremely large arrays are bounded by the WHOOP packet size (max ~400 samples per channel) |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | No new cargo dependencies in this phase; Cargo.lock is committed; --locked flag enforced in all cargo test and cargo build calls |
</threat_model>

<verification>
Phase gate — run in sequence:

1. Schema gate:
   `cd Rust/core && cargo test --locked store_schema_version 2>&1 | tail -5`
   Expected: test result: ok, 0 failed

2. BRIDGE_METHODS sort gate:
   `cd Rust/core && cargo test --locked bridge_methods_must_be_sorted 2>&1 | tail -5`
   Expected: test result: ok, 0 failed

3. Dispatch coverage gate:
   `cd Rust/core && cargo test --locked bridge_methods_constant_matches_dispatcher 2>&1 | tail -5`
   Expected: test result: ok, 0 failed

4. Optical bridge round-trip gate:
   `cd Rust/core && cargo test --locked --test optical_channel_bridge_tests 2>&1 | tail -10`
   Expected: all tests pass

5. Feature flags bridge round-trip gate:
   `cd Rust/core && cargo test --locked --test feature_flags_bridge_tests 2>&1 | tail -10`
   Expected: all tests pass

6. Full suite gate (phase completion):
   `cd Rust/core && cargo test --locked 2>&1 | grep -E "^(test result|FAILED)"`
   Expected: "test result: ok" line; zero FAILED lines
</verification>

<success_criteria>
Phase 113 is complete when ALL of the following are true:

1. (OPT-03) CURRENT_SCHEMA_VERSION = 24; optical_channel_samples table and index created in migration v24
2. (OPT-03) biometrics.insert_v20v21_batch, biometrics.insert_v26_batch, biometrics.optical_between registered in BRIDGE_METHODS and dispatch to working implementations in metrics.rs
3. (FF-03) device_feature_flags table (WITHOUT ROWID) created in migration v24
4. (FF-03) capabilities.get_feature_flags and capabilities.upsert_feature_flags registered in BRIDGE_METHODS and dispatch via new bridge/capabilities.rs
5. (BODY-01) body_composition_history table created in migration v24 (schema only; no bridge methods)
6. (PIP-02) realtime_frames table and index created in migration v24 (schema only; no bridge methods)
7. bridge_methods_must_be_sorted and bridge_methods_constant_matches_dispatcher tests pass
8. optical_channel_bridge_tests.rs and feature_flags_bridge_tests.rs all pass
9. `cd Rust/core && cargo test --locked` exits 0 with zero FAILED lines
</success_criteria>

## Artifacts This Phase Produces

### New Files
| Path | Symbol / Purpose |
|------|-----------------|
| `Rust/core/src/bridge/capabilities.rs` | `dispatch_capabilities`, `get_feature_flags_bridge`, `upsert_feature_flags_bridge`, `GetFeatureFlagsArgs`, `UpsertFeatureFlagsArgs`, `FeatureFlagArg` |
| `Rust/core/tests/optical_channel_bridge_tests.rs` | `test_insert_v20v21_batch_round_trip`, `test_insert_v26_batch_channel_index_zero`, `test_insert_v20v21_batch_idempotent`, `test_optical_between_range_filter` |
| `Rust/core/tests/feature_flags_bridge_tests.rs` | `test_upsert_and_get_feature_flags`, `test_upsert_feature_flags_replaces_value`, `test_get_feature_flags_device_isolation` |

### Modified Files — New Symbols
| File | New Symbol |
|------|-----------|
| `Rust/core/src/store/mod.rs` | `CURRENT_SCHEMA_VERSION = 24`; migration v24 DDL block; `OpticalSampleRow`; `FeatureFlagRow`; `insert_optical_channel_samples`; `query_optical_channel_samples_between`; `upsert_feature_flags`; `get_feature_flags` |
| `Rust/core/src/bridge/metrics.rs` | `OpticalChannelArg`, `OpticalPacketArg`, `InsertV20V21BatchArgs`, `V26PacketArg`, `InsertV26BatchArgs`, `OpticalBetweenArgs`; `insert_v20v21_batch_bridge`; `insert_v26_batch_bridge`; `optical_between_bridge`; dispatch arms for `biometrics.insert_v20v21_batch`, `biometrics.insert_v26_batch`, `biometrics.optical_between` |
| `Rust/core/src/bridge/mod.rs` | `mod capabilities;`; BRIDGE_METHODS entries: `biometrics.insert_v20v21_batch`, `biometrics.insert_v26_batch`, `biometrics.optical_between`, `capabilities.get_feature_flags`, `capabilities.upsert_feature_flags`; `starts_with("capabilities.")` routing arm |

### New Bridge Method Strings (callable from Swift after this phase)
| Method | Dispatches To | Returns |
|--------|--------------|---------|
| `biometrics.insert_v20v21_batch` | `metrics::dispatch_metrics` → `insert_v20v21_batch_bridge` | `{"inserted": N}` |
| `biometrics.insert_v26_batch` | `metrics::dispatch_metrics` → `insert_v26_batch_bridge` | `{"inserted": N}` |
| `biometrics.optical_between` | `metrics::dispatch_metrics` → `optical_between_bridge` | `[{ts, packet_k, version, channel_index, samples_json}, ...]` |
| `capabilities.get_feature_flags` | `capabilities::dispatch_capabilities` → `get_feature_flags_bridge` | `[{flag_index, flag_value, discovered_at}, ...]` |
| `capabilities.upsert_feature_flags` | `capabilities::dispatch_capabilities` → `upsert_feature_flags_bridge` | `{"upserted": N}` |

<output>
Create `.planning/phases/113-schema-v24-optical-bridge-methods/113-01-SUMMARY.md` when done.
</output>
