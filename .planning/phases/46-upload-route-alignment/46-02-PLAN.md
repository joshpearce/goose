---
phase: 46-upload-route-alignment
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - GooseSwift/GooseUploadService.swift
  - GooseSwift/GooseAppModel+Upload.swift
autonomous: false
requirements: [ROUTE-01, ROUTE-02]

must_haves:
  truths:
    - "The deployed server responds 200 (not 404/405) to POST /v1/ingest-frames"
    - "A smoke-test POST then GET against the deployed server round-trips the same frames"
    - "iOS GooseUploadService.uploadRawFrames receives an `inserted` count and logs no server error"
    - "iOS importHistoricalDataFromServer pages /v1/export/frames without 404/405 and imports frames via capture.import_frame_batch"
  artifacts:
    - path: "GooseSwift/GooseUploadService.swift"
      provides: "uploadRawFrames POST to /v1/ingest-frames (already wired — verify contract match)"
      contains: "v1/ingest-frames"
    - path: "GooseSwift/GooseAppModel+Upload.swift"
      provides: "importHistoricalDataFromServer GET /v1/export/frames (already wired — verify contract match)"
      contains: "v1/export/frames"
  key_links:
    - from: "GooseSwift/GooseUploadService.swift"
      to: "POST /v1/ingest-frames"
      via: "URLSession POST, reads json[\"inserted\"]"
      pattern: "ingest-frames"
    - from: "GooseSwift/GooseAppModel+Upload.swift"
      to: "GET /v1/export/frames"
      via: "paged URLSession GET → capture.import_frame_batch"
      pattern: "export/frames"
---

<objective>
Deploy the Phase 46 server changes (raw_frames table + `POST /v1/ingest-frames` + UNION read path) to the user's personal Docker server, smoke-test the round-trip against the live endpoint, and verify the already-wired iOS upload/import paths succeed without 404/405.

The iOS client code is already in place: `GooseUploadService.uploadRawFrames` (GooseUploadService.swift:142) POSTs to `/v1/ingest-frames` and reads `json["inserted"]`, and `importHistoricalDataFromServer` (GooseAppModel+Upload.swift:88) pages `GET /v1/export/frames/{id}` with `from`/`to`/`limit` and feeds frames to `capture.import_frame_batch`. This plan confirms the iOS contract matches the server implementation from plan 01, deploys, and validates the full upload→import cycle on real infrastructure.

Purpose: Plan 01 makes the endpoints exist and pass tests; this plan proves they work in production and that the iOS app no longer hits 404/405 — the actual ROUTE-01/ROUTE-02 user-facing outcome.
Output: Deployed container running the new code, a passing curl smoke-test of the round-trip, and a confirmed iOS upload+import cycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-upload-route-alignment/46-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify iOS upload/import contract matches the server implementation</name>
  <files>GooseSwift/GooseUploadService.swift, GooseSwift/GooseAppModel+Upload.swift</files>
  <read_first>
    - GooseSwift/GooseUploadService.swift (lines 139-185) — uploadRawFrames: builds `{device:{id,mac,name}, frames:[...]}`, POSTs to `v1/ingest-frames`, reads `json["inserted"] as? Int`. Confirm the payload keys and the response key `inserted` match plan 01's IngestFramesBatch model and `{inserted, skipped}` return.
    - GooseSwift/GooseAppModel+Upload.swift (lines 88-160) — importHistoricalDataFromServer: pages `v1/export/frames/{deviceID}` with query items from/to/limit, expects a response envelope with a `frames` array, feeds each page to `capture.import_frame_batch`. Confirm the envelope key `frames` and the per-frame shape {captured_at_unix, frame_hex, source, device_model, device_type, sensitivity} match plan 01's read_device_frames output.
    - server/ingest/app/main.py (the IngestFramesBatch/IngestFrame models and the export_device_frames `{device_id, frames, count}` envelope) — the contract source of truth from plan 01.
  </read_first>
  <action>
    Cross-check the iOS request/response contract against the plan-01 server implementation. Specifically confirm: (1) the POST payload `{device:{id,mac,name}, frames:[...]}` deserialises into `IngestFramesBatch` (device.id, optional mac/name; each frame has captured_at_unix + frame_hex + optional source/device_type/device_model/sensitivity); (2) the iOS-sent frame objects (produced by the `upload.get_raw_frames_for_upload` Rust bridge) carry the field names the server's `IngestFrame` model expects — if there is any field-name mismatch (e.g. iOS sends `frame_hex` but model expects `hex`), the server model is the one to align to the iOS shape, since the iOS payload is the fixed contract; (3) the GET response envelope key `frames` and per-frame keys match what importHistoricalDataFromServer parses. Make NO functional iOS changes unless a concrete mismatch is found; if a mismatch IS found, the correct fix is to adjust the plan-01 server model (note it in the SUMMARY) rather than change the iOS contract. Document the verified contract mapping in the SUMMARY.
  </action>
  <verify>
    <automated>grep -n "v1/ingest-frames" GooseSwift/GooseUploadService.swift && grep -n "v1/export/frames" GooseSwift/GooseAppModel+Upload.swift && grep -n '"inserted"' GooseSwift/GooseUploadService.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep "v1/ingest-frames" GooseSwift/GooseUploadService.swift` matches
    - `grep "v1/export/frames" GooseSwift/GooseAppModel+Upload.swift` matches
    - GooseUploadService.swift reads the response key `inserted` (grep `"inserted"`)
    - The SUMMARY documents the field-by-field mapping between the iOS payload and the server IngestFrame model, explicitly confirming no mismatch (or recording the server-side fix applied)
  </acceptance_criteria>
  <done>iOS upload/import contract is confirmed to match the plan-01 server models and envelopes; any mismatch is resolved on the server side and recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plan 01 added the raw_frames table, POST /v1/ingest-frames, and the UNION read path to the server source (server/db/init.sql, app/store.py, app/read.py, app/main.py).</what-built>
  <how-to-verify>
    Deploy the updated server to your personal Docker host (the existing deploy flow — SCP the server/ tree to the host and rebuild/restart the ingest container; the init.sql migration is idempotent and re-applied on startup by bootstrap_schema, so the raw_frames table is created automatically). Then run a curl smoke-test against the live server (replace HOST, TOKEN):

    1. POST a frame:
       `curl -s -X POST https://HOST/v1/ingest-frames -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"device":{"id":"smoketest","mac":null,"name":"WHOOP Goose"},"frames":[{"captured_at_unix":1700000000.0,"frame_hex":"aa1800ff28020f3de10128663c00000000000000000001010d844e7c","source":"ios.corebluetooth.notification","device_type":"GOOSE","device_model":"WHOOP Goose","sensitivity":"user-owned-capture"}]}'`
       Expect: `{"inserted":1,"skipped":0}` (NOT a 404/405 HTML page).

    2. GET it back:
       `curl -s "https://HOST/v1/export/frames/smoketest?from=0&to=9999999999&limit=5000" -H "Authorization: Bearer TOKEN"`
       Expect: a JSON body whose `frames` array contains one entry with `frame_hex` == the posted hex and `captured_at_unix` == 1700000000.

    3. Re-POST the same frame → expect `{"inserted":0,"skipped":1}` (idempotency).

    4. POST without the Authorization header → expect HTTP 401.
  </how-to-verify>
  <resume-signal>Type "approved" once the smoke-test round-trips (inserted=1, GET returns the frame, re-POST skipped=1, no-auth=401), or paste the curl output if anything returns 404/405/500.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full iOS → server raw-frame upload and re-import cycle, now that the server endpoints are live.</what-built>
  <how-to-verify>
    With the deployed server reachable and the iOS app configured (Server URL + Bearer token set, upload enabled):

    1. Connect the WHOOP and let the app capture some frames (or use existing captured frames). Confirm in the Xcode console that `uploadRawFrames: inserted=N` is logged (N > 0) and there is NO `uploadRawFrames: server error` line — this proves POST /v1/ingest-frames returns 2xx (no 404/405).
    2. Trigger `importHistoricalDataFromServer` (the remote-server import action in the app). Confirm the Xcode console shows frames being paged from `/v1/export/frames/...` and imported via `capture.import_frame_batch` with no 404/405 errors, and that the import completes with totalFrames > 0.
    3. Confirm the imported data appears (health metrics / streams populated from the re-imported frames).
  </how-to-verify>
  <resume-signal>Type "approved" once the iOS upload logs an `inserted` count with no server error AND the import pages /v1/export/frames and completes without 404/405, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app → deployed server | Upload/import traffic over the public tunnel; Bearer-gated |
| operator workstation → Docker host | Deploy (SCP + container rebuild) — operator-controlled, out of app trust scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05 | Spoofing | live POST/GET /v1/export+ingest frames | mitigate | Smoke-test step 4 explicitly asserts a no-Authorization POST returns 401 against the LIVE server (confirms Bearer middleware active in production) |
| T-46-06 | Information Disclosure | smoke-test device id "smoketest" | accept | Test frame is a synthetic public HR sample, not real biometric data; no PII exposed |
</threat_model>

<verification>
1. Live `curl -X POST /v1/ingest-frames` returns `{"inserted":1,"skipped":0}` (checkpoint step 1)
2. Live `curl GET /v1/export/frames/smoketest` returns the posted frame (checkpoint step 2)
3. Re-POST returns `{"inserted":0,"skipped":1}` (checkpoint step 3)
4. No-auth POST returns 401 (checkpoint step 4)
5. iOS console logs `uploadRawFrames: inserted=N` with no server error; import pages export/frames without 404/405
</verification>

<success_criteria>
- Server deployed; POST /v1/ingest-frames live and returning 200 (not 404/405) — closes the iOS 404/405 condition (ROUTE-01)
- Live round-trip confirmed: POST then GET returns the same frame (ROUTE-02)
- Idempotent re-upload and Bearer-auth enforcement confirmed against the live server
- iOS uploadRawFrames and importHistoricalDataFromServer complete the full cycle without 404/405
</success_criteria>

<output>
Create `.planning/phases/46-upload-route-alignment/46-02-SUMMARY.md` when done
</output>
