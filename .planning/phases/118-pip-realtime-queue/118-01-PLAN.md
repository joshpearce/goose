---
phase: 118-pip-realtime-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/store/mod.rs
  - Rust/core/src/store/realtime.rs
  - Rust/core/src/bridge/mod.rs
  - Rust/core/src/bridge/realtime.rs
  - Rust/core/tests/realtime_round_trip.rs
autonomous: true
requirements: [PIP-01, PIP-02]
must_haves:
  truths:
    - "realtime.insert_frame bridge method inserts a row into realtime_frames (PIP-01)"
    - "realtime_frames.captured_at no longer defaults to the literal 'realtime_pip' (PIP-02 schema fix)"
    - "realtime_frames has a source column defaulting to 'realtime_pip' on existing and new databases (D-02)"
    - "BRIDGE_METHODS stays alphabetically sorted and the dispatcher handles realtime.*"
  artifacts:
    - path: "Rust/core/src/bridge/realtime.rs"
      provides: "realtime.insert_frame bridge dispatch + args + handler"
      contains: "dispatch_realtime"
    - path: "Rust/core/src/store/realtime.rs"
      provides: "GooseStore::insert_realtime_frame SQL insert + ensure_realtime_source_column migration"
      contains: "insert_realtime_frame"
    - path: "Rust/core/tests/realtime_round_trip.rs"
      provides: "Round-trip integration test for realtime.insert_frame"
      contains: "realtime.insert_frame"
  key_links:
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/bridge/realtime.rs"
      via: "method.starts_with(\"realtime.\") dispatch arm"
      pattern: "realtime::dispatch_realtime"
    - from: "Rust/core/src/bridge/realtime.rs"
      to: "Rust/core/src/store/realtime.rs"
      via: "store.insert_realtime_frame(...)"
      pattern: "insert_realtime_frame"
---

<objective>
Add the Rust persistence layer for the realtime PIP pipeline: fix the existing
`realtime_frames` schema bug, add a `source` column migration, implement a
`GooseStore::insert_realtime_frame` method, and wire a new `realtime.insert_frame`
bridge method via a dedicated `bridge/realtime.rs` domain module (D-02).

Purpose: PIP-01 requires a bridge method that the Swift `RealtimePIPQueue` (Plan
118-02) calls to persist every live BLE frame into `realtime_frames`. PIP-02's
table already exists (schema v24, Phase 113) but ships with a copy-paste DDL bug
on `captured_at` and is missing the `source` column that D-02 requires.

Output: `realtime.insert_frame` callable through the FFI bridge, inserting into a
corrected `realtime_frames` table; a passing round-trip integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-pip-realtime-queue/118-CONTEXT.md
@.planning/phases/118-pip-realtime-queue/118-RESEARCH.md

# Templates to mirror exactly
@Rust/core/src/bridge/body_composition.rs
@Rust/core/src/bridge/mod.rs
@Rust/core/src/store/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix realtime_frames schema and add insert_realtime_frame store method</name>
  <files>Rust/core/src/store/mod.rs, Rust/core/src/store/realtime.rs</files>
  <read_first>
    - Rust/core/src/store/mod.rs lines 1946-1968 (realtime_frames DDL + the ensure_* migration calls at the end of init_schema)
    - Rust/core/src/store/mod.rs around line 1961 (ensure_raw_evidence_columns) as the pattern for an ensure_* migration function
  </read_first>
  <behavior>
    - A fresh in-memory store has a realtime_frames table whose captured_at column has NO default of the literal string 'realtime_pip'.
    - A fresh in-memory store has a realtime_frames.source column defaulting to 'realtime_pip'.
    - An existing v24 database (table created without source) gains the source column after open via the ensure_* migration (idempotent — running twice is a no-op).
    - insert_realtime_frame(device_uuid, frame_hex, captured_at) inserts one row; re-inserting the same (device_uuid, captured_at, frame_hex) is a no-op (INSERT OR IGNORE).
  </behavior>
  <action>
    Per PIP-02 schema fix and D-02: in the realtime_frames DDL block (store/mod.rs ~line 1946-1954) change the captured_at column definition so it is `captured_at TEXT NOT NULL` with no DEFAULT (the caller always supplies an ISO8601 string), and add a `source TEXT NOT NULL DEFAULT 'realtime_pip'` column to the CREATE TABLE. Do NOT bump the schema version number or PRAGMA user_version — the table already exists at v24; fresh databases get the corrected DDL, existing databases get patched by the migration below.

    Add a new `ensure_realtime_source_column(&self) -> GooseResult<()>` method to GooseStore following the exact pattern of `ensure_raw_evidence_columns` (re-acquires the conn lock independently): query `PRAGMA table_info(realtime_frames)`; if no column named `source` exists, run `ALTER TABLE realtime_frames ADD COLUMN source TEXT NOT NULL DEFAULT 'realtime_pip'`. Make it idempotent (skip the ALTER when the column is present). Register the call in init_schema alongside the other `self.ensure_*()` calls (after `self.ensure_synced_columns()?;`).

    Create a new file `Rust/core/src/store/realtime.rs` containing an `impl GooseStore` block with `pub fn insert_realtime_frame(&self, device_uuid: &str, frame_hex: &str, captured_at: &str) -> GooseResult<()>`. Acquire the conn lock the same way the sibling store methods do, then run `INSERT OR IGNORE INTO realtime_frames (device_uuid, frame_hex, captured_at) VALUES (?1, ?2, ?3)` with `rusqlite::params![device_uuid, frame_hex, captured_at]`. Register the module: add `mod realtime;` to the store module declarations (wherever store submodules like the existing ones are declared — check the top of store/mod.rs; if store is a single file, add the `mod realtime;` declaration in store/mod.rs and ensure realtime.rs is at Rust/core/src/store/realtime.rs).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo build --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --lib succeeds.
    - grep confirms the realtime_frames DDL captured_at line no longer contains `DEFAULT 'realtime_pip'`.
    - grep confirms `source TEXT NOT NULL DEFAULT 'realtime_pip'` appears in the realtime_frames CREATE TABLE.
    - GooseStore::insert_realtime_frame exists in Rust/core/src/store/realtime.rs.
    - ensure_realtime_source_column is called from init_schema.
  </acceptance_criteria>
  <done>realtime_frames schema corrected (no bad captured_at default, has source column via DDL + migration); insert_realtime_frame store method compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add bridge/realtime.rs module and wire realtime.insert_frame</name>
  <files>Rust/core/src/bridge/realtime.rs, Rust/core/src/bridge/mod.rs</files>
  <read_first>
    - Rust/core/src/bridge/body_composition.rs (full file — exact template for a new domain module)
    - Rust/core/src/bridge/mod.rs lines 30-36 (mod declarations)
    - Rust/core/src/bridge/mod.rs lines 69-180 (BRIDGE_METHODS array, alphabetical sort group)
    - Rust/core/src/bridge/mod.rs lines 536-598 (dispatch starts_with chain; capabilities arm at 540)
    - Rust/core/src/bridge/mod.rs lines 1099-1122 (bridge_methods_constant_matches_dispatcher consistency test, include_str! concat block + the comment counting domain files)
  </read_first>
  <action>
    Per D-02, follow the body_composition.rs domain-module pattern exactly. Five edits in bridge/mod.rs plus one new file:

    1. New file Rust/core/src/bridge/realtime.rs with `pub(crate) fn dispatch_realtime(request: &BridgeRequest) -> BridgeResponse` that matches `request.method.as_str()` with a `"realtime.insert_frame"` arm using `request_args::<RealtimeInsertFrameArgs>(request).and_then(insert_frame_bridge).map(|value| bridge_ok(&request.request_id, value)).unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error))`, plus an `unreachable!` default arm. Define `#[derive(Debug, Deserialize)] struct RealtimeInsertFrameArgs { database_path: String, device_uuid: String, frame_hex: String, captured_at: String }` and `fn insert_frame_bridge(args: RealtimeInsertFrameArgs) -> GooseResult<serde_json::Value>` that does `let store = acquire_bridge_conn(&args.database_path)?; store.insert_realtime_frame(&args.device_uuid, &args.frame_hex, &args.captured_at)?; Ok(serde_json::json!({"ok": true, "inserted": 1}))`. Import the same `use super::{...}` and `use crate::GooseResult;` lines that body_composition.rs uses.

    2. Add `mod realtime;` to the bridge mod declarations block (lines 30-36), keeping declarations grouped with the existing siblings.

    3. Add the string `"realtime.insert_frame"` to the BRIDGE_METHODS array in alphabetical position — between `"privacy.lint"` and `"protocol.parse_frame_hex"`.

    4. Add an early-return dispatch arm in handle_bridge_request_inner, placed after the capabilities arm (~line 542) and before the metrics arm: `if method.starts_with("realtime.") { return realtime::dispatch_realtime(&request); }`.

    5. In the bridge_methods_constant_matches_dispatcher test, add `include_str!("realtime.rs"),` to the include_str! concat block (lines 1115-1121) and update the comment that counts the number of domain files (e.g. "Reads all 7 domain files" -> "8 domain files").
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked bridge_methods_constant_matches_dispatcher 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - cargo test bridge_methods_constant_matches_dispatcher passes (BRIDGE_METHODS sorted, dispatcher reachable for realtime.insert_frame).
    - grep confirms `"realtime.insert_frame"` sits between `"privacy.lint"` and `"protocol.parse_frame_hex"` in BRIDGE_METHODS.
    - grep confirms `method.starts_with("realtime.")` dispatch arm exists in mod.rs.
    - grep confirms `include_str!("realtime.rs")` is in the consistency test concat block.
  </acceptance_criteria>
  <done>realtime.insert_frame is registered, sorted, dispatched, and consistency-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Round-trip integration test for realtime.insert_frame</name>
  <files>Rust/core/tests/realtime_round_trip.rs</files>
  <read_first>
    - Rust/core/tests/bridge_tests.rs (existing bridge round-trip test style: GooseStore::open_in_memory / temp db, JSON request through the bridge, assert response)
    - Rust/core/tests/sync_telemetry_round_trip.rs (round-trip insert-then-read pattern)
  </read_first>
  <behavior>
    - Calling realtime.insert_frame with {database_path, device_uuid, frame_hex, captured_at} returns ok=true.
    - After insertion, a direct SELECT against realtime_frames returns exactly one row with the supplied device_uuid, frame_hex, and captured_at, and source = 'realtime_pip'.
    - captured_at stored equals the supplied ISO8601 string (NOT the literal 'realtime_pip') — proves the schema bug is fixed.
    - Inserting the same frame twice yields a single row (INSERT OR IGNORE dedup).
  </behavior>
  <action>
    Create Rust/core/tests/realtime_round_trip.rs. Open a temp-file-backed store (use the same temp-db helper the existing integration tests use; in-memory will not work if the bridge re-opens by path, so use a tempfile path passed both to the bridge and to a verification connection). Build a JSON request `{"method":"realtime.insert_frame","request_id":"t1","args":{"database_path":<path>,"device_uuid":"DEV-1","frame_hex":"aabbcc","captured_at":"2026-06-24T12:34:56.789Z"}}` and dispatch it through the bridge entry point used by the other tests. Assert the response ok flag is true. Then open the same db and `SELECT device_uuid, frame_hex, captured_at, source FROM realtime_frames` and assert exactly one row with the expected values and `source = 'realtime_pip'` and `captured_at = '2026-06-24T12:34:56.789Z'`. Add a second assertion: dispatch the identical request again and assert the row count stays 1.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked --test realtime_round_trip 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo test --test realtime_round_trip passes.
    - Test asserts source = 'realtime_pip' and captured_at equals the supplied ISO8601 value.
    - Test asserts dedup (duplicate insert keeps row count at 1).
  </acceptance_criteria>
  <done>Round-trip test proves realtime.insert_frame persists correct values into a corrected realtime_frames table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift FFI → Rust bridge | JSON args from the iOS app cross into Rust; device_uuid/frame_hex/captured_at are app-supplied strings |
| Rust → SQLite | Parameterized INSERT into local on-device database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | realtime.insert_frame SQL | mitigate | Use rusqlite params (?1/?2/?3), never string interpolation — prevents SQL injection from frame_hex/device_uuid |
| T-118-02 | Denial of Service | realtime_frames growth | accept | Local on-device DB; INSERT OR IGNORE dedups; no external attacker; rate handling deferred (CONTEXT Deferred Ideas) |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added (RESEARCH: "No new external packages") |
</threat_model>

<verification>
- cargo build --lib succeeds.
- cargo test --locked bridge_methods_constant_matches_dispatcher passes.
- cargo test --locked --test realtime_round_trip passes.
- realtime_frames DDL fixed: no `DEFAULT 'realtime_pip'` on captured_at; source column present.
</verification>

<success_criteria>
- realtime.insert_frame is a registered, sorted, dispatched bridge method (PIP-01 bridge layer).
- realtime_frames schema bug fixed and source column added on fresh + existing databases (PIP-02 schema fix, D-02).
- Round-trip test green.
</success_criteria>

<output>
Create `.planning/phases/118-pip-realtime-queue/118-01-SUMMARY.md` when done.
</output>
