---
phase: 118-pip-realtime-queue
plan: 03
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - server/db/init.sql
  - server/ingest/app/store.py
  - server/ingest/app/main.py
  - server/ingest/tests/conftest.py
  - server/ingest/tests/test_realtime_ingest.py
autonomous: true
requirements: [PIP-03]
must_haves:
  truths:
    - "POST /v1/ingest-realtime exists with Bearer auth matching /v1/ingest-frames (PIP-03)"
    - "realtime_frames is a TimescaleDB hypertable partitioned on captured_at (PIP-03)"
    - "Valid payloads return 200 with {inserted, skipped}; missing/invalid auth returns 401; missing device_uuid or frame_hex returns 422"
    - "Re-posting the same frame is idempotent via ON CONFLICT DO NOTHING"
  artifacts:
    - path: "server/ingest/app/main.py"
      provides: "ingest_realtime route + Pydantic request models"
      contains: "/v1/ingest-realtime"
    - path: "server/ingest/app/store.py"
      provides: "insert_realtime_frames_batch"
      contains: "insert_realtime_frames_batch"
    - path: "server/db/init.sql"
      provides: "realtime_frames hypertable + dedup index"
      contains: "create_hypertable('realtime_frames'"
    - path: "server/ingest/tests/test_realtime_ingest.py"
      provides: "endpoint tests (200/401/422/idempotent)"
      contains: "/v1/ingest-realtime"
  key_links:
    - from: "server/ingest/app/main.py"
      to: "server/ingest/app/store.py"
      via: "store.insert_realtime_frames_batch(conn, frames)"
      pattern: "insert_realtime_frames_batch"
    - from: "server/ingest/app/store.py"
      to: "server/db/init.sql"
      via: "INSERT INTO realtime_frames ... ON CONFLICT DO NOTHING"
      pattern: "realtime_frames"
---

<objective>
Add the server ingestion side of the realtime PIP pipeline (PIP-03): a
`POST /v1/ingest-realtime` FastAPI endpoint with the same Bearer auth as
`/v1/ingest-frames`, an idempotent `insert_realtime_frames_batch` store function,
a `realtime_frames` TimescaleDB hypertable migration in `init.sql`, and pytest
coverage for the 200 / 401 / 422 / idempotent cases.

Purpose: PIP-03. This phase delivers the ingestion endpoint; the consumer /
display side is explicitly deferred to Phase 124 (CONTEXT Deferred Ideas).

Output: a working authenticated realtime-frame ingestion endpoint backed by a
time-partitioned hypertable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-pip-realtime-queue/118-CONTEXT.md
@.planning/phases/118-pip-realtime-queue/118-RESEARCH.md

# Templates to mirror exactly
@server/ingest/app/main.py
@server/ingest/app/store.py
@server/db/init.sql
@server/ingest/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: realtime_frames hypertable migration in init.sql</name>
  <files>server/db/init.sql</files>
  <read_first>
    - server/db/init.sql lines 107-122 (raw_frames CREATE TABLE + create_hypertable + dedup unique index — the exact pattern to mirror)
  </read_first>
  <action>
    Append a new migration block at the END of server/db/init.sql, mirroring the raw_frames block. Per Claude's Discretion in CONTEXT and Pitfall 5 (hypertable partition column must be TIMESTAMPTZ):

    Create table realtime_frames IF NOT EXISTS with columns: device_uuid TEXT NOT NULL, frame_hex TEXT NOT NULL, captured_at TIMESTAMPTZ NOT NULL, source TEXT NOT NULL DEFAULT 'realtime_pip', synced INTEGER NOT NULL DEFAULT 0. (captured_at is TIMESTAMPTZ — psycopg casts the iOS ISO8601 string automatically.) Do NOT add a foreign key to devices (the realtime path does not pre-register devices via ensure_device).

    Then SELECT create_hypertable('realtime_frames', 'captured_at', if_not_exists => TRUE).

    Then a UNIQUE INDEX IF NOT EXISTS realtime_frames_dedup ON realtime_frames (device_uuid, captured_at, frame_hex) — this enables ON CONFLICT (device_uuid, captured_at, frame_hex) DO NOTHING.

    Then a plain INDEX IF NOT EXISTS realtime_frames_device_time ON realtime_frames (device_uuid, captured_at).

    Keep the whole block idempotent (every statement uses IF NOT EXISTS / if_not_exists) so re-running init.sql on an existing DB is safe.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -q "create_hypertable('realtime_frames', 'captured_at', if_not_exists => TRUE)" server/db/init.sql && grep -q "captured_at  *TIMESTAMPTZ NOT NULL" server/db/init.sql && grep -q "realtime_frames_dedup" server/db/init.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - init.sql contains a realtime_frames CREATE TABLE with captured_at TIMESTAMPTZ NOT NULL.
    - create_hypertable('realtime_frames', 'captured_at', if_not_exists => TRUE) present.
    - Unique dedup index on (device_uuid, captured_at, frame_hex) present.
    - All statements idempotent.
  </acceptance_criteria>
  <done>realtime_frames hypertable + indexes are created idempotently on deploy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insert_realtime_frames_batch + POST /v1/ingest-realtime</name>
  <files>server/ingest/app/store.py, server/ingest/app/main.py</files>
  <read_first>
    - server/ingest/app/store.py lines 36-70 (insert_raw_frames_batch — the function template)
    - server/ingest/app/main.py lines 57-95 (require_auth, BaseModel patterns) and lines 432-467 (IngestFrame / IngestFramesBatch models + the ingest_frames route)
  </read_first>
  <behavior>
    - insert_realtime_frames_batch(conn, frames) inserts each {device_uuid, frame_hex, captured_at} row and returns {"inserted": N, "skipped": M}.
    - Re-posting an identical frame increments skipped (ON CONFLICT DO NOTHING), not inserted.
    - POST /v1/ingest-realtime with valid Bearer auth and a valid body returns 200 + {"inserted", "skipped"}.
    - Missing/invalid Authorization returns 401.
    - Body missing device_uuid or frame_hex, or frame_hex not matching ^[0-9a-fA-F]+$, returns 422.
  </behavior>
  <action>
    In store.py, add insert_realtime_frames_batch(conn, frames: list) -> dict following insert_raw_frames_batch (lines 36-70) but simpler: loop over frames, execute INSERT INTO realtime_frames (device_uuid, frame_hex, captured_at) VALUES (%s, %s, %s) ON CONFLICT (device_uuid, captured_at, frame_hex) DO NOTHING with (f["device_uuid"], f["frame_hex"], f["captured_at"]). captured_at is an ISO8601 string passed straight to a TIMESTAMPTZ column (psycopg casts it). Count inserted vs skipped via cur.rowcount. Caller commits.

    In main.py, add two Pydantic models near the IngestFrame models (Claude's Discretion / RESEARCH): a RealtimeFrame with device_uuid: str, frame_hex: str = Field(..., pattern=r"^[0-9a-fA-F]+$"), captured_at: str; and an IngestRealtimeBatch with frames: list[RealtimeFrame] = Field(..., max_length=5000). Then add the route @app.post("/v1/ingest-realtime", dependencies=[Depends(require_auth)]) def ingest_realtime(batch: IngestRealtimeBatch) that does payload = batch.model_dump(), opens psycopg.connect(cfg.db_dsn), calls store.insert_realtime_frames_batch(conn, payload["frames"]), conn.commit(), returns the result. Use the SAME require_auth dependency as ingest_frames (no new auth surface — V4/V5 controls inherited).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -q 'def insert_realtime_frames_batch' server/ingest/app/store.py && grep -q '"/v1/ingest-realtime"' server/ingest/app/main.py && grep -q 'pattern=r"\^\[0-9a-fA-F\]+\$"' server/ingest/app/main.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - insert_realtime_frames_batch exists in store.py with ON CONFLICT (device_uuid, captured_at, frame_hex) DO NOTHING.
    - POST /v1/ingest-realtime route exists in main.py with Depends(require_auth).
    - RealtimeFrame enforces frame_hex pattern ^[0-9a-fA-F]+$ and IngestRealtimeBatch caps frames at max_length=5000.
  </acceptance_criteria>
  <done>Authenticated realtime ingestion endpoint persists frames idempotently to realtime_frames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pytest coverage + conftest truncate fix</name>
  <files>server/ingest/tests/test_realtime_ingest.py, server/ingest/tests/conftest.py</files>
  <read_first>
    - server/ingest/tests/conftest.py lines 65-73 (clean_db fixture — the TRUNCATE list that must include realtime_frames for isolation)
    - server/ingest/tests/test_daily.py lines 100-110 (TestClient with Authorization: Bearer secret fixture pattern)
    - server/ingest/tests/test_read_api.py lines 12-22 (client fixture + monkeypatch app config)
  </read_first>
  <behavior>
    - A POST to /v1/ingest-realtime with valid auth + one valid frame returns 200 and inserted == 1.
    - Re-posting the same frame returns 200 with skipped == 1, inserted == 0 (idempotent).
    - A POST without Authorization header returns 401.
    - A POST whose frame omits device_uuid (or frame_hex) returns 422.
  </behavior>
  <action>
    Add realtime_frames to the TRUNCATE table list in the clean_db fixture in conftest.py (currently it truncates devices, raw_frames, ... — append realtime_frames so realtime tests are isolated between runs).

    Create server/ingest/tests/test_realtime_ingest.py following the test_daily.py / test_read_api.py client fixture pattern: a TestClient over the app with default headers {"Authorization": "Bearer secret"} (monkeypatch the app config db_dsn to clean_db and the token to "secret" the same way the existing fixtures do). Write four tests:
    1. test_ingest_realtime_inserts: POST one valid frame, assert status 200 and json()["inserted"] == 1.
    2. test_ingest_realtime_idempotent: POST the same frame twice, assert the second response has skipped == 1 and inserted == 0.
    3. test_ingest_realtime_requires_auth: POST with a client that has NO Authorization header, assert status 401.
    4. test_ingest_realtime_validates_body: POST a frame missing device_uuid, assert status 422.
    Use a fixed valid frame: device_uuid "DEV-1", frame_hex "aabbcc", captured_at "2026-06-24T12:34:56.789Z".
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/server/ingest && python -m pytest tests/test_realtime_ingest.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py clean_db TRUNCATE list includes realtime_frames.
    - test_realtime_ingest.py contains the four named tests covering 200, idempotent, 401, 422.
    - pytest tests/test_realtime_ingest.py passes (or skips cleanly only if docker is unavailable, per the timescale_dsn fixture).
  </acceptance_criteria>
  <done>Endpoint behavior (200/401/422/idempotent) is verified by pytest; test isolation accounts for realtime_frames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS client → server HTTP | Untrusted JSON payload crosses into FastAPI; Bearer-authenticated |
| Server app → TimescaleDB | Parameterized INSERT into realtime_frames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Spoofing | /v1/ingest-realtime | mitigate | Depends(require_auth) Bearer check via secrets.compare_digest — same as all /v1 routes; 401 on missing/invalid |
| T-118-06 | Tampering | INSERT INTO realtime_frames | mitigate | psycopg parameterized query (%s) + Pydantic frame_hex pattern ^[0-9a-fA-F]+$ |
| T-118-07 | Denial of Service | oversized batch | mitigate | IngestRealtimeBatch frames max_length=5000 |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (FastAPI/pydantic/psycopg already in requirements.txt) |
</threat_model>

<verification>
- grep confirms route, store function, hypertable migration, and frame_hex validation present.
- pytest tests/test_realtime_ingest.py passes (200/401/422/idempotent).
- conftest clean_db truncates realtime_frames.
</verification>

<success_criteria>
- POST /v1/ingest-realtime authenticated, returns 200/{inserted,skipped} on valid input, 401 on bad auth, 422 on bad body (PIP-03).
- realtime_frames hypertable partitioned on captured_at, idempotent migration.
- Idempotent re-posts via ON CONFLICT DO NOTHING.
</success_criteria>

<output>
Create `.planning/phases/118-pip-realtime-queue/118-03-SUMMARY.md` when done.
</output>
