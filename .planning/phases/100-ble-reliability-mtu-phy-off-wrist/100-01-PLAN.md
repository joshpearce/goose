---
phase: 100-ble-reliability-mtu-phy-off-wrist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift
  - GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
autonomous: true
requirements:
  - BLE-01
must_haves:
  truths:
    - "LE 2M PHY is requested immediately after connect, before service discovery"
    - "Effective MTU is logged in the BLE session start event (connect.mtu)"
    - "PHY negotiation failure is handled gracefully — connection never stalls or crashes on PHY error"
    - "didUpdatePreferredPHY delegate fires and logs outcome (ok or warn)"
  artifacts:
    - path: "GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift"
      provides: "setPreferredPHY + MTU log in centralManager(_:didConnect:)"
      contains: "setPreferredPHY"
    - path: "GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift"
      provides: "didUpdatePreferredPHY delegate implementation"
      contains: "didUpdatePreferredPHY"
  key_links:
    - from: "centralManager(_:didConnect:)"
      to: "peripheral.setPreferredPHY"
      via: "Called after identity guard, before peripheral.discoverServices"
      pattern: "setPreferredPHY"
    - from: "centralManager(_:didConnect:)"
      to: "peripheral.maximumWriteValueLength"
      via: "Logged immediately as connect.mtu diagnostic"
      pattern: "maximumWriteValueLength"
---

<objective>
Add LE 2M PHY preference and MTU logging to the BLE connect flow in CoreBluetoothBLETransport.

Purpose: Maximise BLE throughput on supported hardware by requesting LE 2M PHY on every connect; log effective MTU for diagnostics. Closes issue #159 (BLE-01).

Output: Two targeted edits — one in CentralDelegate (setPreferredPHY + MTU log in didConnect) and one in PeripheralDelegate (didUpdatePreferredPHY delegate handler).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/100-ble-reliability-mtu-phy-off-wrist/100-CONTEXT.md
@.planning/phases/100-ble-reliability-mtu-phy-off-wrist/100-RESEARCH.md

@GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift
@GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: PHY preference + MTU log in centralManager(_:didConnect:)</name>
  <files>GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift</files>
  <action>
    Read CoreBluetoothBLETransport+CentralDelegate.swift. Find `centralManager(_:didConnect:)`. After the WHOOP identity guard passes (after `rememberPeripheral(...)` or equivalent) and BEFORE the `peripheral.discoverServices(...)` call, insert two lines:

    1. Call `peripheral.setPreferredPHY(tx:rx:)` with LE 2M for both tx and rx. The exact enum name for LE 2M PHY must be verified at compile time — the RESEARCH.md notes this as ASSUMED. Try `CBPeripheral.PHYType.le2M` first; if the compiler rejects that name, check what `CBPeripheral` exposes in the project's iOS SDK and use the correct enum (may be `CBPhy.le2M`, `.le2m`, or similar). Add a brief inline comment: "BLE-01: request LE 2M PHY for improved throughput (fire-and-forget; result handled in delegate)".

    2. Read `peripheral.maximumWriteValueLength(for: .withoutResponse)` into a local `let mtu` constant and pass it to `record(source:title:body:)` with source "ble", title "connect.mtu", and body "mtu=\(mtu) peripheral=\(peripheral.identifier.uuidString)". Add inline comment: "BLE-01: log pre-exchange MTU baseline (may be 20 before ATT exchange completes)". Do NOT call the `record` method from `@MainActor` — check the existing call pattern in this file (all existing `record(...)` calls use `ble.record(...)` or are called on the same queue). Follow the existing pattern exactly.

    Do NOT block `peripheral.discoverServices(...)` on the PHY result. The PHY call is asynchronous; service discovery must proceed immediately. Do NOT add any awaits, completion handlers, or semaphores around the PHY call.
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | grep -E 'error:|warning:.*PHY|warning:.*MTU' | head -20</automated>
  </verify>
  <done>
    Build succeeds with no errors. `setPreferredPHY` call is present in centralManager(_:didConnect:) before discoverServices. `connect.mtu` log entry appears in `centralManager(_:didConnect:)`. PHY call is fire-and-forget (no await, no semaphore, no blocking).
  </done>
</task>

<task type="auto">
  <name>Task 2: didUpdatePreferredPHY delegate handler in PeripheralDelegate</name>
  <files>GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift</files>
  <action>
    Read CoreBluetoothBLETransport+PeripheralDelegate.swift. Add the `peripheral(_:didUpdatePreferredPHY:)` `CBPeripheralDelegate` method. Place it alongside the other `CBPeripheralDelegate` methods in the extension (after `didUpdateValue` or after `didWriteValue` — use the existing ordering in the file as the guide).

    The method body: if `error != nil`, call `record(level: .warn, source: "ble", title: "phy.update.failed", body: error.localizedDescription)`. If no error, call `record(source: "ble", title: "phy.update.ok", body: "le2M preferred")`. Use the existing `record(level:source:title:body:)` / `record(source:title:body:)` overloads — do NOT invent new logging infrastructure.

    The exact `CBPeripheralDelegate` method signature to implement (verify the delegate method name compiles — it is marked ASSUMED in RESEARCH.md; if `didUpdatePreferredPHY` is rejected by the compiler, search the iOS SDK for the correct delegate method name for PHY updates):

    ```
    func peripheral(_ peripheral: CBPeripheral, didUpdatePreferredPHY error: Error?)
    ```

    Follow 2-space indentation, K&R brace style, consistent with the rest of the file.
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | grep -c 'error:' | { read n; [ "$n" -eq 0 ] && echo "PASS: build clean" || echo "FAIL: $n errors"; }</automated>
  </verify>
  <done>
    Build succeeds. `didUpdatePreferredPHY` delegate method exists in PeripheralDelegate extension. Method logs "phy.update.ok" on success and warn-level "phy.update.failed" on error. No new logging infrastructure introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE peripheral → iOS CoreBluetooth | PHY negotiation result and MTU value come from the OS/hardware; no user or network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Denial of Service | PHY negotiation | accept | PHY failure is fire-and-forget; error is logged (warn) and connection continues normally |
| T-100-02 | Tampering | MTU log | accept | MTU is a read-only OS property; no external input; log is diagnostic only |
</threat_model>

<verification>
1. Build for iOS Simulator succeeds with zero errors:
   `xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -quiet`
2. `grep -n "setPreferredPHY" GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift` returns a match before the `discoverServices` line
3. `grep -n "maximumWriteValueLength" GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift` returns a match in `centralManager(_:didConnect:)`
4. `grep -n "didUpdatePreferredPHY" GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift` returns the delegate method
5. No existing test regressions: `xcodebuild test -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5`
</verification>

<success_criteria>
- PHY preference (`setPreferredPHY(tx: .le2M, rx: .le2M)`) is called in `centralManager(_:didConnect:)` before `discoverServices`, after identity guard
- MTU is logged as "connect.mtu" event on every connect
- `didUpdatePreferredPHY` is implemented in PeripheralDelegate; logs ok or warn
- PHY failure never blocks or crashes the connect flow
- Build is clean; no test regressions
- Closes issue #159
</success_criteria>

<output>
Create `.planning/phases/100-ble-reliability-mtu-phy-off-wrist/100-01-SUMMARY.md` when done.

Commit message: `feat(100-01): add LE 2M PHY preference and MTU logging to BLE connect flow

- Call setPreferredPHY(tx: .le2M, rx: .le2M) in centralManager(_:didConnect:) before discoverServices
- Log maximumWriteValueLength as connect.mtu diagnostic event
- Add didUpdatePreferredPHY delegate to log PHY negotiation outcome

Fixes #159`
</output>
