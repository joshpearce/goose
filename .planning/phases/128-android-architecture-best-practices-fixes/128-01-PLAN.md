---
phase: 128-android-architecture-best-practices-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt
autonomous: true
requirements:
  - RUST-AUD-02
  - BP-AND-01
  - BP-AND-02
user_setup: []

must_haves:
  truths:
    - "WhoopBleClient.scope is cancelled when the BLE connection ends (disconnect / GATT disconnect with no reconnect), so coroutines do not outlive the owning ViewModel"
    - "syncInProgress and activeGeneration mutations are serialised under a Mutex; the gatt reference is published safely across threads"
    - "importFrame inspects the GooseBridge.safeHandle() response and logs a Log.e diagnostic when the bridge returns ok:false or an error, instead of discarding it silently"
    - "WhoopBleClient signals sync completion via a SharedFlow<Unit> instead of an onSyncComplete callback, removing the ViewModel->BleClient->ViewModel reference cycle"
    - "No GlobalScope reference exists anywhere in WhoopBleClient.kt"
  artifacts:
    - "android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt"
  key_links:
    - "syncCompleteEvent SharedFlow is the single sync-completion signal consumed by AppViewModel in plan 128-02"
    - "scope cancellation path runs through disconnect() and the terminal GATT-disconnect branch"
---

<objective>
Fix the four WhoopBleClient findings from the Phase 127 audit (A-01 scope lifecycle, A-02 thread-safety, A-03 silent importFrame discard, A-07 callback reference cycle) without changing BLE/sync behaviour.

Purpose: Eliminate the highest-severity Android threading and error-handling gaps in the BLE client (RUST-AUD-02), make JNI bridge failures observable (BP-AND-01), and remove unmanaged coroutine scope leakage (BP-AND-02).
Output: A revised WhoopBleClient.kt with a cancellable scope, Mutex-guarded sync state, a SharedFlow sync-complete signal, and error-propagating frame import.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md
@.planning/phases/127-multi-model-code-audit/127-AUDIT-REPORT.md
@android/app/src/main/kotlin/com/goose/app/bridge/GooseBridge.kt
</context>

<interface_context>
Interface change consumed by plan 128-02 (AppViewModel):
- REMOVE public property: `var onSyncComplete: (() -> Unit)?`
- ADD public property: `val syncCompleteEvent: SharedFlow<Unit>` (backed by a private MutableSharedFlow)
- The MutableSharedFlow is configured per D-07 specifics: `MutableSharedFlow<Unit>(replay = 0, extraBufferCapacity = 1, onBufferOverflow = BufferOverflow.DROP_OLDEST)`.
- completeSyncIfActive() emits to syncCompleteEvent via tryEmit(Unit) (non-suspending; safe from the BLE callback thread) instead of invoking onSyncComplete.

GooseBridge.safeHandle(request: String): String already returns an error-shaped JSON object
`{"ok":false,"result":null,"error":{"message":"..."},"timing":null}` instead of throwing.
Callers must parse the response and branch on the `ok` boolean — they must NOT assume success.
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make WhoopBleClient.scope cancellable and remove the callback reference cycle (A-01, A-07)</name>
  <files>android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt</files>
  <read_first>
    - android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt (full file — scope at line 91, disconnect() at 171, onSyncComplete at 101, completeSyncIfActive at 567, onGattDisconnected at 622)
    - .planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md (D-01, D-07 and the <specifics> SharedFlow config)
  </read_first>
  <action>
    Implement D-01 (scope lifecycle) and D-07 (callback to SharedFlow) together since both touch the same disconnect/sync paths.

    D-01: Keep the existing `scope = CoroutineScope(SupervisorJob() + Dispatchers.IO)` as the WhoopBleClient-owned scope. Add `scope.cancel()` so the scope is torn down when the connection is permanently over. Cancel it in two places: (a) inside disconnect() AFTER gatt?.disconnect() has been requested, only when the caller intends a permanent teardown; (b) the existing onCleared() in AppViewModel already calls disconnect(), so disconnect() is the single teardown entry point. Because SupervisorJob+cancel makes a scope unusable after cancel, guard against reuse: after cancel, a subsequent connect() must rebuild the scope. Convert `scope` from a val to a `private var scope` that is re-assigned to a fresh `CoroutineScope(SupervisorJob() + Dispatchers.IO)` at the top of connect() when the prior scope has been cancelled (check `scope.isActive`). Do NOT cancel the scope on a transient GATT disconnect that will auto-reconnect (willReconnect == true) — the reconnect job and idle-timeout job run on this scope; only cancel on explicit disconnect(). Import kotlinx.coroutines.cancel and kotlinx.coroutines.isActive.

    D-07: Remove the public `var onSyncComplete: (() -> Unit)? = null` property. Add a private `MutableSharedFlow<Unit>` configured exactly as the D-07 specifics describe (replay 0, extraBufferCapacity 1, BufferOverflow.DROP_OLDEST) and expose it as `val syncCompleteEvent: SharedFlow<Unit>` via asSharedFlow(). In completeSyncIfActive(), replace `onSyncComplete?.invoke()` with `syncCompleteEvent` emission using tryEmit(Unit) (tryEmit is non-suspending and safe from the BLE callback thread; the DROP_OLDEST buffer policy makes it fire-and-forget). Add imports: kotlinx.coroutines.channels.BufferOverflow, kotlinx.coroutines.flow.MutableSharedFlow, kotlinx.coroutines.flow.SharedFlow, kotlinx.coroutines.flow.asSharedFlow.

    Preserve all existing connect/reconnect/auth behaviour. Do not introduce any unmanaged global coroutine scope — all coroutines must run on the WhoopBleClient-owned scope.
  </action>
  <verify>
    <automated>cd android &amp;&amp; grep -n 'GlobalScope' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 &amp;&amp; grep -q 'syncCompleteEvent' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; grep -q 'scope.cancel' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; ! grep -q 'onSyncComplete' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GlobalScope' WhoopBleClient.kt` (excluding comment lines) == 0
    - `syncCompleteEvent` public SharedFlow property exists; `onSyncComplete` property is gone
    - `scope.cancel()` is invoked in the disconnect() teardown path
    - connect() rebuilds the scope when the previous scope is no longer active
    - completeSyncIfActive() emits to the SharedFlow via tryEmit instead of invoking a callback
  </acceptance_criteria>
  <done>WhoopBleClient owns a cancellable coroutine scope cancelled on explicit disconnect and rebuilt on reconnect; sync completion is signalled through syncCompleteEvent SharedFlow; the onSyncComplete callback and any GlobalScope usage are removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Serialise sync state with a Mutex and publish gatt safely; propagate importFrame errors (A-02, A-03)</name>
  <files>android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt</files>
  <read_first>
    - android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt (syncInProgress at 133, activeGeneration at 105, gatt at 103, handleNotification at 396, completeSyncIfActive at 567, importFrame at 575, onGattDisconnected at 622)
    - .planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md (D-02, D-03)
    - android/app/src/main/kotlin/com/goose/app/bridge/GooseBridge.kt (safeHandle returns ok:false JSON on failure — never throws)
  </read_first>
  <action>
    Implement D-02 (thread-safety) and D-03 (importFrame error propagation).

    D-02: The sync-state fields (`syncInProgress`, `activeGeneration`) are written from both the BLE callback thread and IO-dispatcher coroutines. Introduce a `private val syncMutex = Mutex()` (kotlinx.coroutines.sync.Mutex). Guard the compound check-and-set transitions in startHistoricalSync() and completeSyncIfActive() with syncMutex so the "already in progress" guard and the completion reset are atomic. Because startHistoricalSync() and completeSyncIfActive() are reached from suspend coroutines on `scope`, perform the guarded transition inside `syncMutex.withLock { ... }` in a coroutine context; where completeSyncIfActive() is called from the non-suspend idle-timeout path, it is already inside `scope.launch { ... }` so withLock is available. For the BLE-callback-thread call sites (onCharacteristicWrite state machine), keep the existing `@Volatile` on syncInProgress/pendingSyncCommand for visibility and only move the start/complete compound transitions under the Mutex — do not attempt to suspend on the BLE callback thread. For the `gatt` reference (read/written across the BLE callback thread and coroutines): annotate it `@Volatile private var gatt: BluetoothGatt?` so writes publish safely (D-02 permits @Volatile as the chosen mechanism). Import kotlinx.coroutines.sync.Mutex and kotlinx.coroutines.sync.withLock.

    D-03: importFrame() currently calls `GooseBridge.safeHandle(request)` and discards the result. safeHandle never throws — on failure it returns `{"ok":false,...,"error":{"message":"..."}}`. Parse the returned string with org.json.JSONObject, read the `ok` boolean, and when `ok` is false log the error message via `Log.e(TAG, "importFrame bridge failure: <message> source=<source>")`. Also wrap the JSON parse in a try/catch and Log.e on parse failure so a malformed response is not silently dropped. Keep the call on the IO dispatcher. Additionally, validate the database path before building the request: if `context.filesDir` does not exist, Log.e and return without calling the bridge (addresses the related MEDIUM null-safety finding on importFrame's hardcoded path).

    Preserve frame routing, source labelling ("historical_sync"/"android_ble"), and live-HR extraction behaviour exactly.
  </action>
  <verify>
    <automated>cd android &amp;&amp; grep -q 'Mutex' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; grep -q '@Volatile private var gatt' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; grep -q 'importFrame bridge failure' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; grep -q 'Log.e' app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A `Mutex` guards the start/complete sync-state transitions
    - The `gatt` reference is annotated `@Volatile`
    - importFrame parses the safeHandle response and calls `Log.e` when `ok` is false
    - importFrame validates filesDir existence before invoking the bridge
    - No behavioural change to frame routing or live-HR extraction
  </acceptance_criteria>
  <done>Sync-state transitions are serialised under a Mutex, the gatt reference is published with @Volatile, and importFrame surfaces bridge failures through Log.e instead of discarding them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WHOOP device -> BLE callback thread | Untrusted device frames cross into the app on the Android BLE thread |
| BLE callback thread <-> IO dispatcher | Shared mutable sync state crosses thread boundaries |
| Kotlin -> Rust (JNI) | importFrame hands frame bytes to native goose_core via GooseBridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-01 | Tampering | syncInProgress/activeGeneration race | high | mitigate | Mutex-serialise start/complete transitions; @Volatile gatt (Task 2) |
| T-128-02 | Information disclosure (loss) | importFrame silent discard | high | mitigate | Parse safeHandle response, Log.e on ok:false (Task 2) |
| T-128-03 | Denial of service | leaked coroutine scope outliving ViewModel | high | mitigate | Cancel scope on disconnect; rebuild on reconnect (Task 1) |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (Kotlin source only) |
</threat_model>

<verification>
- `grep -c GlobalScope WhoopBleClient.kt` (non-comment) == 0 (BP-AND-02)
- `scope.cancel()` present and reachable from disconnect() (A-01)
- `Mutex` + `@Volatile private var gatt` present (A-02)
- importFrame logs `Log.e` on bridge failure (A-03, BP-AND-01)
- `syncCompleteEvent` SharedFlow present; `onSyncComplete` absent (A-07)
- Android compilation is verified end-to-end in plan 128-03 after all wave-1 changes land
</verification>

<success_criteria>
All four WhoopBleClient audit findings (A-01, A-02, A-03, A-07) are addressed with the mechanisms locked in D-01/D-02/D-03/D-07; zero GlobalScope usages remain; BLE/sync/optical behaviour is preserved.
</success_criteria>

<output>
Create `.planning/phases/128-android-architecture-best-practices-fixes/128-01-SUMMARY.md` when done
</output>
