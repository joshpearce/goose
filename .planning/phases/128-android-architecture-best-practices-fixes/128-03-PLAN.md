---
phase: 128-android-architecture-best-practices-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 128-01
  - 128-02
files_modified:
  - android/gradle/libs.versions.toml
  - android/build.gradle.kts
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/kotlin/com/goose/app/GooseApplication.kt
  - android/app/src/main/kotlin/com/goose/app/MainActivity.kt
  - android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt
  - android/app/src/main/kotlin/com/goose/app/viewmodel/SettingsViewModel.kt
  - android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt
autonomous: true
requirements:
  - RUST-AUD-02

must_haves:
  truths:
    - "Hilt is wired into the Android build: KSP + Hilt plugins and hilt-android/hilt-compiler dependencies are declared via the version catalog"
    - "A GooseApplication class annotated @HiltAndroidApp exists and is registered as android:name in the manifest"
    - "MainActivity is annotated @AndroidEntryPoint; MetricsViewModel and SettingsViewModel are @HiltViewModel with @Inject constructors (or, per the D-09a fallback, are made private with a documented deferral)"
    - "The Android module compiles cleanly with the Hilt changes (CI / assembleDebug succeeds), or A-09 is documented as deferred with the exact fallback applied"
  artifacts:
    - "android/app/src/main/kotlin/com/goose/app/GooseApplication.kt"
    - "android/gradle/libs.versions.toml"
    - "android/app/build.gradle.kts"
  key_links:
    - "GooseApplication @HiltAndroidApp is the DI root; manifest android:name must point to it"
    - "MainActivity @AndroidEntryPoint enables hiltViewModel()/by viewModels() injection of the @HiltViewModel sub-ViewModels"
---

<objective>
Integrate Hilt dependency injection for the Android sub-ViewModels (A-09) and verify the whole Phase 128 Android module compiles. This is the wave-2 plan; it depends on the wave-1 source changes in plans 128-01 and 128-02 having landed.

Purpose: Replace direct constructor instantiation of MetricsViewModel/SettingsViewModel with Hilt-managed, ViewModelProvider-lifecycle-aware injection (RUST-AUD-02 / A-09), and confirm no regressions across all nine Phase 128 fixes via a build.
Output: Hilt plumbing in the Gradle build + manifest + Application class, @HiltViewModel sub-ViewModels, and a green Android build — or the D-09a fallback cleanly applied and documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md
@android/gradle/libs.versions.toml
@android/build.gradle.kts
@android/app/build.gradle.kts
@android/app/src/main/AndroidManifest.xml
</context>

<interface_context>
Current build facts (verified during planning):
- Kotlin 2.4.0, AGP 9.2.0, compileSdk 36, minSdk 26 (version catalog at android/gradle/libs.versions.toml).
- No Hilt and no KSP present today. Root build.gradle.kts only declares android.application + kotlin.compose (apply false).
- No Application subclass exists; the manifest <application> tag has no android:name.
- AppViewModel currently constructs `MetricsViewModel(app)` and `SettingsViewModel(app)` directly (post-128-02 these are private fields).

Hilt reference (Context7, dagger.dev/hilt, 2.59.x current line):
- Hilt Gradle plugin id: `com.google.dagger.hilt.android`.
- KSP plugin id: `com.google.devtools.ksp` (KSP version must match the Kotlin 2.4.0 line — use the catalog to pin
  a `kotlin-2.4.0-<ksp>` compatible version; resolve the exact patch via the KSP releases page if assembleDebug
  complains about a Kotlin/KSP mismatch).
- Dependencies: `com.google.dagger:hilt-android` (implementation) + `com.google.dagger:hilt-compiler` (ksp).
- Annotations: @HiltAndroidApp (Application), @AndroidEntryPoint (Activity), @HiltViewModel + @Inject constructor (ViewModel).
- Compose retrieval: androidx.hilt:hilt-navigation-compose provides hiltViewModel(); or use `by viewModels()` from the
  @AndroidEntryPoint Activity. Choose whichever keeps the existing AppViewModel ownership model simplest.
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Hilt + KSP to the Gradle build, version catalog, manifest, and create GooseApplication (A-09 plumbing)</name>
  <files>android/gradle/libs.versions.toml, android/build.gradle.kts, android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml, android/app/src/main/kotlin/com/goose/app/GooseApplication.kt</files>
  <read_first>
    - android/gradle/libs.versions.toml (versions, libraries, plugins blocks — Kotlin 2.4.0, AGP 9.2.0)
    - android/build.gradle.kts (root plugins, apply false pattern)
    - android/app/build.gradle.kts (plugins + dependencies blocks)
    - android/app/src/main/AndroidManifest.xml (<application> tag at line 30 has no android:name)
    - .planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md (D-09 plumbing list, <specifics> GooseApplication)
  </read_first>
  <action>
    Add the Hilt build plumbing per D-09 using the version catalog (this project uses libs.versions.toml).

    1. libs.versions.toml — under [versions] add a `hilt` version (current 2.59.x line) and a `ksp` version
       aligned to Kotlin 2.4.0 (format `2.4.0-<kspPatch>`). Under [libraries] add
       `hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }` and
       `hilt-compiler = { group = "com.google.dagger", name = "hilt-compiler", version.ref = "hilt" }`. If using
       hiltViewModel() from Compose, also add `androidx-hilt-navigation-compose`. Under [plugins] add
       `hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }` and
       `ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }`.
    2. Root android/build.gradle.kts — add `alias(libs.plugins.hilt) apply false` and
       `alias(libs.plugins.ksp) apply false` to the plugins block.
    3. android/app/build.gradle.kts — add `alias(libs.plugins.hilt)` and `alias(libs.plugins.ksp)` to the
       plugins block; add `implementation(libs.hilt.android)`, `ksp(libs.hilt.compiler)`, and (if used)
       `implementation(libs.androidx.hilt.navigation.compose)` to dependencies.
    4. Create android/app/src/main/kotlin/com/goose/app/GooseApplication.kt: `@HiltAndroidApp class GooseApplication : Application()`
       (import android.app.Application and dagger.hilt.android.HiltAndroidApp).
    5. AndroidManifest.xml — add `android:name=".GooseApplication"` to the <application> tag.

    Do NOT change JavaVersion/jvmTarget (stays 11). Keep all existing plugins and deps intact.
  </action>
  <verify>
    <automated>cd android &amp;&amp; grep -q 'HiltAndroidApp' app/src/main/kotlin/com/goose/app/GooseApplication.kt &amp;&amp; grep -q 'android:name=".GooseApplication"' app/src/main/AndroidManifest.xml &amp;&amp; grep -q 'hilt-android' gradle/libs.versions.toml &amp;&amp; grep -q 'com.google.devtools.ksp' gradle/libs.versions.toml &amp;&amp; grep -q 'libs.plugins.hilt' app/build.gradle.kts &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Version catalog declares hilt + ksp versions, hilt-android/hilt-compiler libraries, and hilt/ksp plugins
    - Root and app Gradle files apply the Hilt and KSP plugins; app declares hilt-android + ksp(hilt-compiler)
    - GooseApplication.kt exists with @HiltAndroidApp
    - Manifest <application android:name=".GooseApplication">
  </acceptance_criteria>
  <done>The Android build, version catalog, manifest, and a new @HiltAndroidApp GooseApplication are all wired for Hilt + KSP.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Annotate ViewModels/Activity for Hilt, then verify the Android build compiles (A-09 + CI verification, D-09a fallback)</name>
  <files>android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt, android/app/src/main/kotlin/com/goose/app/viewmodel/SettingsViewModel.kt, android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt, android/app/src/main/kotlin/com/goose/app/MainActivity.kt</files>
  <read_first>
    - android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt (AndroidViewModel(app) constructor)
    - android/app/src/main/kotlin/com/goose/app/viewmodel/SettingsViewModel.kt (AndroidViewModel(app) constructor)
    - android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt (constructs the two sub-ViewModels directly; post-128-02 they are private)
    - android/app/src/main/kotlin/com/goose/app/MainActivity.kt (ComponentActivity; appViewModel by viewModels())
    - .planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md (D-09 annotations, D-09a fallback)
  </read_first>
  <action>
    Apply the Hilt annotations per D-09, then build to verify. Hilt @HiltViewModel requires an @Inject constructor
    whose params Hilt can provide; AndroidViewModel needs the Application, which Hilt provides via
    @ApplicationContext / built-in Application binding.

    1. MainActivity: add `@AndroidEntryPoint` (import dagger.hilt.android.AndroidEntryPoint). Keep
       `by viewModels()` for AppViewModel.
    2. MetricsViewModel and SettingsViewModel: annotate each `@HiltViewModel class ... @Inject constructor(...)`.
       To keep AndroidViewModel(app), inject the Application: `@Inject constructor(app: Application) : AndroidViewModel(app)`.
       (Hilt provides the Application binding.) Imports: dagger.hilt.android.lifecycle.HiltViewModel, javax.inject.Inject.
    3. AppViewModel: stop constructing the sub-ViewModels with `new` directly. The cleanest path that preserves
       AppViewModel as the coordinator is to obtain MetricsViewModel/SettingsViewModel through Hilt rather than
       `MetricsViewModel(app)`. Per D-09, AppViewModel may receive them via constructor injection (make AppViewModel
       itself @HiltViewModel @Inject constructor receiving the sub-ViewModels) OR the Activity obtains the
       sub-ViewModels via hiltViewModel()/viewModels() and passes them in. Choose the approach that compiles with
       the least churn; document the chosen approach in the SUMMARY.

    D-09a FALLBACK (apply ONLY if Hilt wiring does not compile after a reasonable attempt): revert the Hilt
    plumbing/annotations for the sub-ViewModels, keep `private val metricsViewModel`/`private val settingsViewModel`
    in AppViewModel (encapsulation improvement that still partially satisfies the module-org finding), and record
    A-09 as DEFERRED with the exact build error in 128-03-SUMMARY.md and (per CONTEXT) VERIFICATION.md. Do not
    leave the build in a broken state.

    4. CI VERIFICATION (run regardless of path): from the android/ directory run the debug assemble to prove the
       whole Phase 128 Android module (all nine fixes across plans 01/02/03) compiles. Capture the result.
  </action>
  <verify>
    <automated>cd android &amp;&amp; ./gradlew :app:assembleDebug --console=plain -q 2>&amp;1 | tail -5; ./gradlew :app:assembleDebug --console=plain -q &gt;/dev/null 2>&amp;1 &amp;&amp; echo BUILD_PASS || echo BUILD_FAIL_CHECK_FALLBACK</automated>
  </verify>
  <acceptance_criteria>
    - PRIMARY: MainActivity @AndroidEntryPoint; MetricsViewModel + SettingsViewModel are @HiltViewModel with @Inject constructors; AppViewModel no longer constructs them via raw constructor; `./gradlew :app:assembleDebug` succeeds (BUILD_PASS)
    - OR FALLBACK (D-09a): sub-ViewModels are `private`, A-09 is documented as DEFERRED with the build error in the SUMMARY and VERIFICATION.md, and `./gradlew :app:assembleDebug` still succeeds
    - The Android module compiles in either case (no broken build is committed)
  </acceptance_criteria>
  <done>Sub-ViewModels are Hilt-managed (or cleanly deferred per D-09a), MainActivity is an @AndroidEntryPoint, and assembleDebug confirms the full Phase 128 Android module builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build toolchain -> APK | New plugins (Hilt/KSP) and codegen enter the build; a misconfigured version breaks CI |
| DI container -> ViewModel lifecycle | Hilt now owns sub-ViewModel construction instead of manual `new` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-07 | Denial of service (build break) | Hilt/KSP version mismatch with Kotlin 2.4.0 | medium | mitigate | Pin catalog versions; assembleDebug gate; D-09a fallback if it fails (Task 2) |
| T-128-08 | Elevation of privilege | Sub-ViewModels bypassing ViewModelProvider lifecycle | medium | mitigate | @HiltViewModel under ViewModelProvider, or private+deferred (Task 2) |
| T-128-SC | Tampering | Gradle/Maven dependency installs (hilt, ksp) | high | mitigate | Pin exact versions from official coordinates (com.google.dagger, com.google.devtools.ksp); legitimate, well-known publishers; assembleDebug verifies resolution |
</threat_model>

<verification>
- Version catalog + Gradle files declare Hilt and KSP (A-09 plumbing)
- GooseApplication @HiltAndroidApp registered in manifest (A-09)
- MetricsViewModel/SettingsViewModel @HiltViewModel + MainActivity @AndroidEntryPoint, OR D-09a fallback applied and documented
- `./gradlew :app:assembleDebug` succeeds — the single end-to-end compile gate for all nine Phase 128 fixes
</verification>

<success_criteria>
A-09 is satisfied via Hilt-managed sub-ViewModels (or the D-09a fallback is cleanly applied and documented), and the entire Phase 128 Android module compiles via assembleDebug, confirming the wave-1 fixes from plans 128-01 and 128-02 integrate without regression.
</success_criteria>

<output>
Create `.planning/phases/128-android-architecture-best-practices-fixes/128-03-SUMMARY.md` when done
</output>
