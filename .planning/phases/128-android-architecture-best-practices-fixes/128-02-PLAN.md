---
phase: 128-android-architecture-best-practices-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 128-01
files_modified:
  - android/app/src/main/kotlin/com/goose/app/MainActivity.kt
  - android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt
  - android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt
  - android/app/src/main/kotlin/com/goose/app/upload/GooseUploadClient.kt
  - android/app/src/main/kotlin/com/goose/app/ble/WhoopBleClient.kt
  - android/app/src/main/kotlin/com/goose/app/ui/AppShell.kt
  - android/app/src/main/kotlin/com/goose/app/ui/HomeScreen.kt
  - android/app/src/main/kotlin/com/goose/app/ui/HealthScreen.kt
  - android/app/src/main/kotlin/com/goose/app/ui/MoreScreen.kt
autonomous: true
requirements:
  - RUST-AUD-02
  - BP-AND-01

must_haves:
  truths:
    - "MainActivity collects every StateFlow exposed to Compose with collectAsStateWithLifecycle(), so the UI recomposes on state changes and no StateFlow<T> is passed down to AppShell or child screens"
    - "AppViewModel.bleClient is private; the UI reaches BLE state only through ViewModel-exposed StateFlows and methods"
    - "MetricsViewModel.queryScore logs exceptions and ok:false bridge responses via Log.e instead of swallowing them silently"
    - "GooseUploadClient exposes upload progress/errors via an object-level uploadState MutableStateFlow set at every upload() exit path (Uploading at start; Error on get-streams failure; Success(0) on no-pending; Success(count)/Error on the HTTP result; Error in the outer catch), republished by AppViewModel as uploadStatus: StateFlow<UploadState> and observed by MoreScreen"
    - "The deprecated onSyncComplete stub left in WhoopBleClient by plan 128-01 is deleted in this plan; no onSyncComplete reference remains anywhere under android/app/src/main/kotlin"
  artifacts:
    - "android/app/src/main/kotlin/com/goose/app/MainActivity.kt"
    - "android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt"
    - "android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt"
    - "android/app/src/main/kotlin/com/goose/app/upload/GooseUploadClient.kt"
  key_links:
    - "uploadStatus StateFlow connects GooseUploadClient.uploadState -> AppViewModel -> MoreScreen"
    - "AppShell + child-screen (HomeScreen/HealthScreen/MoreScreen) signatures change from StateFlow<T> params to resolved value params so MainActivity owns the lifecycle-aware collection"
    - "AppViewModel collects WhoopBleClient.syncCompleteEvent (added by plan 128-01) in viewModelScope"
---

<objective>
Fix the four Compose/ViewModel/error-handling findings from the Phase 127 audit (A-04 StateFlow collection, A-05 bleClient visibility, A-06 queryScore swallowing, A-08 observable upload state) without changing app behaviour.

Purpose: Make Compose recompose correctly, enforce ViewModel lifecycle boundaries, surface metric-bridge failures (BP-AND-01), and expose upload progress to the UI (RUST-AUD-02).
Output: MainActivity that collects all StateFlows with lifecycle awareness, child screens that receive resolved values, a private bleClient, a logging queryScore, and an observable uploadStatus pipeline ending at MoreScreen.

This is the wave-2 Compose/ViewModel plan. It runs AFTER plan 128-01 has landed because AppViewModel consumes WhoopBleClient.syncCompleteEvent (the SharedFlow introduced by 128-01). Running it in parallel with 128-01 would fail to compile (cross-AI review HIGH-4).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md
@.planning/phases/127-multi-model-code-audit/127-AUDIT-REPORT.md
@.planning/phases/128-android-architecture-best-practices-fixes/128-01-SUMMARY.md
@android/app/src/main/kotlin/com/goose/app/bridge/GooseBridge.kt
</context>

<interface_context>
Current Compose wiring (verified during planning):
- MainActivity.onCreate collects only connectionState with collectAsStateWithLifecycle() (line 21);
  the other five fields (liveHeartRateBPM, recoveryScore, strainScore, sleepScore, serverUrl) are passed
  to AppShell as raw StateFlow<T> (lines 22-26).
- AppShell signature (lines 24-32) declares liveHeartRateBPM/recoveryScore/strainScore/sleepScore as
  StateFlow<T> and serverUrl as StateFlow<String>; it forwards these StateFlows to HomeScreen
  (line 66, liveHeartRateBPM: StateFlow<Int?>), HealthScreen (lines 71-76, three StateFlow<Float?>),
  and MoreScreen (line 80, serverUrl: StateFlow<String>).
- HomeScreen (line 22) collects liveHeartRateBPM itself; HealthScreen (lines 25-27) collects its three
  flows itself; MoreScreen (line 23) collects serverUrl itself.

This plan moves ALL lifecycle-aware collection up into MainActivity. After this plan:
- AppShell receives resolved values (Int?, Float?, String, and the new UploadState) instead of
  StateFlow<T>; zero StateFlow<T> parameters remain in AppShell or any child screen signature.
- HomeScreen / HealthScreen / MoreScreen signatures change from StateFlow<T> to the resolved value type;
  each drops its own collectAsStateWithLifecycle() call.

Interface added by plan 128-01 (already landed in wave 1):
- WhoopBleClient.syncCompleteEvent: SharedFlow<Unit> is the sync-completion signal; AppViewModel collects it
  in viewModelScope.
- WhoopBleClient still carries a `@Deprecated` no-op `onSyncComplete` stub that plan 128-01 retained ONLY
  to keep wave 1 compilable in isolation (cross-AI review HIGH-3). THIS plan (128-02) must DELETE that stub
  from WhoopBleClient.kt as part of Task 2, together with the AppViewModel assignment, so the dead callback
  surface does not ship. WhoopBleClient.kt is therefore added to this plan's files_modified for the single
  purpose of removing the stub property and its @Deprecated annotation/import.

No-Compose-Preview note (cross-AI review HIGH-5, Codex): AppShell.kt, HomeScreen.kt, HealthScreen.kt, and
MoreScreen.kt contain NO `@Preview` composables and NO `MutableStateFlow(...)` literals (verified during
planning: `grep -rn '@Preview\|MutableStateFlow' ui/` returns nothing). Therefore the signature changes
from StateFlow<T> to value types cannot break any preview function. No preview-update task is required; the
executor must NOT invent preview functions. This concern is documented as not-applicable to this codebase.

New public AppViewModel surface consumed by MainActivity:
- `val uploadStatus: StateFlow<UploadState>` (UploadState sealed class defined in AppViewModel.kt or a
  sibling UploadState.kt).

GooseUploadClient is a singleton `object` (object GooseUploadClient). Per cross-AI review MEDIUM, it
exposes a companion-level `val uploadState: MutableStateFlow<UploadState>` directly on the object (the
simplest mechanism — no refactor to a class). AppViewModel reads/republishes it. The process-global
nature is acceptable for a single-upload-at-a-time client.

GooseBridge.safeHandle() returns ok:false JSON on failure and never throws — callers branch on `ok`.
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lifecycle-collect all StateFlows in MainActivity, push resolved values to child screens, make bleClient private (A-04, A-05)</name>
  <files>android/app/src/main/kotlin/com/goose/app/MainActivity.kt, android/app/src/main/kotlin/com/goose/app/ui/AppShell.kt, android/app/src/main/kotlin/com/goose/app/ui/HomeScreen.kt, android/app/src/main/kotlin/com/goose/app/ui/HealthScreen.kt, android/app/src/main/kotlin/com/goose/app/ui/MoreScreen.kt, android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt</files>
  <read_first>
    - android/app/src/main/kotlin/com/goose/app/MainActivity.kt (lines 21-36: only connectionState is collected; the other five are passed raw)
    - android/app/src/main/kotlin/com/goose/app/ui/AppShell.kt (signature at 24-32; HomeScreen call at 66, HealthScreen call at 71-76, MoreScreen call at 78-82)
    - android/app/src/main/kotlin/com/goose/app/ui/HomeScreen.kt (signature at 19-23 takes liveHeartRateBPM: StateFlow<Int?>; collects at line 24)
    - android/app/src/main/kotlin/com/goose/app/ui/HealthScreen.kt (signature at 19-24 takes three StateFlow<Float?>; collects at lines 25-27)
    - android/app/src/main/kotlin/com/goose/app/ui/MoreScreen.kt (signature at 18-22 takes serverUrl: StateFlow<String>; collects at line 23)
    - android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt (bleClient public val at line 26)
    - .planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Implement D-04 (collectAsStateWithLifecycle for all flows, in MainActivity only) and D-05 (private bleClient).

    D-04: In MainActivity.onCreate's setContent block, replace the five raw StateFlow pass-throughs
    (liveHeartRateBPM, recoveryScore, strainScore, sleepScore, serverUrl) with
    `by appViewModel.<flow>.collectAsStateWithLifecycle()` resolved values, matching the existing
    connectionState pattern already in place (line 21).

    Change the AppShell composable signature so ALL these parameters become resolved values (full list,
    cross-AI review MEDIUM — the AppShell signature change must be stated explicitly, not inferred):
    `liveHeartRateBPM: Int?, recoveryScore: Float?, strainScore: Float?, sleepScore: Float?,
    serverUrl: String` — instead of StateFlow<T>. (uploadStatus: UploadState is added to AppShell's
    signature in Task 2 — Task 1 leaves it out and Task 2 extends the signature.) Remove the
    `import kotlinx.coroutines.flow.StateFlow` from AppShell.kt (no longer needed). Update AppShell's
    MoreScreen/HomeScreen/HealthScreen call sites to pass the resolved values straight through to the child
    screens (no intermediate collection inside AppShell).

    Cascade the SAME change into the three child screens AppShell calls (HIGH-3 — these files were
    previously missing from the plan and MUST be updated or compilation breaks):
      - HomeScreen.kt: change `liveHeartRateBPM: StateFlow<Int?>` to `liveHeartRateBPM: Int?`, delete the
        `val hr by liveHeartRateBPM.collectAsStateWithLifecycle()` line and use the parameter directly as
        `hr`; remove the now-unused collectAsStateWithLifecycle and StateFlow imports.
      - HealthScreen.kt: change the three `StateFlow<Float?>` params to `Float?`, delete the three
        `.collectAsStateWithLifecycle()` lines and use the parameters directly; remove the now-unused
        collectAsStateWithLifecycle and StateFlow imports.
      - MoreScreen.kt: change `serverUrl: StateFlow<String>` to `serverUrl: String`, delete the
        `val url by serverUrl.collectAsStateWithLifecycle()` line and bind the OutlinedTextField `value`
        to the `serverUrl` parameter directly; remove the now-unused collectAsStateWithLifecycle and
        StateFlow imports.
    Keep onServerUrlChange and connectionState wiring unchanged (connectionState is already a resolved
    value passed through AppShell to HomeScreen).

    D-05: Change `val bleClient = WhoopBleClient(...)` to `private val bleClient = WhoopBleClient(...)`
    in AppViewModel. The existing delegated StateFlows (connectionState, liveHeartRateBPM) and onCleared()
    teardown already go through the ViewModel, so no UI call site should reference bleClient directly. If
    any reference outside AppViewModel exists, route it through a new ViewModel method rather than
    re-exposing the client.
  </action>
  <verify>
    <automated>cd android &amp;&amp; grep -q 'private val bleClient' app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt &amp;&amp; test "$(grep -c 'collectAsStateWithLifecycle' app/src/main/kotlin/com/goose/app/MainActivity.kt)" -ge 6 &amp;&amp; ! grep -rn 'appViewModel.bleClient' app/src/main/kotlin &amp;&amp; ! grep -q 'StateFlow' app/src/main/kotlin/com/goose/app/ui/AppShell.kt &amp;&amp; ! grep -q 'StateFlow' app/src/main/kotlin/com/goose/app/ui/HomeScreen.kt &amp;&amp; ! grep -q 'StateFlow' app/src/main/kotlin/com/goose/app/ui/HealthScreen.kt &amp;&amp; ! grep -q 'StateFlow' app/src/main/kotlin/com/goose/app/ui/MoreScreen.kt &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - MainActivity calls collectAsStateWithLifecycle() for all six exposed flows (connectionState + the five previously raw)
    - AppShell, HomeScreen, HealthScreen, and MoreScreen signatures take resolved values, not StateFlow<T> — zero StateFlow references remain in those four files
    - `private val bleClient` in AppViewModel; no `appViewModel.bleClient` references anywhere in the UI layer
    - Child screens no longer call collectAsStateWithLifecycle() themselves
  </acceptance_criteria>
  <done>Every Compose-consumed StateFlow is collected once in MainActivity with lifecycle awareness, AppShell and all three child screens receive resolved values (no StateFlow<T> in any of the four signatures), and bleClient is private to AppViewModel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Log queryScore failures and add an observable uploadStatus pipeline (A-06, A-08)</name>
  <files>android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt, android/app/src/main/kotlin/com/goose/app/upload/GooseUploadClient.kt, android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt, android/app/src/main/kotlin/com/goose/app/MainActivity.kt, android/app/src/main/kotlin/com/goose/app/ui/AppShell.kt, android/app/src/main/kotlin/com/goose/app/ui/MoreScreen.kt</files>
  <read_first>
    - android/app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt (queryScore at 51-63 — catch (_: Exception) { null } swallows everything; ok:false returns null silently at 56)
    - android/app/src/main/kotlin/com/goose/app/upload/GooseUploadClient.kt (object singleton; upload() at 33; get-streams ok:false at 47; empty-result at 53; success post at 61; catch at 63)
    - android/app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt (triggerUpload at 55-59; init wires onSyncComplete at 44-50 — replace with syncCompleteEvent collection)
    - android/app/src/main/kotlin/com/goose/app/ui/MoreScreen.kt (where uploadStatus will be displayed; signature updated in Task 1 to take resolved values)
    - .planning/phases/128-android-architecture-best-practices-fixes/128-CONTEXT.md (D-06, D-08 and the <specifics> UploadState shape)
    - .planning/phases/128-android-architecture-best-practices-fixes/128-01-SUMMARY.md (syncCompleteEvent SharedFlow signature)
  </read_first>
  <behavior>
    - queryScore: when the bridge response has ok:false, the error path runs and the operator sees a Log.e
      diagnostic with the error message; the function still returns null (no crash, no behavioural regression).
    - queryScore: when GooseBridge.safeHandle / JSON parsing raises, the catch logs via Log.e and returns null.
    - UploadState progression for a successful upload: Idle -> Uploading -> Success(count).
    - UploadState progression for a failed upload: Idle -> Uploading -> Error(message).
    - AppViewModel.uploadStatus reflects the latest UploadState; MoreScreen renders it.
  </behavior>
  <action>
    Implement D-06 (queryScore logging) and D-08 (observable upload state).

    D-06: In MetricsViewModel.queryScore, replace `catch (_: Exception) { null }` with
    `catch (e: Exception) { Log.e("MetricsViewModel", "queryScore failed method=$method", e); null }`.
    Also handle ok:false explicitly: when `json.optBoolean("ok", false)` is false, read the error message
    (`json.optJSONObject("error")?.optString("message")`) and Log.e it before returning null. Add
    `import android.util.Log`. Do not change the success path or the returned Float? contract.

    D-08: Define a `sealed class UploadState` with the shape from the <specifics>:
    `object Idle`, `object Uploading`, `data class Success(val count: Int)`, `data class Error(val msg: String)`
    (place it in AppViewModel.kt or a sibling UploadState.kt — Claude's discretion per CONTEXT).

    GooseUploadClient observable state (MEDIUM fix — singleton object, simplest mechanism, with EXACT
    per-exit-path emit assignments per cross-AI review): GooseUploadClient is an `object`. Add at object level
    `private val _uploadState = MutableStateFlow<UploadState>(UploadState.Idle)` and
    `val uploadState: StateFlow<UploadState> = _uploadState.asStateFlow()`. Do NOT refactor it into a class.
    The current upload() has the result-fetch/skip logic; postToServer() performs the HTTP call and currently
    swallows the outcome (it only logs). To make Success/Error observable per the actual HTTP result,
    postToServer() must return its outcome to upload() (e.g. change its signature to return the HTTP response
    code as Int, or -1 on IOException) so upload() can set the terminal state. Set `_uploadState.value` at
    EACH exit path of upload() exactly as follows (5 paths total — leave the empty-serverUrl skip emitting
    NOTHING so a disabled upload does not flip the UI):
      - empty serverUrl skip (line 34-37): no state change (return early, stays Idle).
      - immediately after the empty-serverUrl skip, before the bridge call: `_uploadState.value = UploadState.Uploading`.
      - get-streams ok:false branch (line 47-50): `_uploadState.value = UploadState.Error(<error message>)` then return.
      - no-pending-frames branch (result null/empty, line 53-56): `_uploadState.value = UploadState.Success(0)` then return.
      - successful path: capture postToServer()'s returned HTTP code; if it is in 200..299
        `_uploadState.value = UploadState.Success(count)` else `_uploadState.value = UploadState.Error("HTTP $code")`.
        `count` = number of decoded streams uploaded, derived from the `result` JSONObject (e.g. `result.length()`,
        the number of top-level stream keys; Claude's discretion if the payload shape differs — use the most
        meaningful integer available, never a hardcoded constant).
      - outer catch (Exception, line 63-65): `_uploadState.value = UploadState.Error(e.message ?: "upload failed")`.
    Keep all existing Log.* lines and the empty-serverUrl skip. Add imports for MutableStateFlow/StateFlow/
    asStateFlow and the UploadState type. (UploadState is in com.goose.app.viewmodel; import accordingly.)

    AppViewModel wiring: add `val uploadStatus: StateFlow<UploadState> = GooseUploadClient.uploadState`
    (republish the object's StateFlow directly — no extra MutableStateFlow needed since the object owns it).
    Replace the `bleClient.onSyncComplete = { metricsViewModel.refresh(); triggerUpload() }` init block
    (AppViewModel.kt:44-50) with collection of `bleClient.syncCompleteEvent` (the SharedFlow from plan 128-01)
    in viewModelScope. Dispatcher safety (cross-AI review MEDIUM): the collector itself runs on the Main
    dispatcher (viewModelScope default), which is correct for the suspend collect, but `triggerUpload()`
    already launches its network work on `Dispatchers.IO` (AppViewModel.kt:56) and `metricsViewModel.refresh()`
    must not block the main thread — keep refresh()'s existing internal threading; if refresh() does any
    blocking JNI/IO inline, wrap that emission body in `viewModelScope.launch(Dispatchers.IO) { ... }` rather
    than running it on Main. Structure as:
    `viewModelScope.launch { bleClient.syncCompleteEvent.collect { metricsViewModel.refresh(); triggerUpload() } }`.
    triggerUpload() keeps calling `GooseUploadClient.upload(getApplication(), serverUrl.value)` on
    Dispatchers.IO; the upload() now drives uploadState internally so no callback wiring is needed here.

    Delete the deprecated stub from WhoopBleClient (cross-AI review HIGH-3 completion): now that the
    AppViewModel assignment is gone, remove the `@Deprecated(...) var onSyncComplete: (() -> Unit)? = null`
    property that plan 128-01 left in WhoopBleClient.kt (and its now-unused @Deprecated reference). After this,
    `grep onSyncComplete` over the whole android/ source tree must return nothing. This is the only change this
    plan makes to WhoopBleClient.kt.

    MoreScreen status display: surface uploadStatus in MoreScreen by adding an `uploadStatus: UploadState`
    parameter (resolved value). Collect it in MainActivity with collectAsStateWithLifecycle() (the Task 1
    pattern), thread it through AppShell -> MoreScreen, and render a short status line in MoreScreen
    (e.g. "Uploading...", "Uploaded N", "Upload error: ...", or nothing for Idle). Update AppShell's
    signature and its MoreScreen call site to pass the resolved uploadStatus value.
  </action>
  <verify>
    <automated>cd android &amp;&amp; grep -q 'queryScore failed' app/src/main/kotlin/com/goose/app/viewmodel/MetricsViewModel.kt &amp;&amp; grep -rq 'sealed class UploadState' app/src/main/kotlin/com/goose/app/viewmodel/ &amp;&amp; grep -q 'val uploadState' app/src/main/kotlin/com/goose/app/upload/GooseUploadClient.kt &amp;&amp; grep -q 'UploadState.Uploading' app/src/main/kotlin/com/goose/app/upload/GooseUploadClient.kt &amp;&amp; grep -q 'val uploadStatus' app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt &amp;&amp; grep -q 'syncCompleteEvent' app/src/main/kotlin/com/goose/app/viewmodel/AppViewModel.kt &amp;&amp; ! grep -rq 'onSyncComplete' app/src/main/kotlin &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - queryScore logs via Log.e on both the exception path and the ok:false path; still returns null
    - A `sealed class UploadState` with Idle/Uploading/Success/Error exists
    - GooseUploadClient exposes `uploadState: StateFlow<UploadState>` and sets it at each of the 5 upload() exit paths (Uploading at start; Error on get-streams ok:false; Success(0) on no-pending; Success(count)/Error on HTTP result; Error in outer catch); empty-serverUrl skip leaves state unchanged (object, not refactored to a class)
    - AppViewModel exposes `uploadStatus: StateFlow<UploadState>` and collects syncCompleteEvent in viewModelScope; the onSyncComplete assignment is removed AND the deprecated stub is deleted from WhoopBleClient.kt (no `onSyncComplete` anywhere under app/src/main/kotlin)
    - AppShell signature carries `uploadStatus: UploadState` (resolved value) threaded MainActivity -> AppShell -> MoreScreen
    - MoreScreen renders the current upload status (resolved UploadState param, collected in MainActivity)
  </acceptance_criteria>
  <done>Metric-bridge failures are logged, upload progress/errors flow from GooseUploadClient.uploadState through AppViewModel.uploadStatus to MoreScreen, and the sync-complete trigger is driven by the syncCompleteEvent SharedFlow from plan 128-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel -> Compose UI | StateFlows cross into the Compose runtime; incorrect collection drops updates |
| UI -> BLE client | Public bleClient lets the UI bypass ViewModel lifecycle guards |
| App -> self-hosted server | Upload HTTP I/O crosses the network boundary; failures must be visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-04 | Tampering | UI bypassing ViewModel via public bleClient | medium | mitigate | Make bleClient private; route access through ViewModel (Task 1) |
| T-128-05 | Information disclosure (loss) | queryScore swallowing bridge errors | medium | mitigate | Log.e on exception and ok:false (Task 2) |
| T-128-06 | Information disclosure (loss) | invisible upload failures | medium | mitigate | Observable UploadState surfaced in MoreScreen (Task 2) |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (Kotlin source only) |
</threat_model>

<verification>
- `grep -c collectAsStateWithLifecycle MainActivity.kt` >= 6 (A-04)
- Zero StateFlow<T> parameters in AppShell/HomeScreen/HealthScreen/MoreScreen signatures (A-04, cross-AI review MEDIUM)
- `private val bleClient` present; no `appViewModel.bleClient` references (A-05)
- queryScore Log.e on failure paths (A-06, BP-AND-01)
- `sealed class UploadState` + GooseUploadClient.uploadState (set at all 5 upload() exit paths) + `uploadStatus: StateFlow<UploadState>` present; threaded MainActivity -> AppShell -> MoreScreen; MoreScreen renders it (A-08)
- onSyncComplete removed from AppViewModel AND its deprecated stub deleted from WhoopBleClient.kt; `grep -r onSyncComplete app/src/main/kotlin` returns nothing; syncCompleteEvent collected in viewModelScope instead (A-07 consumer side, cross-AI review HIGH-3)
- Android compilation is verified end-to-end in plan 128-03 after all wave-1 and wave-2 changes land
</verification>

<success_criteria>
All four Compose/ViewModel audit findings (A-04, A-05, A-06, A-08) are addressed per D-04/D-05/D-06/D-08; the UI recomposes correctly with no StateFlow passthrough, bleClient is encapsulated, metric failures are logged, and upload status is observable end-to-end via GooseUploadClient.uploadState.
</success_criteria>

<output>
Create `.planning/phases/128-android-architecture-best-practices-fixes/128-02-SUMMARY.md` when done
</output>
