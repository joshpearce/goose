---
phase: 23-strain-calories
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - Rust/core/src/metrics.rs
  - Rust/core/src/bridge.rs
autonomous: true
requirements: [ALG-STR-02, ALG-STR-03]
must_haves:
  truths:
    - "Banister TRIMP uses sex-specific b constants (1.92 male / 1.67 female / 1.795 unknown) so male and female TRIMP differ for an identical HR trace"
    - "banister_trimp_zone_midpoint_approximation quality flag is always emitted"
    - "goose_strain_v1 bridge method returns both Edwards and Banister scores"
    - "fit_strain_denominator fits D in 21*ln(TRIMP+1)/ln(D) from >= 2 pairs via least squares and is callable as metrics.fit_strain_denominator"
  artifacts:
    - path: "Rust/core/src/metrics.rs"
      provides: "banister_trimp_zone_midpoint, fit_strain_denominator, goose_strain_v1, GOOSE_STRAIN_V1_ID"
      contains: "fn banister_trimp_zone_midpoint"
    - path: "Rust/core/src/bridge.rs"
      provides: "metrics.goose_strain_v1 and metrics.fit_strain_denominator dispatch + BRIDGE_METHODS entries"
      contains: "metrics.fit_strain_denominator"
  key_links:
    - from: "Rust/core/src/bridge.rs"
      to: "goose_strain_v1"
      via: "match arm dispatch on metrics.goose_strain_v1"
      pattern: "metrics.goose_strain_v1"
    - from: "goose_strain_v1"
      to: "resolve_effective_hrmax"
      via: "effective HRmax feeds Banister HRR fractions"
      pattern: "resolve_effective_hrmax"
---

<objective>
Implement Banister TRIMP with sex-specific constants and a denominator-calibration helper, exposed through a new `goose_strain_v1` bridge method (ALG-STR-02, ALG-STR-03).

Purpose: The fork's strain uses only the Edwards zone-load model. WHOOP-aligned strain uses a Banister TRIMP with sex-dependent exponential weighting and a fitted logarithmic denominator. This plan delivers both, alongside the existing Edwards output, and a calibration helper to fit the denominator from real (TRIMP, WHOOP-strain) pairs.
Output: `banister_trimp_zone_midpoint`, `fit_strain_denominator`, `goose_strain_v1` (emits Edwards + Banister), and two new bridge methods.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-strain-calories/23-CONTEXT.md
@.planning/phases/23-strain-calories/23-01-SUMMARY.md

# Strain code + the helpers added in 23-01
# StrainInput (~line 397), goose_strain_v0 (~line 1777), GOOSE_STRAIN_V0_ID (line 17)
@Rust/core/src/metrics.rs

# Bridge registration pattern:
#   - BRIDGE_METHODS sorted array (~line 176, entries ~243-245)
#   - import line ~102, dispatch arms ~2149-2154 (goose_sleep_v1 / goose_strain_v0)
@Rust/core/src/bridge.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: banister_trimp_zone_midpoint + fit_strain_denominator</name>
  <files>Rust/core/src/metrics.rs</files>
  <read_first>
    - `goose_strain_v0` (~line 1777) — reuse `hr_zone_minutes` (5-zone) layout and validation idioms
    - 23-01 helpers `resolve_effective_hrmax`, `tanaka_hrmax`
  </read_first>
  <behavior>
    - Test: `banister_trimp_zone_midpoint` with identical zone minutes but sex "male" vs "female" returns different totals (male b=1.92 > female b=1.67 → male TRIMP larger for the same HRR fractions).
    - Test: unknown/None sex uses b=1.795 (mean of 1.92 and 1.67), and result lies strictly between the male and female results.
    - Test: zone-midpoint HRs use 55/65/75/85/95% of HRmax; HRR fraction x_i = (zone_mid_hr - resting_hr)/(hrmax - resting_hr); total = Σ minutes_i * x_i * 0.64 * exp(b * x_i) over 5 zones.
    - Test: `fit_strain_denominator` with two synthetic pairs generated from a known D (e.g. D=7201, strain=21*ln(TRIMP+1)/ln(D)) recovers D within tolerance via least squares.
    - Test: `fit_strain_denominator` with fewer than 2 pairs returns None.
  </behavior>
  <action>
    Implement `pub fn banister_trimp_zone_midpoint(hr_zone_minutes: &[f64], resting_hr_bpm: f64, hrmax: f64, sex: Option<&str>) -> f64` (per ALG-STR-02): zone midpoints as % of HRmax = [0.55, 0.65, 0.75, 0.85, 0.95]; for each zone compute `zone_mid_hr = pct * hrmax`, `x = ((zone_mid_hr - resting_hr) / (hrmax - resting_hr)).clamp(0.0, 1.0)`, accumulate `minutes * x * 0.64 * exp(b * x)`. Select `b` by sex: "male"→1.92, "female"→1.67, else→1.795 (mean per 23-CONTEXT specifics, exact f64). Implement `pub fn fit_strain_denominator(pairs: &[(f64, f64)]) -> Option<f64>` (per ALG-STR-03): require >= 2 pairs, fit D minimizing Σ(21*ln(TRIMP_i+1)/ln(D) - whoop_strain_i)². Because `strain = C * (1/ln D)` is linear in `m = 1/ln(D)` with `C_i = 21*ln(TRIMP_i+1)`, solve closed-form least squares for `m = Σ(C_i*s_i)/Σ(C_i²)`, then `D = exp(1/m)`; guard non-finite/degenerate inputs → None. Add unit tests for all <behavior> cases.
  </action>
  <verify>
    <automated>cargo test -p goose-core banister_trimp fit_strain_denominator -- --nocapture</automated>
  </verify>
  <done>banister_trimp_zone_midpoint shows the male>unknown>female ordering; fit_strain_denominator recovers a known D; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: goose_strain_v1 (Edwards + Banister) + two bridge methods</name>
  <files>Rust/core/src/metrics.rs, Rust/core/src/bridge.rs</files>
  <read_first>
    - `goose_strain_v0` (~line 1777) for AlgorithmRunResult shape and quality_flags/provenance idioms
    - `bridge.rs` BRIDGE_METHODS array (~176), import line ~102, dispatch arms ~2149-2154
  </read_first>
  <behavior>
    - Test: `goose_strain_v1` output contains both an Edwards-derived score (reusing goose_strain_v0 zone-load logic) and a Banister score; quality_flags always contains "banister_trimp_zone_midpoint_approximation".
    - Test: goose_strain_v1 uses resolve_effective_hrmax and reports hrmax_source in provenance.
    - Test: bridge request `{"method":"metrics.fit_strain_denominator","args":{"pairs":[[t1,s1],[t2,s2]]}}` returns the fitted D (pure computation, no DB access).
    - Test: bridge request `{"method":"metrics.goose_strain_v1","args":{<StrainInput>}}` returns a result with both scores.
    - Test: "metrics.fit_strain_denominator" and "metrics.goose_strain_v1" are present in BRIDGE_METHODS and the array stays sorted.
  </behavior>
  <action>
    Add `GOOSE_STRAIN_V1_ID = "goose.strain.v1"` and a matching version const. Implement `pub fn goose_strain_v1(input: &StrainInput) -> AlgorithmRunResult<StrainScoreOutput>`: resolve effective HRmax via `resolve_effective_hrmax(input.max_hr_bpm, input.profile_age, &[])` (history empty for now — session max + Tanaka), compute the Edwards zone-load score (reuse v0 logic) AND `banister_trimp_zone_midpoint(...)` using `input.profile_sex.as_deref()`; surface both in the output (Banister as an additional ScoreComponent and/or default D=7201 logarithmic strain `21*ln(TRIMP+1)/ln(7201)`); always push the `banister_trimp_zone_midpoint_approximation` quality flag; record `hrmax_source` and the b-constant in provenance. Add a serde args struct `FitStrainDenominatorArgs { pairs: Vec<(f64, f64)>, #[serde(default)] database_path: Option<String> }`. In `bridge.rs`: add `goose_strain_v1` to the metrics import (~line 102); add `"metrics.fit_strain_denominator"` and `"metrics.goose_strain_v1"` to BRIDGE_METHODS preserving sort order; add dispatch arms next to the existing strain arm (~2153) — strain_v1 via `request_args::<StrainInput>`, fit via `request_args::<FitStrainDenominatorArgs>` calling `fit_strain_denominator` and returning JSON `{ "denominator": D }` (or error JSON when None). Add unit + bridge round-trip tests per <behavior>.
  </action>
  <verify>
    <automated>cargo test -p goose-core goose_strain_v1 fit_strain_denominator bridge -- --nocapture</automated>
  </verify>
  <done>goose_strain_v1 emits Edwards+Banister with the approximation flag; both bridge methods dispatch and round-trip; BRIDGE_METHODS sorted; cargo test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust FFI (JSON) | StrainInput and calibration pairs arrive as untrusted JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | fit_strain_denominator least-squares | mitigate | guard < 2 pairs, non-finite, and degenerate Σ(C²)=0 → return None instead of NaN/Inf |
| T-23-04 | Denial of Service | banister exp(b*x) overflow | mitigate | x clamped [0,1] so exp(b*x) bounded (<= e^1.92); zone minutes non-negative validated |
</threat_model>

<verification>
- `cargo test -p goose-core` green.
- `BRIDGE_METHODS` remains sorted (existing list-ordering test, if present, passes).
- goose_strain_v0 unchanged and still passing.
</verification>

<success_criteria>
- Male vs female Banister TRIMP differ for identical HR traces (sex constants applied).
- banister_trimp_zone_midpoint_approximation quality flag always present.
- goose_strain_v1 returns Edwards + Banister scores; callable via bridge.
- metrics.fit_strain_denominator fits D from >= 2 pairs and is callable.
</success_criteria>

<artifacts_this_phase_produces>
- `Rust/core/src/metrics.rs`: `banister_trimp_zone_midpoint`, `fit_strain_denominator`, `goose_strain_v1`, `GOOSE_STRAIN_V1_ID`.
- `Rust/core/src/bridge.rs`: `metrics.goose_strain_v1`, `metrics.fit_strain_denominator` dispatch + BRIDGE_METHODS entries.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/23-strain-calories/23-02-SUMMARY.md` when done.
</output>
