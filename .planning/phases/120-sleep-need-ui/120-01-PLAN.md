---
phase: "120"
plan: "120-01"
type: execute
wave: 1
depends_on: ["Phase 114"]
files_modified:
  - GooseSwift/HealthDataStore.swift
  - GooseSwift/HealthDataStore+Sleep.swift
  - GooseSwift/HealthDataStore+Snapshots.swift
  - GooseSwift/HealthDataStore+Utilities.swift
  - GooseSwift/HealthSleepOverviewViews.swift
  - GooseSwift/HealthSleepSheetsViews.swift
  - GooseSwift/SleepV2ScheduleViews.swift
autonomous: true
requirements: [SLP-NEED-03]

must_haves:
  truths:
    - "dynamicSleepNeed is populated from sleep.compute_need bridge on Sleep tab onAppear and after band sync"
    - "sleep_need_minutes fallback is 450.0 (not 480.0) at all four call sites: Snapshots:28, Snapshots:68, Utilities:128, Utilities:153"
    - "SleepV2SleepNeededSheet displays dynamic need label (e.g. '7h 45m recommended tonight') and breakdown row; both hidden when dynamicSleepNeed is nil"
    - "SleepV2SleepNeededSheet has its own .task trigger for runDynamicSleepNeed() so data loads if sheet opens before overview onAppear fires"
    - "SleepV2SleepWindowCard shows dynamic value at line 52 (action-row) and line 366 (clock-ring VStack label); both replaced — no hardcoded '7h 39m' remains in SleepV2ScheduleViews.swift"
    - "runDynamicSleepNeed() is called FIRST in refreshSleepAfterBandSync, before runPacketInputs()"
    - "iOS build compiles; #Preview macro compiles in DEBUG"
  artifacts:
    - "GooseSwift/HealthDataStore.swift — plain var dynamicSleepNeed: DynamicSleepNeed? (no @Published; HealthDataStore is @Observable) stored after imuStepCountResult"
    - "GooseSwift/HealthDataStore+Sleep.swift — DynamicSleepNeed struct + runDynamicSleepNeed() async method"
    - "GooseSwift/HealthDataStore+Snapshots.swift — 480.0 replaced at both sites (lines ~28 and ~68)"
    - "GooseSwift/HealthDataStore+Utilities.swift — 480.0 replaced at both sites (lines ~128 and ~153)"
    - "GooseSwift/HealthSleepSheetsViews.swift — @Environment(HealthDataStore.self) added; sleepNeededText dynamic; breakdown row present; .task trigger added"
    - "GooseSwift/SleepV2ScheduleViews.swift — both '7h 39m' hardcoded instances replaced (line ~52 and line ~366)"
  key_links:
    - "runDynamicSleepNeed() calls hkUserAge() (now internal) in HealthDataStore+Snapshots.swift"
    - "runDynamicSleepNeed() must be first in refreshSleepAfterBandSync — before runPacketInputs() — because runPacketInputs() calls runPacketScores() which reads dynamicSleepNeed at Snapshots:28"
    - "SleepV2SleepNeededSheet requires HealthDataStore in its environment — injected by SleepV2OverviewPage parent"
    - "All four sleep_need_minutes sites must read dynamicSleepNeed?.totalNeedMinutes ?? 450.0 for UI/scoring consistency"
---

<objective>
Wire the existing sleep.compute_need Rust bridge method (shipped Phase 114) into Swift UI, replacing every hardcoded sleep-need value with a live dynamic result.

Purpose: SLP-NEED-03 — the Sleep dashboard must display a dynamic nightly sleep need (hours + minutes) derived from the Harvard model, with an optional breakdown row for base / debt / strain components.

Output: DynamicSleepNeed struct + plain var property on HealthDataStore (@Observable — no @Published); bridge call in HealthDataStore+Sleep.swift; hkUserAge() made internal; fallback constant updated from 480.0 to 450.0 at ALL FOUR sites (Snapshots:28, Snapshots:68, Utilities:128, Utilities:153); three display sites updated (sheet label, sheet breakdown row, schedule card including both hardcoded "7h 39m" instances at line 52 and line 366); sheet gains its own .task data trigger (D-01 through D-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-sleep-need-ui/120-CONTEXT.md
@.planning/phases/120-sleep-need-ui/120-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DynamicSleepNeed model + bridge call + hkUserAge access fix + ordering fix</name>
  <files>
    GooseSwift/HealthDataStore.swift,
    GooseSwift/HealthDataStore+Sleep.swift,
    GooseSwift/HealthDataStore+Snapshots.swift
  </files>
  <action>
    THREE edits across three files. Make them in this order to avoid compiler errors:

    **1. GooseSwift/HealthDataStore+Snapshots.swift — make hkUserAge() internal (per D-03)**

    Find the declaration `private func hkUserAge()` (around line 1032). Remove the `private` keyword so it becomes `func hkUserAge()`. This allows HealthDataStore+Sleep.swift to call it. No other change to Snapshots in this task (fallback replacement is Task 2).

    **2. GooseSwift/HealthDataStore+Sleep.swift — add DynamicSleepNeed struct + runDynamicSleepNeed() (per D-01, D-02, D-03)**

    At the very top of the file, before the `extension HealthDataStore` block, add a MARK comment followed by the DynamicSleepNeed struct with four Double properties: totalNeedMinutes, baseNeedMinutes, debtAdjustmentMinutes, strainAdjustmentMinutes.

    Inside the `extension HealthDataStore` block, add `runDynamicSleepNeed()` following the exact pattern of `runRecoveryV1()` in HealthDataStore+Recovery.swift:

    - Read `databasePath` into a local `let db`.
    - Call `hkUserAge()` and convert to `UInt8?` via `UInt8(min(max(age, 0), 120))`. If nil, omit age_years from bridgeArgs entirely (do NOT pass nil into the dict — JSON encoding of nil produces "null" which Rust rejects; simply leave the key absent so serde default kicks in).
    - Build `bridgeArgs: [String: Any] = ["database_path": db]`; if age is non-nil add `bridgeArgs["age_years"] = age`. Do NOT add `prior_strain` (D-03: omit entirely, Rust serde default is nil).
    - Call `try await bridge.requestAsync(method: "sleep.compute_need", args: bridgeArgs)`.
    - Extract fields using `Self.doubleValue(report["total_need_minutes"])` etc. for all four snake_case keys.
    - Guard on `total` being non-nil. If nil or catch, set `self.dynamicSleepNeed = nil` and return.
    - On success, assign `self.dynamicSleepNeed = DynamicSleepNeed(totalNeedMinutes: total, baseNeedMinutes: base ?? 450, debtAdjustmentMinutes: debt ?? 0, strainAdjustmentMinutes: strain ?? 0)`.
    - The method is `func runDynamicSleepNeed() async` — HealthDataStore is @MainActor so assignment is safe without extra dispatch.

    **3. GooseSwift/HealthDataStore.swift — add stored property + fix call ordering (per D-01)**

    IMPORTANT: HealthDataStore is @Observable (NOT @ObservableObject). Do NOT add @Published. Existing stored properties (lines 88–120) are all plain `var` with no wrapper — match that pattern exactly.

    Find the block of optional stored properties (around line 109) that includes `imuStepCountResult`. Add directly after it:

      // Dynamic sleep need from sleep.compute_need bridge (SLP-NEED-03).
      // Stored here because Swift extensions cannot add stored properties to @Observable classes.
      var dynamicSleepNeed: DynamicSleepNeed?

    Also update `refreshSleepAfterBandSync` (around line 328). The existing calls are `runPacketInputs()`, `runSleepScore()`, `runSleepStaging()`. Insert `await runDynamicSleepNeed()` as the FIRST call — before `runPacketInputs()`. This is mandatory because runPacketInputs() internally calls runPacketScores(), which reads dynamicSleepNeed at Snapshots:28. If runDynamicSleepNeed() runs after runPacketInputs(), the first band sync feeds a stale nil value into packet score calculations. Correct order:

      await runDynamicSleepNeed()   // FIRST — must precede all score callers
      await runPacketInputs()
      await runSleepScore()
      await runSleepStaging()

    Do not use fenced code blocks inside action prose — identifiers listed above are sufficient for implementation.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'generic/platform=iOS Simulator' -quiet 2>&1 | grep -E 'error:|BUILD SUCCEEDED|BUILD FAILED' | head -20</automated>
  </verify>
  <done>
    - DynamicSleepNeed struct declared in HealthDataStore+Sleep.swift before extension block
    - runDynamicSleepNeed() async method present in the extension
    - var dynamicSleepNeed: DynamicSleepNeed? stored in HealthDataStore.swift base body — plain var, no @Published
    - hkUserAge() is internal (no private keyword) in HealthDataStore+Snapshots.swift
    - refreshSleepAfterBandSync calls runDynamicSleepNeed() FIRST, before runPacketInputs()
    - Build succeeds with no new errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all four 480.0 fallbacks — Snapshots (×2) + Utilities (×2) (per D-06)</name>
  <files>
    GooseSwift/HealthDataStore+Snapshots.swift,
    GooseSwift/HealthDataStore+Utilities.swift
  </files>
  <action>
    Replace `"sleep_need_minutes": 480.0` with `"sleep_need_minutes": dynamicSleepNeed?.totalNeedMinutes ?? 450.0` at ALL FOUR sites across two files.

    **GooseSwift/HealthDataStore+Snapshots.swift — two sites:**
    - Around line 28: inside the args dict in `runPacketScores` (or `runPacketInputs` wrapper that builds score args).
    - Around line 68: inside the args dict in `runSleepScore`.

    **GooseSwift/HealthDataStore+Utilities.swift — two sites:**
    - Around line 128: inside `sleepScoreReport(baseArgs:)`.
    - Around line 153: inside `recoveryScoreBridgeArgs()`.

    Both files are @MainActor extensions so `self.dynamicSleepNeed` is accessible without synchronization.

    The value 450.0 aligns with Phase 114 D-03: when age is nil, the Rust model defaults to the 26–64 bracket (7.5 h = 450 min). The previous 480.0 was a stale constant.

    Do not touch any other lines in these files. The hkUserAge() access modifier was already fixed in Task 1.
  </action>
  <verify>
    <automated>grep -n "sleep_need_minutes" /Users/francisco/Documents/goose/GooseSwift/HealthDataStore+Snapshots.swift /Users/francisco/Documents/goose/GooseSwift/HealthDataStore+Utilities.swift</automated>
  </verify>
  <done>
    - All four occurrences of 480.0 for the sleep_need_minutes key are replaced with dynamicSleepNeed?.totalNeedMinutes ?? 450.0
    - No occurrence of "sleep_need_minutes.*480" remains in either file (grep confirms)
    - Build still succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Update all display sites — sheet label + breakdown row + sheet trigger + both schedule card hardcoded values (per D-04, D-05, D-07)</name>
  <files>
    GooseSwift/HealthSleepSheetsViews.swift,
    GooseSwift/SleepV2ScheduleViews.swift,
    GooseSwift/HealthSleepOverviewViews.swift
  </files>
  <action>
    FOUR display site updates across three files. Read each file before editing.

    **1. GooseSwift/HealthSleepSheetsViews.swift — SleepV2SleepNeededSheet (per D-04, D-05, D-07)**

    The struct SleepV2SleepNeededSheet currently has no HealthDataStore access. Add at the top of the struct body (matching the pattern from SleepV2BandSyncCard in the same file):

      @Environment(HealthDataStore.self) private var healthStore

    Replace the `sleepNeededText` computed property (around line 149). The new implementation must:
    - Return an empty String when `healthStore.dynamicSleepNeed == nil` (so the Text is absent per D-04).
    - When non-nil: compute `h = Int(need.totalNeedMinutes / 60)` and `m = Int(need.totalNeedMinutes.truncatingRemainder(dividingBy: 60))`.
    - Return `"\(h)h \(m)m recommended tonight"` when m != 0, or `"\(h)h recommended tonight"` when m == 0.

    Add a private helper `breakdownText(_ need: DynamicSleepNeed) -> String` on the struct that returns "Base Xh · Debt ±Ym · Strain ±Zm". Specifically: baseH = need.baseNeedMinutes / 60 formatted to one decimal with String(format: "%.1f", baseH); debt and strain shown as Int with a leading + when >= 0 and no prefix when negative (e.g. +15m or -5m).

    In the view body, immediately below the hero label that renders `sleepNeededText`, add a conditional block that shows the breakdown Text only when `healthStore.dynamicSleepNeed != nil`. Use `.font(.caption.weight(.medium))` and `.foregroundStyle(palette.secondaryText)` matching the palette access pattern already present in the sheet. Use an `if let need = healthStore.dynamicSleepNeed` guard for the breakdown Text.

    The two existing `Text(sleepNeededText)` calls (lines ~25 and ~114) automatically benefit from the updated computed property without additional changes.

    Add a `.task` modifier on the sheet's root view to ensure data loads even when the sheet is opened before the overview's onAppear fires:

      .task { await healthStore.runDynamicSleepNeed() }

    This handles the case where a user navigates directly to the sheet via deep link or opens it before SleepV2OverviewPage has appeared. Place the .task alongside any existing .onAppear or at the end of the root view's modifier chain.

    **2. GooseSwift/SleepV2ScheduleViews.swift — SleepV2SleepWindowCard (per D-07) — TWO hardcoded sites**

    Add `@Environment(HealthDataStore.self) private var healthStore` at the top of the `SleepV2SleepWindowCard` struct body. This is consistent with SleepV2BandSyncCard in HealthSleepSheetsViews.swift. Note: line ~85 of this file already has @Environment on a different struct (SleepV2BandSyncCard), so there is no duplication — this injection is for SleepV2SleepWindowCard specifically.

    Add a private computed property `sleepNeedDisplayText: String` on SleepV2SleepWindowCard that:
    - Returns "--" when `healthStore.dynamicSleepNeed == nil`.
    - Otherwise returns the h/m format without "recommended tonight" suffix: "\(h)h \(m)m" (or "\(h)h" when m == 0). This keeps the card value concise for the row layout.

    SITE A — around line 52 (action-row `value:` argument):
    Replace the hardcoded `value: "7h 39m"` argument with `value: sleepNeedDisplayText`.

    SITE B — around line 366 (clock-ring VStack label with .font(.title2.weight(.semibold))):
    This is a more prominent display site — the center label of the sleep clock dial. Replace the hardcoded `Text("7h 39m")` with `Text(sleepNeedDisplayText)`. Keep all surrounding modifiers (.font, .foregroundStyle, etc.) unchanged.

    After this task, zero occurrences of the literal "7h 39m" must remain in SleepV2ScheduleViews.swift.

    **3. GooseSwift/HealthSleepOverviewViews.swift — onAppear trigger (per D-03 Claude's Discretion)**

    Find the `.onAppear` block on `SleepV2OverviewPage` (around line 144–148). It currently has tasks for `loadBridgeCatalogsIfNeeded()` and `runSleepStaging()`. Add a new task:

      Task { await healthStore.runDynamicSleepNeed() }

    Place it alongside the other `Task { ... }` calls — order does not matter here since the result is used only on next render cycle.

    No other changes to HealthSleepOverviewViews.swift.
  </action>
  <verify>
    <automated>grep -n "7h 39m" /Users/francisco/Documents/goose/GooseSwift/SleepV2ScheduleViews.swift && echo "FOUND — fix required" || echo "CLEAN — no hardcoded value remains"</automated>
  </verify>
  <done>
    - SleepV2SleepNeededSheet has @Environment(HealthDataStore.self) and dynamic sleepNeededText
    - SleepV2SleepNeededSheet has .task { await healthStore.runDynamicSleepNeed() } on root view
    - Breakdown row appears below hero label when dynamicSleepNeed != nil; absent when nil
    - SleepV2SleepWindowCard shows dynamic value at BOTH sites (line ~52 and line ~366) — "Xh Ym" or "--"
    - Zero occurrences of "7h 39m" remain in SleepV2ScheduleViews.swift
    - HealthSleepOverviewViews.swift onAppear fires runDynamicSleepNeed()
    - Build succeeds with no new errors or warnings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust bridge | JSON args built in Swift passed to goose_bridge_handle_json; return dict parsed in Swift |
| UserDefaults read | dateOfBirth read from local UserDefaults; user-controlled data used as bridge arg |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-01 | Tampering | age_years UInt8 cast | low | mitigate | Clamp via UInt8(min(max(age, 0), 120)) before passing to bridge; never cast Double directly |
| T-120-02 | Denial of Service | runDynamicSleepNeed() on every onAppear + sheet .task | low | accept | Bridge call is fast (SQLite read); no throttling needed for single-tab use; sheet .task cancels on dismiss |
| T-120-03 | Information Disclosure | dynamicSleepNeed exposed in UI | low | accept | Health data already displayed in same tab; no new disclosure surface |
</threat_model>

<verification>
Build verification — run after each task:

  xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'generic/platform=iOS Simulator' -quiet 2>&1 | grep -E 'error:|BUILD SUCCEEDED|BUILD FAILED'

Grep checks after Task 2:
  grep -n "sleep_need_minutes" GooseSwift/HealthDataStore+Snapshots.swift GooseSwift/HealthDataStore+Utilities.swift
  # Must show dynamicSleepNeed?.totalNeedMinutes ?? 450.0 at all four sites; no 480.0 remaining

Grep check after Task 3:
  grep -n "7h 39m" GooseSwift/SleepV2ScheduleViews.swift
  # Must return no results

Functional checks (simulator):
1. Open Sleep tab → SleepV2SleepWindowCard action-row shows dynamic value (not "7h 39m")
2. Clock-ring center label shows same dynamic value (not "7h 39m") — line ~366
3. Tap the card → sheet opens → hero label shows e.g. "7h 30m recommended tonight"
4. Open sheet cold (before overview loads) → data still appears via sheet's .task trigger
5. Breakdown row shows "Base 7.5h · Debt +0m · Strain +0m" (or similar) below hero label
6. When dynamicSleepNeed is nil: hero label empty, breakdown row absent, card shows "--"
</verification>

<success_criteria>
- SLP-NEED-03: Sleep dashboard replaces static "8h recommended" with dynamic SleepNeedResult.total_need_minutes + optional breakdown
- dynamicSleepNeed populated from sleep.compute_need on every Sleep tab appearance, after band sync, and when sheet opens independently
- sleep_need_minutes fallback is 450.0 (not 480.0) at all four call sites: Snapshots (×2) + Utilities (×2)
- runDynamicSleepNeed() is first in refreshSleepAfterBandSync — before runPacketInputs() — ensuring packet scores receive fresh value
- All display sites updated: SleepV2SleepNeededSheet (sleepNeededText + breakdown row + .task trigger), SleepV2SleepWindowCard (both line ~52 and line ~366)
- Zero hardcoded "7h 39m" strings remain in SleepV2ScheduleViews.swift
- iOS build compiles without new errors or warnings
- #Preview macro compiles with a static DynamicSleepNeed value
</success_criteria>

<output>
Create `.planning/phases/120-sleep-need-ui/120-01-SUMMARY.md` when done
</output>
