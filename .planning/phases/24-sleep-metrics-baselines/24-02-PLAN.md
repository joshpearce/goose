---
phase: 24-sleep-metrics-baselines
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/baselines.rs
  - Rust/core/src/lib.rs
  - Rust/core/src/store.rs
  - Rust/core/src/bridge.rs
autonomous: true
requirements: [ALG-SLP-02]
must_haves:
  truths:
    - "baselines.rs implements an EWMA baseline (mean + variance) with alpha = 0.10 reconstructable from daily_recovery_metrics rows"
    - "fold_history rebuilds EWMA state from daily_recovery_metrics ordered by date with no new SQLite table"
    - "Cold-start guard returns None for z-score until night_count >= 4; trust levels calibrating/provisional/trusted are exposed"
    - "EWMA update is idempotent: BEGIN EXCLUSIVE transaction + WHERE last_updated_date < ? guard prevents double-update on the same date"
    - "store.ewma_baseline_fold_history and store.ewma_baseline_update bridge methods are callable from Swift"
  artifacts:
    - path: "Rust/core/src/baselines.rs"
      provides: "EwmaState, EwmaBaseline, EwmaTrustLevel, fold_history, update, deviation/z-score"
      contains: "pub struct EwmaState"
      min_lines: 80
    - path: "Rust/core/src/lib.rs"
      provides: "pub mod baselines declaration"
      contains: "pub mod baselines"
    - path: "Rust/core/src/store.rs"
      provides: "GooseStore methods backing fold_history and idempotent EWMA update"
    - path: "Rust/core/src/bridge.rs"
      provides: "store.ewma_baseline_fold_history + store.ewma_baseline_update dispatch arms and args structs"
      contains: "ewma_baseline"
  key_links:
    - from: "Rust/core/src/baselines.rs"
      to: "daily_recovery_metrics"
      via: "fold_history reads ordered rows via GooseStore"
      pattern: "daily_recovery_metrics|daily_recovery_metrics_between"
    - from: "Rust/core/src/bridge.rs"
      to: "Rust/core/src/baselines.rs"
      via: "bridge dispatch calls fold_history / update"
      pattern: "ewma_baseline"
---

<objective>
Implement ALG-SLP-02: a new baselines.rs EWMA engine that reconstructs per-metric
baseline state (HRV RMSSD, resting HR) from existing daily_recovery_metrics rows,
applies cold-start and trust-level gating, and writes idempotent daily updates under
a BEGIN EXCLUSIVE transaction with a date guard. Expose fold_history and update via
two bridge methods.

Purpose: Recovery Score v1 (Phase 25) needs a personal EWMA baseline to Z-score HRV
and RHR against. ALG-SLP-02 builds that engine now, reusing daily_recovery_metrics as
the source of truth so no new SQLite table is introduced (per 24-CONTEXT decision).

Output: baselines.rs module, lib.rs declaration, GooseStore backing methods, two
bridge dispatch arms with args structs, all covered by cargo test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sleep-metrics-baselines/24-CONTEXT.md

# Source artifacts — interface contracts (read these first)
@Rust/core/src/lib.rs            # module declaration list ~line 18; add pub mod baselines
@Rust/core/src/store.rs          # daily_recovery_metrics schema ~line 1133; DailyRecoveryMetricRow ~line 504; daily_recovery_metrics_between ~line 3510; immediate_transaction ~line 921; insert_gravity_rows ~line 6049 (model for new impl GooseStore methods)
@Rust/core/src/bridge.rs         # METHODS const list ~line 240-300; gravity bridge args/fns ~line 3329-3360; dispatch arms ~line 2665; open_bridge_store usage
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: baselines.rs EWMA engine with cold-start + trust levels</name>
  <files>Rust/core/src/baselines.rs, Rust/core/src/lib.rs</files>
  <behavior>
    Create Rust/core/src/baselines.rs (add `pub mod baselines;` to lib.rs). Write tests
    first (RED), then implement:
    - EwmaState { mean: f64, variance: f64, night_count: usize } with a fold method
      applying alpha = 0.10 per 24-CONTEXT <specifics>:
      mean_new = 0.9 * mean_old + 0.1 * x; variance_new = 0.9 * variance_old + 0.1 * (x - mean_old)^2.
      Test: folding a known sequence reproduces the hand-computed mean and variance after
      N nights; night_count increments per fold.
    - EwmaTrustLevel enum { Calibrating, Provisional, Trusted } via a from_night_count
      classifier: < 4 = Calibrating, 4..=13 = Provisional, >= 14 = Trusted. Test: exact
      boundaries at 3, 4, 13, 14.
    - EwmaBaseline wrapping per-metric EwmaState for "hrv" and "resting_hr". A z_score(value)
      method returns None when night_count < 4 (cold-start guard, MIN_NIGHTS_SEED=4) and
      otherwise (value - mean) / sqrt(variance) with a small variance floor to avoid div-by-zero.
      Test: z_score is None at 3 nights, Some at 4 nights; a value one std above mean yields
      ~+1.0.
    - An "inactive until 7 nights" readiness flag (baseline ready-but-untrusted until
      night_count >= 7) exposed as a bool, per 24-CONTEXT domain note. Test: false at 6, true at 7.
  </behavior>
  <action>
    Implement the EWMA engine as described, keeping fold/z_score/trust classification as
    pure functions/methods with no DB dependency so they unit-test in isolation (DB folding
    is Task 2). Use alpha = 0.10 (10-night memory constant) per 24-CONTEXT. Define
    MIN_NIGHTS_SEED = 4 and MIN_NIGHTS_TRUST = 14 as consts. Write #[cfg(test)] tests in
    baselines.rs covering: EWMA mean/variance recurrence, trust-level boundaries (3/4/13/14),
    cold-start z_score None below 4 nights, z_score magnitude, readiness flag at 6 vs 7.
  </action>
  <verify>
    <automated>cargo test -p goose-core baselines</automated>
  </verify>
  <done>baselines.rs exists, declared in lib.rs, with passing unit tests for EWMA recurrence, trust boundaries, cold-start gate, and readiness flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fold_history from daily_recovery_metrics + idempotent EWMA update</name>
  <files>Rust/core/src/baselines.rs, Rust/core/src/store.rs</files>
  <behavior>
    - fold_history(store: &GooseStore, ...) -> GooseResult&lt;EwmaBaseline&gt;: reads
      daily_recovery_metrics rows ordered by date (reuse daily_recovery_metrics_between or
      add an ordered accessor on GooseStore), folding hrv_rmssd_ms and resting_hr_bpm into
      the per-metric EwmaState; rows with NULL metric values are skipped (do not increment
      that metric's night_count). Test (store.open_in_memory + inserted daily rows):
      reconstructed mean matches the hand-computed EWMA over the inserted sequence.
    - An idempotent daily update on GooseStore: ewma_baseline_update(date_key, hrv_rmssd,
      rhr_bpm) wrapped in `BEGIN EXCLUSIVE` (use the existing immediate_transaction helper
      pattern at store.rs:921 or an explicit BEGIN EXCLUSIVE) with a
      `WHERE last_updated_date < ?` guard so a second call for the same date is a no-op.
      Because 24-CONTEXT specifies NO new table, persist the update by upserting the
      corresponding daily_recovery_metrics row for date_key (the EWMA state is always
      reconstructed via fold_history, so the "update" writes the night's raw metrics and
      the guard prevents double-application). Test: calling update twice for the same
      date_key inserts/updates once and the second call reports skipped=true.
  </behavior>
  <action>
    Add fold_history to baselines.rs taking &GooseStore. Add the ordered-read and idempotent
    write as `impl GooseStore` methods in store.rs (mirror the insert_gravity_rows /
    gravity_rows_between pattern at store.rs:6049). For the date guard, track the most recent
    applied date_key (derive from the max date_key already present for the EWMA source) and
    use a SQL `WHERE last_updated_date < ?1` predicate so re-running the same date is a no-op;
    return a bool/usize indicating whether a write occurred. Use BEGIN EXCLUSIVE (not
    IMMEDIATE) for the write transaction per ALG-SLP-02 success criterion. Write store-backed
    tests using GooseStore::open_in_memory(): insert daily_recovery_metrics rows, fold them,
    assert reconstructed mean; call update twice for one date and assert idempotency.
  </action>
  <verify>
    <automated>cargo test -p goose-core baselines fold_history ewma_baseline_update</automated>
  </verify>
  <done>fold_history reconstructs EWMA from daily_recovery_metrics; the EXCLUSIVE-guarded update is idempotent across repeated same-date calls; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Bridge methods store.ewma_baseline_fold_history and store.ewma_baseline_update</name>
  <files>Rust/core/src/bridge.rs</files>
  <action>
    Add two args structs (EwmaBaselineFoldHistoryArgs { database_path, ... } and
    EwmaBaselineUpdateArgs { database_path, date_key, hrv_rmssd, rhr_bpm }) and two bridge
    functions mirroring insert_gravity_rows_bridge / gravity_rows_between_bridge
    (bridge.rs:3343-3360): each opens the store via open_bridge_store, calls the
    baselines.rs / GooseStore method, and returns a JSON value. fold_history returns
    { hrv: {mean, variance, night_count, trust}, resting_hr: {...} }; update returns
    { skipped: bool }. Register both method names ("store.ewma_baseline_fold_history",
    "store.ewma_baseline_update") in the METHODS const list (~line 240-300) and add the two
    dispatch match arms next to the existing "store.gravity_*" arms (~line 2665) using the
    same .and_then(...).map(bridge_ok).unwrap_or_else(bridge_error) shape. Use the bridge
    method names from 24-CONTEXT <decisions>. Add a bridge-level test asserting both methods
    dispatch and round-trip against an in-memory-seeded store.
  </action>
  <verify>
    <automated>cargo test -p goose-core ewma_baseline</automated>
  </verify>
  <done>Both bridge methods appear in METHODS, dispatch correctly, and a round-trip test passes; cargo test -p goose-core green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift -> Rust FFI (JSON) | ewma_baseline_update/fold_history args cross from app into Rust |
| concurrent bridge calls -> SQLite | two updates for the same date may race |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | duplicate same-date EWMA update inflating baseline | mitigate | BEGIN EXCLUSIVE transaction + WHERE last_updated_date < ? date guard (idempotent) |
| T-24-05 | Tampering | NaN/Inf or out-of-range metric values in update args | mitigate | reject non-finite hrv/rhr before folding; daily_recovery_metrics columns remain nullable, skipped when invalid |
| T-24-06 | Denial of Service | concurrent EXCLUSIVE locks blocking writes | accept | single-writer app; EXCLUSIVE held briefly per daily update |
</threat_model>

<verification>
- cargo test -p goose-core green (baselines unit + store-backed fold/update + bridge round-trip)
- No new SQLite table created (grep confirms baselines reads/writes daily_recovery_metrics only)
- Both bridge method names present in the METHODS const and dispatch table
</verification>

<success_criteria>
- baselines.rs EWMA engine (alpha 0.10) with cold-start (>= 4 nights) and trust levels (calibrating/provisional/trusted) per ALG-SLP-02
- fold_history rebuilds state from daily_recovery_metrics; no new table
- Idempotent BEGIN EXCLUSIVE update with WHERE last_updated_date < ? guard
- store.ewma_baseline_fold_history + store.ewma_baseline_update bridge methods callable
</success_criteria>

<output>
Create `.planning/phases/24-sleep-metrics-baselines/24-02-SUMMARY.md` when done
</output>
