---
phase: 10-hr-monitor-scan-connect-ui
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - GooseSwift/HRMonitorView.swift
autonomous: false
requirements: [WEAR-04, WEAR-05]
must_haves:
  truths:
    - "User sees a live scan list of nearby HR monitors showing device name and RSSI (WEAR-04)"
    - "The scan starts automatically on appear and stops on disappear, with no manual scan button (D-04)"
    - "Tapping a device shows a sheet with the device name and a Connect button; confirming starts the connection (WEAR-05, D-05)"
    - "While connecting, an inline ProgressView appears on the target row and the list stays visible (D-06)"
    - "When connected, the view shows live BPM, device name, reconnect state when non-idle, and a Disconnect button (D-07)"
    - "When Bluetooth is unavailable, the view shows an explanatory message instead of a scan list"
  artifacts:
    - path: "GooseSwift/HRMonitorView.swift"
      provides: "HRMonitorView and all private sub-views for scan/connect/connected states"
      contains: "struct HRMonitorView"
      min_lines: 180
  key_links:
    - from: "HRMonitorView.onAppear"
      to: "GooseBLEClient.startHRMonitorScan()"
      via: "auto-scan guarded by hrConnectionState != connected"
      pattern: "ble.startHRMonitorScan\\(\\)"
    - from: "HRMonitorScanList ForEach"
      to: "GooseBLEClient.discoveredHRDevices"
      via: "observed @Published array"
      pattern: "ble.discoveredHRDevices"
    - from: "HRMonitorDeviceSheet Connect button"
      to: "GooseBLEClient.connectHRMonitor(_:)"
      via: "sheet confirm action"
      pattern: "ble.connectHRMonitor"
    - from: "HRDisconnectButton"
      to: "GooseBLEClient.disconnectHRMonitor()"
      via: "destructive button action"
      pattern: "ble.disconnectHRMonitor\\(\\)"
---

<objective>
Build `HRMonitorView.swift` — the complete HR monitor scan/connect screen — wiring exclusively to the `@Published` state on `GooseBLEClient` (promoted in Plan 01). The view follows the `DeviceView` visual pattern (D-02) and implements the four-state machine (Bluetooth-unavailable, scanning, connecting, connected) from the UI-SPEC.

Purpose: This is the user-facing deliverable for WEAR-04 (scan list with name + RSSI) and WEAR-05 (tap-to-connect). All BLE logic already exists; this plan is a pure rendering/wiring concern with no new BLE code.

Output: One new Swift file, `GooseSwift/HRMonitorView.swift`, containing `HRMonitorView` (public) and six private sub-structs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-CONTEXT.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-RESEARCH.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-PATTERNS.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-UI-SPEC.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-01-SUMMARY.md
@GooseSwift/DeviceView.swift
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 portion)

New file `GooseSwift/HRMonitorView.swift` with these symbols:

- `HRMonitorView` — public `View` struct (NavigationStack destination for `MoreRoute.hrMonitor`, wired in Plan 03)
- `HRMonitorContentView` — private `View` taking `@ObservedObject var ble: GooseBLEClient` + view-local `@State`
- `HRMonitorHeader` — private `View` (status text + device name; NO "LAST SYNC" column)
- `HRMonitorScanList` — private `View` (DISCOVERED section + empty state + device rows)
- `HRMonitorDeviceRow` — private `View` (name + RSSI + inline ProgressView when connecting)
- `HRMonitorConnectedPanel` — private `View` (HEART RATE BPM display + reconnect state + Disconnect button)
- `HRMonitorDeviceSheet` — private `View` (device name + RSSI + Connect button)
- File-scope `private let` visual tokens copied from DeviceView.swift (deviceScreenBackground, devicePrimaryText, controlBackground, dividerColor, secondaryText, mutedText, connectedGreen, disconnectedRed, deviceLabelFont, deviceBodyFont)
- View-local `@State`: `selectedDevice: GooseDiscoveredDevice?`, `connectingDeviceID: UUID?`

This plan reads `hrMonitorManager.connectedDeviceName` for the connected device name. If the inner view cannot reach `hrMonitorManager` (it is internal), expose the connected name read path during execution by reading `ble`'s existing exposure; do NOT add a new `@Published` without recording it here. Preferred: read `model.ble.hrMonitorManager.connectedDeviceName` only if `hrMonitorManager` is accessible from the view module; otherwise fall back to a connected-name source already published. Resolve this in Task 1 and note the choice in the SUMMARY.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Build HRMonitorView with all sub-views and the four-state machine</name>
  <files>GooseSwift/HRMonitorView.swift</files>
  <read_first>
    - GooseSwift/DeviceView.swift (lines 1-11 view shell; 18-22 content view @ObservedObject; 24-74 ZStack/ScrollView/VStack + nav modifiers + onAppear; 204-248 DeviceConnectionHeader; 671-702 visual token constants — copy verbatim)
    - GooseSwift/ConnectionView.swift lines 97-121 (existing scan-row layout with RSSI for reference)
    - GooseSwift/GooseBLEClient.swift lines 6-40 (confirm `discoveredHRDevices`, `hrConnectionState`, `liveHeartRateBPM`, `hrReconnectState`, `bluetoothState` are all observable @Published)
    - GooseSwift/GooseBLEClient+HRMonitor.swift (confirm `startHRMonitorScan()`, `stopHRMonitorScan()`, `connectHRMonitor(_:)`, `disconnectHRMonitor()` signatures; confirm `connectedDeviceName` location on the manager)
    - GooseSwift/GooseBLETypes.swift lines 52-57 (`GooseDiscoveredDevice` — Identifiable for ForEach; id/name/rssi/generation)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-UI-SPEC.md (FULL — view hierarchy, spacing scale, typography, color, header design, state machine, interaction flows, copywriting, accessibility)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-PATTERNS.md "HRMonitorView.swift" section (every sub-view's exact structure)
  </read_first>
  <action>
    Create `GooseSwift/HRMonitorView.swift` with `import SwiftUI` and `import UIKit`, 2-space indentation, K&R braces. Copy the file-scope `private let` visual tokens verbatim from DeviceView.swift lines 671-702 (deviceScreenBackground, devicePrimaryText, controlBackground, dividerColor, secondaryText, mutedText, connectedGreen, disconnectedRed, deviceLabelFont, deviceBodyFont) — each file declares its own copies per project convention.

    Public `HRMonitorView`: `@EnvironmentObject private var model: GooseAppModel`; body renders `HRMonitorContentView(ble: model.ble).environmentObject(model)`. Mirror the DeviceView shell exactly.

    Private `HRMonitorContentView`: `@EnvironmentObject private var model`, `@ObservedObject var ble: GooseBLEClient`, `@State private var selectedDevice: GooseDiscoveredDevice?`, `@State private var connectingDeviceID: UUID?`. Body: `ZStack { deviceScreenBackground.ignoresSafeArea(); ScrollView { VStack(alignment: .leading, spacing: 0) { HRMonitorHeader(...).padding(.bottom, 32); <state body> } .padding(.horizontal, 24).padding(.top, 36).padding(.bottom, 48) } }` with `.navigationTitle("HR Monitor")`, `.navigationBarTitleDisplayMode(.inline)`, `.toolbarBackground(.hidden, for: .navigationBar)`, `.tint(devicePrimaryText)`.

    State machine (D-03: two mutually exclusive primary states — scanning vs. connected — plus the bluetooth-unavailable and connecting variants; drive off `ble.bluetoothState` and `ble.hrConnectionState` per UI-SPEC §State Machine):
    - State 1 BLUETOOTH_UNAVAILABLE: when `ble.bluetoothState != "poweredOn"` — header status `"BLUETOOTH OFF"` or `"NOT AUTHORISED"` in `disconnectedRed`; body shows the copy from UI-SPEC §Copywriting ("Enable Bluetooth to scan for HR monitors." / "Bluetooth access is required. Open Settings to allow access."). No scan list.
    - State 2 SCANNING: `hrConnectionState == "disconnected"` and Bluetooth on — header `"SCANNING"` (secondaryText), name `"HR Monitor"`; body `HRMonitorScanList`.
    - State 3 CONNECTING: `hrConnectionState == "connecting"` — header `"CONNECTING"` (secondaryText); scan list still visible with inline ProgressView on the row whose `id == connectingDeviceID`.
    - State 4 CONNECTED: `hrConnectionState == "connected"` — header `"CONNECTED"` (connectedGreen), name = connected device name; body `HRMonitorConnectedPanel`.

    Lifecycle (D-04/D-04b): `.onAppear { guard ble.hrConnectionState != "connected" else { return }; ble.startHRMonitorScan() }` and `.onDisappear { ble.stopHRMonitorScan() }`. No manual scan button anywhere (D-04 prohibits it).

    Sheet (D-05/D-06): `.sheet(item: $selectedDevice) { device in HRMonitorDeviceSheet(device: device) { connectingDeviceID = device.id; selectedDevice = nil; ble.connectHRMonitor(device) } .presentationDetents([.height(220)]) .presentationDragIndicator(.visible) }`. Clear `connectingDeviceID` when `ble.hrConnectionState` returns to `"connected"` or `"disconnected"` (use `.onChange(of: ble.hrConnectionState)` to reset it once the connecting phase ends).

    `HRMonitorHeader` (NEW private struct — do NOT reuse `DeviceConnectionHeader`; omit the "LAST SYNC" right column per RESEARCH Pitfall 5): props `statusText: String`, `statusColor: Color`, `deviceDisplayName: String`. `HStack(alignment: .bottom, spacing: 16) { VStack(alignment: .leading, spacing: 8) { Text(statusText).font(deviceLabelFont).foregroundStyle(statusColor); Text(deviceDisplayName.uppercased()).font(deviceBodyFont.weight(.black)).foregroundStyle(devicePrimaryText) } .frame(maxWidth: .infinity, alignment: .leading) }`.

    `HRMonitorScanList`: `VStack(alignment: .leading, spacing: 12)` with `Text("DISCOVERED")` (deviceLabelFont, secondaryText); if `ble.discoveredHRDevices.isEmpty` show `Text("No HR monitors found. Make sure your device is nearby and powered on.")` (deviceBodyFont, mutedText); else `ForEach(ble.discoveredHRDevices)` rendering `HRMonitorDeviceRow(device:isConnecting:)` with a 1pt `dividerColor` bottom rule per row and `.onTapGesture { selectedDevice = device }`.

    `HRMonitorDeviceRow`: props `device`, `isConnecting: Bool`. `HStack(spacing: 12) { VStack(alignment: .leading, spacing: 4) { Text(device.name).font(deviceBodyFont.weight(.black)).foregroundStyle(devicePrimaryText); Text("\(device.rssi) dBm").font(.system(size: 12, weight: .bold)).foregroundStyle(mutedText) }; Spacer(minLength: 16); if isConnecting { ProgressView().scaleEffect(0.8).tint(secondaryText).accessibilityLabel("Connecting to \(device.name)") } } .padding(.vertical, 12).contentShape(Rectangle()).accessibilityLabel("\(device.name), \(device.rssi) dBm")`.

    `HRMonitorConnectedPanel` (D-07): `VStack(alignment: .leading, spacing: 24)` — HEART RATE label (deviceLabelFont, secondaryText) + BPM `HStack(alignment: .bottom, spacing: 4)` with `Text(ble.liveHeartRateBPM.map(String.init) ?? "--").font(.system(size: 52, weight: .black)).foregroundStyle(devicePrimaryText)` and `Text("BPM").font(deviceBodyFont).foregroundStyle(secondaryText).padding(.bottom, 8)`, with `.accessibilityLabel("\(bpm) beats per minute")`; if `ble.hrReconnectState != "idle"` show `Text(ble.hrReconnectState).font(deviceLabelFont).foregroundStyle(disconnectedRed)`; then a `Button("Disconnect", role: .destructive) { ble.disconnectHRMonitor() }` styled `.frame(maxWidth: .infinity, minHeight: 46).foregroundStyle(disconnectedRed).background(controlBackground, in: RoundedRectangle(cornerRadius: 8))` with `.accessibilityLabel("Disconnect HR monitor")`.

    `HRMonitorDeviceSheet`: props `device`, `onConnect: () -> Void`; `@Environment(\.dismiss)`. `VStack(spacing: 20) { VStack(alignment: .leading, spacing: 8) { Text(device.name.uppercased()).font(.system(size: 17, weight: .black)); Text("\(device.rssi) dBm").font(deviceLabelFont).foregroundStyle(secondaryText) }; Button("Connect") { onConnect() }.frame(maxWidth: .infinity, minHeight: 50).font(.system(size: 17, weight: .black)).foregroundStyle(.white).background(connectedGreen, in: RoundedRectangle(cornerRadius: 12)).accessibilityLabel("Connect to \(device.name)") } .padding(24)`.

    For the connected device name: read it via the manager exposure decided in `<artifacts_produced>` (resolve accessibility of `hrMonitorManager.connectedDeviceName` from the view; fall back to `"HR Monitor"` when nil). Do NOT call `GooseRustBridge` (no bridge calls in this phase) and do NOT read `ble.hrMonitorManager.discoveredHRDevices` directly (read the promoted `ble.discoveredHRDevices` — RESEARCH anti-pattern). Do NOT use a `List` (D-02 / RESEARCH anti-pattern — use the VStack scroll structure).
  </action>
  <verify>
    <automated>test -f GooseSwift/HRMonitorView.swift && grep -c "struct HRMonitorView\|struct HRMonitorContentView\|struct HRMonitorHeader\|struct HRMonitorScanList\|struct HRMonitorDeviceRow\|struct HRMonitorConnectedPanel\|struct HRMonitorDeviceSheet" GooseSwift/HRMonitorView.swift; grep -c "ble.startHRMonitorScan\|ble.stopHRMonitorScan\|ble.connectHRMonitor\|ble.disconnectHRMonitor\|ble.discoveredHRDevices" GooseSwift/HRMonitorView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `GooseSwift/HRMonitorView.swift` exists
    - All seven structs are declared (source assertion: the first grep returns `7`)
    - The view calls `ble.startHRMonitorScan()` in `onAppear` and `ble.stopHRMonitorScan()` in `onDisappear` (source: `grep -q "ble.startHRMonitorScan()" && grep -q "ble.stopHRMonitorScan()"`)
    - `onAppear` is guarded by `ble.hrConnectionState != "connected"` (source: `grep -q 'hrConnectionState != "connected"'`) — implements D-04b
    - The scan list iterates `ble.discoveredHRDevices` (source: `grep -q "ForEach(ble.discoveredHRDevices"` or `grep -q "ble.discoveredHRDevices"`) — NOT `ble.hrMonitorManager.discoveredHRDevices` (source: `grep -c "hrMonitorManager.discoveredHRDevices"` returns `0`)
    - The sheet Connect path calls `ble.connectHRMonitor(` (source assertion succeeds)
    - The Disconnect button calls `ble.disconnectHRMonitor()` (source assertion succeeds)
    - No `List(` used for the scan list (source: `grep -c "List {" GooseSwift/HRMonitorView.swift` returns `0`)
    - No `GooseRustBridge` reference (source: `grep -c "GooseRustBridge" GooseSwift/HRMonitorView.swift` returns `0`)
    - BPM-unavailable copy is the literal `"--"` (source: `grep -q '"--"'`) per UI-SPEC
    - File is at least 180 lines (`wc -l < GooseSwift/HRMonitorView.swift` >= 180)
  </acceptance_criteria>
  <done>`HRMonitorView.swift` renders all four UI states from observed `GooseBLEClient` state, auto-scans on appear, presents the connect sheet on tap, shows inline connecting spinner, and renders the connected panel with live BPM and a working Disconnect — implementing WEAR-04 and WEAR-05 at the view tier.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify HRMonitorView renders and connects on device/simulator</name>
  <files>GooseSwift/HRMonitorView.swift</files>
  <read_first>
    - GooseSwift/HRMonitorView.swift (the view under verification)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-UI-SPEC.md (state machine + copywriting to verify against)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-VALIDATION.md (manual-only verification rows)
  </read_first>
  <action>
    Human-only manual verification on a physical iPhone with a real BLE HR monitor. The executor has already automated everything Claude can (the view is built and compiles); this task confirms the rendered behaviour against the UI-SPEC. Follow the steps in how-to-verify and pause for the user's approval signal. Do not write code in this task — it is a verification gate.
  </action>
  <what-built>
    `HRMonitorView.swift` with the full four-state machine (Bluetooth-off, scanning, connecting, connected), auto-scan on appear, tap-to-connect sheet, inline connecting spinner, and connected panel with live BPM + Disconnect. (Navigation entry from More tab is wired in Plan 03 — until then, verify via a temporary preview or by wiring the destination locally if Plan 03 has not run.)
  </what-built>
  <how-to-verify>
    Note: this screen requires a real BLE HR monitor (Polar H10 or similar) — CoreBluetooth scanning cannot run in the iOS Simulator (VALIDATION.md manual-only rows). On a physical device:
    1. Build and run GooseSwift on a physical iPhone (Bluetooth on, HR monitor powered and nearby).
    2. Reach `HRMonitorView` (via Plan 03's More > HR Monitor entry, or a temporary NavigationLink).
    3. Verify header shows "SCANNING" and the DISCOVERED list populates with your HR monitor showing name and RSSI in "<n> dBm" form. Confirm the list updates as the device's RSSI changes.
    4. Tap the device row — confirm the sheet appears with the uppercased name, RSSI, and a green "Connect" button.
    5. Tap "Connect" — confirm the sheet dismisses, an inline spinner appears on the row, the header shows "CONNECTING", then "CONNECTED" once linked.
    6. In the connected panel, confirm live BPM updates (a real numeral, not "--") and the device name shows in the header.
    7. Tap "Disconnect" — confirm the view returns to the scanning state and the scan restarts.
    8. With Bluetooth off, confirm the Bluetooth-unavailable copy appears instead of a scan list.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any rendering/interaction issues.</resume-signal>
  <verify>
    <human-check>User confirms on a physical device: scan list populates with name + RSSI; tap shows the connect sheet; Connect transitions connecting -> connected with live BPM; Disconnect returns to scanning; Bluetooth-off shows the unavailable copy.</human-check>
  </verify>
  <acceptance_criteria>
    - User explicitly approves ("approved") after completing all eight verification steps, OR reports specific issues to fix
    - Scan list shows each device as name + "<rssi> dBm" (WEAR-04 behaviour assertion)
    - Tap -> sheet -> Connect reaches the connected panel with a non-"--" BPM (WEAR-05 behaviour assertion)
  </acceptance_criteria>
  <done>User has verified the HR monitor scan/connect flow end to end on hardware and approved, or filed specific issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE peripheral -> displayed UI | Untrusted advertised device names rendered as text in the scan list and sheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering / Injection | `HRMonitorDeviceRow` / `HRMonitorDeviceSheet` rendering `device.name` | mitigate | Names are pre-sanitised at ingestion (`prefix(64)` + trim in Plan 01's `didDiscover`); SwiftUI `Text` does not interpret markup, so no injection surface at render time (ASVS V5) |
| T-10-05 | DoS (battery) | Unbounded scan if view never disappears | mitigate | `.onDisappear { ble.stopHRMonitorScan() }` stops scanning when the screen is left |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Apple system frameworks only (RESEARCH Package Legitimacy Audit: Not applicable) |
</threat_model>

<verification>
- `cargo test -p goose-core` passes (repo regression gate).
- GooseSwift target compiles with the new view file.
- Manual on-device verification per Task 2 (BLE hardware required; simulator cannot scan).
</verification>

<success_criteria>
- `HRMonitorView.swift` exists with all seven structs and the four-state machine.
- Scan list renders `ble.discoveredHRDevices` with name + RSSI (WEAR-04).
- Tap opens the sheet; Connect calls `ble.connectHRMonitor` (WEAR-05).
- Connected panel shows live BPM and a Disconnect that calls `ble.disconnectHRMonitor()`.
- No `List`, no `GooseRustBridge`, no direct `hrMonitorManager.discoveredHRDevices` read.
</success_criteria>

<output>
Create `.planning/phases/10-hr-monitor-scan-connect-ui/10-02-SUMMARY.md` when done.
</output>
