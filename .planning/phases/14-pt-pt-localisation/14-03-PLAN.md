---
phase: 14-pt-pt-localisation
plan: 03
type: execute
wave: 3
depends_on: [14-01]
files_modified:
  - GooseSwift/Localizable.xcstrings
autonomous: true
requirements: [L10N-01]

must_haves:
  truths:
    - "More tab sections and rows render in pt-PT"
    - "Connection, Device, and HR Monitor static strings render in pt-PT"
    - "Capture, Debug, Raw Export, and Remote Server static strings render in pt-PT"
    - "Onboarding flow static strings render in pt-PT"
    - "All catalog entries added in this wave are well-formed JSON with state translated"
  artifacts:
    - path: "GooseSwift/Localizable.xcstrings"
      provides: "pt-PT translations for More / Connection / Capture / Debug / Export / Onboarding static strings"
      contains: "pt-PT"
  key_links:
    - from: "GooseSwift More/Connection/Onboarding view literals"
      to: "GooseSwift/Localizable.xcstrings"
      via: "SwiftUI Text/Section/Label/navigationTitle auto-resolution"
      pattern: "\"state\" : \"translated\""
---

<objective>
Translate the remaining static UI strings into pt-PT: the More tab sections, the
Connection / Device / HR Monitor screens, the Capture / Debug / Raw Export / Remote Server
developer surfaces, and the Onboarding flow. Catalog-only additions (~150 strings, D-01
comprehensive scope, D-05 Wave 3).

This wave runs in parallel with Wave 2 — both only append to `Localizable.xcstrings` (string-key
de-duplication makes concurrent additions safe), and both depend only on the infrastructure from
Wave 1. The dynamic `@Published` status strings are explicitly out of scope here (Wave 4 / D-04).

Purpose: Complete static-string coverage so no English literal remains in any user-visible screen.
Output: Expanded `Localizable.xcstrings` covering More + Connection + Onboarding + developer screens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-pt-pt-localisation/14-CONTEXT.md
@.planning/phases/14-pt-pt-localisation/14-RESEARCH.md
@.planning/phases/14-pt-pt-localisation/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate More tab, Connection, Device, and HR Monitor static strings</name>
  <files>GooseSwift/Localizable.xcstrings</files>
  <action>
Add catalog entries (key = exact English literal, `pt-PT` stringUnit `state: translated`) for the
user-visible static string literals in:
- GooseSwift/MoreView.swift
- GooseSwift/MoreProfileViews.swift
- GooseSwift/MoreInfoViews.swift
- GooseSwift/MoreRemoteServerViews.swift (static labels — "Server", "Authentication", "Upload",
  "Save", "Now", "Server reachable", "Server unreachable", etc.)
- GooseSwift/ConnectionView.swift ("Status", "Actions", "Discovered", "Bluetooth", "Connection",
  "Reconnect", "Request Bluetooth", "Scan", "Stop Scan", "Connect Selected",
  "Reconnect Remembered", "No devices yet", "Sync Error", "Done", "Connect")
- GooseSwift/DeviceView.swift (static labels — "Device", "Connection", "Historical sync", etc.)
- GooseSwift/HRMonitorView.swift (static labels AND the hardcoded status display literals
  "CONNECTED", "CONNECTING", "SCANNING", "BLUETOOTH OFF", "NOT AUTHORISED", "DISCOVERED",
  "HEART RATE", "BPM", "Disconnect" — per research these are view-embedded display constants, not
  @Published values, so they belong in the catalog as static keys)
- GooseSwift/RootView.swift ("Sync Error", "Done")

Discovery command:
`grep -rnoE '(Text|Section|Button|Label|LabeledContent|navigationTitle|accessibilityLabel)\("[^"]+"' GooseSwift/MoreView.swift GooseSwift/MoreProfile*.swift GooseSwift/MoreInfo*.swift GooseSwift/MoreRemoteServer*.swift GooseSwift/ConnectionView.swift GooseSwift/DeviceView.swift GooseSwift/HRMonitorView.swift GooseSwift/RootView.swift`

Apply the same INCLUDE/EXCLUDE rules as Wave 2 Task 1: exclude `Text(verbatim:)`, log/record
strings, SF Symbol names, and any value of a `@Published` status property (Wave 4 / D-04 — e.g. do
NOT add `ble.bluetoothState` raw values like "poweredOn"; those are mapped in LocalizedStatusStrings).
Use the glossary (Connect → Ligar, Disconnect → Desligar, Save → Guardar, Done → Concluído,
Scanning → A procurar..., Settings → Definições, Sync → Sincronizar, etc.). De-duplicate keys.
  </action>
  <verify>
    <automated>python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && echo OK</automated>
  </verify>
  <done>More, Connection, Device, and HR Monitor static strings (including HRMonitorView display constants) have pt-PT catalog entries; catalog is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Translate Capture, Debug, Raw Export, and Onboarding static strings</name>
  <files>GooseSwift/Localizable.xcstrings</files>
  <action>
Add catalog entries for the user-visible static string literals in:
- GooseSwift/MoreCaptureViews.swift ("Session", "Start Capture", "Stop Capture",
  "Overnight Guard", "Start Guard", "Stop Guard", "Final Sync", etc.)
- GooseSwift/MoreDebugViews.swift ("Debug", "Rust And Parser", "Run Parser Probe",
  "Debug Session", "Health Packet Capture", etc.)
- GooseSwift/MoreRawExportViews.swift ("Raw Export", "Window", "Filters", "Data Families",
  "Export", "Save Local Data File", etc.)
- GooseSwift/OnboardingStepViews.swift and any other Onboarding* view files
  ("Connect your WHOOP", "Search again", "Back", units labels, etc.)

Discovery command:
`grep -rnoE '(Text|Section|Button|Label|LabeledContent|navigationTitle|accessibilityLabel)\("[^"]+"' GooseSwift/MoreCaptureViews.swift GooseSwift/MoreDebugViews.swift GooseSwift/MoreRawExportViews.swift GooseSwift/Onboarding*.swift`

Same INCLUDE/EXCLUDE rules as Task 1. Exclude `@Published` status values (Wave 4), log/record
strings, and `Text(verbatim:)`. Use the established glossary (Export → Exportar,
Import → Importar, etc.); supply accurate pt-PT for any new domain terms. De-duplicate keys
already present from earlier waves.
  </action>
  <verify>
    <automated>python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && echo OK</automated>
  </verify>
  <done>Capture, Debug, Raw Export, and Onboarding static strings have pt-PT catalog entries; catalog is valid JSON.</done>
</task>

</tasks>

<verification>
- `Localizable.xcstrings` parses as JSON after both tasks.
- More tab, Connection/Device/HR Monitor, Capture/Debug/Raw Export, and Onboarding static screens render pt-PT on a Portuguese device.
- No `@Published` status-string values were added to the catalog (reserved for Wave 4).
</verification>

<success_criteria>
- ~150 additional static strings localised to pt-PT (D-01, D-05 Wave 3).
- Catalog-only edits; no Swift code changes; static-string coverage is comprehensive across all screens.
</success_criteria>

<output>
Create `.planning/phases/14-pt-pt-localisation/14-03-SUMMARY.md` when done.
</output>
