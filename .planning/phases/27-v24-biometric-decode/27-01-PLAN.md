---
phase: 27-v24-biometric-decode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/protocol.rs
  - Rust/core/tests/v24_biometric_protocol_tests.rs
autonomous: true
requirements:
  - BIO-01
must_haves:
  truths:
    - "DataPacketBodySummary::V24History variant exists and is serialisable"
    - "parse_data_packet_body_summary() routes packet_k == 24 to the new variant instead of NormalHistory"
    - "Unit test with synthetic 79-byte V24 payload asserts all field values match verify_v24.py::decode_v24() output"
    - "Short-payload returns Ok + None fields + warning string (no panic)"
  artifacts:
    - path: "Rust/core/src/protocol.rs"
      provides: "DataPacketBodySummary::V24History variant + parse_v24_body_summary() function"
      contains: "V24History"
    - path: "Rust/core/tests/v24_biometric_protocol_tests.rs"
      provides: "Unit tests for field offsets and short-payload tolerance"
      contains: "test_v24_body_summary_field_offsets"
  key_links:
    - from: "protocol.rs parse_data_packet_body_summary()"
      to: "DataPacketBodySummary::V24History"
      via: "match arm 24 => parse_v24_body_summary(payload)"
      pattern: "24 => parse_v24_body_summary"
---

<objective>
Add the DataPacketBodySummary::V24History variant to protocol.rs and wire parse_data_packet_body_summary() to route packet_k == 24 to it. Verify all byte offsets from verify_v24.py::decode_v24() with a synthetic-payload unit test.

Purpose: The existing code at line 542 lumps packet_k == 24 into NormalHistory — this loses all V24 biometric fields. This plan extracts every field at the verified byte offsets.

Output: V24History variant + parse_v24_body_summary() function + unit test in integration test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DataPacketBodySummary::V24History variant and parse_v24_body_summary()</name>
  <files>Rust/core/src/protocol.rs</files>
  <read_first>
    Read Rust/core/src/protocol.rs lines 137-166 (DataPacketBodySummary enum) and lines 531-555 (parse_data_packet_body_summary function) to understand exact insertion points.
  </read_first>
  <behavior>
    - V24History fields: hr: Option&lt;u8&gt;, rr_intervals_ms: Vec&lt;u16&gt;, ppg_green: Option&lt;u16&gt;, ppg_red_ir: Option&lt;u16&gt;, gravity_x: Option&lt;f32&gt;, gravity_y: Option&lt;f32&gt;, gravity_z: Option&lt;f32&gt;, skin_contact: Option&lt;u8&gt;, spo2_red: Option&lt;u16&gt;, spo2_ir: Option&lt;u16&gt;, skin_temp_raw: Option&lt;u16&gt;, ambient: Option&lt;u16&gt;, led1: Option&lt;u16&gt;, led2: Option&lt;u16&gt;, resp_raw: Option&lt;u16&gt;, sig_quality: Option&lt;u16&gt;, warnings: Vec&lt;String&gt;
    - Short payload (data.len() &lt; 77) returns Ok + all biometric fields None + warning "v24_payload_too_short"
    - rr_intervals_ms: read up to 4 × u16 at data[16+2i] (i=0..min(rr_count,4)), skip zeros
    - gravity values read as f32 little-endian (4 bytes each)
  </behavior>
  <action>
    In protocol.rs, add V24History { ... } as a new variant to the DataPacketBodySummary enum (after RawMotionK21, before the closing brace). All fields are Option&lt;T&gt; except rr_intervals_ms (Vec) and warnings (Vec).

    Add a private function parse_v24_body_summary(payload: &[u8]) -> (Option&lt;DataPacketBodySummary&gt;, Vec&lt;String&gt;). The payload parameter is the full frame payload (data = payload[3:]). Use this helper:

      fn read_u16_le_data(data: &[u8], offset: usize) -> Option&lt;u16&gt; where data is pkt[3:].

    The function reads (all from data = payload[3:]):
      - hr: data.get(14).copied()
      - rr_count: data.get(15).copied().unwrap_or(0) as usize, clamped to 4
      - rr_intervals_ms: (0..rr_count).filter_map(|i| { let o = 16 + 2*i; read_u16_le(data, o) }).filter(|&v| v != 0).collect()
      - ppg_green: read_u16_le(data, 26)  [existing read_u16_le reads from slice directly]
      - ppg_red_ir: read_u16_le(data, 28)
      - gravity_x: read_f32_le(data, 33)  [implement read_f32_le if absent: f32::from_le_bytes]
      - gravity_y: read_f32_le(data, 37)
      - gravity_z: read_f32_le(data, 41)
      - skin_contact: data.get(48).copied()
      - spo2_red: read_u16_le(data, 61)
      - spo2_ir: read_u16_le(data, 63)
      - skin_temp_raw: read_u16_le(data, 65)
      - ambient: read_u16_le(data, 67)
      - led1: read_u16_le(data, 69)
      - led2: read_u16_le(data, 71)
      - resp_raw: read_u16_le(data, 73)
      - sig_quality: read_u16_le(data, 75)

    data = &payload[3..] (slice from byte 3 onward). If payload.len() &lt; 3, return (None, vec!["v24_payload_too_short"]).

    If data.len() &lt; 77 (highest offset 75 + 2 bytes for u16): all biometric fields are None, push "v24_payload_too_short" to warnings, still return Some(V24History { ... }) with None fields.

    In parse_data_packet_body_summary(), change the match arm:
      Before: 7 | 9 | 12 | 18 | 24 => (Some(NormalHistory { ... }), Vec::new())
      After:  7 | 9 | 12 | 18 => (Some(NormalHistory { ... }), Vec::new())
    Add a new arm: 24 => parse_v24_body_summary(payload)

    Also update the body_hex exclusion pattern: change matches!(packet_k, Some(10) | Some(21)) to matches!(packet_k, Some(10) | Some(21) | Some(24)) so V24 frames also omit body_hex from cached JSON.

    Note: read_f32_le does not exist yet. Add:
      fn read_f32_le(data: &[u8], offset: usize) -> Option&lt;f32&gt; {
          let bytes = data.get(offset..offset+4)?;
          Some(f32::from_le_bytes([bytes[0], bytes[1], bytes[2], bytes[3]]))
      }

    Do not add any free-standing crate imports. f32::from_le_bytes is in core.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo check -p goose-core 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check passes. V24History variant compiles. parse_v24_body_summary() routes packet_k==24 correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test — synthetic V24 payload field offsets</name>
  <files>Rust/core/tests/v24_biometric_protocol_tests.rs</files>
  <read_first>
    Read Rust/core/tests/ directory listing to understand the naming convention for integration test files. Check if a v24 test file already exists.
  </read_first>
  <behavior>
    - test_v24_body_summary_field_offsets: 79-byte payload with known values at each field offset asserts V24History fields match exactly
    - test_v24_short_payload: 10-byte payload returns Some(V24History) with all biometric fields None and warnings containing "v24_payload_too_short"
    - test_v24_rr_zero_skip: rr_count=3 with one zero value at data[18] produces rr_intervals_ms with only 2 entries (zero skipped)
  </behavior>
  <action>
    Create Rust/core/tests/v24_biometric_protocol_tests.rs.

    Import: use goose_core::protocol::{DataPacketBodySummary, ParsedPayload};

    Construct a synthetic 82-byte frame (3-byte pkt header + 79-byte data) where data = &payload[3:]:
      - data[14] = 72u8  (hr = 72 bpm)
      - data[15] = 2u8   (rr_count = 2)
      - data[16..18] = 900u16.to_le_bytes()  (rr[0] = 900 ms)
      - data[18..20] = 950u16.to_le_bytes()  (rr[1] = 950 ms)
      - data[26..28] = 1000u16.to_le_bytes() (ppg_green = 1000)
      - data[28..30] = 2000u16.to_le_bytes() (ppg_red_ir = 2000)
      - data[33..37] = 0.98f32.to_le_bytes() (gravity_x = 0.98)
      - data[37..41] = 0.01f32.to_le_bytes() (gravity_y = 0.01)
      - data[41..45] = 0.05f32.to_le_bytes() (gravity_z = 0.05)
      - data[48] = 1u8   (skin_contact = 1)
      - data[61..63] = 800u16.to_le_bytes()  (spo2_red = 800)
      - data[63..65] = 1200u16.to_le_bytes() (spo2_ir = 1200)
      - data[65..67] = 930u16.to_le_bytes()  (skin_temp_raw = 930)
      - data[67..69] = 250u16.to_le_bytes()  (ambient = 250)
      - data[69..71] = 100u16.to_le_bytes()  (led1 = 100)
      - data[71..73] = 200u16.to_le_bytes()  (led2 = 200)
      - data[73..75] = 450u16.to_le_bytes()  (resp_raw = 450)
      - data[75..77] = 9000u16.to_le_bytes() (sig_quality = 9000)

    All other bytes = 0. The frame is 3 + 79 = 82 bytes (data runs to offset 76 inclusive within the 79-byte slice).

    The parse function is not directly exposed; construct a ParsedFrame or call parse_v24_body_summary indirectly. Since the function is private, use the public parse_frame_batch or a test-only path. Check if protocol.rs exposes a pub(crate) or #[cfg(test)] entrypoint. If parse_v24_body_summary is private, make it pub(crate) in Task 1.

    Alternatively: in Task 1, mark parse_v24_body_summary as pub(crate) so tests can call it directly.

    In the test: call goose_core::protocol::parse_v24_body_summary_for_test(&payload) — add a #[cfg(test)] pub fn wrapper in protocol.rs that calls parse_v24_body_summary.

    Actually use this pattern (simpler): add to protocol.rs:
      #[cfg(test)]
      pub fn parse_v24_body_for_test(payload: &[u8]) -> (Option&lt;DataPacketBodySummary&gt;, Vec&lt;String&gt;) {
          parse_v24_body_summary(payload)
      }

    Then in the test file:
      use goose_core::protocol::parse_v24_body_for_test;

    Assert in test_v24_body_summary_field_offsets:
      - matches!(summary, Some(DataPacketBodySummary::V24History { hr: Some(72), .. }))
      - rr_intervals_ms == [900, 950]
      - ppg_green == Some(1000)
      - spo2_red == Some(800), spo2_ir == Some(1200)
      - skin_temp_raw == Some(930)
      - skin_contact == Some(1)
      - resp_raw == Some(450)
      - sig_quality == Some(9000)
      - gravity_x approximately 0.98 (f32 comparison with epsilon 1e-5)
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core v24_biometric_protocol_tests 2>&1 | tail -15</automated>
  </verify>
  <done>All 3 tests (field offsets, short payload, rr zero-skip) pass. cargo test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE frame → Rust parser | Raw bytes from WHOOP device; offsets unverified at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | parse_v24_body_summary offset reads | mitigate | All reads via get() returning Option — no unchecked indexing; short-payload path returns None fields |
| T-27-02 | Denial of Service | rr_count field drives loop bound | mitigate | rr_count clamped to 4 before loop; no unbounded allocation |
| T-27-SC | Tampering | No new package installs | accept | No crate additions; f32::from_le_bytes is core std |
</threat_model>

<verification>
cargo test -p goose-core v24_biometric_protocol_tests — all 3 tests green
cargo check -p goose-core — zero errors
</verification>

<success_criteria>
- DataPacketBodySummary::V24History variant compiles and is Serialize/Deserialize
- parse_data_packet_body_summary() routes packet_k==24 to parse_v24_body_summary() (no longer falls through to NormalHistory)
- Synthetic 82-byte payload: all 16 fields decoded at correct byte offsets verified by test
- Short payload (< 77 data bytes): returns Some(V24History) with None fields + "v24_payload_too_short" warning
- rr zero-skip: zero u16 values excluded from rr_intervals_ms
- Body_hex exclusion updated to include Some(24)
</success_criteria>

<output>
Create .planning/phases/27-v24-biometric-decode/27-01-SUMMARY.md when done
</output>
