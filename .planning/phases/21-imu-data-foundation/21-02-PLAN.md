---
phase: 21-imu-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/store.rs
  - Rust/core/tests/store_tests.rs
autonomous: true
requirements: [IMU-02]
must_haves:
  truths:
    - "The gravity table exists in the SQLite schema at user_version 15"
    - "gravity has columns (device_id TEXT, ts REAL, x REAL, y REAL, z REAL) and an index on (device_id, ts)"
    - "GooseStore::insert_gravity_rows inserts a batch of gravity rows"
    - "GooseStore::gravity_rows_between returns rows for a device within a [ts_start, ts_end) window, ordered by ts"
  artifacts:
    - path: "Rust/core/src/store.rs"
      provides: "schema v15 gravity DDL + insert_gravity_rows + gravity_rows_between"
      contains: "CREATE TABLE IF NOT EXISTS gravity"
    - path: "Rust/core/tests/store_tests.rs"
      provides: "Tests for table creation, insert, and time-range query"
      contains: "gravity_rows_between"
  key_links:
    - from: "GooseStore::initialize"
      to: "gravity table + idx_gravity_device_ts"
      via: "migration v15 DDL gated under schema_version < 15"
      pattern: "VALUES \\(15\\)"
    - from: "GooseStore::gravity_rows_between"
      to: "gravity table"
      via: "SELECT ... WHERE device_id = ?1 AND ts >= ?2 AND ts < ?3 ORDER BY ts"
      pattern: "FROM gravity"
---

<objective>
Add the `gravity` SQLite table via schema migration v14 → v15 and implement the `insert_gravity_rows` (batch insert) and `gravity_rows_between` (time-range query) methods on `GooseStore`. This is the persistence layer for IMU acceleration data; the bridge extraction in plan 21-03 calls these methods.

Purpose: Provide a storage destination for per-sample gravity rows (IMU-02).
Output: `gravity` table at user_version 15 with a `(device_id, ts)` index, plus two store methods covered by tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-imu-data-foundation/21-CONTEXT.md

@Rust/core/src/store.rs
@Rust/core/tests/store_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gravity table schema migration v15</name>
  <files>Rust/core/src/store.rs</files>
  <read_first>
    - Rust/core/src/store.rs:934-1425 — `GooseStore::initialize`: the single `execute_batch` DDL block. Table CREATEs are at the top; the migration markers `INSERT OR IGNORE INTO goose_schema_migrations(version) VALUES (N)` run 1..14 at lines 1409-1422; `PRAGMA user_version = 14;` is at line 1423.
    - Rust/core/src/store.rs:1435-1439 — `schema_version` reads `PRAGMA user_version`.
    - Rust/core/src/store.rs:939-970 — example CREATE TABLE + the project's `IF NOT EXISTS` and `created_at` default conventions.
  </read_first>
  <action>
    Inside the `execute_batch` DDL string in `GooseStore::initialize`:
    1. Add a new table CREATE alongside the others (placement near the end of the CREATE block is fine):
       `CREATE TABLE IF NOT EXISTS gravity (device_id TEXT NOT NULL, ts REAL NOT NULL, x REAL NOT NULL, y REAL NOT NULL, z REAL NOT NULL, created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')));`
       (Columns per IMU-02: device_id TEXT, ts REAL, x REAL, y REAL, z REAL. created_at follows the existing convention used by every other table in this block.)
    2. Add the index: `CREATE INDEX IF NOT EXISTS idx_gravity_device_ts ON gravity(device_id, ts);`
    3. Add the migration marker after the VALUES (14) line: `INSERT OR IGNORE INTO goose_schema_migrations(version) VALUES (15);`
    4. Change `PRAGMA user_version = 14;` to `PRAGMA user_version = 15;`

    Do not introduce a separate `ensure_*` helper (the gravity table has no columns added to a pre-existing table, so the in-DDL `IF NOT EXISTS` pattern matches v1-14; per 21-CONTEXT Schema Migration Pattern).
  </action>
  <verify>
    <automated>cd Rust/core && cargo build -p goose-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CREATE TABLE IF NOT EXISTS gravity" Rust/core/src/store.rs` returns one match.
    - `grep -n "idx_gravity_device_ts" Rust/core/src/store.rs` returns one match.
    - `grep -n "VALUES (15)" Rust/core/src/store.rs` returns one match and `grep -n "PRAGMA user_version = 15" Rust/core/src/store.rs` returns one match (no remaining `= 14`).
    - `cd Rust/core && cargo build -p goose-core` succeeds.
  </acceptance_criteria>
  <done>gravity table + index created; schema migration v15 marker present; user_version bumped to 15; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement insert_gravity_rows + gravity_rows_between with tests</name>
  <files>Rust/core/src/store.rs, Rust/core/tests/store_tests.rs</files>
  <read_first>
    - Rust/core/src/store.rs:3817-3951 — `insert_step_counter_sample` / `step_counter_samples_between`: the canonical pattern for a batch-ish insert + a `_between` query using `prepare` + `query_map` + `params!` and validation helpers (`validate_non_negative`, `validate_window_order`).
    - Rust/core/src/store.rs:1846-1906 — `insert_raw_evidence` / `insert_decoded_frame`: `self.conn.execute(...)` returning a changed-row count.
    - Rust/core/tests/store_tests.rs (top ~60 lines) — how a test `GooseStore` is opened (temp DB) and the existing `*_between` test pattern.
  </read_first>
  <behavior>
    - Test 1: After `insert_gravity_rows` with 3 rows for device "A", `gravity_rows_between("A", t0, t0+10.0)` returns all 3 rows in ascending ts order with x/y/z intact.
    - Test 2: `gravity_rows_between` excludes rows at or after the end bound (half-open `[start, end)`) and excludes other devices' rows.
    - Test 3: Inserting an empty slice is a no-op returning 0 inserted rows; the table stays empty.
  </behavior>
  <action>
    Define a row type and an input shape near the other store types:
    - A `GravityRow` struct: `{ device_id: String, ts: f64, x: f64, y: f64, z: f64 }` with `#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]` (do NOT derive Eq — f64 is not Eq).
    - `pub fn insert_gravity_rows(&self, device_id: &str, rows: &[(f64, f64, f64, f64)]) -> GooseResult<usize>` where each tuple is `(ts, x, y, z)`. Validate device_id is non-empty (`validate_required("device_id", device_id)`). Iterate and `self.conn.execute("INSERT INTO gravity (device_id, ts, x, y, z) VALUES (?1, ?2, ?3, ?4, ?5)", params![device_id, ts, x, y, z])` accumulating the inserted count; return the total. Empty slice → return Ok(0). (Per 21-CONTEXT Discretion: one row per sample timestamp.)
    - `pub fn gravity_rows_between(&self, device_id: &str, ts_start: f64, ts_end: f64) -> GooseResult<Vec<GravityRow>>`. Validate device_id non-empty and `ts_start <= ts_end` (return GooseError on inverted window, mirroring `validate_window_order`). Prepare `SELECT device_id, ts, x, y, z FROM gravity WHERE device_id = ?1 AND ts >= ?2 AND ts < ?3 ORDER BY ts`, `query_map` into `GravityRow`, collect.

    Match existing method placement and `GooseResult`/`params!`/`GooseError::from` conventions (21-CONTEXT Rust Conventions).
  </action>
  <verify>
    <automated>cd Rust/core && cargo test -p goose-core gravity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn insert_gravity_rows" Rust/core/src/store.rs` and `grep -n "pub fn gravity_rows_between" Rust/core/src/store.rs` each return one match.
    - `grep -n "FROM gravity" Rust/core/src/store.rs` shows the half-open window query (`ts >= ?2 AND ts < ?3`).
    - `grep -c "gravity" Rust/core/tests/store_tests.rs` returns at least 3 (table/insert/query test coverage).
    - `cd Rust/core && cargo test -p goose-core` green; the gravity tests cover insert, time-range query (half-open + device isolation), and empty-insert no-op.
  </acceptance_criteria>
  <done>insert_gravity_rows and gravity_rows_between implemented and callable; cargo test green covering table creation (implicit), insert, and time-range query.</done>
</task>

</tasks>

<verification>
- `cd Rust/core && cargo build -p goose-core` succeeds.
- `cd Rust/core && cargo test -p goose-core` green.
- A fresh DB reports `PRAGMA user_version == 15` and the gravity table + index exist.
</verification>

<success_criteria>
1. gravity table exists at schema v15 with the specified columns and `(device_id, ts)` index.
2. insert_gravity_rows and gravity_rows_between are callable on GooseStore.
3. Tests cover insert, half-open time-range query, device isolation, and empty-insert no-op.
4. `cargo test -p goose-core` green.
</success_criteria>

<output>
Create `.planning/phases/21-imu-data-foundation/21-02-SUMMARY.md` when done.
</output>
