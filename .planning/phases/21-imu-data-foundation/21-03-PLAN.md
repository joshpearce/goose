---
phase: 21-imu-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - Rust/core/src/bridge.rs
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements: [IMU-03, IMU-04]
must_haves:
  truths:
    - "K10/K21 decoded frames populate the gravity Vec with LSB-to-g converted rows instead of an empty placeholder"
    - "An IMU_LSB_PER_G named constant (3900.0) drives the LSB→g conversion and is the single configurable factor"
    - "store.insert_gravity_rows and store.gravity_rows_between are exposed as bridge methods and registered in BRIDGE_METHODS"
    - "A code comment documents that TOGGLE_IMU_MODE (command 106) is already sent in production startPhysiologyCapture (IMU-04, no behavioural change)"
  artifacts:
    - path: "Rust/core/src/bridge.rs"
      provides: "K10 gravity extraction + IMU_LSB_PER_G constant + two new bridge methods + IMU-04 doc comment"
      contains: "IMU_LSB_PER_G"
    - path: "Rust/core/tests/bridge_tests.rs"
      provides: "Test: known K10 payload yields LSB-to-g converted gravity rows; insert+query roundtrip"
      contains: "gravity"
  key_links:
    - from: "upload_get_recent_decoded_streams_bridge K10 arm"
      to: "gravity Vec"
      via: "full_samples axes (accelerometer_x/y/z) / IMU_LSB_PER_G per sample"
      pattern: "IMU_LSB_PER_G"
    - from: "store.insert_gravity_rows bridge method"
      to: "GooseStore::insert_gravity_rows"
      via: "bridge dispatch arm + BRIDGE_METHODS registration"
      pattern: "store.insert_gravity_rows"
---

<objective>
Replace the empty-Vec gravity placeholder in `bridge.rs` with real K10 (and, where parseable, K21) gravity extraction: read the `accelerometer_x/y/z` axes' `full_samples` (from plan 21-01), convert each LSB sample to g via a named `IMU_LSB_PER_G` constant, and emit one gravity row per sample timestamp. Expose `store.insert_gravity_rows` and `store.gravity_rows_between` (plan 21-02) as bridge methods. Document that TOGGLE_IMU_MODE (command 106) is already sent in production (IMU-04 — no code change beyond a comment).

Purpose: Complete the IMU data pipeline so acceleration data reaches the gravity table (IMU-03) and record the already-shipped IMU toggle status (IMU-04).
Output: Populated gravity stream in the upload payload; two callable bridge methods; an IMU-04 documentation comment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-imu-data-foundation/21-CONTEXT.md

@Rust/core/src/bridge.rs
@Rust/core/tests/bridge_tests.rs
@.planning/phases/21-imu-data-foundation/21-01-PLAN.md
@.planning/phases/21-imu-data-foundation/21-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and dispatch the two gravity bridge methods</name>
  <files>Rust/core/src/bridge.rs</files>
  <read_first>
    - Rust/core/src/bridge.rs:176-298 — `BRIDGE_METHODS: &[&str]` allowlist (alphabetically grouped; "storage.check"/"storage.compact_raw_evidence" at 293-294, "upload.get_recent_decoded_streams" at 297). New methods must be added here or they are rejected.
    - Rust/core/src/bridge.rs:2115-2664 — `match request.method.as_str()` dispatch; see the `upload.get_recent_decoded_streams` arm (2655-2660) and `storage.compact_raw_evidence` arm (2661-2664) for the `request_args::<T>(&request).and_then(fn).map(bridge_ok).unwrap_or_else(bridge_error)` pattern.
    - Rust/core/src/bridge.rs:3069-3074 — `UploadGetRecentDecodedStreamsArgs` struct (shows `database_path`/`device_id` arg-struct conventions and `#[derive(Deserialize)]`).
    - Rust/core/src/store.rs — `insert_gravity_rows(&self, device_id, rows: &[(f64,f64,f64,f64)])` and `gravity_rows_between(&self, device_id, ts_start, ts_end)` signatures from plan 21-02 (consume them as-is).
  </read_first>
  <action>
    1. Define two arg structs near the other bridge arg structs (mirroring `UploadGetRecentDecodedStreamsArgs`):
       - `InsertGravityRowsArgs { database_path: String, device_id: String, rows: Vec<GravityRowArg> }` where `GravityRowArg { ts: f64, x: f64, y: f64, z: f64 }` (both `#[derive(Debug, Clone, Deserialize)]`). This matches 21-CONTEXT: each gravity row is `{"ts", "x", "y", "z"}`.
       - `GravityRowsBetweenArgs { database_path: String, device_id: String, ts_start: f64, ts_end: f64 }` (per 21-CONTEXT: `"store.gravity_rows_between"` accepts `{device_id, ts_start, ts_end, database_path}`).
    2. Write two handler fns:
       - `fn insert_gravity_rows_bridge(args: InsertGravityRowsArgs) -> GooseResult<serde_json::Value>`: open the store via `open_bridge_store(&args.database_path)?`, map `args.rows` to `(ts, x, y, z)` tuples, call `store.insert_gravity_rows(&args.device_id, &tuples)?`, return `json!({"inserted": inserted_count})`.
       - `fn gravity_rows_between_bridge(args: GravityRowsBetweenArgs) -> GooseResult<serde_json::Value>`: open store, call `store.gravity_rows_between(&args.device_id, args.ts_start, args.ts_end)?`, map each `GravityRow` to `json!({"ts":..,"x":..,"y":..,"z":..})`, return `json!({"rows": rows})`.
    3. Add two dispatch arms in the `match request.method.as_str()` block (alphabetical placement near other `store.*`/`storage.*` methods is fine), each using the `request_args::<T>(&request).and_then(handler).map(|v| bridge_ok(&request.request_id, v)).unwrap_or_else(|e| bridge_error(&request.request_id, "method_error", e))` pattern.
    4. Register both method names in `BRIDGE_METHODS`: `"store.gravity_rows_between"` and `"store.insert_gravity_rows"` (keep the array's existing ordering style).
  </action>
  <verify>
    <automated>cd Rust/core && cargo build -p goose-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"store.insert_gravity_rows"' Rust/core/src/bridge.rs` returns at least 2 (BRIDGE_METHODS entry + dispatch arm).
    - `grep -c '"store.gravity_rows_between"' Rust/core/src/bridge.rs` returns at least 2.
    - `grep -n "fn insert_gravity_rows_bridge" Rust/core/src/bridge.rs` and `grep -n "fn gravity_rows_between_bridge" Rust/core/src/bridge.rs` each return one match.
    - `cd Rust/core && cargo build -p goose-core` succeeds.
  </acceptance_criteria>
  <done>Both bridge methods are registered in BRIDGE_METHODS, dispatched, and call the store methods; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract K10 gravity rows with LSB-to-g conversion + IMU-04 doc</name>
  <files>Rust/core/src/bridge.rs</files>
  <read_first>
    - Rust/core/src/bridge.rs:3087-3231 — `upload_get_recent_decoded_streams_bridge`: the empty `let gravity: Vec<serde_json::Value> = Vec::new();` placeholder is at line 3106; the K10 arm (`DataPacketBodySummary::RawMotionK10 { heart_rate, .. }`) is at 3150-3155; the K21 `no direct extraction` comment is at 3161-3164; `gravity` is returned in the result json at 3228.
    - Rust/core/src/protocol.rs:586-602 — `parse_k10_raw_motion_summary`: K10 axes are pushed in order `accelerometer_x` (offset 85), `accelerometer_y` (285), `accelerometer_z` (485), then 3 gyroscope axes; each axis has `full_samples` after plan 21-01. NOTE: 21-CONTEXT cites offsets 82/282/482 but the authoritative source uses 85/285/485 — match the axis by its `name` field ("accelerometer_x/y/z"), not by a hardcoded offset, to stay robust.
    - Rust/core/src/protocol.rs:108-122 — `DataPacket` carries `timestamp_seconds: Option<u32>` and `timestamp_subseconds: Option<u16>`; the bridge already binds `timestamp_seconds` (used at 3127/3133 as `ts_unix`).
  </read_first>
  <behavior>
    - Test 1: A decoded K10 frame whose accelerometer_x/y/z full_samples are known integers produces gravity rows where each x/y/z equals the raw LSB divided by IMU_LSB_PER_G (e.g. raw 3900 → 1.0 g), one row per sample index.
    - Test 2: The gravity row count equals the number of aligned samples across the three accel axes (min of the three lengths); the first row's ts equals the frame's base ts.
    - Test 3: Roundtrip — inserting the extracted gravity rows via store.insert_gravity_rows then querying via store.gravity_rows_between returns the same converted values.
  </behavior>
  <action>
    1. Add a module-level constant near the top of bridge.rs (or just above the upload handler): `const IMU_LSB_PER_G: f64 = 3900.0;` with a comment noting it is the configurable WHOOP accelerometer scale (21-CONTEXT: research-confirmed ~3900 LSB/g).
    2. Change the placeholder at 3106 to `let mut gravity: Vec<serde_json::Value> = Vec::new();`.
    3. In the K10 arm (3150-3155), in addition to the existing heart_rate push, locate the three accel axes by name from the `axes: Vec<I16SeriesSummary>` (find axis where `name == "accelerometer_x"`, etc.). Read each axis's `full_samples` (Some(Vec<i16>) after plan 21-01). Compute `n = min(len_x, len_y, len_z)`; for `i in 0..n`, push `json!({"ts": ts_unix, "x": x[i] as f64 / IMU_LSB_PER_G, "y": y[i] as f64 / IMU_LSB_PER_G, "z": z[i] as f64 / IMU_LSB_PER_G})`. Use the frame's `ts_unix` base timestamp for `ts` (per-sample sub-timestamp interpolation is deferred per 21-CONTEXT Deferred Ideas — base ts is acceptable for v5.0). If any accel axis or its full_samples is missing, skip gravity extraction for that frame (no panic).
    4. For the K21 arm (3161-3164): K21 axes are grouped/variable-count and the per-axis physical mapping is not yet confirmed (21-CONTEXT Deferred: "Type-51/K21 body may have different format; defer until empirical data available"). Keep K21 as a documented skip — replace the comment with one stating K21 gravity extraction is deferred (axis-to-physical mapping unconfirmed) and that only K10 accel axes are converted in this phase. Do NOT fabricate a K21 conversion.
    5. IMU-04 documentation: add a comment in this handler (or just above the K10 arm) stating: "TOGGLE_IMU_MODE (command 106) is already sent in production by SensorStreamCommandKind.startPhysiologyCapture / stopPhysiologyCapture (Swift); no Rust toggle needed — this pipeline only stores the resulting K10/K21 data." This satisfies IMU-04's documentation requirement (per 21-CONTEXT IMU-04: no action beyond noting in code).
  </action>
  <verify>
    <automated>cd Rust/core && cargo test -p goose-core 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const IMU_LSB_PER_G: f64 = 3900.0" Rust/core/src/bridge.rs` returns one match.
    - `grep -n "let mut gravity" Rust/core/src/bridge.rs` returns one match (placeholder is now mutable and populated).
    - `grep -n "TOGGLE_IMU_MODE" Rust/core/src/bridge.rs` returns at least one match (IMU-04 doc comment).
    - K21 retains an explicit "deferred" comment (no fabricated conversion): `grep -ni "k21.*defer\|defer.*k21\|unconfirmed" Rust/core/src/bridge.rs` returns at least one match.
    - `cd Rust/core && cargo test -p goose-core` green; a bridge test asserts LSB-to-g conversion (raw 3900 → 1.0) and the insert/query roundtrip.
  </acceptance_criteria>
  <done>K10 frames populate gravity with LSB-to-g rows via IMU_LSB_PER_G; K21 is explicitly deferred with a comment; IMU-04 status documented; cargo test green.</done>
</task>

</tasks>

<verification>
- `cd Rust/core && cargo build -p goose-core` succeeds.
- `cd Rust/core && cargo test -p goose-core` green.
- The upload payload's `gravity` array is non-empty for a K10 frame with accelerometer full_samples.
- IMU-04 status and K21-deferred rationale are both documented in code.
</verification>

<success_criteria>
1. K10/K21 frames no longer return an empty gravity placeholder — K10 accel axes are converted LSB→g via IMU_LSB_PER_G (K21 explicitly deferred with rationale).
2. store.insert_gravity_rows and store.gravity_rows_between are callable bridge methods, registered in BRIDGE_METHODS.
3. TOGGLE_IMU_MODE already-in-production status is documented in code (IMU-04).
4. cargo test green; tests cover extraction with a known K10 payload, LSB-to-g conversion correctness, and the insert/query roundtrip.
</success_criteria>

<output>
Create `.planning/phases/21-imu-data-foundation/21-03-SUMMARY.md` when done.
</output>
