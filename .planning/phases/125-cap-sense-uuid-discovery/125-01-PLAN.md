---
phase: "125"
plan: "125-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
  - GooseSwift/MoreDebugViews.swift
  - .planning/research/whoop-5/CAPSENSE-UUID.md
autonomous: true
requirements:
  - CAPSENSE-01
must_haves:
  truths:
    - "D-01: UUID fd4b0004-cce1-4033-93ce-002d5875f58a (EVENTS_FROM_STRAP) is the cap sense characteristic — already subscribed, no new subscription needed"
    - "D-02: isOnWrist is set to true when EVENTS_FROM_STRAP notification carries event type 10 (STRAP_DETECTED); set to false for event type 11 (STRAP_REMOVED); no mutation for any other event type"
    - "D-03: handleCapSenseEventValue wraps isOnWrist assignment in DispatchQueue.main.async { [weak self] in ... } per codebase pattern, consistent with handleBodyLocationValue at HistoricalHandlers.swift:1084 — both isOnWrist setters reach main via the same mechanism (per C-001/C-002)"
    - "D-04: cmd 0x54 path in HistoricalHandlers.swift:1084 and the new cap sense path co-exist; both set isOnWrist; last-write wins; no conflict"
    - "D-05: .planning/research/whoop-5/CAPSENSE-UUID.md exists and documents UUID fd4b0004, event codes 10/11, and event byte layout"
    - "D-06: Section(WHOOP Event Signals) in MoreDebugViews.swift contains a Cap Sense row showing On wrist / Off wrist / Unknown with the fd4b0004 UUID label"
  artifacts:
    - GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift (handleCapSenseEventValue method added; fan-in call after handleBodyLocationValue per D-03)
    - GooseSwift/MoreDebugViews.swift (MoreInfoRow for Cap Sense in Section("WHOOP Event Signals") per D-06)
    - .planning/research/whoop-5/CAPSENSE-UUID.md (cap sense UUID + event code documentation per D-05)
  key_links:
    - handleCapSenseEventValue uses DispatchQueue.main.async { [weak self] in ... } for isOnWrist assignment per D-03 — same pattern as handleBodyLocationValue; main→main dispatch is a safe no-op; [weak self] prevents retain cycle from escaping closure
    - notificationCharacteristicIDs.contains(characteristic.uuid) guard covers both fd4b0004 and 61080004 (PUFFIN parity, per Pitfall 2)
    - cmd 0x54 path in HistoricalHandlers.swift:1084 remains unchanged and co-exists per D-04
---

<objective>
Parse EVENTS_FROM_STRAP (fd4b0004-cce1-4033-93ce-002d5875f58a) notifications to detect cap sense on-wrist state in real time. Add handleCapSenseEventValue to the peripheral delegate fan-out, expose the state in the Debug tab, and document the UUID + event codes in CAPSENSE-UUID.md.

Decisions implemented:
- D-01: UUID fd4b0004 (EVENTS_FROM_STRAP) confirmed as cap sense characteristic; already subscribed; documented in CAPSENSE-UUID.md
- D-02: Event types 10 (STRAP_DETECTED) and 11 (STRAP_REMOVED) from bytes 2-3 (UInt16 LE) set isOnWrist true/false; all other types ignored
- D-03: handleCapSenseEventValue added to handlePeripheralValueUpdate fan-out in CoreBluetoothBLETransport+PeripheralDelegate.swift; isOnWrist assignment wrapped in DispatchQueue.main.async { [weak self] in ... } per codebase pattern (matches handleBodyLocationValue at HistoricalHandlers.swift:1084)
- D-04: cmd 0x54 path (HistoricalHandlers.swift:1084) and cap sense path co-exist; both set isOnWrist; last-write wins
- D-05: .planning/research/whoop-5/CAPSENSE-UUID.md created with UUID, event codes 10/11, and byte layout
- D-06: Section("WHOOP Event Signals") in MoreDebugViews.swift gains a Cap Sense row showing on-wrist state with fd4b0004 UUID label

Output:
- handleCapSenseEventValue method in CoreBluetoothBLETransport+PeripheralDelegate.swift
- Fan-in call site after handleBodyLocationValue in handlePeripheralValueUpdate
- MoreInfoRow in Section("WHOOP Event Signals") in MoreDebugViews.swift
- .planning/research/whoop-5/CAPSENSE-UUID.md
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
@GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
@GooseSwift/MoreDebugViews.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add handleCapSenseEventValue and fan-in call to peripheral delegate (D-01, D-02, D-03, D-04)</name>
  <files>GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift</files>
  <action>
    Read CoreBluetoothBLETransport+PeripheralDelegate.swift in full to locate handlePeripheralValueUpdate and its existing handler fan-out (currently ends with handleFeatureFlagValue).

    Step 1 — Add the fan-in call site in handlePeripheralValueUpdate.

    Inside handlePeripheralValueUpdate, after the existing handleBodyLocationValue call and after the existing handleFeatureFlagValue call, add one new line per D-03:

      handleCapSenseEventValue(value, characteristic: characteristic)

    Keep it on its own line, consistent with the surrounding call style.

    Step 2 — Add handleCapSenseEventValue method.

    Add the method in the same extension file, grouped logically near the other handlers (after handleBodyLocationValue or at the end of the extension block). The method signature and body follow the handleAlarmEvent pattern verified in RESEARCH.md §4:

    - Guard: notificationCharacteristicIDs.contains(characteristic.uuid) — this covers both fd4b0004 AND 61080004 without naming either UUID explicitly, giving automatic PUFFIN parity (per Pitfall 2 in RESEARCH.md).
    - Iterate frames(in: value). For each frame, extract payload(in: frame).
    - Guard: payload.count >= 4, payload.first == V5PacketType.event.
    - Construct eventType as UInt16(payload[2]) | UInt16(payload[3]) << 8 (little-endian, per D-02 and Pitfall 4).
    - Switch on eventType. For case 10 and 11, wrap the isOnWrist assignment and record call inside DispatchQueue.main.async { [weak self] in ... } per D-03 and C-001/C-002. Pattern to follow exactly as handleBodyLocationValue does at HistoricalHandlers.swift:1084:
        case 10: // STRAP_DETECTED
          DispatchQueue.main.async { [weak self] in
            self?.isOnWrist = true
            self?.record(source: ..., title: ..., body: "STRAP_DETECTED isOnWrist=true")
          }
        case 11: // STRAP_REMOVED
          DispatchQueue.main.async { [weak self] in
            self?.isOnWrist = false
            self?.record(source: ..., title: ..., body: "STRAP_REMOVED isOnWrist=false")
          }
        default: break
    - The [weak self] capture list is mandatory because the closure escapes (async dispatch). Every DispatchQueue.main.async block in this file uses [weak self] — this must not be the exception.
    - Note: shouldDispatchNotificationSideEffectsToMain already pre-routes V5PacketType.event frames to main before handlePeripheralValueUpdate is called; the main→main async dispatch is a safe no-op. Add a // SAFETY: comment in the method noting this pre-condition.

    Name the cases by comment: case 10 gets comment "STRAP_DETECTED", case 11 gets comment "STRAP_REMOVED". This satisfies the documentation requirement in CAPSENSE-01 without needing separate constant declarations.

    The record(source:title:body:) call signature should match existing usage in the file. If the method signature differs, match whatever is already used for comparable handlers.
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E '^(.*error:|Build succeeded|BUILD SUCCEEDED)' | head -20</automated>
  </verify>
  <done>
    - handleCapSenseEventValue method exists in CoreBluetoothBLETransport+PeripheralDelegate.swift
    - Fan-in call handleCapSenseEventValue(value, characteristic: characteristic) appears after handleBodyLocationValue in handlePeripheralValueUpdate
    - Build succeeds with no new errors
    - Event type 10 → DispatchQueue.main.async { [weak self] in self?.isOnWrist = true }; event type 11 → self?.isOnWrist = false; other event types → no mutation
    - isOnWrist assignment is inside DispatchQueue.main.async { [weak self] in ... } — consistent with handleBodyLocationValue (per D-03, C-001, C-002)
  </done>
</task>

<task type="auto">
  <name>Task 2: Debug tab cap sense row and CAPSENSE-UUID.md documentation (D-05, D-06)</name>
  <files>GooseSwift/MoreDebugViews.swift, .planning/research/whoop-5/CAPSENSE-UUID.md</files>
  <action>
    Part A — MoreDebugViews.swift.

    Read MoreDebugViews.swift to locate Section("WHOOP Event Signals") (around line 258). Find the "Latest Event" MoreInfoRow. Insert a new MoreInfoRow for cap sense immediately after it (or at an appropriate position within the section) per D-06.

    The row reads model.ble.isOnWrist (already @Published; no new plumbing needed). Use an exhaustive switch on Optional Bool:

      case .some(true)  → value string: "On wrist (fd4b0004)"
      case .some(false) → value string: "Off wrist (fd4b0004)"
      case nil          → value string: "Unknown — no event received"

    Use systemImage "sensor.tag.radiowaves.forward" (or the closest available symbol; check availability for iOS 26).
    Set status to .pending when isOnWrist is nil, .ready otherwise (matching the MoreInfoRow status convention used by neighboring rows).

    The title string for the row is "Cap Sense". This satisfies SC-3.

    Part B — CAPSENSE-UUID.md.

    Create .planning/research/whoop-5/CAPSENSE-UUID.md with the following content:

    - Title: "WHOOP 5 Cap Sense UUID — Resolved"
    - Date: 2026-06-28
    - Status: RESOLVED (supersedes CAPSENSE-INVESTIGATION.md which was BLOCKED)
    - UUID: fd4b0004-cce1-4033-93ce-002d5875f58a (EVENTS_FROM_STRAP characteristic)
    - Subscription: already in notificationCharacteristicIDs; no new subscription needed
    - PUFFIN equivalent: 61080004-8d6d-82b8-614a-1c8cb0f8dcc6 (covered by notificationCharacteristicIDs guard)
    - Event byte layout table:
        Byte 0: packet type (V5PacketType.event)
        Byte 1: flags/header
        Bytes 2-3: event type (UInt16 little-endian)
        Bytes 4+: event body (varies by event type)
    - Event type codes:
        10 (0x000A): STRAP_DETECTED — device is on-wrist; sets isOnWrist = true
        11 (0x000B): STRAP_REMOVED — device is off-wrist; sets isOnWrist = false
        Other values: ignored by handleCapSenseEventValue
    - Implementation reference: handleCapSenseEventValue in CoreBluetoothBLETransport+PeripheralDelegate.swift
    - Co-existence note: cmd 0x54 (GET_BODY_LOCATION_AND_STATUS) also sets isOnWrist at reconnect; both paths are active; last-write wins
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E '^(.*error:|Build succeeded|BUILD SUCCEEDED)' | head -20</automated>
  </verify>
  <done>
    - MoreDebugViews.swift Section("WHOOP Event Signals") contains a "Cap Sense" MoreInfoRow displaying On wrist/Off wrist/Unknown based on isOnWrist, with "(fd4b0004)" in the value string
    - .planning/research/whoop-5/CAPSENSE-UUID.md exists and documents UUID, event codes 10/11, byte layout, and co-existence note
    - Build succeeds with no new errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE peripheral → CoreBluetooth | Raw notification bytes from WHOOP device; treated as untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-01 | Tampering | handleCapSenseEventValue — payload byte parsing | low | mitigate | Guard payload.count >= 4 before indexing bytes 2-3; malformed payloads are silently skipped via continue |
| T-125-02 | Denial of Service | Flood of fd4b0004 notifications with arbitrary event types | low | accept | Existing shouldDispatchNotificationSideEffectsToMain throttles main-thread dispatch; handleCapSenseEventValue switch default:break discards unknown types cheaply |
| T-125-03 | Spoofing | Rogue BLE peripheral advertising fd4b0004 with crafted event types | low | accept | iOS CoreBluetooth peripheral pairing and WHOOP device identity established upstream; cap sense state is display-only and does not gate security-sensitive operations |
</threat_model>

<verification>
1. Build: xcodebuild build succeeds with no new errors after both tasks.
2. Method present: grep -c "handleCapSenseEventValue" GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift returns >= 2 (definition + call site).
3. Fan-in order: handleCapSenseEventValue call appears after handleBodyLocationValue in handlePeripheralValueUpdate.
4. Event parsing: UInt16 little-endian construction (payload[2]) | (payload[3] << 8) present in implementation.
5. Debug row: grep -c "Cap Sense" GooseSwift/MoreDebugViews.swift returns >= 1.
6. UUID in value string: grep -c "fd4b0004" GooseSwift/MoreDebugViews.swift returns >= 1.
7. Documentation: .planning/research/whoop-5/CAPSENSE-UUID.md exists and contains "STRAP_DETECTED" and "STRAP_REMOVED".
8. DispatchQueue.main.async present: grep -c "DispatchQueue.main.async" GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift returns a count reflecting the new call(s) in handleCapSenseEventValue; [weak self] capture list present in the new closures — consistent with handleBodyLocationValue pattern (C-001/C-002).
</verification>

<success_criteria>
- CAPSENSE-01 satisfied: isOnWrist updated from real-time EVENTS_FROM_STRAP events (types 10/11) distinct from cmd 0x54 path
- Build passes on iOS Simulator (no new errors)
- Debug tab shows cap sense state with characteristic UUID label (fd4b0004)
- CAPSENSE-UUID.md resolves the previously BLOCKED investigation and documents UUID + event byte layout
- cmd 0x54 co-existence: HistoricalHandlers.swift untouched; both isOnWrist setters active
</success_criteria>

<output>
Create `.planning/phases/125-cap-sense-uuid-discovery/125-01-SUMMARY.md` when done.
</output>
