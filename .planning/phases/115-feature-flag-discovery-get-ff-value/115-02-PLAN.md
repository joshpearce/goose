---
phase: 115-feature-flag-discovery-get-ff-value
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - GooseSwift/MoreInfoViews.swift
autonomous: false
requirements: [FF-02, FF-03]

must_haves:
  truths:
    - "The About/Info Debug tab Runtime section shows discovered feature flags as 0x%02X → 0x%02X hex pairs (D-03, FF-02)"
    - "When DeviceCapabilities.featureFlags is empty, the row shows None discovered (D-03, FF-02)"
    - "The display reads from model.ble.connectedCapabilities.featureFlags, which is persisted to and readable from the device_feature_flags table via capabilities.get_feature_flags (FF-02, FF-03)"
  artifacts:
    - path: "GooseSwift/MoreInfoViews.swift"
      provides: "Feature Flags MoreInfoRow in the existing Runtime section"
      contains: "Feature Flags"
  key_links:
    - from: "GooseSwift/MoreInfoViews.swift"
      to: "GooseSwift/CoreBluetoothBLETransport.swift"
      via: "model.ble.connectedCapabilities?.featureFlags read in the Runtime section row"
      pattern: "featureFlags"
---

<objective>
Display discovered feature flags in the existing Debug tab device info (About → Runtime) section
as a list of "0x%02X → 0x%02X" hex pairs, or "None discovered" when empty.

Purpose: Closes the UI-exposure half of FF-02 (#165) and confirms the FF-03 read path
(capabilities.get_feature_flags → connectedCapabilities) surfaces to the user. Minimal UI change
per D-03 — no new section header.
Output: A "Feature Flags" MoreInfoRow added to the Runtime section of MoreInfoViews.swift.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-feature-flag-discovery-get-ff-value/115-CONTEXT.md
@.planning/phases/115-feature-flag-discovery-get-ff-value/115-RESEARCH.md
@.planning/phases/115-feature-flag-discovery-get-ff-value/115-01-PLAN.md

@GooseSwift/MoreInfoViews.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Feature Flags row to the Runtime section</name>
  <files>GooseSwift/MoreInfoViews.swift</files>
  <read_first>
    - GooseSwift/MoreInfoViews.swift lines 110–144 (Runtime Section, MoreInfoRow usage, model access)
    - GooseSwift/MoreInfoViews.swift lines 146–170 (MoreInfoRow component signature: title, value, systemImage, status)
    - GooseSwift/GooseBLETypes.swift DeviceCapabilities.featureFlags field (added in 115-01)
  </read_first>
  <action>
    In the existing Section("Runtime") block (lines 120–123) of MoreInfoViews.swift, add one
    MoreInfoRow titled "Feature Flags" after the existing Device and Hello rows (D-03: existing
    device info section, no new header). Compute the value string from
    model.ble.connectedCapabilities?.featureFlags: if the dictionary is nil or empty, the value is
    "None discovered" (D-03 — covers the 3s-timeout and Gen4 cases); otherwise join the pairs as
    String(format: "0x%02X → 0x%02X", key, value) sorted by key ascending, separated by ", "
    (per CONTEXT.md specifics). Use systemImage "flag" and status .ready when flags are present,
    .pending when "None discovered" (matching the existing Hello/Device status convention). Add a
    private computed property on the view (e.g. featureFlagsSummary) for the value string to keep
    the body readable, mirroring the existing appVersion computed property at lines 139–143. Read
    flags directly from connectedCapabilities — do NOT add a separate get_feature_flags bridge call
    in the view; the value is already on the connected capabilities after 115-01 persists+populates it.
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E 'error:|BUILD SUCCEEDED|BUILD FAILED'</automated>
  </verify>
  <done>A "Feature Flags" MoreInfoRow exists in the Runtime section, shows "None discovered" when empty and "0x%02X → 0x%02X" hex pairs when populated, sorted by key; the project builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visually verify Feature Flags row</name>
  <action>Blocking human checkpoint. Confirm the Feature Flags row renders correctly in About → Runtime for both empty ("None discovered") and populated (hex-pair) states. See what-built / how-to-verify below.</action>
  <what-built>
    The "Feature Flags" row in About → Runtime. With no device connected (or a device that returns
    no flags / a Gen4 device that times out) it shows "None discovered". With flags discovered it
    shows the hex-pair list.
  </what-built>
  <how-to-verify>
    1. Build and run GooseSwift on the iOS Simulator (or a connected device with a WHOOP strap).
    2. Navigate to the More tab → About.
    3. In the Runtime section, confirm a "Feature Flags" row appears below "Hello".
    4. With no flags / no device: confirm it reads "None discovered".
    5. If a real device returns flags: confirm the value is a comma-separated list of "0x__ → 0x__"
       hex pairs sorted ascending by index, and matches the flags logged by handleFeatureFlagValue.
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/value issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only display of already-validated, in-memory featureFlags; no new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04 | Information Disclosure | Feature Flags shown in Debug UI | accept | Local device-only debug surface; flag indices/values are not secrets (UInt8 identifiers) |
</threat_model>

<verification>
- xcodebuild build succeeds for the GooseSwift scheme.
- grep confirms a "Feature Flags" MoreInfoRow reading connectedCapabilities?.featureFlags exists in MoreInfoViews.swift.
- Human-verify checkpoint confirms "None discovered" and populated states render correctly.
</verification>

<success_criteria>
- Feature flags appear in the existing Debug tab Runtime section as hex pairs (FF-02, D-03).
- Empty state shows "None discovered" (D-03).
- Read path from connectedCapabilities (populated/persisted in 115-01, readable via capabilities.get_feature_flags from Phase 113) is confirmed end-to-end (FF-02, FF-03).
</success_criteria>

<output>
Create `.planning/phases/115-feature-flag-discovery-get-ff-value/115-02-SUMMARY.md` when done
</output>
