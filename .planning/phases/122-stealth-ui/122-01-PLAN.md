---
phase: "122"
plan: "122-01"
type: execute
wave: 1
depends_on: ["Phase 119"]
files_modified:
  - GooseSwift/HealthModels.swift
  - GooseSwift/HealthDataStore+Vitals.swift
  - GooseSwift/HealthDataStore+StaticSnapshots.swift
  - GooseSwift/HealthDataStore+Snapshots.swift
  - GooseSwift/HealthDataStore+ActivitySnapshots.swift
  - GooseSwift/StealthMetricsView.swift
  - GooseSwift/MoreRouteModels.swift
  - GooseSwift/MoreView.swift
  - GooseSwift/MoreDataStore.swift
  - GooseSwift.xcodeproj/project.pbxproj
autonomous: true
requirements: [STEALTH-03, STEALTH-04]

must_haves:
  truths:
    - HealthMetricSnapshot has stealthKey: String = "" — default empty so all 18 existing call sites compile unchanged (per STEALTH-04, per research correction overriding D-04)
    - displayValue in HealthMetricSnapshot returns em dash U+2014 when GooseStealthMode.isHidden(metric: stealthKey) is true and stealthKey is non-empty (STEALTH-04)
    - replacingHealthMonitorSnapshot in HealthDataStore+Vitals.swift forwards stealthKey: snapshot.stealthKey so live builders for resting_hr and hrv_rmssd preserve the key set on the static base
    - The 6 static base snapshots carry stealthKey — "sleep" in baseLandingSnapshots gets "sleep_performance", "recovery" gets "recovery_score", "strain" gets "strain_score", "stress" gets "stress_score", "resting-hr" in baseHealthMonitorSnapshots gets "resting_hr", "resting-hrv" gets "hrv_rmssd"
    - All explicit HealthMetricSnapshot constructors in the 5 live builders for the 6 metrics also carry stealthKey — sleepSnapshot (lines 361, 380), sleepHealthMonitorSnapshot (lines 431, 449), recoverySnapshot (lines 475, 491, 508), strainSnapshot in HealthDataStore+ActivitySnapshots.swift (lines 364 proxy and zeroStrainSnapshot path), stressSnapshot uses replacingHealthMonitorSnapshot which will forward stealthKey after the Vitals.swift fix
    - The 6 metric keys passed to GooseStealthMode.isHidden(metric:) are exactly (per D-05): "recovery_score", "strain_score", "hrv_rmssd", "resting_hr", "sleep_performance", "stress_score" — matching the StealthStorage suffix convention verbatim
    - StealthMetricsView.swift exists with 6 Toggle rows bound to @AppStorage(StealthStorage.<key>) and StealthMaskKey + EnvironmentValues extension at bottom (STEALTH-03, D-02, D-03, D-06)
    - MoreRoute.stealthMetrics case exists and appears in all 4 exhaustive switch arms (title, subtitle, systemImage, statusKeyPath) in MoreRouteModels.swift (D-01)
    - MoreRouteStatus has stealthMetrics: MoreStatusKind field and both MoreRouteStatus(...) initialisers in MoreDataStore.swift include stealthMetrics: .ready (D-01)
    - settingsRoutes array in MoreRouteModels.swift contains .stealthMetrics (D-01)
    - MoreView.swift destination switch handles .stealthMetrics returning StealthMetricsView() (D-01)
    - StealthMetricsView.swift registered in project.pbxproj at 4 locations: PBXBuildFile, PBXFileReference, PBXGroup (GooseSwift sources group), PBXSourcesBuildPhase (D-06)
    - xcodebuild with CODE_SIGNING_ALLOWED=NO produces zero error: lines
  artifacts:
    - GooseSwift/StealthMetricsView.swift
  key_links:
    - GooseStealthMode.isHidden(metric:) called from HealthMetricSnapshot.displayValue — the sole production gate point
    - stealthKey on static base snapshot propagates through replacingHealthMonitorSnapshot via snapshot.stealthKey forwarding
    - MoreRoute.stealthMetrics exhaustiveness enforced by Swift compiler across MoreRouteModels + MoreView + MoreDataStore
---

<objective>
Phase 122 delivers two features on top of Phase 119's completed stealth data layer:

1. Health dashboard "—" gate — HealthMetricSnapshot gains stealthKey: String = "" and displayValue returns an em dash (U+2014) for any of the 6 targeted metrics when the user has hidden it via GooseStealthMode.isHidden(metric:). The gate is at the model layer, not the view layer, because all metric cards render generically via snapshot.displayValue.

2. Settings toggle list — New StealthMetricsView.swift with 6 @AppStorage-backed Toggle rows, accessible from More tab > Section("Settings") > "Metrics Privacy" NavigationLink, wired through the MoreRoute enum and MoreDataStore.

Purpose: Users can hide individual sensitive metrics from the Health dashboard without losing the underlying data.
Output: StealthMetricsView.swift (new), modifications to HealthModels.swift, HealthDataStore+Vitals.swift, HealthDataStore+StaticSnapshots.swift, HealthDataStore+Snapshots.swift, HealthDataStore+ActivitySnapshots.swift, MoreRouteModels.swift, MoreView.swift, MoreDataStore.swift, project.pbxproj.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-stealth-ui/122-CONTEXT.md
@.planning/phases/122-stealth-ui/122-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HealthMetricSnapshot stealthKey field + displayValue gate + propagation fix</name>
  <files>
    GooseSwift/HealthModels.swift,
    GooseSwift/HealthDataStore+Vitals.swift,
    GooseSwift/HealthDataStore+StaticSnapshots.swift,
    GooseSwift/HealthDataStore+Snapshots.swift,
    GooseSwift/HealthDataStore+ActivitySnapshots.swift
  </files>
  <action>
    STEP A — HealthModels.swift: Add stealthKey: String = "" as the last stored property of HealthMetricSnapshot (after tint, before trend, or at end of stored properties, before the displayValue computed property). Then modify displayValue: prepend a guard block — if stealthKey is non-empty and GooseStealthMode.isHidden(metric: stealthKey) returns true, return the em dash string literal U+2014 ("—") immediately before any other logic. The existing unit/value logic is untouched. Add import for GooseStealthMode if needed (it lives in GooseSwift module — no import required, same module).

    STEP B — HealthDataStore+Vitals.swift: The replacingHealthMonitorSnapshot helper at line 593 constructs HealthMetricSnapshot without forwarding stealthKey from the base snapshot. After adding the field (Step A), this constructor will use the default "" — causing stressSnapshot and strainSnapshot (which go through replacingHealthMonitorSnapshot) to lose the stealthKey set on the static base. Fix: add stealthKey: snapshot.stealthKey to the HealthMetricSnapshot(...) constructor inside replacingHealthMonitorSnapshot, immediately after tint: snapshot.tint or before trend: trend (match the field order from Step A).

    STEP C — HealthDataStore+StaticSnapshots.swift: Set stealthKey on exactly these 6 base snapshot entries. The snapshot(...) factory call is a static func — add stealthKey: "<value>" as a named argument to each:
      - baseLandingSnapshots entry id: "sleep" → stealthKey: "sleep_performance"
      - baseLandingSnapshots entry id: "recovery" → stealthKey: "recovery_score"
      - baseLandingSnapshots entry id: "strain" → stealthKey: "strain_score"
      - baseLandingSnapshots entry id: "stress" → stealthKey: "stress_score"
      - baseHealthMonitorSnapshots entry id: "resting-hr" → stealthKey: "resting_hr"
      - baseHealthMonitorSnapshots entry id: "resting-hrv" → stealthKey: "hrv_rmssd"
    All other entries in those arrays (health-monitor, trends, cardio-load, energy-bank, packet-inputs, algorithms, calibration, respiratory-rate, oxygen-saturation, wrist-temperature, health-sleep) do NOT get stealthKey — they keep the default "".
    Note: the static snapshot(...) factory function must accept stealthKey: String = "" as a parameter and forward it to the HealthMetricSnapshot initialiser. Read the factory function signature before editing.

    STEP D — HealthDataStore+Snapshots.swift: The live builder methods for sleep, recovery, and sleep-health-monitor construct HealthMetricSnapshot(...) directly (not via replacingHealthMonitorSnapshot). These constructors must include stealthKey: snapshot.stealthKey to forward the key from the base argument. Target constructors:
      - sleepSnapshot(base:) at lines ~361 and ~380 — both HealthMetricSnapshot(...) constructors
      - sleepHealthMonitorSnapshot(base:) at lines ~431 and ~449 — both HealthMetricSnapshot(...) constructors
      - recoverySnapshot(base:) at lines ~475, ~491, and ~508 — all three HealthMetricSnapshot(...) constructors
    For each: add stealthKey: snapshot.stealthKey after tint: snapshot.tint (or match the field order established in Step A). The base snapshot already has the stealthKey from Step C.

    STEP E — HealthDataStore+ActivitySnapshots.swift: strainSnapshot(base:) at line 358 uses replacingHealthMonitorSnapshot for both its return paths (HK proxy at ~364, and the successful path at ~383). After Step B, replacingHealthMonitorSnapshot already forwards stealthKey. No direct HealthMetricSnapshot(...) constructor calls in this function require changes. Verify this is true by reading the function before acting — if any direct HealthMetricSnapshot(...) call exists inside strainSnapshot(base:), add stealthKey: snapshot.stealthKey there too.

    Do NOT add stealthKey to: ScoreDateTimeline.datedSnapshot (HealthModels.swift lines 131, 148, 175) — these are date-picker copies that inherit the existing key via snapshot.stealthKey since Step A's default is ""; but since these constructors are explicit, they will compile with the default. However, to preserve the key for date-picker views, add stealthKey: snapshot.stealthKey to those constructors as well (lines 131, 148, 175 in HealthModels.swift) — omitting it silently drops the key for date-selected recovery/sleep/strain cards.

    VERIFY COMPLETENESS: After all edits, run:
    grep -rn "HealthMetricSnapshot(" GooseSwift/ --include="*.swift"
    For each of the 16 known call sites, confirm either (a) it is one of the 6 targeted metrics and has stealthKey: snapshot.stealthKey, (b) it uses replacingHealthMonitorSnapshot which now forwards it, or (c) it is a non-stealth metric and correctly uses the default "".
  </action>
  <verify>
    <automated>xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep 'error:'</automated>
  </verify>
  <done>
    - HealthMetricSnapshot has stealthKey: String = "" field
    - displayValue returns "—" (em dash U+2014) when GooseStealthMode.isHidden(metric: stealthKey) is true for a non-empty stealthKey
    - replacingHealthMonitorSnapshot forwards stealthKey: snapshot.stealthKey
    - All 6 static base entries in StaticSnapshots carry correct stealthKey values
    - Live builders for sleep, sleepHealthMonitor, recovery carry stealthKey: snapshot.stealthKey in all their HealthMetricSnapshot constructors
    - strainSnapshot paths go through replacingHealthMonitorSnapshot (already fixed)
    - stressSnapshot paths go through replacingHealthMonitorSnapshot (already fixed)
    - xcodebuild produces zero error: lines
  </done>
</task>

<task type="auto">
  <name>Task 2: StealthMetricsView.swift — toggle list + EnvironmentKey</name>
  <files>
    GooseSwift/StealthMetricsView.swift,
    GooseSwift.xcodeproj/project.pbxproj
  </files>
  <action>
    Create GooseSwift/StealthMetricsView.swift (new file). Structure:

    Imports: SwiftUI only.

    StealthMetricsView struct conforming to View. Six private @AppStorage properties using the StealthStorage key constants from GooseStealthMode.swift (Phase 119):
      - @AppStorage(StealthStorage.recoveryScore) private var hideRecovery = false
      - @AppStorage(StealthStorage.strainScore)   private var hideStrain = false
      - @AppStorage(StealthStorage.hrvRmssd)      private var hideHRV = false
      - @AppStorage(StealthStorage.restingHr)     private var hideRestingHR = false
      - @AppStorage(StealthStorage.sleepPerf)     private var hideSleep = false
      - @AppStorage(StealthStorage.stressScore)   private var hideStress = false

    body: List with one Section. Section contains 6 Toggle rows (per D-02): "Recovery Score", "Strain Score", "HRV (RMSSD)", "Resting HR", "Sleep Performance", "Stress Score" bound to $hideRecovery, $hideStrain, $hideHRV, $hideRestingHR, $hideSleep, $hideStress respectively. Section has a footer Text: "Hidden metrics show — on the Health dashboard." (note: the — in the footer is the em dash U+2014 character). Apply .listStyle(.insetGrouped). Apply .navigationTitle("Metrics Privacy") and .navigationBarTitleDisplayMode(.inline). Apply .gooseListBackground() modifier (same as other More detail views — read MorePrivacyView.swift or any other More list view to confirm the exact modifier name before writing).

    Below the view struct, add a MARK: - EnvironmentKey comment, then define:
      - struct StealthMaskKey: EnvironmentKey with static let defaultValue = StealthMask.none
      - extension EnvironmentValues adding var stealthMask: StealthMask { get { self[StealthMaskKey.self] } set { self[StealthMaskKey.self] = newValue } }

    Add a #Preview block at the bottom: StealthMetricsView() wrapped with .environment(\.stealthMask, StealthMask(hidden: ["recovery_score"])).

    Use 2-space indentation throughout (project convention). No doc comments (///). No TODO markers.

    pbxproj registration — add StealthMetricsView.swift at the same 4 locations as GooseStealthMode.swift (confirmed pattern from project.pbxproj lines 47, 298, 670, 1074). Generate two new UUIDs using the A1.../A2... prefix convention observed in the file. Insert:
      - PBXBuildFile section (near line 47): new A1-prefixed UUID referencing the A2-prefixed file ref UUID
      - PBXFileReference section (near line 298): new A2-prefixed UUID with path = StealthMetricsView.swift
      - PBXGroup GooseSwift sources (near line 670): A2 UUID entry, alphabetically ordered near other Stealth* entries
      - PBXSourcesBuildPhase main target (near line 1074): A1 UUID entry
    Do NOT add a test target entry — StealthMetricsView has no corresponding test file in this phase.
  </action>
  <verify>
    <automated>xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep 'error:'</automated>
  </verify>
  <done>
    - GooseSwift/StealthMetricsView.swift exists
    - File has 6 @AppStorage Toggle rows with correct StealthStorage key constants
    - StealthMaskKey + EnvironmentValues extension present at bottom of file
    - #Preview with .environment(\.stealthMask, ...) present
    - pbxproj has 4 new entries for StealthMetricsView.swift
    - xcodebuild produces zero error: lines
  </done>
</task>

<task type="auto">
  <name>Task 3: MoreRoute.stealthMetrics wiring — MoreRouteModels + MoreView + MoreDataStore</name>
  <files>
    GooseSwift/MoreRouteModels.swift,
    GooseSwift/MoreView.swift,
    GooseSwift/MoreDataStore.swift
  </files>
  <action>
    Read all three files fully before editing — exact line numbers matter for exhaustiveness.

    MoreRouteModels.swift — 7 changes:

    1. Enum case: add "case stealthMetrics" to the MoreRoute enum. Place after the last existing case (check the current enum body — add after .metricExplorer or at end).

    2. title switch: add "case .stealthMetrics: String(localized: "Metrics Privacy")". Place in the same switch as all other title cases — after the .metricExplorer case.

    3. subtitle switch: add "case .stealthMetrics: String(localized: "Hide individual metrics from the Health dashboard")". Same switch structure.

    4. systemImage switch: add "case .stealthMetrics: "eye.slash"". Same switch.

    5. statusKeyPath switch: add "case .stealthMetrics: \.stealthMetrics". Same switch.

    6. settingsRoutes array: change [.privacy, .remoteServer] to [.privacy, .remoteServer, .stealthMetrics].

    7. MoreRouteStatus struct: add "var stealthMetrics: MoreStatusKind" after the existing "var metricExplorer: MoreStatusKind" field (line 145).

    MoreView.swift — 1 change:

    8. destination(for:) switch: add "case .stealthMetrics: StealthMetricsView()" case. Read the existing switch structure — it is a @ViewBuilder func or similar returning AnyView/some View. Match the existing case pattern exactly (look at how .privacy or .remoteServer cases are written). Place after the .metricExplorer case.

    MoreDataStore.swift — 2 changes:

    9. routeStatus init at line 12: the MoreRouteStatus(...) initialiser currently ends with "metricExplorer: .ready". Add "stealthMetrics: .ready" as the next field after metricExplorer, before the closing paren.

    10. refreshRouteStatus at line 151: same MoreRouteStatus(...) initialiser. Add "stealthMetrics: .ready" after "metricExplorer: .ready".

    Swift compiler enforces exhaustiveness on all switch statements over MoreRoute — any missed case produces a compile error. The build verify step will catch omissions.
  </action>
  <verify>
    <automated>xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep 'error:'</automated>
  </verify>
  <done>
    - MoreRoute enum has .stealthMetrics case
    - All 4 switch arms in MoreRouteModels.swift handle .stealthMetrics (title, subtitle, systemImage, statusKeyPath)
    - settingsRoutes array includes .stealthMetrics
    - MoreRouteStatus struct has stealthMetrics: MoreStatusKind field
    - MoreView.swift destination switch handles .stealthMetrics → StealthMetricsView()
    - Both MoreRouteStatus(...) initialisers in MoreDataStore.swift include stealthMetrics: .ready
    - xcodebuild produces zero error: lines
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserDefaults → displayValue | Stealth toggle state is read from UserDefaults.standard synchronously at render time; no untrusted input crosses — values are boolean, written only by @AppStorage bindings in StealthMetricsView |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-122-01 | Information Disclosure | GooseStealthMode.isHidden reads UserDefaults.standard | low | accept | UserDefaults is local to app sandbox; no network exposure; toggle state is not sensitive data in itself |
| T-122-02 | Tampering | @AppStorage bindings write UserDefaults immediately | low | accept | UserDefaults is sandboxed to app; no integrity concern for UI preference state |
</threat_model>

<verification>
After all three tasks complete:

1. Build check (all tasks):
   xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep 'error:'
   Must return empty.

2. Stealth key coverage check:
   grep -rn "HealthMetricSnapshot(" GooseSwift/ --include="*.swift"
   Cross-check all 16+ sites: each site for the 6 targeted metrics has stealthKey populated; all others compile with default "".

3. Em dash literal check:
   grep -rn "return \"" GooseSwift/HealthModels.swift
   Confirm the displayValue guard returns the em dash U+2014, not a hyphen.

4. MoreRoute exhaustiveness: confirmed implicitly by zero-error build (Swift compiler enforces switch exhaustiveness).

5. Simulator verification (manual, after build):
   - Launch app in iOS Simulator.
   - Navigate to More tab > Settings section > "Metrics Privacy" row — confirm it appears and NavigationLink opens StealthMetricsView.
   - Enable "Recovery Score" toggle.
   - Switch to Health tab — confirm Recovery card shows "—" (em dash) instead of a score value.
   - Disable toggle — confirm score value returns.
</verification>

<success_criteria>
- HealthMetricSnapshot.displayValue returns "—" (U+2014) for any of the 6 metrics when the user has hidden it via the toggle
- More tab > Settings section shows "Metrics Privacy" NavigationLink
- Metrics Privacy screen shows 6 Toggle rows; toggling persists immediately via @AppStorage
- No regressions: all non-stealth metric snapshots are unaffected (stealthKey default "" causes no gate evaluation)
- Zero xcodebuild error: lines
</success_criteria>

<output>
Create .planning/phases/122-stealth-ui/122-01-SUMMARY.md when done.
</output>
