---
phase: "05"
plan: "05-03"
title: "Integrate Medium Risk PRs (#7, #4, #5)"
type: execute
wave: 3
depends_on:
  - "05-02"
autonomous: true
files_modified:
  - "Rust/core/src/bridge.rs"
  - "GooseSwift/"
requirements:
  - FORK-07
  - FORK-04
  - FORK-05
---

# Plan 05-03: Integrate Medium Risk PRs (#7, #4, #5)

<objective>
Integrate three medium-risk upstream PRs: PR #7 (new `core.list_methods` RPC in the Rust bridge — additive, no conflict with existing methods), PR #4 (SwiftUI scroll frame drop reduction on Home and Health views — UI-only changes), and PR #5 (Apple Health fallback for sleep/recovery/strain/vitals — HealthKit integration changes). Run `cargo test` after PR #7 (touches Rust). Verify that fork-specific upload client and server infrastructure are untouched.
</objective>

<context>
**Decisions from CONTEXT.md:**
- D-01: Merge order step 6: PR #7, step 7: PR #4, step 8: PR #5
- D-02: `git merge --no-ff` for every PR
- D-05: `cargo test` after PRs touching Rust/ — PR #7 touches bridge.rs
- D-06: After each merge, verify `server/` and fork-specific iOS files intact
- D-09: Resolve conflicts inline; priority: preserve fork-specific functionality
- Files to protect: `GooseSwift/GooseAppModel+Upload.swift`, `GooseSwift/MoreRemoteServerViews.swift`

**PR details:**
- PR #7: "feat(bridge): add core.list_methods RPC" — new JSON-RPC method in bridge.rs, additive only
- PR #4: "Reduce scroll frame drops on Home and Health views" — SwiftUI performance optimisations
- PR #5: "Apple Health fallback for sleep, recovery, strain, vitals" — HealthKit integration

**User requirement (from CONTEXT.md specifics):** Explain what each PR does before merging.
**Wave 3 requires Wave 2 complete** (PRs #3, #6, #1, #13, #10 already merged).
</context>

<tasks>

## Task 1: Review and Merge PR #7 (core.list_methods RPC)

<task id="1">
<type>execute</type>
<title>Explain PR #7 changes, merge, run cargo test</title>

<read_first>
- `Rust/core/src/bridge.rs` — current bridge dispatch table to understand existing RPC methods before PR #7
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-02, D-05, D-06
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/7/head:upstream-pr-7`
2. Show and explain the diff: `git diff main...upstream-pr-7` — present to user:
   - What `core.list_methods` does: introspects available bridge RPC method names and returns them as a JSON array — useful for debugging and capability discovery
   - Confirm it is additive only (new match arm in dispatch, no existing arms modified)
   - Confirm no changes to Swift files or server/
3. Merge: `git checkout main && git merge --no-ff upstream-pr-7 -m "merge: upstream PR #7 — feat(bridge): add core.list_methods RPC"`
4. Resolve conflicts in bridge.rs if any:
   - Fork may have added custom RPC methods — keep ALL arms from both sides (additive, not mutually exclusive)
   - Do NOT remove or alter existing dispatch arms
5. Run tests (D-05): `cd Rust/core && cargo test 2>&1`
6. Verify fork files: `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift GooseSwift/MoreRemoteServerViews.swift server/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #7"
- `cargo test` in `Rust/core/` exits 0 after merge
- `grep "list_methods" Rust/core/src/bridge.rs` returns at least one match
- `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift` is empty
- `git diff HEAD~1 -- server/` is empty
- Both upstream's `core.list_methods` arm and any fork-specific bridge methods coexist in bridge.rs
</acceptance_criteria>
</task>

## Task 2: Review and Merge PR #4 (Scroll frame drop reduction)

<task id="2">
<type>execute</type>
<title>Explain PR #4 changes, merge, verify fork UI files preserved</title>

<read_first>
- `GooseSwift/` — scan for HomeView, HealthDashboardViews, and any scroll-heavy view files PR #4 might touch
- `GooseSwift/MoreRemoteServerViews.swift` — read to understand fork's remote server UI before reviewing PR #4 (protect this file)
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-06, D-09
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/4/head:upstream-pr-4`
2. Show and explain the diff: `git diff main...upstream-pr-4` — present to user:
   - What performance optimisations PR #4 applies: `.id()` stabilisation, `LazyVStack` adoption, `@State` scope reduction, `equatable` conformance — patterns that prevent unnecessary SwiftUI redraws
   - Which views are affected (Home, Health tabs)
   - Check if PR #4 touches `MoreView.swift`, `MoreRouteModels.swift`, or `GooseAppModel.swift` (conflict surface with fork's Phase 2/3 additions)
3. Merge: `git checkout main && git merge --no-ff upstream-pr-4 -m "merge: upstream PR #4 — Reduce scroll frame drops on Home and Health views"`
4. Resolve conflicts in SwiftUI view files:
   - If PR #4 touches `GooseAppModel.swift`: keep both upstream performance patches AND fork's `@Published` state for upload status
   - If PR #4 touches any `More*` view: keep fork's remote server UI additions alongside upstream performance patches
   - NEVER remove fork UI additions (Phase 2 work)
5. Verify fork files: `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift GooseSwift/MoreRemoteServerViews.swift server/ Rust/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #4"
- `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift` is empty (fork upload client untouched)
- `git diff HEAD~1 -- GooseSwift/MoreRemoteServerViews.swift` is empty OR diff shows only upstream UI performance changes with zero conflict with fork's server settings logic
- `git diff HEAD~1 -- server/` is empty
- `git diff HEAD~1 -- Rust/` is empty (PR #4 is SwiftUI only)
</acceptance_criteria>
</task>

## Task 3: Review and Merge PR #5 (Apple Health fallback)

<task id="3">
<type>execute</type>
<title>Explain PR #5 changes, merge, verify HealthKit coexistence</title>

<read_first>
- `GooseSwift/GooseSwift.entitlements` — verify `com.apple.developer.healthkit` already present (fork uses HealthKit for body mass autofill)
- `GooseSwift/HealthDataStore.swift` — read to understand existing health data structure before PR #5 adds fallbacks
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-06, D-09
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/5/head:upstream-pr-5`
2. Show and explain the diff: `git diff main...upstream-pr-5` — present to user:
   - What PR #5 adds: HealthKit HKSampleQuery calls for sleep analysis, recovery score, strain data, and vitals — these activate as fallback when WHOOP data is unavailable
   - Which HealthDataStore extension files are modified
   - The HealthKit entitlement already exists in the fork — PR #5 should not need to add it
   - No upload client or server files touched
3. Merge: `git checkout main && git merge --no-ff upstream-pr-5 -m "merge: upstream PR #5 — Apple Health fallback for sleep, recovery, strain, vitals"`
4. Resolve conflicts in health data layer:
   - Keep both: all WHOOP-based data queries (fork + upstream) AND PR #5's HealthKit fallback additions
   - If PR #5 tries to add the HealthKit entitlement: it already exists — skip that hunk or keep existing
5. Verify fork files: `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift GooseSwift/MoreRemoteServerViews.swift server/ Rust/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #5"
- `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift` is empty
- `git diff HEAD~1 -- GooseSwift/MoreRemoteServerViews.swift` is empty
- `git diff HEAD~1 -- server/` is empty
- `git diff HEAD~1 -- Rust/` is empty (PR #5 is HealthKit/Swift only)
- `grep "HKSampleQuery\|HKObjectType" GooseSwift/HealthDataStore+*.swift` shows HealthKit fallback queries present (PR #5 integrated)
- `grep "healthkit" GooseSwift/GooseSwift.entitlements` confirms single entitlement entry (not duplicated)
</acceptance_criteria>
</task>

## Task 4: Wave 3 final verification

<task id="4">
<type>execute</type>
<title>Verify all three Wave 3 PRs integrated and requirements satisfied</title>

<read_first>
- `Rust/core/src/bridge.rs` — verify core.list_methods present
- `.planning/REQUIREMENTS.md` — FORK-04, FORK-05, FORK-07 definitions
</read_first>

<action>
1. Verify merge history: `git log --oneline | grep -E "PR #(7|4|5)"` — 3 merge commits present
2. Verify `core.list_methods` in bridge: `grep -n "list_methods" Rust/core/src/bridge.rs`
3. Run final cargo test: `cd Rust/core && cargo test 2>&1`
4. Confirm fork-specific iOS files intact:
   `git log --oneline -- GooseSwift/GooseAppModel+Upload.swift GooseSwift/MoreRemoteServerViews.swift | head -5`
   (No commits from Wave 3 upstream merges should appear for these files)
5. Total merge commits to date: `git log --oneline | grep "upstream PR #" | wc -l` — should be 8 (PRs #3, #6, #1, #13, #10, #7, #4, #5)
</action>

<acceptance_criteria>
- `git log --oneline | grep -E "PR #(7|4|5)"` shows 3 merge commits
- `grep "list_methods" Rust/core/src/bridge.rs` returns matches
- Final `cargo test` passes
- FORK-04 (PR #4), FORK-05 (PR #5), FORK-07 (PR #7) requirements satisfied
- Fork-specific files (`server/`, upload client, settings UI) untouched by all Wave 3 merges
- 8 upstream PR merge commits in total across Waves 1-3
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cargo test` in `Rust/core/` passes after PR #7 merge and final Wave 3 check
2. `git log --oneline | grep -E "PR #(7|4|5)"` shows all three merge commits
3. `grep "list_methods" Rust/core/src/bridge.rs` confirms PR #7 integrated (FORK-07)
4. `grep "HKSampleQuery" GooseSwift/HealthDataStore+*.swift` confirms PR #5 integrated (FORK-05)
5. FORK-04, FORK-05, FORK-07 requirements satisfied
6. `GooseAppModel+Upload.swift` and `MoreRemoteServerViews.swift` unchanged by all Wave 3 PRs
</verification>

<must_haves>
  <truths>
    - T-05-11: PR #7 merged via --no-ff; `core.list_methods` in bridge.rs; cargo test passes (FORK-07)
    - T-05-12: PR #4 merged via --no-ff; fork UI files (`MoreRemoteServerViews.swift`) preserved (FORK-04)
    - T-05-13: PR #5 merged via --no-ff; Apple Health fallback coexists with WHOOP data queries (FORK-05)
    - T-05-14: No fork-specific upload client code (`GooseAppModel+Upload.swift`) lost or conflicted
    - T-05-15: `server/` directory state unchanged by all Wave 3 merges
  </truths>
  <goal>Three medium-risk PRs (#7, #4, #5) integrated preserving all fork-specific functionality</goal>
</must_haves>

<threat_model>
- **PR #7 conflict in bridge.rs dispatch:** If fork added custom RPC methods, match arms conflict. Mitigation: keep all arms from both sides — additive, not mutually exclusive.
- **PR #4 modifies More tab views:** If PR #4 touches `MoreView.swift` or `MoreRouteModels.swift`, it may conflict with fork's Phase 2 remote server navigation. Mitigation: keep both — PR #4's performance patches + fork's new navigation routes.
- **PR #5 HealthKit entitlement conflict:** PR #5 may attempt to add an entitlement the fork already has. Mitigation: keep existing entitlement, skip/ignore duplicate addition from PR #5.
- **PR #5 HealthKit query duplication:** Fork already has HealthKit for body mass. PR #5 adds new query types — not duplicates. Verify distinct query object types.
- **Swift compilation failure not detectable without Xcode:** PRs #4 and #5 are SwiftUI/HealthKit only. `cargo test` won't catch Swift errors. Note any compilation concerns for manual Xcode build check post-merge.
</threat_model>
