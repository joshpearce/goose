---
phase: "05"
plan: "05-04"
title: "Integrate PR #12 (FFI Bridge Serialization + Background Threading)"
type: execute
wave: 4
depends_on:
  - "05-03"
autonomous: false
files_modified:
  - "Rust/core/src/bridge.rs"
  - "Rust/core/src/"
  - "GooseSwift/GooseRustBridge.swift"
requirements:
  - FORK-09
---

# Plan 05-04: Integrate PR #12 (FFI Bridge Serialization + Background Threading)

<objective>
Integrate the highest-risk upstream PR — PR #12 ("Optimize FFI bridge serialization and background threading") — after Phases 2, 3, and 4 of the fork are fully executed. This plan is marked `autonomous: false`: the executor MUST stop and present the full PR diff to the user for review before proceeding with the merge. PR #12 modifies the FFI bridge layer that is also used by the fork's upload client (Phase 3), so the user must confirm there are no conflicts before merging.
</objective>

<context>
**Decisions from CONTEXT.md:**
- D-01: PR #12 is step 9 (LAST) in the merge order — after all Phases 2+3+4 executed
- D-07: PR #12 marked `autonomous: false` — requires manual verification before merging
- D-08: Before merging #12: read full diff and verify conflicts with `GooseAppModel+Upload.swift` and any new FFI bridge code added by fork phases
- D-02: `git merge --no-ff`
- D-05: `cargo test` after merge (touches Rust/ heavily)
- D-09: Resolve conflicts inline, priority: preserve fork-specific upload client

**PR details:**
- PR #12: "Optimize FFI bridge serialization and background threading"
  - Expected changes: JSON serialization optimization in `bridge.rs` (e.g., streaming serializer, reduced allocations), background thread dispatching for heavy bridge calls
  - High risk because: fork's upload client (Phase 3) may call `goose_bridge_handle_json` from specific dispatch queues; PR #12's threading changes could interact with the upload client's queue management

**MANDATORY PREREQUISITE:** This plan must not execute until:
- Phase 2 (iOS Server Settings) is executed
- Phase 3 (iOS Upload Client) is executed  
- Phase 4 (Upload Status Feedback) is executed

The executor MUST verify this before proceeding. If the prerequisite phases are not executed, `autonomous: false` means the user is prompted to confirm whether to proceed anyway.
</context>

<tasks>

## Task 1: Verify Prerequisites (BLOCKING)

<task id="1">
<type>execute</type>
<title>Verify Phases 2, 3, 4 are executed before proceeding</title>

<read_first>
- `.planning/STATE.md` — check current phase execution status
- `GooseSwift/GooseAppModel+Upload.swift` — must exist (Phase 3 delivered this file)
- `GooseSwift/MoreRemoteServerViews.swift` — must exist (Phase 2 delivered this file)
</read_first>

<action>
1. Check for Phase 3 deliverable: `test -f GooseSwift/GooseAppModel+Upload.swift && echo "Phase 3 EXECUTED — upload client present" || echo "BLOCKER: Phase 3 not yet executed — GooseAppModel+Upload.swift missing"`
2. Check for Phase 2 deliverable: `test -f GooseSwift/MoreRemoteServerViews.swift && echo "Phase 2 EXECUTED — server settings UI present" || echo "BLOCKER: Phase 2 not yet executed — MoreRemoteServerViews.swift missing"`
3. Read `GooseSwift/GooseAppModel+Upload.swift` to understand the FFI bridge usage pattern in the upload client (which queues it uses, how it calls `goose_bridge_handle_json`)
4. If either Phase 2 or Phase 3 deliverables are missing: STOP and present a checkpoint to the user explaining that PR #12 cannot be safely merged until those phases are executed. The user must explicitly confirm to proceed.
</action>

<acceptance_criteria>
- `GooseSwift/GooseAppModel+Upload.swift` exists OR user explicitly confirmed to proceed without it
- `GooseSwift/MoreRemoteServerViews.swift` exists OR user explicitly confirmed to proceed without it
- Executor has read the upload client code and understands its FFI bridge usage pattern before reviewing PR #12
</acceptance_criteria>
</task>

## Task 2: Fetch and Review PR #12 in Full (CHECKPOINT — user must review before merge)

<task id="2">
<type>execute</type>
<title>Present full PR #12 diff for user review</title>

<read_first>
- `Rust/core/src/bridge.rs` — current bridge implementation after Waves 1-3 merges
- `GooseSwift/GooseRustBridge.swift` — current Swift-side bridge wrapper
- `GooseSwift/GooseAppModel+Upload.swift` — upload client (read to understand its bridge call pattern)
- `GooseSwift/GooseSwift-Bridging-Header.h` — C FFI header
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/12/head:upstream-pr-12` (or `gh pr checkout 12 --repo b-nnett/goose -b upstream-pr-12`)
2. Show the COMPLETE diff: `git diff main...upstream-pr-12`
3. Analyse for the following specific risks:
   a. **Function signature changes in `goose_bridge_handle_json`:** Any change to the C FFI signature requires matching changes in `GooseSwift-Bridging-Header.h` and `GooseRustBridge.swift`. If the signature changed, the upload client's calls become invalid.
   b. **Background threading changes:** If PR #12 makes bridge calls non-blocking or moves execution to a background thread internally, verify this does not double-dispatch with the upload client's own `DispatchQueue` management.
   c. **Memory management changes:** If the allocator strategy or return type of `goose_bridge_free_string` changed, verify Swift-side memory cleanup is still correct.
   d. **JSON format changes:** If the request/response envelope format changes (e.g., new required fields), all callers must be updated.
4. Present a structured summary to the user covering each of the 4 risk areas above with findings.

**STOP HERE AND PRESENT TO USER:** Output a checkpoint box with the full risk analysis and ask for explicit approval before proceeding to merge.
</action>

<acceptance_criteria>
- PR #12 diff has been fetched and fully analysed
- Risk analysis covers: FFI signature, threading, memory management, JSON format
- User has been presented with the risk analysis in a checkpoint box
- User has explicitly approved the merge OR requested changes before proceeding
</acceptance_criteria>
</task>

## Task 3: Merge PR #12 (only after user approval from Task 2)

<task id="3">
<type>execute</type>
<title>Merge PR #12 with careful conflict resolution</title>

<read_first>
- `Rust/core/src/bridge.rs` — current state to resolve conflicts
- `GooseSwift/GooseRustBridge.swift` — current Swift bridge to detect FFI signature conflicts
- `GooseSwift/GooseAppModel+Upload.swift` — upload client to protect during merge
- Risk analysis output from Task 2
</read_first>

<action>
1. `git checkout main`
2. `git merge --no-ff upstream-pr-12 -m "merge: upstream PR #12 — Optimize FFI bridge serialization and background threading"`
3. **Conflict resolution strategy (in priority order):**
   a. If `bridge.rs` conflicts: keep PR #12's serialization/threading optimizations + all fork-specific RPC methods (`core.list_methods`, any upload-related methods). DO NOT drop any bridge method.
   b. If C FFI header conflicts: verify function signature compatibility. If signature changed, update `GooseRustBridge.swift` to match the new signature AND update `GooseAppModel+Upload.swift` to use the new calling convention.
   c. If `GooseRustBridge.swift` conflicts: keep the updated signature from PR #12 + fork's existing error handling patterns.
   d. If `GooseAppModel+Upload.swift` is affected by signature change: update call sites to match new FFI signature while preserving all upload logic.
4. After resolving all conflicts: `git add -A && git merge --continue` (or `git commit` if already staged)
5. Run: `cd Rust/core && cargo test 2>&1`
6. Verify: `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift` — document any changes (expected: potentially updated to new FFI signature; NOT expected: upload logic removed)
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit with "PR #12"
- `cargo test` in `Rust/core/` exits 0
- `GooseSwift/GooseAppModel+Upload.swift` still contains upload logic (POST /v1/ingest-decoded with Bearer token, retry logic)
- If FFI signature changed: `GooseRustBridge.swift` and `GooseAppModel+Upload.swift` updated consistently
- All existing RPC methods (including `core.list_methods` from PR #7 and any fork additions) still present in `bridge.rs`
</acceptance_criteria>
</task>

## Task 4: Final Verification — All 9 PRs Integrated

<task id="4">
<type>execute</type>
<title>Verify complete Phase 5 state — all 9 PRs integrated</title>

<read_first>
- `.planning/REQUIREMENTS.md` — FORK-01 through FORK-10 definitions
- `Rust/core/src/bridge.rs` — verify all RPC methods present
- `GooseSwift/GooseAppModel+Upload.swift` — verify upload client intact after all merges
</read_first>

<action>
1. Check all merge commits present:
   ```
   git log --oneline | grep -E "PR #(1|3|4|5|6|7|10|12|13)"
   ```
   Should show 9 lines, one per PR.

2. Verify upstream remote configured:
   ```
   git remote -v | grep upstream
   ```
   Should show `https://github.com/b-nnett/goose`.

3. Run final cargo test: `cd Rust/core && cargo test 2>&1`

4. Verify all fork-specific files intact:
   ```
   test -d server/ && echo "server/ present" || echo "server/ absent"
   test -f GooseSwift/GooseAppModel+Upload.swift && echo "Upload client present" || echo "Upload client absent"
   test -f GooseSwift/MoreRemoteServerViews.swift && echo "Server settings UI present" || echo "Server settings UI absent"
   ```

5. Verify no upstream PR has broken the fork's BLE-to-SQLite pipeline:
   ```
   grep -r "goose_bridge_handle_json" GooseSwift/ | wc -l
   ```
   Count should be similar to pre-integration (bridge calls still in place).

6. Check .planning/ unchanged by merges (planning docs must not have been altered by upstream content):
   ```
   git log --oneline -- .planning/ | head -5
   ```
   Should show only GSD planning commits, no upstream merge commits modifying `.planning/`.
</action>

<acceptance_criteria>
- `git log --oneline | grep "PR #"` shows exactly 9 merge commits (PRs #1, #3, #4, #5, #6, #7, #10, #12, #13)
- `git remote -v | grep upstream` shows `https://github.com/b-nnett/goose`
- Final `cargo test` exits 0
- `server/` directory present (if Phase 1 executed) or gracefully absent
- `GooseAppModel+Upload.swift` present and contains upload logic (if Phase 3 executed)
- `MoreRemoteServerViews.swift` present (if Phase 2 executed)
- `.planning/` not modified by any upstream merge commit
- FORK-01 through FORK-10: all 10 requirements satisfied
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `git log --oneline | grep "PR #"` shows 9 merge commits — all PRs integrated (FORK-02 through FORK-10)
2. `git remote -v | grep upstream` confirms FORK-01 (remote configured)
3. `cargo test` in `Rust/core/` passes after all merges
4. `GooseAppModel+Upload.swift` intact with upload logic (fork not corrupted)
5. `server/` directory state matches Phase 1 output (not corrupted)
6. All 10 FORK requirements satisfied
</verification>

<must_haves>
  <truths>
    - T-05-16: PR #12 only merged after user explicitly reviews and approves (autonomous: false)
    - T-05-17: PR #12 merged via --no-ff; cargo test passes (FORK-09)
    - T-05-18: FFI bridge signature compatibility maintained — if PR #12 changed it, GooseAppModel+Upload.swift updated consistently
    - T-05-19: All 9 upstream PRs present as merge commits in git log
    - T-05-20: All FORK-01 through FORK-10 requirements satisfied at end of plan
    - T-05-21: Upload client upload logic preserved after PR #12 (not dropped or corrupted)
  </truths>
  <goal>PR #12 safely integrated with user oversight, completing the full upstream PR integration with all fork-specific infrastructure intact</goal>
</must_haves>

<threat_model>
- **FFI signature change in PR #12 breaks upload client:** HIGH risk — the upload client calls `goose_bridge_handle_json` directly. If PR #12 changes the C ABI, every call site must be updated. Mitigation: Task 2 explicitly checks for signature changes before merge; Task 3 updates call sites if needed.
- **Background threading in PR #12 causes race conditions with upload client:** If PR #12 makes bridge calls non-blocking internally, the upload client's synchronous DispatchQueue pattern may deadlock. Mitigation: Analyse threading model in Task 2; test upload after merge.
- **PR #12 conflicts with fork's bridge additions from PR #7:** Medium risk — both touch bridge.rs. Mitigation: keep all match arms; do not drop either upstream's or fork's methods.
- **cargo test failures from PR #12 threading changes:** High probability of flaky tests if threading is involved. Mitigation: run cargo test multiple times if flaky behaviour observed.
- **Prerequisite phases not yet executed:** If Phase 3 (upload client) is not executed, the risk analysis in Task 2 cannot be fully performed. Mitigation: Task 1 blocks execution and surfaces this to the user.
- **Memory unsafety from FFI changes:** PR #12 modifies the memory boundary between Rust and Swift. Any change to ownership semantics in `goose_bridge_free_string` could cause use-after-free. Mitigation: Task 2 explicitly checks memory management changes.
</threat_model>
