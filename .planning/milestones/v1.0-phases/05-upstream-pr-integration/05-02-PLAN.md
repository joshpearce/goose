---
phase: "05"
plan: "05-02"
title: "Integrate Medium-Low Risk PRs (#1, #13, #10)"
type: execute
wave: 2
depends_on:
  - "05-01"
autonomous: true
files_modified:
  - "Rust/core/src/"
  - "Rust/core/tests/"
  - ".github/workflows/"
requirements:
  - FORK-02
  - FORK-10
  - FORK-08
---

# Plan 05-02: Integrate Medium-Low Risk PRs (#1, #13, #10)

<objective>
Integrate three upstream PRs that involve Rust source code changes: PR #1 (fix stale timeout message and deduplicate duration parsing — bug fix), PR #13 (fix Rust core integration tests and Windows compatibility — test fixes), and PR #10 (add Rust CI workflow and fix bugs surfaced by CI — CI + bug fixes). Run `cargo test` after each merge. Verify fork-specific files are untouched throughout.
</objective>

<context>
**Decisions from CONTEXT.md:**
- D-01: Merge order step 3: PR #1, step 4: PR #13, step 5: PR #10
- D-02: `git merge --no-ff` for every PR
- D-05: `cargo test` after each PR touching Rust/ — all three PRs touch Rust source
- D-06: Verify `server/` and fork-specific iOS files after each merge
- D-09: Resolve conflicts inline, keep fork-specific functionality; priority: preserve fork-specific files

**PR details:**
- PR #1: "Fix stale timeout message and deduplicate duration parsing" — bug fix in Rust core, low risk
- PR #13: "Fix Rust core integration tests and Windows compatibility" — test fixes + platform compat, low-medium risk  
- PR #10: "Add Rust CI workflow and fix bugs it surfaces" — additional CI workflow + bug fixes, medium risk

**User requirement (from CONTEXT.md specifics):** Explain what each PR does before merging.
**Wave 2 requires Wave 1 complete** (upstream remote configured, PRs #3 and #6 already merged).
</context>

<tasks>

## Task 1: Review and Merge PR #1 (Fix stale timeout + duplicate duration parsing)

<task id="1">
<type>execute</type>
<title>Explain PR #1 changes, merge, run cargo test</title>

<read_first>
- `Rust/core/src/` — scan for timeout/duration-related modules to understand PR #1 context
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-02, D-05, D-06
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/1/head:upstream-pr-1`
2. Show and explain the diff: `git diff main...upstream-pr-1` — present to user:
   - What PR #1 fixes: stale timeout message (corrects log/UI text shown when BLE operation times out without new data); duration parsing deduplication (removes repeated parse logic into shared utility)
   - Which Rust source files are touched
   - Confirm no Swift, server, or planning files changed
3. Merge: `git checkout main && git merge --no-ff upstream-pr-1 -m "merge: upstream PR #1 — Fix stale timeout message and deduplicate duration parsing"`
4. Resolve conflicts if any in Rust source:
   - Timeout logic: accept upstream's corrected message/logic; preserve any fork additions to the same module
5. Run tests (D-05): `cd Rust/core && cargo test 2>&1`
6. Verify fork files: `git diff HEAD~1 -- GooseSwift/GooseAppModel+Upload.swift GooseSwift/MoreRemoteServerViews.swift server/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #1"
- `cargo test` in `Rust/core/` exits 0 after merge
- `git diff HEAD~1 -- GooseSwift/` is empty
- `git diff HEAD~1 -- server/` is empty
- Merge commit has two parents (non-fast-forward): `git show HEAD --format="%P" | tr ' ' '\n' | wc -l` outputs 2
</acceptance_criteria>
</task>

## Task 2: Review and Merge PR #13 (Integration tests + Windows compat)

<task id="2">
<type>execute</type>
<title>Explain PR #13 changes, merge, run cargo test</title>

<read_first>
- `Rust/core/tests/` — existing integration test files to understand what PR #13 modifies
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-05, D-09
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/13/head:upstream-pr-13`
2. Show and explain the diff: `git diff main...upstream-pr-13` — present to user:
   - Which integration tests were failing and what fixes were applied
   - What Windows compatibility issues were addressed (e.g., path separator handling, platform-specific conditionals)
   - Confirm changes confined to `Rust/core/tests/` and `Rust/core/src/` for compat fixes
3. Merge: `git checkout main && git merge --no-ff upstream-pr-13 -m "merge: upstream PR #13 — Fix Rust core integration tests and Windows compatibility"`
4. Resolve conflicts if any in test files:
   - Keep upstream's corrected assertions; preserve any fork-specific test utilities or fixtures
   - Platform compat changes: accept all (no fork-specific platform handling expected)
5. Run tests (D-05 — this PR is specifically about fixing tests, so all should pass): `cd Rust/core && cargo test 2>&1`
6. Verify fork files: `git diff HEAD~1 -- GooseSwift/ server/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #13"
- `cargo test` exits 0 — all integration tests pass (this is the specific goal of PR #13)
- `git diff HEAD~1 -- GooseSwift/` is empty
- `git diff HEAD~1 -- server/` is empty
</acceptance_criteria>
</task>

## Task 3: Review and Merge PR #10 (Rust CI workflow + bug fixes)

<task id="3">
<type>execute</type>
<title>Explain PR #10 changes, merge, run cargo test</title>

<read_first>
- `.github/workflows/` — current state after PR #6 merge (detect naming collision with PR #10)
- `Rust/core/src/` — current state to understand what CI-surfaced bugs PR #10 fixes
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-05, D-09
</read_first>

<action>
1. Fetch PR: `git fetch upstream pull/10/head:upstream-pr-10`
2. Show and explain the diff: `git diff main...upstream-pr-10` — present to user:
   - New/modified CI workflow files (check for naming collision with PR #6's workflow)
   - Rust source bug fixes that CI surfaced (compilation warnings-as-errors, missing trait impls, etc.)
   - If naming collision with PR #6 workflow: document resolution plan
3. Merge: `git checkout main && git merge --no-ff upstream-pr-10 -m "merge: upstream PR #10 — Add Rust CI workflow and fix bugs it surfaces"`
4. Resolve conflicts:
   - CI workflow naming collision: rename PR #10's workflow to avoid overwriting PR #6's addition (e.g., `rust-ci-pr10.yml`)
   - Rust source conflicts: accept upstream bug fixes; preserve any fork additions in same files
5. Run tests (D-05): `cd Rust/core && cargo test 2>&1`
6. Verify fork files and that PR #6 workflow survived: `git diff HEAD~1 -- GooseSwift/ server/` + `ls .github/workflows/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #10"
- `cargo test` exits 0 after merge
- `git diff HEAD~1 -- GooseSwift/` is empty
- `git diff HEAD~1 -- server/` is empty
- `ls .github/workflows/` shows both PR #6's workflow and PR #10's workflow (neither overwrote the other)
</acceptance_criteria>
</task>

## Task 4: Wave 2 final verification

<task id="4">
<type>execute</type>
<title>Verify all three Wave 2 PRs integrated and requirements satisfied</title>

<read_first>
- `.planning/REQUIREMENTS.md` — FORK-02, FORK-08, FORK-10 definitions
</read_first>

<action>
1. Verify merge history: `git log --oneline | grep -E "PR #(1|13|10)"` — 3 merge commits present
2. Run final cargo test as full-wave check: `cd Rust/core && cargo test 2>&1`
3. Confirm fork-specific files intact:
   ```
   git log --oneline -- GooseSwift/GooseAppModel+Upload.swift | head -3
   git log --oneline -- GooseSwift/MoreRemoteServerViews.swift | head -3
   ```
   Commits touching these files should be only from Phase 2/3 work, not from any upstream PR merge
4. Total merge commits to date: `git log --oneline | grep "upstream PR #" | wc -l` — should be 5 (PRs #3, #6, #1, #13, #10)
</action>

<acceptance_criteria>
- `git log --oneline | grep -E "PR #(1|13|10)"` shows 3 merge commits
- Final `cargo test` exits 0
- FORK-02 (PR #1), FORK-08 (PR #10), FORK-10 (PR #13) requirements satisfied
- Fork-specific files unmodified by any Wave 2 merge
- Total of 5 upstream PR merge commits in log (Waves 1+2 complete)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cargo test` in `Rust/core/` passes after each of the three PR merges
2. `git log --oneline | grep -E "PR #(1|13|10)"` shows three merge commits
3. FORK-02, FORK-08, FORK-10 requirements satisfied
4. Fork-specific files (`server/`, upload client, settings UI) unchanged throughout Wave 2
5. No PR #6 CI workflow was overwritten when PR #10's CI workflow was added
</verification>

<must_haves>
  <truths>
    - T-05-06: PR #1 merged via --no-ff; cargo test passes after merge (FORK-02)
    - T-05-07: PR #13 merged via --no-ff; all integration tests pass (FORK-10)
    - T-05-08: PR #10 merged via --no-ff; cargo test passes after merge (FORK-08)
    - T-05-09: No CI workflow from Wave 1 (PR #6) was overwritten during Wave 2
    - T-05-10: Fork-specific files (`server/`, upload client) untouched by all Wave 2 merges
  </truths>
  <goal>Three Rust-touching PRs integrated in sequence with cargo test passing after each, preserving all fork-specific infrastructure</goal>
</must_haves>

<threat_model>
- **PR #1 conflicts with fork Rust changes:** If fork has modifications to timeout/duration modules, resolve by accepting upstream fix + keeping fork additions.
- **PR #13 test conflicts:** Integration tests may have fork-specific additions. Keep both upstream fixes and fork test utilities.
- **PR #10 CI naming collision with PR #6:** High probability. Mitigation: rename PR #10's workflow to avoid overwriting PR #6's addition.
- **cargo test fails after merge:** Stop, diagnose root cause (was it pre-existing? introduced by PR?), resolve before continuing to Wave 3.
- **PR #10 bug fixes conflict with fork's Rust additions:** Medium probability. Resolve by keeping both — they are not mutually exclusive.
</threat_model>
