---
phase: 14-pt-pt-localisation
plan: 04
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - GooseSwift/LocalizedStatusStrings.swift
  - GooseSwift/Localizable.xcstrings
  - GooseSwift/ConnectionView.swift
  - GooseSwift/HRMonitorView.swift
  - GooseSwift/DeviceView.swift
  - GooseSwift/MoreCaptureViews.swift
  - GooseSwift/MoreDebugViews.swift
  - GooseSwift/MoreRouteModels.swift
  - GooseSwift/HomeDashboardView.swift
  - GooseSwift/OnboardingStepViews.swift
  - GooseSwift/SleepV2ScheduleViews.swift
  - GooseSwift/SleepBridgeViews.swift
autonomous: true
requirements: [L10N-02, L10N-01]

must_haves:
  truths:
    - "All 14 @Published status display strings render in pt-PT via display-layer extension methods"
    - "Raw @Published status values (connectionState, bluetoothState, etc.) remain unchanged English constants"
    - "State machine guards comparing raw status values still work (logic unaffected)"
    - "MoreStatusKind.title display value renders in pt-PT"
    - "The app compiles with xcodebuild for the simulator with no errors"
    - "No hardcoded English text remains visible in main user-facing flows on a pt-PT device"
  artifacts:
    - path: "GooseSwift/LocalizedStatusStrings.swift"
      provides: "extension String display methods mapping raw status values to pt-PT"
      contains: "extension String"
      min_lines: 60
  key_links:
    - from: "GooseSwift/ConnectionView.swift"
      to: "GooseSwift/LocalizedStatusStrings.swift"
      via: "ble.connectionState.localizedConnectionState at display sites"
      pattern: "\\.localized[A-Z]"
    - from: "GooseSwift/LocalizedStatusStrings.swift"
      to: "GooseSwift/Localizable.xcstrings"
      via: "String(localized:) inside extension methods"
      pattern: "String\\(localized:"
---

<objective>
Localise the dynamic `@Published` status strings (D-04: 14 properties across `GooseBLEClient` and
`GooseAppModel`) via a display-layer extension, update every view display site to use the
`.localizedXxx` variant, localise `MoreStatusKind.title`, run a final sweep for missed static
strings, and verify the whole app still compiles.

Per D-03, the raw `@Published` values stay English (they are state-machine constants used in
guards/comparisons) — only the display boundary is translated. This is the only wave that touches
view code (display-site rewiring), so it depends on all three earlier waves being merged.

Purpose: Close the last localisation gap (L10N-02 dynamic strings) and prove the phase compiles
(L10N-01/L10N-02 integration), satisfying ROADMAP success criteria 2 and 3.
Output: `LocalizedStatusStrings.swift`, updated display sites, final catalog entries, green build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-pt-pt-localisation/14-CONTEXT.md
@.planning/phases/14-pt-pt-localisation/14-RESEARCH.md
@.planning/phases/14-pt-pt-localisation/14-01-SUMMARY.md
@.planning/phases/14-pt-pt-localisation/14-02-SUMMARY.md
@.planning/phases/14-pt-pt-localisation/14-03-SUMMARY.md
@GooseSwift/GooseBLEClient.swift
@GooseSwift/GooseAppModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LocalizedStatusStrings.swift with display-layer extensions (D-03, D-04)</name>
  <files>GooseSwift/LocalizedStatusStrings.swift, GooseSwift/Localizable.xcstrings</files>
  <action>
Create `GooseSwift/LocalizedStatusStrings.swift` with `import Foundation` and a single
`extension String { ... }` containing one computed display method per dynamic status property
(D-04). Each method switches on the raw English value and returns `String(localized: "<key>")`;
the `default` branch returns `self` (unknown states fall back to the raw English value — Pitfall 2
safety). Add a `pt-PT` catalog entry for every translated value used inside these methods.

Read `GooseSwift/GooseBLEClient.swift` and `GooseSwift/GooseAppModel.swift` to enumerate the exact
raw default/possible values for each property before writing the switch cases. Implement these
methods (D-04 list):
- GooseBLEClient: `localizedConnectionState`, `localizedHRConnectionState`, `localizedBluetoothState`,
  `localizedHRBluetoothState`, `localizedReconnectState`, `localizedHRReconnectState`,
  `localizedHistoricalSyncStatus`, `localizedStrapClockStatus`, `localizedBatteryPowerStatus`
- GooseAppModel: `localizedCaptureStatus`, `localizedCaptureTargetSummary`,
  `localizedOvernightGuardStatus`, `localizedActivityDetectionStatus`, `localizedPacketImportStatus`

Use the glossary for known values (e.g. "disconnected" → "Desligado", "connecting" → "A ligar...",
"connected" → "Ligado", "poweredOn" → "Ativo", "scanning" → "A procurar...", "idle" → "Inativo").
For prefixed/composed status strings (e.g. reconnect states like "reconnecting (attempt 3/10)"),
match on `hasPrefix(...)` and localise the prefix while preserving the dynamic numeric tail.

Do NOT modify any `@Published` property declaration or its raw values in GooseBLEClient.swift or
GooseAppModel.swift — this file is additive only (D-03).
  </action>
  <verify>
    <automated>test -f GooseSwift/LocalizedStatusStrings.swift && test $(grep -c 'var localized' GooseSwift/LocalizedStatusStrings.swift) -ge 14 && python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && echo OK</automated>
  </verify>
  <done>LocalizedStatusStrings.swift defines >=14 localizedXxx display methods, each falling back to self for unknown states; catalog has entries for the translated values; no @Published declarations changed.</done>
</task>

<task type="auto">
  <name>Task 2: Update all display sites to use .localizedXxx variants and localise MoreStatusKind.title</name>
  <files>GooseSwift/ConnectionView.swift, GooseSwift/HRMonitorView.swift, GooseSwift/DeviceView.swift, GooseSwift/MoreCaptureViews.swift, GooseSwift/MoreDebugViews.swift, GooseSwift/MoreRouteModels.swift, GooseSwift/HomeDashboardView.swift, GooseSwift/SleepV2ScheduleViews.swift, GooseSwift/SleepBridgeViews.swift</files>
  <action>
For every view display site that renders a raw `@Published` status value, append the matching
`.localizedXxx` accessor (D-04). Audit each site — only rewrite DISPLAY usages (inside
`Text(...)`, `LabeledContent(..., value:)`, `value:` arguments, statusText). Do NOT rewrite
comparisons/guards (`== "ready"`, `switch ble.connectionState { ... }` used for control flow,
`if ... == "idle"`) — those must keep the raw value (Pitfall 2).

Known display sites (from research D-04 table — verify each with grep before editing):
- ConnectionView.swift: `bluetoothState`→`.localizedBluetoothState`, `connectionState`→
  `.localizedConnectionState`, `reconnectState`→`.localizedReconnectState`,
  `hrReconnectState`→`.localizedHRReconnectState`, `historicalSyncStatus`→`.localizedHistoricalSyncStatus`
- HRMonitorView.swift: `hrReconnectState` Text display → `.localizedHRReconnectState`
  (the "CONNECTED"/"SCANNING" status header literals were handled as static catalog keys in Wave 3;
  leave the control-flow switch on `hrConnectionState`/`hrBluetoothState` raw values intact)
- DeviceView.swift: `connectionState`, `historicalSyncStatus`, `batteryPowerStatus`,
  `strapClockStatus` display sites → respective `.localizedXxx`
- MoreCaptureViews.swift: `healthPacketCaptureStatus`→`.localizedCaptureStatus`,
  `healthPacketCaptureTargetSummary`→`.localizedCaptureTargetSummary`,
  `overnightGuardStatus`→`.localizedOvernightGuardStatus`
- MoreDebugViews.swift / HomeDashboardView.swift: `activityDetectionStatus`→
  `.localizedActivityDetectionStatus`, `packetImportStatus`→`.localizedPacketImportStatus`
- SleepV2ScheduleViews.swift / SleepBridgeViews.swift: `historicalSyncStatus` display →
  `.localizedHistoricalSyncStatus`

Also localise `MoreStatusKind.title` in MoreRouteModels.swift (currently `rawValue.capitalized`):
switch on the case and return `String(localized:)` for each (ready/pending/blocked/unavailable/
stale → Pronto/Pendente/Bloqueado/Indisponível/Desatualizado). Add those 5 catalog entries.

Use grep to find every display occurrence before editing, e.g.:
`grep -rn 'connectionState\|bluetoothState\|reconnectState\|historicalSyncStatus\|batteryPowerStatus\|strapClockStatus\|CaptureStatus\|CaptureTargetSummary\|overnightGuardStatus\|activityDetectionStatus\|packetImportStatus' GooseSwift/*.swift`
and classify each hit as display (rewrite) vs guard/comparison (leave).
  </action>
  <verify>
    <automated>test $(grep -rc '\.localized[A-Z]' GooseSwift/ConnectionView.swift GooseSwift/DeviceView.swift GooseSwift/MoreCaptureViews.swift | awk -F: '{s+=$2} END {print s}') -ge 8 && grep -q 'String(localized:' GooseSwift/MoreRouteModels.swift && python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && echo OK</automated>
  </verify>
  <done>Status display sites use .localizedXxx variants; control-flow comparisons still reference raw values; MoreStatusKind.title returns String(localized:) for all 5 cases; catalog updated and valid.</done>
</task>

<task type="auto">
  <name>Task 3: Final sweep for missed static strings and verify the app compiles</name>
  <files>GooseSwift/Localizable.xcstrings</files>
  <action>
Run a final comprehensive sweep across all GooseSwift view files for any user-visible static
literal that has no catalog entry yet, and add the missing pt-PT translations. Sweep command:
`grep -rnoE '(Text|Section|Button|Label|LabeledContent|navigationTitle|accessibilityLabel)\("[^"]+"' GooseSwift/*.swift`
Cross-check each found literal against the keys already in `Localizable.xcstrings`; add any that
are missing (applying the same EXCLUDE rules: Text(verbatim:), record/log strings, SF Symbols,
@Published raw values).

Then run the full build to confirm no compilation errors were introduced by the code changes in
Tasks 1-2 and that the catalog is accepted by the build toolchain. The build is the integration
gate for the whole phase (L10N-01 + L10N-02). State-machine guards must still compile and behave
(Pitfall 2 / success criterion 5).
  </action>
  <verify>
    <automated>python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && xcodebuild -scheme GooseSwift -sdk iphonesimulator build 2>&1 | grep -E "error:|Build succeeded" | head -20</automated>
  </verify>
  <done>Final sweep added any remaining missed static strings; `xcodebuild -scheme GooseSwift -sdk iphonesimulator build` reports "Build succeeded" with no `error:` lines.</done>
</task>

</tasks>

<verification>
- `LocalizedStatusStrings.swift` provides all 14 dynamic display methods (D-04) with self-fallback.
- All `@Published` raw status values are unchanged; control-flow guards still reference raw English values (success criterion 5).
- Every status display site uses a `.localizedXxx` accessor; `MoreStatusKind.title` is localised.
- `Localizable.xcstrings` is valid JSON and covers all swept static strings.
- `xcodebuild -scheme GooseSwift -sdk iphonesimulator build` succeeds.
</verification>

<success_criteria>
- Dynamic status strings (BLE connection/sync/upload/capture state) display in pt-PT (ROADMAP criterion 2, D-04).
- No hardcoded English text remains visible in main user-facing flows (ROADMAP criterion 3).
- State machine logic is unaffected (ROADMAP criterion 5 / D-03).
- The app builds cleanly for the simulator.
</success_criteria>

<output>
Create `.planning/phases/14-pt-pt-localisation/14-04-SUMMARY.md` when done.
</output>
