---
phase: 15
plan: "01"
name: Recovery Formula V2 — SDNN Accuracy Fix
type: fix
autonomous: true
wave: 1
depends_on: []
requirements: []
---

# Phase 15 Plan 01: Recovery Formula V2 — SDNN Accuracy Fix

## Objective

Fix `hkRecoveryScore()` in `HealthDataStore+Snapshots.swift` to use Apple Watch SDNN values
directly instead of dividing by 1.2 (inaccurate population SDNN→RMSSD approximation). Rename
the internal variable from `hkHRVRmssdMs` to `hkHRVSDNNMs` everywhere for clarity.

## Context

- `15-CONTEXT.md` — decisions D-01 through D-04
- Reference: OKKHALIL3 review comment on upstream PR #5

## Tasks

### Task 1 (auto): Rename hkHRVRmssdMs → hkHRVSDNNMs and fix hkRecoveryScore formula

**Files:**
- `GooseSwift/HealthDataStore.swift` — rename `@Published var hkHRVRmssdMs`
- `GooseSwift/HealthKitFullImporter.swift` — rename struct field and initialiser argument
- `GooseSwift/HealthDataStore+Sleep.swift` — update two read sites
- `GooseSwift/HealthDataStore+Snapshots.swift` — rename guard, remove /1.2, update baseline
- `GooseSwift/HealthDataStore+CoachSummaries.swift` — rename three read sites
- `GooseSwift/HealthDataStore+Vitals.swift` — rename one read site
- `GooseSwift/HealthDashboardViews.swift` — rename `.onChange` key path

**Changes in `hkRecoveryScore()`:**
1. `guard let sdnn = hkHRVSDNNMs` (renamed)
2. `let sdnnMs = sdnn` (remove `/1.2`)
3. Rolling mean map: `{ $0.sdnn }` (remove `/ 1.2`)
4. Population baseline: `50.0` (was `40.0`)
5. Score expression: `sdnnMs / hrvBaseline` (was `rmssdEquiv / hrvBaseline`)

## Verification / Success Criteria

1. `grep -rn "hkHRVRmssdMs\|hrvRmssdMs" GooseSwift/` returns empty
2. `grep -n "/ 1\.2" GooseSwift/HealthDataStore+Snapshots.swift` returns empty
3. Population baseline is `50.0` in `hkRecoveryScore()`
4. `cargo test -p goose-core` passes (no Rust changes)
