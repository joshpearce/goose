---
phase: 10-hr-monitor-scan-connect-ui
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - GooseSwift/MoreRouteModels.swift
  - GooseSwift/MoreView.swift
  - GooseSwift/MoreDataStore.swift
autonomous: true
requirements: [WEAR-04, WEAR-05]
must_haves:
  truths:
    - "An 'HR Monitor' entry appears in the Device section of the More tab (D-01)"
    - "Tapping the entry navigates to HRMonitorView"
    - "The entry shows a ready status when an HR monitor is connected and pending otherwise"
  artifacts:
    - path: "GooseSwift/MoreRouteModels.swift"
      provides: "MoreRoute.hrMonitor case + all switch arms + deviceRoutes inclusion + MoreRouteStatus.hrMonitor"
      contains: "case hrMonitor"
    - path: "GooseSwift/MoreView.swift"
      provides: "destination(for:) -> HRMonitorView() for .hrMonitor"
      contains: "HRMonitorView()"
    - path: "GooseSwift/MoreDataStore.swift"
      provides: "hrMonitor status argument in MoreRouteStatus(...)"
      contains: "hrMonitor:"
  key_links:
    - from: "MoreRoute.deviceRoutes"
      to: "MoreView Device section rendering"
      via: "static array inclusion"
      pattern: "deviceRoutes: \\[MoreRoute\\] = \\[.device, .hrMonitor\\]"
    - from: "MoreView.destination(for:)"
      to: "HRMonitorView"
      via: "switch arm"
      pattern: "case .hrMonitor"
---

<objective>
Wire the new `HRMonitorView` (Plan 02) into the More tab as a `MoreRoute.hrMonitor` entry in the Device section, satisfying D-01. This means adding the enum case and all four exhaustive `switch` arms, the `MoreRouteStatus.hrMonitor` property, the `deviceRoutes` inclusion, the `destination(for:)` arm, and the `MoreRouteStatus(...)` constructor argument.

Purpose: Without this wiring the user has no entry point to reach the scan/connect screen. This closes the WEAR-04/WEAR-05 user path end to end (More > HR Monitor > scan > connect).

Output: Three edited Swift files completing the navigation integration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-CONTEXT.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-RESEARCH.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-PATTERNS.md
@.planning/phases/10-hr-monitor-scan-connect-ui/10-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 portion)

- `MoreRoute.hrMonitor` — new enum case in `MoreRouteModels.swift`
- `MoreRouteStatus.hrMonitor` — new `MoreStatusKind` stored property
- `MoreRoute.deviceRoutes` — updated to `[.device, .hrMonitor]`
- New switch arms in `MoreRoute.title`, `.subtitle`, `.systemImage`, `.statusKeyPath`
- New `case .hrMonitor: HRMonitorView()` in `MoreView.destination(for:)`
- New `hrMonitor:` argument in `MoreDataStore.routeStatus(ble:model:)`'s `MoreRouteStatus(...)` call

No new types. This plan only extends existing enums/structs/switches.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add MoreRoute.hrMonitor case, switch arms, status property, and deviceRoutes inclusion</name>
  <files>GooseSwift/MoreRouteModels.swift</files>
  <read_first>
    - GooseSwift/MoreRouteModels.swift (FULL — enum cases lines 4-17; `title` 21-38; `subtitle` 40-57; `systemImage` 59-76; `statusKeyPath` 78-95; `deviceRoutes` line 97; `MoreRouteStatus` struct 105-120)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-PATTERNS.md "MoreRouteModels.swift" section (exact arm values)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-RESEARCH.md Pitfalls 2, 3, 4 (switch exhaustion + deviceRoutes + MoreRouteStatus property must all be updated together)
  </read_first>
  <action>
    Edit `GooseSwift/MoreRouteModels.swift`:
    1. Add `case hrMonitor` to the `MoreRoute` enum, immediately after `case device` (line 5).
    2. Add `case .hrMonitor: "HR Monitor"` to the `title` switch (after the `.device` arm).
    3. Add `case .hrMonitor: "Connect and view live heart rate from a Bluetooth HR monitor"` to the `subtitle` switch.
    4. Add `case .hrMonitor: "heart.circle"` to the `systemImage` switch.
    5. Add `case .hrMonitor: \.hrMonitor` to the `statusKeyPath` switch.
    6. Change `static let deviceRoutes: [MoreRoute] = [.device]` (line 97) to `static let deviceRoutes: [MoreRoute] = [.device, .hrMonitor]`.
    7. Add `var hrMonitor: MoreStatusKind` to the `MoreRouteStatus` struct, immediately after `var device: MoreStatusKind` (line 107).

    All four switches (`title`, `subtitle`, `systemImage`, `statusKeyPath`) must get the new arm or the compiler emits "Switch must be exhaustive" (RESEARCH Pitfall 3). The `statusKeyPath` arm `\.hrMonitor` requires the `MoreRouteStatus.hrMonitor` property from step 7 (RESEARCH Pitfall 2). Use 2-space indentation matching the file.
  </action>
  <verify>
    <automated>grep -c "case hrMonitor" GooseSwift/MoreRouteModels.swift; grep -c "case .hrMonitor:" GooseSwift/MoreRouteModels.swift; grep -c "var hrMonitor: MoreStatusKind" GooseSwift/MoreRouteModels.swift; grep -c "deviceRoutes: \[MoreRoute\] = \[.device, .hrMonitor\]" GooseSwift/MoreRouteModels.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case hrMonitor" GooseSwift/MoreRouteModels.swift` returns `1` (enum declaration)
    - `grep -c "case .hrMonitor:" GooseSwift/MoreRouteModels.swift` returns `4` (title, subtitle, systemImage, statusKeyPath arms)
    - `grep -c "var hrMonitor: MoreStatusKind" GooseSwift/MoreRouteModels.swift` returns `1` (MoreRouteStatus property)
    - `deviceRoutes` includes `.hrMonitor` (source: `grep -q "deviceRoutes: \[MoreRoute\] = \[.device, .hrMonitor\]"`)
    - Title arm value is exactly `"HR Monitor"` (source: `grep -q 'case .hrMonitor: "HR Monitor"'`)
    - systemImage arm value is exactly `"heart.circle"` (source: `grep -q 'case .hrMonitor: "heart.circle"'`)
  </acceptance_criteria>
  <done>`MoreRoute` has a `.hrMonitor` case with complete, exhaustive switch coverage, a matching `MoreRouteStatus.hrMonitor` property, and inclusion in the Device section's `deviceRoutes` array.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HRMonitorView destination and hrMonitor route status</name>
  <files>GooseSwift/MoreView.swift, GooseSwift/MoreDataStore.swift</files>
  <read_first>
    - GooseSwift/MoreView.swift lines 119-153 (`destination(for:)` switch; insertion point after the `.device` arm at line 122-123)
    - GooseSwift/MoreDataStore.swift lines 129-146 (`routeStatus(ble:model:)` building `MoreRouteStatus(...)`; `device:` argument is line 132 — add `hrMonitor:` adjacent)
    - GooseSwift/HRMonitorView.swift (confirm `HRMonitorView()` is the public initializer with no required args)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-PATTERNS.md "MoreView.swift" section (destination + routeStatus argument)
    - .planning/phases/10-hr-monitor-scan-connect-ui/10-RESEARCH.md Open Question 1 (recommendation: `.ready` when connected, `.pending` otherwise)
  </read_first>
  <action>
    1. In `GooseSwift/MoreView.swift` `destination(for:)`, add `case .hrMonitor: HRMonitorView()` immediately after the `case .device: DeviceView()` arm (lines 122-123). `HRMonitorView` reads `model.ble` via `@EnvironmentObject`, so no parameters are passed (the environment is already injected by the parent — same as `DeviceView()`).

    2. In `GooseSwift/MoreDataStore.swift` `routeStatus(ble:model:)`, add the argument `hrMonitor: ble.hrConnectionState == "connected" ? .ready : .pending,` to the `MoreRouteStatus(...)` initializer call, placed immediately after the `device:` argument (line 132). This uses the `@Published hrConnectionState` promoted in Plan 01 and follows RESEARCH Open Question 1's recommendation (`.ready` when connected, `.pending` otherwise).

    Adding `.hrMonitor` to `MoreRoute` (Task 1) makes the `MoreRouteStatus(...)` call require the `hrMonitor:` argument; omitting it is a compile error.
  </action>
  <verify>
    <automated>grep -c "case .hrMonitor:" GooseSwift/MoreView.swift; grep -c "HRMonitorView()" GooseSwift/MoreView.swift; grep -c 'hrMonitor: ble.hrConnectionState == "connected"' GooseSwift/MoreDataStore.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case .hrMonitor:" GooseSwift/MoreView.swift` returns `1`
    - `grep -c "HRMonitorView()" GooseSwift/MoreView.swift` returns `1` (destination wired)
    - `MoreDataStore` supplies the `hrMonitor` status argument (source: `grep -q 'hrMonitor: ble.hrConnectionState == "connected" ? .ready : .pending'`)
    - The `MoreRouteStatus(...)` call in `MoreDataStore.swift` still compiles with all arguments present (behavior assertion: no missing-argument compile error — verified at wave build)
  </acceptance_criteria>
  <done>Tapping "HR Monitor" in the More tab's Device section navigates to `HRMonitorView`, and the entry's status row reflects connection state — completing the end-to-end WEAR-04/WEAR-05 user path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User navigation -> view instantiation | No untrusted input; navigation is enum-driven and internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | `MoreRoute` switch exhaustiveness | mitigate | Swift compiler enforces exhaustive switches; all four arms + `MoreRouteStatus` property added together so the build fails closed if any is missing (RESEARCH Pitfalls 2-3) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Apple system frameworks only (RESEARCH Package Legitimacy Audit: Not applicable) |
</threat_model>

<verification>
- `cargo test -p goose-core` passes (repo regression gate).
- GooseSwift target compiles with the new route, destination, and status argument (exhaustive-switch and missing-argument errors are the primary failure modes — both caught at build).
- grep gates in each task pass.
</verification>

<success_criteria>
- `MoreRoute.hrMonitor` exists with complete switch coverage and `MoreRouteStatus.hrMonitor`.
- `deviceRoutes` includes `.hrMonitor` so the entry renders in the Device section.
- `destination(for: .hrMonitor)` returns `HRMonitorView()`.
- `MoreDataStore.routeStatus` supplies the `hrMonitor` status.
</success_criteria>

<output>
Create `.planning/phases/10-hr-monitor-scan-connect-ui/10-03-SUMMARY.md` when done.
</output>
