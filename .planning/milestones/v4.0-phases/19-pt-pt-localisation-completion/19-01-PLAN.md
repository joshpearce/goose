---
phase: 19-pt-pt-localisation-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/Localizable.xcstrings
autonomous: true
requirements: [L10N-03, PERF-04, UX-01]

must_haves:
  truths:
    - "All 128 non-trivial strings missing pt-PT are translated in Localizable.xcstrings"
    - "AI provider/model brand names (Claude Sonnet 4.6, Claude Opus 4.8, Claude Haiku 4.5, GPT-5.5 High/Low/Medium, Gemini 2.5 Pro/Flash, Google Client ID) intentionally have no pt-PT entry"
    - "xcodebuild for the simulator completes with Build succeeded and no error: lines"
    - "Python scan confirms 0 non-trivial strings without pt-PT (excluding brand names)"
    - "Localizable.xcstrings remains valid JSON (python3 -m json.tool passes)"
    - "The 3 startup fixes already shipped are confirmed working via the build gate"
  artifacts:
    - path: "GooseSwift/Localizable.xcstrings"
      provides: "Complete pt-PT translations for all 128 non-trivial missing strings"
      contains: "pt-PT"
  key_links:
    - from: "GooseSwift/CoachSettingsSheet.swift"
      to: "GooseSwift/Localizable.xcstrings"
      via: "Text(\"...\") literals auto-resolved from catalog"
      pattern: "Text\\("
---

<objective>
Translate all 128 non-trivial strings currently missing pt-PT entries in GooseSwift/Localizable.xcstrings,
completing the v4.0 localisation pass for Phase 16-18 strings (Deep Link Security, @Observable
Migration, Coach Multi-Provider).

Per D-01: translate ALL non-trivial strings.
Per D-02: AI provider/model brand names stay in English (no pt-PT entry).
Per D-03: single plan, no waves.
Per D-04/D-05: verification gate is xcodebuild pass + Python scan = 0.

Purpose: The app is fully Portuguese (pt-PT) when the OS language is set to Portuguese.
Coach settings, health UI, sleep timeline, cardio load, and all new Phase 18 provider config
strings render in pt-PT.

Output: Updated GooseSwift/Localizable.xcstrings with 128 new pt-PT entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-pt-pt-localisation-completion/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pt-PT translations for Coach / Provider config strings (D-01, D-02, per L10N-03)</name>

  <read_first>
    - GooseSwift/Localizable.xcstrings — read the full file to understand current structure and locate each key before editing
    - GooseSwift/CoachSettingsSheet.swift — primary source of Coach/provider config string literals (Phase 18)
  </read_first>

  <files>GooseSwift/Localizable.xcstrings</files>

  <action>
For each string key listed below, add a pt-PT localization block under its existing entry's
"localizations" dict (or create a "localizations" key if it has none). The JSON format for
each entry is:

"pt-PT" : {
  "stringUnit" : {
    "state" : "translated",
    "value" : "Tradução em Português"
  }
}

INTENTIONALLY EXCLUDED (D-02) — do NOT add pt-PT for these brand names:
- "Claude Sonnet 4.6"
- "Claude Opus 4.8"
- "Claude Haiku 4.5"
- "GPT-5.5 High"
- "GPT-5.5 Low"
- "GPT-5.5 Medium"
- "Gemini 2.5 Pro"
- "Gemini 2.5 Flash"
- "Google Client ID"

GROUP A — Coach / Provider config strings (~29 strings):

"API Key" → "Chave de API"
"Anthropic API key" → "Chave de API Anthropic"
"Base URL" → "URL de Base"
"Bearer token (API key)" → "Token Bearer (chave de API)"
"Calibrate" → "Calibrar"
"Change" → "Alterar"
"Coach Settings" → "Definições do Treinador"
"Coach settings" → "Definições do treinador"
"Configuration" → "Configuração"
"Enter an API key first" → "Introduza primeiro uma chave de API"
"Filters" → "Filtros"
"Generation stopped" → "Geração interrompida"
"Key saved" → "Chave guardada"
"Model ID" → "ID do modelo"
"Must start with https://" → "Tem de começar com https://"
"No key saved" → "Nenhuma chave guardada"
"Not signed in" → "Não autenticado"
"Provider" → "Fornecedor"
"Remove API Key" → "Remover chave de API"
"Remove Key" → "Remover chave"
"Save API Key" → "Guardar chave de API"
"Save Endpoint" → "Guardar ponto de acesso"
"Select a provider above to get started." → "Selecione um fornecedor acima para começar."
"Sign Out?" → "Terminar sessão?"
"Sign in with ChatGPT" → "Iniciar sessão com ChatGPT"
"Sign in with Google" → "Iniciar sessão com Google"
"Sign-in failed. Try again." → "Falha ao iniciar sessão. Tente novamente."
"Signed in" → "Sessão iniciada"
"Signing in..." → "A iniciar sessão..."
"The key will be deleted from Keychain. You can add a new key at any time." → "A chave será eliminada do Keychain. Pode adicionar uma nova chave a qualquer momento."
"You will need to sign in again to use this provider." → "Terá de iniciar sessão novamente para utilizar este fornecedor."
"https://hostname:8770" → "https://hostname:8770"

Note: "https://hostname:8770" is a placeholder/example URL — keep the value identical (technical label, not user-facing prose).

After adding all GROUP A entries, validate JSON:
  python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && echo "JSON OK"
  </action>

  <verify>
    <automated>python3 -c "
import json
with open('GooseSwift/Localizable.xcstrings') as f:
    data = json.load(f)
keys_to_check = ['API Key','Anthropic API key','Base URL','Bearer token (API key)',
  'Calibrate','Change','Coach Settings','Coach settings','Configuration',
  'Enter an API key first','Filters','Generation stopped','Key saved','Model ID',
  'Must start with https://','No key saved','Not signed in','Provider',
  'Remove API Key','Remove Key','Save API Key','Save Endpoint',
  'Select a provider above to get started.','Sign Out?','Sign in with ChatGPT',
  'Sign in with Google','Sign-in failed. Try again.','Signed in','Signing in...',
  'The key will be deleted from Keychain. You can add a new key at any time.',
  'You will need to sign in again to use this provider.']
missing = [k for k in keys_to_check if 'pt-PT' not in data['strings'].get(k, {}).get('localizations', {})]
if missing:
    print('MISSING:', missing)
    exit(1)
print('All GROUP A Coach strings have pt-PT. OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - Every string in GROUP A has a pt-PT entry with state "translated"
    - The 9 brand name strings (Claude Sonnet 4.6, Claude Opus 4.8, Claude Haiku 4.5, GPT-5.5 High/Low/Medium, Gemini 2.5 Pro/Flash, Google Client ID) have NO pt-PT entry added
    - python3 -m json.tool GooseSwift/Localizable.xcstrings exits 0
  </acceptance_criteria>

  <done>All ~32 Coach/provider config strings have pt-PT entries; brand names are intentionally excluded; JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add pt-PT translations for Health / Sleep / Cardio / General UI strings (D-01, per L10N-03, UX-01)</name>

  <read_first>
    - GooseSwift/Localizable.xcstrings — must re-read after Task 1 to see current state before editing
    - GooseSwift/CoachSettingsSheet.swift — verify any remaining coach strings not caught in Task 1
  </read_first>

  <files>GooseSwift/Localizable.xcstrings</files>

  <action>
Continue adding pt-PT localizations to Localizable.xcstrings for all remaining non-trivial strings
not covered by Task 1 or excluded by D-02.

GROUP B — Health / Sleep / Cardio / Workout strings (~60 strings):

"Add Sleep" → "Adicionar sono"
"Add Sleep creates the first local sleep row once manual entry is available." → "Adicionar sono cria a primeira entrada de sono local quando a introdução manual estiver disponível."
"Band sync" → "Sincronização com banda"
"Cardio Load helps gauge whether training is building, maintaining, or trending toward overtraining or detraining. It compares your short-term load from recent activity against a longer-term baseline." → "A Carga Cardio ajuda a avaliar se o treino está a progredir, a manter-se ou a tender para o excesso ou falta de treino. Compara a sua carga de curto prazo com a atividade recente em relação a uma linha de base de longo prazo."
"Cardio Load needs HR and activity data." → "A Carga Cardio necessita de dados de FC e atividade."
"Cardio Load needs activity sessions and heart-rate data." → "A Carga Cardio necessita de sessões de atividade e dados de frequência cardíaca."
"Cardio Load, %@, %@" → "Carga Cardio, %@, %@"
"End" → "Fim"
"Energy %lld | Stress %lld" → "Energia %lld | Stress %lld"
"Energy Bank needs stress, sleep, and activity data." → "O Banco de Energia necessita de dados de stress, sono e atividade."
"Energy Bank needs stress, sleep, and activity inputs." → "O Banco de Energia necessita de entradas de stress, sono e atividade."
"Goose uses historical activity and heart-rate data to calibrate Cardio Load. Consistent workout logging improves the dynamic range, and up to 6 weeks of data gives the most reliable status." → "O Goose utiliza dados históricos de atividade e frequência cardíaca para calibrar a Carga Cardio. O registo consistente de treinos melhora o intervalo dinâmico e até 6 semanas de dados fornecem o estado mais fiável."
"Heart rate unavailable" → "Frequência cardíaca indisponível"
"Latest value" → "Valor mais recente"
"Log more nights and tags to improve the confidence score." → "Registe mais noites e etiquetas para melhorar a pontuação de confiança."
"Low confidence" → "Confiança baixa"
"Metric families" → "Famílias de métricas"
"No Energy Data" → "Sem dados de energia"
"No Sleep Timeline" → "Sem linha cronológica de sono"
"No Weekly Load" → "Sem carga semanal"
"No data" → "Sem dados"
"No energy or stress data" → "Sem dados de energia ou stress"
"No weekly load data" → "Sem dados de carga semanal"
"Open Activity" → "Abrir atividade"
"Period" → "Período"
"Primary Sleep" → "Sono principal"
"Primary sleep" → "Sono principal"
"Refresh Health" → "Atualizar saúde"
"Refresh Score" → "Atualizar pontuação"
"Refresh score" → "Atualizar pontuação"
"Score drivers from the latest sleep window" → "Fatores da pontuação da última janela de sono"
"Sleep Insights" → "Perspetivas de sono"
"Sleep insights" → "Perspetivas de sono"
"Stages" → "Fases"
"Target sleep" → "Sono alvo"
"The shaded range represents your current adaptive band. Consistent activity and heart-rate data make the band more reliable over time." → "O intervalo sombreado representa a sua banda adaptativa atual. Dados consistentes de atividade e frequência cardíaca tornam a banda mais fiável ao longo do tempo."
"Today's stress" → "Stress de hoje"
"Use this as the time-in-bed target. Your actual sleep score still depends on sleep continuity, wake time, and stage balance." → "Utilize isto como objetivo de tempo na cama. A sua pontuação de sono real depende ainda da continuidade do sono, hora de despertar e equilíbrio das fases."
"Wake" → "Despertar"
"Wake time" → "Hora de despertar"
"Workout Details" → "Detalhes do treino"
"avg" → "méd."
"sleep needed" → "sono necessário"
"vs avg" → "vs méd."

GROUP C — General UI / Alarm / Device strings (~36 strings):

"Choose the vibration pattern Goose should use for this alarm profile." → "Escolha o padrão de vibração que o Goose deve utilizar para este perfil de alarme."
"Command Evidence" → "Evidência de comando"
"Connect a band to write alarms" → "Ligue uma banda para configurar alarmes"
"Connect to %@" → "Ligar a %@"
"Connecting to %@" → "A ligar a %@"
"Controls Locked" → "Controlos bloqueados"
"Data source" → "Fonte de dados"
"Disable WHOOP Alarms" → "Desativar alarmes WHOOP"
"Disable alarm on band" → "Desativar alarme na banda"
"Duration %@" → "Duração %@"
"Duration: %@" → "Duração: %@"
"Hold the pause/resume button for 5 seconds to unlock it." → "Mantenha premido o botão de pausa/retomar durante 5 segundos para desbloquear."
"Needed" → "Necessário"
"Off" → "Desligado"
"Opens date picker" → "Abre o seletor de data"
"Ready to write to band" → "Pronto para escrever na banda"
"Regular" → "Regular"
"Remove" → "Remover"
"Route" → "Rota"
"Save to band" → "Guardar na banda"
"Saved" → "Guardado"
"Set Alarm" → "Configurar alarme"
"Smart" → "Inteligente"
"Start Activity" → "Iniciar atividade"
"Stopped" → "Parado"
"Sync from band" → "Sincronizar da banda"
"Test the vibration or clear the alarm from the connected band." → "Teste a vibração ou limpe o alarme da banda ligada."

GROUP D — Strings with format specifiers that have human-readable text:

"%@ | %@-%@" — already trivial (format only, skip)
"%@, %lld dBm" → "%@, %lld dBm"  (keep same — format label, no prose to translate)
"%@RSSI %lld" → "%@RSSI %lld"  (keep same — technical label)
"%lld beats per minute" → "%lld batimentos por minuto"
"%lld dBm" → "%lld dBm"  (keep same — unit abbreviation)
"%lld load | %@" → "%lld carga | %@"
"%lld records acked" → "%lld registos confirmados"
", active" → ", ativo"
"Avg. %@" → "Méd. %@"
"Cardio Load, %@, %@" — already in GROUP B above
"Gen %@" → "Gen %@"  (technical abbreviation, keep same)
"Gen %@ · %lld dBm" → "Gen %@ · %lld dBm"  (technical, keep same)
"ZONE %lld" → "ZONA %lld"

GROUP E — Time/placeholder display strings:

"0 min" → "0 min"  (unit, keep same)
"0h" → "0h"  (unit, keep same)
"30 MIN AGO" → "HÁ 30 MIN"
"7h 39m" → "7h 39m"  (time format, keep same)
"NOW" → "AGORA"

After adding all GROUP B, C, D, E entries, run:
  python3 -m json.tool GooseSwift/Localizable.xcstrings > /dev/null && echo "JSON valid"
  </action>

  <verify>
    <automated>python3 -c "
import json, re

with open('GooseSwift/Localizable.xcstrings') as f:
    data = json.load(f)

strings = data['strings']

def is_trivial(key):
    if not key.strip():
        return True
    cleaned = re.sub(r'%[@lld0-9\$]+', '', key)
    cleaned = re.sub(r'[^a-zA-Z]', '', cleaned)
    return len(cleaned) == 0

# Brand names intentionally excluded per D-02
brand_names = {
    'Claude Sonnet 4.6', 'Claude Opus 4.8', 'Claude Haiku 4.5',
    'GPT-5.5 High', 'GPT-5.5 Low', 'GPT-5.5 Medium',
    'Gemini 2.5 Pro', 'Gemini 2.5 Flash', 'Google Client ID'
}

missing = []
for key, val in strings.items():
    locs = val.get('localizations', {})
    if 'pt-PT' not in locs and key not in brand_names and not is_trivial(key):
        missing.append(key)

if missing:
    print(f'STILL MISSING pt-PT ({len(missing)} strings):')
    for k in sorted(missing):
        print(' ', repr(k))
    exit(1)
else:
    print(f'SUCCESS: 0 non-trivial strings missing pt-PT (brand names excluded per D-02)')
"</automated>
  </verify>

  <acceptance_criteria>
    - Python scan returns "SUCCESS: 0 non-trivial strings missing pt-PT"
    - All strings in GROUP B (health/sleep/cardio), GROUP C (general UI/alarm), GROUP D (format specifiers with text), and GROUP E (time display) have pt-PT entries
    - "%lld beats per minute" → "pt-PT value is '%lld batimentos por minuto'"
    - "30 MIN AGO" → "pt-PT value is 'HÁ 30 MIN'"
    - "NOW" → "pt-PT value is 'AGORA'"
    - "ZONE %lld" → "pt-PT value is 'ZONA %lld'"
    - python3 -m json.tool GooseSwift/Localizable.xcstrings exits 0
  </acceptance_criteria>

  <done>All GROUP B/C/D/E strings translated; Python scan confirms 0 non-trivial strings missing pt-PT; JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 3: Verification gate — xcodebuild passes and Python scan confirms zero gaps (D-04, D-05, per PERF-04)</name>

  <read_first>
    - GooseSwift/Localizable.xcstrings — final state after Tasks 1 and 2
  </read_first>

  <files>GooseSwift/Localizable.xcstrings</files>

  <action>
Run the two-part verification gate:

PART 1 — Python scan (D-04):
Run the exact scan below. It must return "SUCCESS: 0 non-trivial strings missing pt-PT".
If any strings appear in the missing list that are NOT brand names from D-02 and NOT trivial,
add the missing pt-PT entries now before proceeding to PART 2.

Brand names excluded per D-02:
  Claude Sonnet 4.6, Claude Opus 4.8, Claude Haiku 4.5,
  GPT-5.5 High, GPT-5.5 Low, GPT-5.5 Medium,
  Gemini 2.5 Pro, Gemini 2.5 Flash, Google Client ID

Trivial: strings where after stripping all format specifiers (%@, %lld, %1$@, etc.) and
non-alphabetic characters, no alphabetic characters remain.

PART 2 — xcodebuild (D-04, D-05):
Run: xcodebuild -scheme GooseSwift -sdk iphonesimulator build 2>&1 | grep -E "error:|Build succeeded"

Expected: output contains "Build succeeded" and no "error:" lines from source files.
(Linker warnings and deprecation notes are acceptable; actual compilation errors are not.)

If xcodebuild fails: diagnose the error. The only file changed in this phase is
Localizable.xcstrings — if the build fails, the most likely cause is malformed JSON or a
string key that conflicts with an existing Xcode-generated key. Fix by correcting the JSON.

Do NOT modify any Swift source files in this task.
  </action>

  <verify>
    <automated>python3 -c "
import json, re

with open('GooseSwift/Localizable.xcstrings') as f:
    data = json.load(f)

strings = data['strings']

def is_trivial(key):
    if not key.strip():
        return True
    cleaned = re.sub(r'%[@lld0-9\$]+', '', key)
    cleaned = re.sub(r'[^a-zA-Z]', '', cleaned)
    return len(cleaned) == 0

brand_names = {
    'Claude Sonnet 4.6', 'Claude Opus 4.8', 'Claude Haiku 4.5',
    'GPT-5.5 High', 'GPT-5.5 Low', 'GPT-5.5 Medium',
    'Gemini 2.5 Pro', 'Gemini 2.5 Flash', 'Google Client ID'
}

missing = [k for k, v in strings.items()
           if 'pt-PT' not in v.get('localizations', {})
           and k not in brand_names
           and not is_trivial(k)]

if missing:
    print(f'FAIL: {len(missing)} strings still missing pt-PT')
    for k in sorted(missing): print(' ', repr(k))
    exit(1)
print('SCAN PASS: 0 non-trivial strings missing pt-PT')
" && xcodebuild -scheme GooseSwift -sdk iphonesimulator build 2>&1 | grep -E "error:|Build succeeded" | head -30</automated>
  </verify>

  <acceptance_criteria>
    - Python scan exits 0 and prints "SCAN PASS: 0 non-trivial strings missing pt-PT"
    - xcodebuild output contains "Build succeeded"
    - xcodebuild output contains no "error:" lines from Swift source compilation
    - The 3 startup fixes (overnight recovery background thread, defaultDatabasePath caching, skip button) are confirmed working by a clean build (D-05)
  </acceptance_criteria>

  <done>Python scan confirms 0 gaps; xcodebuild reports Build succeeded; startup fixes confirmed via build gate; Phase 19 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Localizable.xcstrings → Xcode build | File is read by Xcode at compile time; malformed JSON causes build failure (compiler error), not a security issue |
| pt-PT string values → iOS rendering | String values from the catalog are displayed in SwiftUI Text views; values are static constants, not user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | GooseSwift/Localizable.xcstrings | accept | File is a static build artifact, not runtime data; no user input flows through it; tampering requires local filesystem access (already outside app trust boundary) |
| T-19-02 | Information Disclosure | pt-PT string values | accept | Translated strings contain no secrets; all values are user-visible UI text — disclosure is intentional by definition |
| T-19-03 | Denial of Service | Malformed JSON in xcstrings | mitigate | python3 -m json.tool validation gate in Task 1 and Task 2 catches JSON errors before xcodebuild; Task 3 build gate is the final integration check |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no packages; no package manager invocations; T-19-SC is vacuous but retained for schema completeness |

No HIGH severity threats identified. This phase modifies only a static string catalog; there is no user input, no network access, no cryptographic material, and no change to the Swift runtime behaviour.
</threat_model>

<verification>
- python3 -m json.tool GooseSwift/Localizable.xcstrings exits 0 (JSON well-formed)
- Python scan: 0 non-trivial strings missing pt-PT (brand names excluded per D-02)
- xcodebuild -scheme GooseSwift -sdk iphonesimulator build → "Build succeeded", no "error:" lines
- GooseSwift/Localizable.xcstrings total pt-PT coverage: 597 existing + 128 new = 725 strings with pt-PT out of 754 total (29 trivial format-only strings intentionally left without pt-PT)
- 9 brand name strings intentionally have no pt-PT (D-02): Claude Sonnet 4.6, Claude Opus 4.8, Claude Haiku 4.5, GPT-5.5 High, GPT-5.5 Low, GPT-5.5 Medium, Gemini 2.5 Pro, Gemini 2.5 Flash, Google Client ID
</verification>

<success_criteria>
- All 128 non-trivial strings missing pt-PT are translated (D-01, L10N-03)
- App is fully Portuguese when OS is set to pt-PT — Coach settings, health UI, sleep timeline, cardio load, alarm config all render in Portuguese
- AI provider/model brand names remain English (D-02) — OS falls back to English source string
- xcodebuild passes — startup fixes from prior commits (overnight recovery background thread, defaultDatabasePath caching, skip button) confirmed working (D-05, PERF-04, UX-01)
- Zero regressions in existing pt-PT translations from Phase 14 (only adds new entries, does not modify existing ones)
</success_criteria>

## Artifacts This Phase Produces

| Symbol / Entry | File | Type |
|----------------|------|------|
| pt-PT entry: "API Key" = "Chave de API" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Anthropic API key" = "Chave de API Anthropic" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Base URL" = "URL de Base" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Bearer token (API key)" = "Token Bearer (chave de API)" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Coach Settings" = "Definições do Treinador" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Coach settings" = "Definições do treinador" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Configuration" = "Configuração" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Provider" = "Fornecedor" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Model ID" = "ID do modelo" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Save API Key" = "Guardar chave de API" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Remove API Key" = "Remover chave de API" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Sign in with ChatGPT" = "Iniciar sessão com ChatGPT" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Sign in with Google" = "Iniciar sessão com Google" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Generation stopped" = "Geração interrompida" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Add Sleep" = "Adicionar sono" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "No Sleep Timeline" = "Sem linha cronológica de sono" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Sleep Insights" = "Perspetivas de sono" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Workout Details" = "Detalhes do treino" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Stages" = "Fases" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Wake" = "Despertar" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "Open Activity" = "Abrir atividade" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "NOW" = "AGORA" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "30 MIN AGO" = "HÁ 30 MIN" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "ZONE %lld" = "ZONA %lld" | GooseSwift/Localizable.xcstrings | String catalog entry |
| pt-PT entry: "%lld beats per minute" = "%lld batimentos por minuto" | GooseSwift/Localizable.xcstrings | String catalog entry |
| (+ 103 additional entries across Groups B, C, D, E — all enumerated in task actions) | GooseSwift/Localizable.xcstrings | String catalog entries |

<output>
Create `.planning/phases/19-pt-pt-localisation-completion/19-01-SUMMARY.md` when done.
</output>
