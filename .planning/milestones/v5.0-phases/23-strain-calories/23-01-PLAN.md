---
phase: 23-strain-calories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/metrics.rs
autonomous: true
requirements: [ALG-STR-01]
must_haves:
  truths:
    - "StrainInput accepts optional profile_sex and profile_age without breaking existing callers"
    - "tanaka_hrmax(age) returns 208 - 0.7*age and differs from 220-age by >= 2 bpm for age > 40"
    - "estimate_hrmax_from_history returns the 99.5th percentile when >= 600 samples exist, else None"
  artifacts:
    - path: "Rust/core/src/metrics.rs"
      provides: "profile_sex/profile_age fields on StrainInput; tanaka_hrmax; estimate_hrmax_from_history; effective_hrmax resolver with hrmax_source"
      contains: "fn tanaka_hrmax"
  key_links:
    - from: "estimate_hrmax_from_history"
      to: "StrainInput"
      via: "effective HRmax resolution selecting observed/tanaka/fallback"
      pattern: "tanaka_hrmax|estimate_hrmax_from_history"
---

<objective>
Add personal-profile inputs and a physiologically-grounded HRmax to the strain pipeline (ALG-STR-01). Make `profile_sex` and `profile_age` available on `StrainInput`, implement the Tanaka HRmax formula and a history-percentile HRmax estimator, and resolve an effective HRmax with a tracked source label.

Purpose: WHOOP strain is computed against a personalized HRmax. The fork currently has no sex/age inputs and no Tanaka formula. This plan establishes the additive input surface and HRmax functions consumed by the Banister TRIMP work in 23-02.
Output: Extended `StrainInput`, `tanaka_hrmax`, `estimate_hrmax_from_history`, and an effective-HRmax resolver with `hrmax_source` ∈ {observed, tanaka, fallback}, all unit-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-strain-calories/23-CONTEXT.md

# Existing strain code — read before editing
# StrainInput struct (~line 397), StrainScoreOutput (~line 410)
# goose_strain_v0 + zone logic (~line 1777), GOOSE_STRAIN_V0_ID (line 17)
@Rust/core/src/metrics.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive profile fields on StrainInput + Tanaka HRmax + history estimator</name>
  <files>Rust/core/src/metrics.rs</files>
  <read_first>
    - `Rust/core/src/metrics.rs` StrainInput struct at ~line 397 — note existing `#[serde(default)]` on `input_ids` as the additive-field pattern to mirror
    - `goose_strain_v0` at ~line 1777 — note `clamp_fraction`, `clamp_0_to` helpers and HR-reserve math already present
  </read_first>
  <behavior>
    - Test: deserializing a StrainInput JSON that omits `profile_sex` and `profile_age` succeeds (serde default → None), proving additive/non-breaking (per ALG-STR-01).
    - Test: `tanaka_hrmax(50.0)` == 208.0 - 0.7*50.0 == 173.0 (exact f64).
    - Test: for age 41..=80, `(tanaka_hrmax(age) - (220.0 - age)).abs() >= 2.0` holds (Tanaka differs from 220-age by >= 2 bpm for age > 40).
    - Test: `estimate_hrmax_from_history` with 599 samples returns None; with exactly 600 samples returns Some(value); value equals element at index `ceil(0.995 * len)` after ascending sort.
    - Test: `estimate_hrmax_from_history` ignores non-finite samples / returns None when fewer than 600 finite samples remain.
  </behavior>
  <action>
    Add `profile_sex: Option<String>` and `profile_age: Option<f64>` to `StrainInput`, each annotated `#[serde(default)]`, mirroring the existing `input_ids` additive pattern (per ALG-STR-01) — do NOT reorder or rename existing fields. Implement `pub fn tanaka_hrmax(age: f64) -> f64` returning `208.0 - 0.7 * age` with the exact constants (no rounding). Implement `pub fn estimate_hrmax_from_history(hr_history: &[f64]) -> Option<f64>`: filter to finite values, return None if fewer than 600 remain, otherwise sort ascending and return the element at index `((0.995 * len as f64).ceil() as usize).min(len - 1)` (p99.5 per 23-CONTEXT specifics). Add unit tests in the metrics test module covering every case in <behavior>. Do not modify `goose_strain_v0` in this task.
  </action>
  <verify>
    <automated>cargo test -p goose-core tanaka_hrmax estimate_hrmax_from_history strain_input -- --nocapture</automated>
  </verify>
  <done>tanaka_hrmax and estimate_hrmax_from_history exist with passing tests; StrainInput round-trips JSON with the two new fields absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Effective-HRmax resolver with hrmax_source label</name>
  <files>Rust/core/src/metrics.rs</files>
  <read_first>
    - The two functions added in Task 1 (`tanaka_hrmax`, `estimate_hrmax_from_history`)
    - `goose_strain_v0` at ~line 1777 — `input.max_hr_bpm` is the existing session-observed max HR
  </read_first>
  <behavior>
    - Test: when a history slice with >= 600 samples is supplied, resolver returns the p99.5 value and source `observed`.
    - Test: when history is insufficient (< 600) but `profile_age` is Some, resolver returns `max(session_max_hr, tanaka_hrmax(age))` and source `tanaka`.
    - Test: when history insufficient and `profile_age` is None, resolver returns `220 - 0` fallback path using session data and source `fallback` (220-age requires age; with no age, fall back to session_max_hr and label `fallback`).
    - Test: hrmax_source is exactly one of "observed", "tanaka", "fallback".
  </behavior>
  <action>
    Implement `pub fn resolve_effective_hrmax(session_max_hr: f64, profile_age: Option<f64>, hr_history: &[f64]) -> (f64, String)` returning the effective HRmax and its source label (per ALG-STR-01 success criterion: `hrmax_source` ∈ {observed, tanaka, fallback}). Resolution order: (1) if `estimate_hrmax_from_history` returns Some → (value, "observed"); (2) else if `profile_age` is Some → (`session_max_hr.max(tanaka_hrmax(age))`, "tanaka") per 23-CONTEXT ("use max(session_max_hr, tanaka_hrmax(age))"); (3) else → (`session_max_hr`, "fallback"). Add unit tests covering each branch and the label invariant. Do not wire this into a bridge method yet — 23-02 introduces goose_strain_v1 which consumes it.
  </action>
  <verify>
    <automated>cargo test -p goose-core resolve_effective_hrmax -- --nocapture</automated>
  </verify>
  <done>resolve_effective_hrmax returns correct value + source across all three branches; cargo test -p goose-core green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust FFI (JSON) | profile_sex/profile_age arrive as untrusted JSON; serde defaults guard absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | StrainInput JSON deserialization | mitigate | `#[serde(default)]` on new optional fields; non-finite history samples filtered in estimate_hrmax_from_history |
| T-23-02 | Denial of Service | estimate_hrmax_from_history percentile index | mitigate | index clamped with `.min(len - 1)`; empty/short slices return None before indexing |
</threat_model>

<verification>
- `cargo test -p goose-core` is green.
- Existing `goose_strain_v0` tests still pass unchanged (additive proof).
</verification>

<success_criteria>
- StrainInput carries optional profile_sex and profile_age (serde default).
- tanaka_hrmax(age) = 208 - 0.7*age; differs from 220-age by >= 2 bpm for age > 40 (tested).
- estimate_hrmax_from_history returns p99.5 only when >= 600 finite samples.
- resolve_effective_hrmax yields hrmax_source ∈ {observed, tanaka, fallback}.
</success_criteria>

<artifacts_this_phase_produces>
- `Rust/core/src/metrics.rs`: `StrainInput.profile_sex`, `StrainInput.profile_age`, `tanaka_hrmax`, `estimate_hrmax_from_history`, `resolve_effective_hrmax` (+ tests). Consumed by 23-02.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/23-strain-calories/23-01-SUMMARY.md` when done.
</output>
