---
phase: 23-strain-calories
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - Rust/core/src/energy_rollup.rs
  - Rust/core/src/bridge.rs
autonomous: true
requirements: [ALG-CAL-01, ALG-CAL-02]
must_haves:
  truths:
    - "rmr_mifflin_st_jeor replaces the weight_kg*22.0 resting proxy when height and age are available"
    - "A quality flag is emitted when profile_height_cm is absent and the proxy is used"
    - "Keytel and Harris-Benedict coefficients match the Ghidra-confirmed values exactly"
    - "Active EE uses Keytel above the 30% HRR threshold; resting EE uses Harris-Benedict otherwise"
  artifacts:
    - path: "Rust/core/src/energy_rollup.rs"
      provides: "profile_height_cm on EnergyDailyRollupOptions; rmr_mifflin_st_jeor; keytel_active_kcal_per_min; H-B coefficients; rollup wiring + quality flag"
      contains: "fn rmr_mifflin_st_jeor"
    - path: "Rust/core/src/bridge.rs"
      provides: "profile_height_cm plumbed through EnergyDailyRollupArgs into all EnergyDailyRollupOptions constructions"
      contains: "profile_height_cm"
  key_links:
    - from: "Rust/core/src/bridge.rs"
      to: "EnergyDailyRollupOptions"
      via: "profile_height_cm arg → options field at all 4 construction sites"
      pattern: "profile_height_cm"
    - from: "resting_kcal / active_kcal"
      to: "rmr_mifflin_st_jeor / keytel_active_kcal_per_min"
      via: "30% HRR threshold split"
      pattern: "rmr_mifflin_st_jeor|keytel_active_kcal_per_min"
---

<objective>
Replace the crude calorie proxies with Ghidra-confirmed formulas (ALG-CAL-01, ALG-CAL-02): Mifflin-St Jeor RMR for resting energy, and Keytel (active) + Harris-Benedict (resting) coefficients matching the WHOOP binary byte-for-byte, split on the 30% HRR threshold.

Purpose: The fork's resting EE is `weight_kg * 22.0` and active EE is a MET model — neither matches WHOOP. This plan installs the exact reverse-engineered coefficients and the personalized RMR, gated on profile height/age with explicit quality flags when inputs are missing.
Output: `rmr_mifflin_st_jeor`, `keytel_active_kcal_per_min`, Harris-Benedict coefficients, `profile_height_cm` on options + bridge args, and rollup wiring with quality flags. NOTE: `profile_age_years` already exists on `EnergyDailyRollupOptions` — reuse it; only `profile_height_cm` is new.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-strain-calories/23-CONTEXT.md

# Energy code:
#   - EnergyDailyRollupOptions (~line 36, already has profile_age_years + profile_sex)
#   - quality_flags assembly (~line 357-414)
#   - rollup call site for resting_kcal/active_kcal (~line 416-437)
#   - resting_kcal (~line 1159, weight_kg*22.0 proxy), active_kcal (~line 1164, MET model)
@Rust/core/src/energy_rollup.rs

# Bridge plumbing:
#   - EnergyDailyRollupArgs struct (~line 727, has profile_weight_kg/profile_age_years/profile_sex)
#   - energy_daily_rollup_bridge (~line 4414) with 4 EnergyDailyRollupOptions constructions (~4419, 4451, 4490, 4514)
@Rust/core/src/bridge.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mifflin RMR + Keytel + Harris-Benedict functions with exact coefficients</name>
  <files>Rust/core/src/energy_rollup.rs</files>
  <read_first>
    - `resting_kcal` (~line 1159) and `active_kcal` (~line 1164) — current proxy and MET model
    - 23-CONTEXT.md <decisions> ALG-CAL-01/ALG-CAL-02 for the exact coefficient table
  </read_first>
  <behavior>
    - Test: `rmr_mifflin_st_jeor(weight, height, age, sex)` male = 10*w + 6.25*h - 5*a + 5; female = ... - 161; unknown = ... - 78; assert exact f64 for a known input.
    - Test: `keytel_active_kcal_per_min` male coefficients exactly (-55.0969, 0.6309, 0.1988, 0.2017) and female (-20.4022, 0.4472, -0.1263, 0.0740); divisor 251.04; result clamped >= 0; HR capped at hrmax.
    - Test: Harris-Benedict male (88.362, 13.397, 479.9, -5.677) with height as height_cm/100; female (447.593, 9.247, 309.8, -4.330); assert exact coefficient-driven output for a known input.
    - Test: Keytel EE never negative for low HR (clamp >= 0).
  </behavior>
  <action>
    Implement `pub fn rmr_mifflin_st_jeor(weight_kg: f64, height_cm: f64, age: f64, sex: Option<&str>) -> f64` with intercepts +5 (male) / -161 (female) / -78 (unknown mean) per ALG-CAL-01, exact f64 constants. Implement `pub fn keytel_active_kcal_per_min(hr: f64, weight_kg: f64, age: f64, sex: Option<&str>, hrmax: f64) -> f64` per ALG-CAL-02: cap `hr` at `hrmax`, male = `-55.0969 + 0.6309*hr + 0.1988*weight_kg + 0.2017*age`, female = `-20.4022 + 0.4472*hr - 0.1263*weight_kg + 0.0740*age` (unknown = mean of the two), divide by 251.04 (VO2→kcal/min, Ghidra-confirmed), clamp result `>= 0.0`. Implement `pub fn harris_benedict_rmr_kcal_day(weight_kg: f64, height_cm: f64, age: f64, sex: Option<&str>) -> f64` per ALG-CAL-02: male = `88.362 + 13.397*weight_kg + 479.9*(height_cm/100.0) - 5.677*age`, female = `447.593 + 9.247*weight_kg + 309.8*(height_cm/100.0) - 4.330*age` (unknown = mean). Use literal f64 constants verbatim — NO rounding. Add unit tests asserting the exact coefficient-driven values per <behavior>.
  </action>
  <verify>
    <automated>cargo test -p goose-core mifflin keytel harris_benedict -- --nocapture</automated>
  </verify>
  <done>All three functions exist with exact Ghidra coefficients; tests assert exact values and clamping; cargo test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add profile_height_cm + wire Mifflin/Keytel into rollup with quality flags</name>
  <files>Rust/core/src/energy_rollup.rs, Rust/core/src/bridge.rs</files>
  <read_first>
    - quality_flags assembly (~line 357-414) and the `profile_age_missing`/`profile_sex_missing` flag pattern
    - rollup call site (~line 416-437) where `resting_kcal` and `active_kcal` are invoked
    - bridge `EnergyDailyRollupArgs` (~line 727) and the 4 `EnergyDailyRollupOptions` constructions in `energy_daily_rollup_bridge` (~4419, 4451, 4490, 4514)
  </read_first>
  <behavior>
    - Test: rollup with profile_height_cm + profile_age + sex present uses Mifflin RMR for resting (resting_kcal differs from weight*22.0/1440*minutes) and does NOT emit the height-absent flag.
    - Test: rollup with profile_height_cm absent falls back to the weight*22.0 proxy and emits the `resting_kcal_mifflin_height_absent` quality flag.
    - Test: active EE above the 30% HRR threshold uses Keytel; below threshold contributes Harris-Benedict resting path (split per ALG-CAL-02).
    - Test: bridge `metrics.energy_daily_rollup` request carrying `profile_height_cm` plumbs the value through (a rollup with vs without height yields different resting_kcal).
  </behavior>
  <action>
    Add `pub profile_height_cm: Option<f64>` to `EnergyDailyRollupOptions` (additive; reuse existing `profile_age_years` for age — do NOT add a duplicate age field) and to its `Default` impl. In the quality_flags block (~357-414) push `resting_kcal_mifflin_height_absent` when `profile_height_cm.is_none()` (mirroring existing `profile_age_missing` style). At the rollup call site (~416-437): when `profile_height_cm` AND `profile_age_years` are present, compute resting via `rmr_mifflin_st_jeor(effective_weight_kg, height_cm, age, options.profile_sex)` scaled to the covered window (`rmr_per_day * covered_minutes / 1440.0`); otherwise keep the existing `resting_kcal` proxy. For active EE, apply the 30% HRR split (per ALG-CAL-02): when `average_hr >= resting_hr + 0.30*(hrmax - resting_hr)` and age+hr available, use `keytel_active_kcal_per_min` accumulated over covered minutes; otherwise retain the existing MET-based active path (full Keytel rollup integration through the session pipeline is a 23-CONTEXT deferred item — do NOT attempt it). In `bridge.rs`: add `profile_height_cm: Option<f64>` (`#[serde(default)]`) to `EnergyDailyRollupArgs` and set `profile_height_cm: args.profile_height_cm` in ALL FOUR `EnergyDailyRollupOptions` constructions in `energy_daily_rollup_bridge`. Add tests per <behavior> (rollup-level + bridge round-trip).
  </action>
  <verify>
    <automated>cargo test -p goose-core energy_daily_rollup resting_kcal mifflin_height_absent -- --nocapture</automated>
  </verify>
  <done>profile_height_cm flows from bridge args into all 4 option sites; Mifflin RMR replaces the proxy when height+age present; height-absent flag emitted on fallback; 30% HRR Keytel split active; cargo test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust FFI (JSON) | profile_height_cm and profile data arrive as untrusted JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering | EnergyDailyRollupArgs.profile_height_cm | mitigate | `#[serde(default)]` Option; rollup falls back to proxy + quality flag when absent/invalid |
| T-23-06 | Information Disclosure | calorie outputs from coefficients | accept | derived metrics only; no PII beyond profile values already accepted by existing rollup args |
</threat_model>

<verification>
- `cargo test -p goose-core` green.
- Coefficient values asserted byte-equivalent to 23-CONTEXT table (no rounding).
- Existing energy rollup tests still pass (additive field, proxy fallback intact).
</verification>

<success_criteria>
- profile_height_cm added to EnergyDailyRollupOptions + bridge args (all 4 sites wired).
- rmr_mifflin_st_jeor replaces weight*22.0 proxy when height+age available; height-absent quality flag on fallback.
- Keytel + Harris-Benedict coefficients exact (Ghidra-confirmed); Keytel clamped >= 0, HR capped at hrmax.
- Active/resting EE split on 30% HRR threshold.
</success_criteria>

<artifacts_this_phase_produces>
- `Rust/core/src/energy_rollup.rs`: `rmr_mifflin_st_jeor`, `keytel_active_kcal_per_min`, `harris_benedict_rmr_kcal_day`, `EnergyDailyRollupOptions.profile_height_cm`, rollup wiring + `resting_kcal_mifflin_height_absent` flag.
- `Rust/core/src/bridge.rs`: `EnergyDailyRollupArgs.profile_height_cm` plumbed into all 4 option constructions.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/23-strain-calories/23-03-SUMMARY.md` when done.
</output>
