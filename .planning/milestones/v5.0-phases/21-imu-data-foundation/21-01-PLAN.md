---
phase: 21-imu-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/protocol.rs
  - Rust/core/tests/protocol_tests.rs
autonomous: true
requirements: [IMU-01]
must_haves:
  truths:
    - "I16SeriesSummary carries a full_samples: Option<Vec<i16>> field"
    - "summarize_i16_series populates full_samples with all parsed_count values (not capped at 8)"
    - "The preview field remains capped at 8 values and unchanged in behaviour"
    - "All existing K10/K21/R17 protocol tests pass (full-struct assert updated to include the new field)"
  artifacts:
    - path: "Rust/core/src/protocol.rs"
      provides: "I16SeriesSummary.full_samples field + population in summarize_i16_series"
      contains: "full_samples"
    - path: "Rust/core/tests/protocol_tests.rs"
      provides: "Test asserting full_samples preserves all 100 K10 samples"
      contains: "full_samples"
  key_links:
    - from: "summarize_i16_series"
      to: "I16SeriesSummary.full_samples"
      via: "field assignment of the full parsed Vec<i16>"
      pattern: "full_samples"
---

<objective>
Add a `full_samples: Option<Vec<i16>>` field to `I16SeriesSummary` in `protocol.rs` and populate it inside `summarize_i16_series` with the complete set of parsed samples, while keeping the existing `preview` field (capped at 8) unchanged. This is the data-availability foundation: K10/K21 motion axes hold 100 samples per axis, but only the first 8 are currently retained. Sleep staging and the gravity table (later plans) need the full series.

Purpose: Stop discarding 92 of every 100 IMU samples at the parse layer (IMU-01).
Output: `I16SeriesSummary` with an additive `full_samples` field; tests proving all 100 K10 samples survive parsing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-imu-data-foundation/21-CONTEXT.md

@Rust/core/src/protocol.rs
@Rust/core/tests/protocol_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add full_samples field to I16SeriesSummary and populate it</name>
  <files>Rust/core/src/protocol.rs</files>
  <read_first>
    - Rust/core/src/protocol.rs:168-178 — `I16SeriesSummary` struct definition (current fields: name, offset, expected_count, parsed_count, min, max, sum, preview)
    - Rust/core/src/protocol.rs:648-705 — `summarize_i16_series` function (two construction sites: the `expected_count == 0` early return at 656, and the main return at 693; the `preview` cap-at-8 loop is at 680-689)
    - Rust/core/src/protocol.rs:586-646 — `parse_k10_raw_motion_summary` / `parse_k21_raw_motion_summary` (callers; 100 samples per K10 axis)
  </read_first>
  <behavior>
    - Test 1: For a 100-sample K10 axis, full_samples contains exactly 100 values matching the raw i16 sequence; preview still contains only the first 8.
    - Test 2: For the `expected_count == 0` early-return path, full_samples is Some(empty Vec) (an empty, non-truncated series), preview is empty.
    - Test 3: For a truncated axis (parsed_count < expected_count), full_samples length equals parsed_count, not expected_count.
  </behavior>
  <action>
    Add a public field `pub full_samples: Option<Vec<i16>>` to the `I16SeriesSummary` struct (after `preview` at protocol.rs:177). Keep all derives unchanged (`#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]` — `Vec<i16>` and `Option` satisfy Eq).

    In `summarize_i16_series`:
    - At the `expected_count == 0` early-return construction (line 656), set `full_samples: Some(Vec::new())`.
    - In the main parse loop (680-689), build the complete series. Push every `value` into a `full` Vec (do NOT cap it); keep the existing `preview` push guarded by `if preview.len() < 8`. At the final construction (693), set `full_samples: Some(full)`.

    Do NOT change the `preview` field semantics, the cap of 8, the min/max/sum computation, or the truncation warning logic. The field is purely additive.

    Per 21-CONTEXT.md Claude's Discretion: this is additive — `preview` unchanged, no behavioural change to existing min/max/sum/warnings.
  </action>
  <verify>
    <automated>cd Rust/core && cargo build -p goose-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub full_samples: Option<Vec<i16>>" Rust/core/src/protocol.rs` returns one match in the struct.
    - `grep -c "full_samples:" Rust/core/src/protocol.rs` returns at least 3 (struct decl + two construction sites).
    - `cd Rust/core && cargo build -p goose-core` succeeds with no errors.
  </acceptance_criteria>
  <done>I16SeriesSummary has full_samples; summarize_i16_series populates it at both construction sites; the crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update existing full-struct assertion and add full_samples preservation tests</name>
  <files>Rust/core/tests/protocol_tests.rs</files>
  <read_first>
    - Rust/core/tests/protocol_tests.rs:260-285 — the R17 test uses a FULL `assert_eq!` against a complete `I16SeriesSummary { ... }` struct literal (line 269-278). Adding a field makes this literal incomplete; it MUST gain `full_samples: ...` or the test will not compile.
    - Rust/core/tests/protocol_tests.rs:319-365 — `parses_k10_raw_motion_offsets_without_claiming_units` (field-by-field assertions; unaffected by the additive field, but is the place to add the 100-sample preservation assertion)
    - Rust/core/tests/protocol_tests.rs:367-473 — K21 and truncation tests (field-by-field; unaffected, but truncation test is where to assert full_samples length == parsed_count)
  </read_first>
  <behavior>
    - The R17 full-struct `assert_eq!` compiles after adding the `full_samples` field to its literal with the correct expected value.
    - A new assertion in the K10 test confirms `axes[0].full_samples` is `Some` with length 100 and that its first three values match the injected samples (1, -2, 3).
    - A new assertion in the truncation test confirms `axes[0].full_samples.unwrap().len() == axes[0].parsed_count` (76, not 100).
  </behavior>
  <action>
    1. In the R17 test (line 269-278), add `full_samples: Some(vec![1000, -1000, 200]),` to the `I16SeriesSummary { ... }` literal so the full-struct `assert_eq!` still matches. The R17 payload at protocol_tests.rs:289-294 parses 3 samples, so full_samples == preview here.

    2. In `parses_k10_raw_motion_offsets_without_claiming_units` (319-365), after the existing `axes[0]` assertions, add:
       - assert `axes[0].full_samples.as_ref().unwrap().len() == 100`
       - assert the first three values equal the injected sequence at offsets 85/87/89 (put_i16 values 1, -2, 3 → `axes[0].full_samples.as_ref().unwrap()[0..3] == [1, -2, 3]`)
       - assert `axes[0].preview.len() == 8` (preview still capped) to lock the preview/full distinction.

    3. In `truncated_long_motion_frame_keeps_partial_samples_with_quality_warnings` (419-473), after the existing `axes[0].parsed_count == 76` assertion, add `assert_eq!(axes[0].full_samples.as_ref().unwrap().len(), 76);`.

    Do not modify any other assertions. The K10/K21 field-by-field tests already pass with the additive field; only the R17 full-struct literal needs the new key.
  </action>
  <verify>
    <automated>cd Rust/core && cargo test -p goose-core 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "full_samples" Rust/core/tests/protocol_tests.rs` returns at least 4 (R17 literal + 3 new assertions).
    - `cd Rust/core && cargo test -p goose-core` is green (0 failures).
    - The K10 test asserts a full_samples length of 100; the truncation test asserts length 76.
  </acceptance_criteria>
  <done>cargo test -p goose-core green; full_samples preservation of all 100 K10 values and the parsed_count==len invariant are both covered.</done>
</task>

</tasks>

<verification>
- `cd Rust/core && cargo build -p goose-core` succeeds.
- `cd Rust/core && cargo test -p goose-core` green.
- `full_samples` is additive: no existing assertion's expected value changed except the R17 full-struct literal gaining the new key.
</verification>

<success_criteria>
1. `I16SeriesSummary.full_samples: Option<Vec<i16>>` exists and is populated in `summarize_i16_series`.
2. `preview` behaviour unchanged (still capped at 8).
3. Tests prove 100 K10 samples preserved and full_samples length tracks parsed_count under truncation.
4. `cargo test -p goose-core` green.
</success_criteria>

<output>
Create `.planning/phases/21-imu-data-foundation/21-01-SUMMARY.md` when done.
</output>
