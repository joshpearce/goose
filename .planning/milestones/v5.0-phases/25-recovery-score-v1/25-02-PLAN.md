---
phase: 25-recovery-score-v1
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - GooseSwift/HealthDataStore+Recovery.swift
  - GooseSwift/HealthRecoveryStressViews.swift
  - GooseSwift.xcodeproj/project.pbxproj
autonomous: false
requirements: [ALG-REC-03]

must_haves:
  truths:
    - "HealthDataStore calls metrics.goose_recovery_v1 through the Rust bridge and publishes the result"
    - "RecoveryV2OverviewPage shows an 'A calibrar' state when trust_level is calibrating"
    - "The recovery hero/header reflects the colour band (verde=green, amarelo=orange, vermelho=red)"
    - "When trust is provisional a 'Provisório' label is shown; when trusted no trust label is shown"
  artifacts:
    - path: "GooseSwift/HealthDataStore+Recovery.swift"
      provides: "recoveryV1Result publishing + goose_recovery_v1 bridge call"
      contains: "metrics.goose_recovery_v1"
    - path: "GooseSwift/HealthRecoveryStressViews.swift"
      provides: "A calibrar state + colour band indicator in RecoveryV2OverviewPage"
      contains: "A calibrar"
  key_links:
    - from: "GooseSwift/HealthDataStore+Recovery.swift"
      to: "metrics.goose_recovery_v1"
      via: "bridge.request"
      pattern: "metrics.goose_recovery_v1"
    - from: "GooseSwift/HealthRecoveryStressViews.swift RecoveryV2OverviewPage"
      to: "store.recoveryV1Result"
      via: "@Published consumption"
      pattern: "recoveryV1Result"
---

<objective>
Surface the Phase 25 personal-baseline Recovery score in the iOS UI. Add a new
`HealthDataStore+Recovery.swift` extension that calls `metrics.goose_recovery_v1` over the
Rust bridge and publishes a `recoveryV1Result`, then update `RecoveryV2OverviewPage` to show
an "A calibrar" state while the baseline is calibrating and to reflect the colour band.

Purpose: ALG-REC-03 — wire the v1 recovery output into the dashboard with trust-aware states.
Output: New Swift extension file (registered in the Xcode project) + dashboard view changes;
verified visually in the simulator (UI checkpoint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-recovery-score-v1/25-CONTEXT.md
@.planning/phases/25-recovery-score-v1/25-01-SUMMARY.md

# Bridge call pattern: bridge.request(method:args:) with "database_path": databasePath.
@GooseSwift/HealthDataStore+Cardio.swift
# HealthDataStore owns `bridge`, `databasePath` (defaultDatabasePath), and @Published state.
@GooseSwift/HealthDataStore.swift
# Dashboard view to update — RecoveryV2OverviewPage (recoveryScore ~line 186,
#   hero ~line 45, onAppear runPacketScores ~line 170).
@GooseSwift/HealthRecoveryStressViews.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: HealthDataStore+Recovery.swift — recoveryV1Result + bridge call</name>
  <files>GooseSwift/HealthDataStore+Recovery.swift, GooseSwift.xcodeproj/project.pbxproj</files>
  <action>
    Create `GooseSwift/HealthDataStore+Recovery.swift` (imports Foundation; 2-space indent,
    K&R braces, per project conventions). Define a value type
    `struct RecoveryV1Result { let score: Int?; let trustLevel: String; let colourBand: String;
    let zHRV: Double?; let zRHR: Double? }` and a SwiftUI colour mapping helper
    `var bandColor: Color` (verde → .green, amarelo → .orange, vermelho → .red) — place the
    Color extension in this file (import SwiftUI for it). Add
    `@Published var recoveryV1Result: RecoveryV1Result?` to HealthDataStore via this extension
    (per 25-CONTEXT Claude's Discretion). Implement
    `func runRecoveryV1(deviceID: String, dateKey: String, hrvRmssdMs: Double, restingHrBpm: Double)`
    that calls `bridge.request(method: "metrics.goose_recovery_v1", args: ["database_path":
    databasePath, "device_id": deviceID, "date_key": dateKey, "hrv_rmssd_ms": hrvRmssdMs,
    "resting_hr_bpm": restingHrBpm])` (mirror HealthDataStore+Cardio.swift line 97), parses
    `score_0_to_100` (may be null → nil; round to Int), `trust_level`, `colour_band`, `z_hrv`,
    `z_rhr` from the response dict, and assigns `recoveryV1Result` on the main actor. On a
    thrown bridge error, set `recoveryV1Result = nil` and do not crash. Add a computed
    `var recoveryV1IsCalibrating: Bool { recoveryV1Result?.trustLevel == "calibrating" }` and
    `var recoveryV1TrustLabel: String?` returning "A calibrar" for calibrating, "Provisório"
    for provisional, nil for trusted (per 25-CONTEXT specifics — pt-PT labels). Register the
    new file in `GooseSwift.xcodeproj/project.pbxproj` (PBXBuildFile + PBXFileReference +
    Sources build phase + group membership) following the exact pattern used for sibling
    HealthDataStore+*.swift entries — search for "HealthDataStore+Cardio.swift" and replicate
    all four references with fresh 24-hex object IDs.
  </action>
  <verify>
    <automated>grep -q "metrics.goose_recovery_v1" GooseSwift/HealthDataStore+Recovery.swift &amp;&amp; grep -c "HealthDataStore+Recovery.swift" GooseSwift.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>HealthDataStore+Recovery.swift exists with recoveryV1Result, runRecoveryV1 (calls metrics.goose_recovery_v1), trust label + band colour helpers; file referenced ≥3 times in project.pbxproj (build file, file ref, sources phase).</done>
</task>

<task type="auto">
  <name>Task 2: RecoveryV2OverviewPage — "A calibrar" state + colour band indicator</name>
  <files>GooseSwift/HealthRecoveryStressViews.swift</files>
  <action>
    Update `RecoveryV2OverviewPage` to consume `store.recoveryV1Result`. In the hero region
    (~line 45), when `store.recoveryV1IsCalibrating` is true, render an "A calibrar" treatment:
    replace the numeric score with the "A calibrar" label (and, if present, show the
    `recoveryV1TrustLabel`) so no fabricated score is shown while score is nil. When not
    calibrating, drive the hero/gauge accent and a small band indicator from
    `store.recoveryV1Result?.bandColor` (verde/amarelo/vermelho). Add an `.onAppear` /
    `.onChange(of: model.packetImportRevision)` call to `store.runRecoveryV1(...)` alongside
    the existing `store.runPacketScores()` (~line 170), sourcing deviceID/dateKey/hrv/rhr from
    the existing recovery snapshot accessors already used on this page (recoveryHRVDisplayText
    / recoveryRestingHRDisplayText source values) — if a clean numeric source is not directly
    available, add a private helper that reads the same snapshot the page already uses; do NOT
    hardcode placeholder metrics. Keep the existing recoveryScore fallback path intact for
    dates other than today. Strings "A calibrar" and "Provisório" are pt-PT literals consistent
    with the localisation approach; do not introduce English placeholders.
  </action>
  <verify>
    <automated>grep -q "A calibrar" GooseSwift/HealthRecoveryStressViews.swift &amp;&amp; grep -q "recoveryV1" GooseSwift/HealthRecoveryStressViews.swift</automated>
  </verify>
  <done>RecoveryV2OverviewPage shows "A calibrar" when calibrating, reflects band colour otherwise, and triggers runRecoveryV1 on appear/import; references store.recoveryV1Result.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>HealthDataStore+Recovery.swift wired to metrics.goose_recovery_v1, and RecoveryV2OverviewPage updated with an "A calibrar" calibrating state and colour-band-driven hero.</what-built>
  <how-to-verify>
    1. Build and run the app on the iOS simulator (XcodeBuildMCP build_run_sim, scheme GooseSwift).
    2. Open the Health tab → Recovery (Recovery V2 page).
    3. With no/low baseline history (< 4 nights), confirm the hero shows "A calibrar" instead of a fabricated numeric score.
    4. If you have ≥ 4 nights of recovery metrics, confirm a numeric score appears and the hero/band indicator colour matches the band: Verde (green) ≥ 67, Amarelo (orange) 34–66, Vermelho (red) < 34.
    5. Confirm the trust label reads "Provisório" for 4–13 nights and no trust label for ≥ 14 nights.
    6. Confirm no console crash and no "Publishing changes from background threads" warning.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. wrong band colour, calibrating state not showing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift UI→bridge | recovery metric inputs cross into Rust over the JSON FFI |
| bridge→main actor | bridge runs synchronously; result must be published on @MainActor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Denial of Service | synchronous bridge call on main actor | mitigate | Follow project rule: dispatch goose_recovery_v1 bridge call off @MainActor, publish recoveryV1Result back via Task { @MainActor } (mirrors existing HealthDataStore pattern) |
| T-25-04 | Tampering | nil score handling in UI | mitigate | When score is null (calibrating) UI shows "A calibrar", never a fabricated 0 or population number |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No external dependencies — Swift/SwiftUI/URLSession only (per CLAUDE.md iOS constraint) |
</threat_model>

<verification>
- App builds and runs on simulator.
- Calibrating state ("A calibrar") shown for < 4 nights; numeric score + correct band colour for ≥ 4 nights.
- Trust label "Provisório" for provisional, none for trusted.
- No background-thread publishing warning, no crash.
</verification>

<success_criteria>
- ALG-REC-03: HealthDataStore+Recovery.swift calls metrics.goose_recovery_v1; RecoveryV2OverviewPage shows "A calibrar" state and trust level + colour band indicator.
- Human verification of the dashboard states passed.
</success_criteria>

<output>
Create `.planning/phases/25-recovery-score-v1/25-02-SUMMARY.md` when done.
</output>
