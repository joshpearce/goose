---
phase: 61-ble-bonding-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseBLETypes.swift
  - GooseSwift/GooseBLEBondingManager.swift
  - GooseSwift/LocalizedStatusStrings.swift
autonomous: true
requirements: [BLE-BOND-01]

must_haves:
  truths:
    - "A GooseBLEBondingState enum with 5 cases exists and compiles"
    - "A GooseBLEBondingManager class exists that persists state to UserDefaults and exposes a state-change callback"
    - "The bonding state has a localized display string following the existing LocalizedStatusStrings pattern"
  artifacts:
    - path: "GooseSwift/GooseBLEBondingManager.swift"
      provides: "GooseBLEBondingManager final class with transition(to:) + UserDefaults persistence"
      min_lines: 40
      contains: "final class GooseBLEBondingManager"
    - path: "GooseSwift/GooseBLETypes.swift"
      provides: "GooseBLEBondingState enum"
      contains: "enum GooseBLEBondingState"
    - path: "GooseSwift/LocalizedStatusStrings.swift"
      provides: "localizedDescription on GooseBLEBondingState"
      contains: "extension GooseBLEBondingState"
  key_links:
    - from: "GooseSwift/GooseBLEBondingManager.swift"
      to: "GooseBLEBondingState"
      via: "bondingState property + transition(to:)"
      pattern: "bondingState: GooseBLEBondingState"
    - from: "GooseSwift/GooseBLEBondingManager.swift"
      to: "UserDefaults"
      via: "persistState / loadPersistedState"
      pattern: "UserDefaults.standard"
---

<objective>
Create the foundation types for the formal BLE bonding state machine (BLE-BOND-01): the `GooseBLEBondingState` enum (5 WHOOP-equivalent states), the `GooseBLEBondingManager` class that owns the state with UserDefaults persistence and a change callback, and the localized display string. No integration with `GooseBLEClient` yet — this plan delivers self-contained, independently-compiling types.

Purpose: Establish the typed, testable bonding lifecycle that Wave 2 wires into the existing BLE delegate flow without breaking the string-based `connectionState` API surface (33 comparison sites across 13 files).
Output: `GooseBLEBondingManager.swift` (new), `GooseBLEBondingState` enum in `GooseBLETypes.swift`, `localizedDescription` extension in `LocalizedStatusStrings.swift`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-ble-bonding-state-machine/61-RESEARCH.md
@.planning/phases/61-ble-bonding-state-machine/61-PATTERNS.md
@GooseSwift/GooseBLEReconnect.swift
@GooseSwift/GooseBLETypes.swift
@GooseSwift/LocalizedStatusStrings.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GooseBLEBondingState enum to GooseBLETypes.swift</name>
  <files>GooseSwift/GooseBLETypes.swift</files>
  <read_first>
    - GooseSwift/GooseBLETypes.swift (study the existing enum style — GooseSyncToastPhase lines ~106-110, WhoopGeneration lines ~212-291 for associated-value + computed-property pattern, and the `// MARK: -` section separator convention)
    - .planning/phases/61-ble-bonding-state-machine/61-RESEARCH.md (Pattern 1: GooseBLEBondingState Enum; State Machine Transition Table; Current connectionState String Inventory)
  </read_first>
  <behavior>
    - GooseBLEBondingState has exactly 5 cases: notStarted, started, subscribed, completed(deviceID: UUID), cancelled(reason: String)
    - connectionStateString returns "disconnected" for .notStarted, "connecting" for .started, "discovering" for .subscribed, "ready" for .completed, and for .cancelled returns the reason if non-empty else "disconnected"
    - isReady returns true only for .completed
    - persistenceKey returns "completed" for .completed, "notStarted" for both .notStarted and .cancelled (restart-safe per Pitfall 5), "started" for .started, "subscribed" for .subscribed
  </behavior>
  <action>
    Append a new `// MARK: - BLE Bonding State` section at the END of GooseBLETypes.swift (after the final closing brace of the last type), separated by two blank lines per the file's top-level declaration spacing.

    Declare `enum GooseBLEBondingState: Equatable` with cases: `case notStarted`, `case started`, `case subscribed`, `case completed(deviceID: UUID)`, `case cancelled(reason: String)`. Equatable is synthesised automatically because UUID and String are Equatable — do NOT hand-write `==`.

    Add three computed properties exactly as specified in 61-RESEARCH.md Pattern 1:
    - `var isReady: Bool` — returns true only `if case .completed = self`.
    - `var connectionStateString: String` — maps cases to the existing connectionState string vocabulary: .notStarted -> "disconnected", .started -> "connecting", .subscribed -> "discovering", .completed -> "ready", .cancelled(let r) -> r.isEmpty ? "disconnected" : r. These strings MUST match the existing values consumed at the 33 `connectionState ==` comparison sites; do NOT invent new string values.
    - `var persistenceKey: String` — .notStarted -> "notStarted", .started -> "started", .subscribed -> "subscribed", .completed -> "completed", .cancelled -> "notStarted" (Pitfall 5: .cancelled never persists as a stuck state).

    Use 2-space indentation, K&R braces, no doc comments (`///`), matching the file's existing style. This satisfies BLE-BOND-01's "5 formal states".
  </action>
  <verify>
    <automated>grep -c "case notStarted\|case started\|case subscribed\|case completed\|case cancelled" GooseSwift/GooseBLETypes.swift | grep -q 5 && grep -q "enum GooseBLEBondingState: Equatable" GooseSwift/GooseBLETypes.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enum GooseBLEBondingState: Equatable" GooseSwift/GooseBLETypes.swift` succeeds
    - `grep -c "case completed(deviceID: UUID)" GooseSwift/GooseBLETypes.swift` returns 1
    - `grep -c "case cancelled(reason: String)" GooseSwift/GooseBLETypes.swift` returns 1
    - `grep -q 'var connectionStateString: String' GooseSwift/GooseBLETypes.swift` succeeds
    - `grep -q 'var persistenceKey: String' GooseSwift/GooseBLETypes.swift` succeeds
    - `grep -q 'return "ready"' GooseSwift/GooseBLETypes.swift` succeeds (the .completed mapping)
  </acceptance_criteria>
  <done>GooseBLEBondingState enum compiles with 5 cases and the three computed properties; connectionStateString returns the exact existing string vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GooseBLEBondingManager.swift</name>
  <files>GooseSwift/GooseBLEBondingManager.swift</files>
  <read_first>
    - GooseSwift/GooseBLEReconnect.swift (primary analog — focused BLE state-owning type, imports `import CoreBluetooth` / `import Foundation`)
    - GooseSwift/GooseBLEClient.swift lines 335-353 (DefaultsKey namespace pattern: keys use the "goose.swift.*" prefix)
    - GooseSwift/GooseBLEClient+Commands.swift lines 104-116 (main-thread dispatch idiom for @Observable mutations)
    - .planning/phases/61-ble-bonding-state-machine/61-RESEARCH.md (Pattern 2: GooseBLEBondingManager Class)
    - .planning/phases/61-ble-bonding-state-machine/61-PATTERNS.md (GooseBLEBondingManager.swift section — final class, weak self callback, UserDefaults persistence, no OSLog import)
  </read_first>
  <behavior>
    - transition(to:) is a no-op when newState equals the current bondingState (guard against redundant transitions)
    - transition(to:) persists the new state to UserDefaults synchronously, then invokes onBondingStateChange on the main thread
    - persistState writes bondingState.persistenceKey under "goose.swift.ble.bondingState"; when .completed, also writes the deviceID uuidString under "goose.swift.ble.bondingDeviceID"
    - loadPersistedState (called from init) restores .completed(deviceID:) only when both the "completed" key and a valid stored UUID are present; every other persisted value resolves to .notStarted (Pitfall 5)
  </behavior>
  <action>
    Create a NEW file GooseSwift/GooseBLEBondingManager.swift. Start with `import CoreBluetooth` then `import Foundation` (two import lines, no blank line between, matching GooseBLEReconnect.swift). Do NOT import OSLog — logging stays in GooseBLEClient extensions per 61-PATTERNS.md.

    Declare `final class GooseBLEBondingManager`. Add `private(set) var bondingState: GooseBLEBondingState = .notStarted` and `var onBondingStateChange: ((GooseBLEBondingState) -> Void)?`.

    Add two static key constants (owned by the manager, NOT in GooseBLEClient.DefaultsKey): `static let bondingStateKey = "goose.swift.ble.bondingState"` and `static let bondingDeviceIDKey = "goose.swift.ble.bondingDeviceID"`.

    Implement `init()` calling `loadPersistedState()`.

    Implement `func transition(to newState: GooseBLEBondingState)`: `guard newState != bondingState else { return }`, assign `bondingState = newState`, call `persistState()`, then dispatch `onBondingStateChange?(bondingState)` on the main thread using `DispatchQueue.main.async { [weak self] in guard let self else { return }; self.onBondingStateChange?(self.bondingState) }` (mirrors GooseBLEClient+Commands.swift main-thread idiom).

    Implement `private func persistState()` and `private func loadPersistedState()` exactly as in 61-RESEARCH.md Pattern 2: persist writes the persistenceKey and, for .completed, the device UUID string; load restores .completed only when key=="completed" AND a valid UUID is stored, defaulting all else to .notStarted.

    2-space indent, K&R braces, no `///` doc comments. This class is the BLE-BOND-01 manager type required by Success Criterion 1.
  </action>
  <verify>
    <automated>grep -q "final class GooseBLEBondingManager" GooseSwift/GooseBLEBondingManager.swift && grep -q 'goose.swift.ble.bondingState' GooseSwift/GooseBLEBondingManager.swift && grep -q "func transition(to newState: GooseBLEBondingState)" GooseSwift/GooseBLEBondingManager.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "final class GooseBLEBondingManager" GooseSwift/GooseBLEBondingManager.swift` succeeds
    - `grep -q 'static let bondingStateKey = "goose.swift.ble.bondingState"' GooseSwift/GooseBLEBondingManager.swift` succeeds
    - `grep -q 'static let bondingDeviceIDKey = "goose.swift.ble.bondingDeviceID"' GooseSwift/GooseBLEBondingManager.swift` succeeds
    - `grep -q "var onBondingStateChange:" GooseSwift/GooseBLEBondingManager.swift` succeeds
    - `grep -q "private func loadPersistedState" GooseSwift/GooseBLEBondingManager.swift` succeeds
    - `grep -c "import OSLog" GooseSwift/GooseBLEBondingManager.swift` returns 0
    - `grep -q "DispatchQueue.main.async" GooseSwift/GooseBLEBondingManager.swift` succeeds
  </acceptance_criteria>
  <done>GooseBLEBondingManager final class exists with transition/persist/load, UserDefaults keys under the goose.swift.ble.* namespace, and a main-thread change callback. No OSLog import.</done>
</task>

<task type="auto">
  <name>Task 3: Add localizedDescription for GooseBLEBondingState and build clean</name>
  <files>GooseSwift/LocalizedStatusStrings.swift</files>
  <read_first>
    - GooseSwift/LocalizedStatusStrings.swift (study the existing `extension String { var localizedConnectionState ... }` switch pattern, the `String(localized:)` usage — never NSLocalizedString — and the two-blank-line spacing between top-level extension blocks)
    - .planning/phases/61-ble-bonding-state-machine/61-PATTERNS.md (LocalizedStatusStrings.swift section — extension is on GooseBLEBondingState, NOT on String)
  </read_first>
  <action>
    Append a new top-level `extension GooseBLEBondingState` (NOT an extension on String) at the end of LocalizedStatusStrings.swift, separated from the preceding closing brace by two blank lines.

    Add `var localizedDescription: String` with a switch over self: .notStarted -> `String(localized: "Não iniciado")`, .started -> `String(localized: "A iniciar...")`, .subscribed -> `String(localized: "A descobrir...")`, .completed -> `String(localized: "Ligado")`, .cancelled(let r) -> `r.isEmpty ? String(localized: "Cancelado") : r`. Reuse the exact "A descobrir..." and "Ligado" strings already present in localizedConnectionState so display stays consistent.

    Use `String(localized:)` only (no NSLocalizedString), 2-space indent, K&R braces.

    After editing, run the build command to confirm the three Wave-1 files compile together with zero errors (this is the Wave 0 / foundation gate from 61-VALIDATION.md).
  </action>
  <verify>
    <automated>grep -q "extension GooseBLEBondingState" GooseSwift/LocalizedStatusStrings.swift && grep -q "var localizedDescription: String" GooseSwift/LocalizedStatusStrings.swift && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 17' build 2>&1 | grep -qE "BUILD SUCCEEDED" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "extension GooseBLEBondingState" GooseSwift/LocalizedStatusStrings.swift` succeeds
    - `grep -c "import OSLog\|NSLocalizedString" GooseSwift/LocalizedStatusStrings.swift` returns 0
    - `grep -q 'String(localized: "Ligado")' GooseSwift/LocalizedStatusStrings.swift` succeeds
    - `xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 17' build` reports BUILD SUCCEEDED (zero errors)
  </acceptance_criteria>
  <done>GooseBLEBondingState.localizedDescription exists using String(localized:); the project builds clean with all three Wave-1 foundation files present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app ↔ UserDefaults | Bonding state label + peripheral UUID persisted to app-private UserDefaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | UserDefaults bonding state keys | accept | iOS app sandbox isolates UserDefaults; bond state label + peripheral UUID are non-sensitive (no secrets). Per 61-RESEARCH Security Domain, no Keychain required. |
| T-61-02 | Information Disclosure | bonding state persistence | accept | No PII; stored data is a state label and a CoreBluetooth peripheral UUID (already stored as rememberedDeviceID). |

No npm/pip/cargo installs in this plan — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- All three Wave-1 files compile together: `xcodebuild ... build` returns BUILD SUCCEEDED.
- `GooseBLEBondingState` has 5 cases and the three computed properties.
- `GooseBLEBondingManager` persists under goose.swift.ble.* and exposes onBondingStateChange.
- No new external dependencies introduced (CoreBluetooth + Foundation only).
</verification>

<success_criteria>
- GooseBLEBondingState enum exists with notStarted/started/subscribed/completed/cancelled.
- GooseBLEBondingManager final class exists with transition(to:), UserDefaults persistence, and a main-thread callback.
- GooseBLEBondingState.localizedDescription exists.
- Project builds with zero errors after Wave 1.
</success_criteria>

<output>
Create `.planning/phases/61-ble-bonding-state-machine/61-01-SUMMARY.md` when done.
</output>
