---
phase: 60-band-first-sync-align-goose-ble-sync-architecture-with-whoop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseAppModel+OvernightRun.swift
  - GooseSwift/GooseAppModel+OvernightState.swift
  - GooseSwift/GooseAppModel+OvernightRecovery.swift
  - GooseSwift/GooseAppModel.swift
  - GooseSwift/HealthPacketCaptureTypes.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "The overnight guard subsystem source files no longer exist in the project"
    - "GooseAppModel.swift declares no overnightGuard* properties or constants"
    - "GooseAppModel.init does not wire overnight spool BLE callbacks"
    - "OvernightSQLiteMirrorQueue file is retained but dormant (no callers)"
  artifacts:
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "Coordinator with overnight guard state fully removed"
      contains: "let overnightSQLiteMirror"
    - path: "GooseSwift/OvernightSQLiteMirrorQueue.swift"
      provides: "Retained dormant queue (D-04)"
  key_links:
    - from: "GooseSwift/GooseAppModel.swift"
      to: "OvernightSQLiteMirrorQueue"
      via: "retained property declaration only, no callers"
      pattern: "let overnightSQLiteMirror"
---

<objective>
Remove the overnight guard subsystem core: delete the three overnight extension files and strip every overnight property, static constant, init callback, init call, and deinit cleanup from GooseAppModel.swift, plus the five overnight struct type definitions from HealthPacketCaptureTypes.swift.

Purpose: The overnight BLE polling model is replaced by WHOOP's band-first model (D-01). This plan removes the largest body of dead code (~1,900 lines across 3 files + 64 references) atomically so no dangling overnight symbol remains in the model layer.
Output: Three deleted files, a GooseAppModel.swift with zero overnight state, and a HealthPacketCaptureTypes.swift with the five overnight structs removed. The build will NOT compile after this plan alone — secondary call sites in other files (cleaned in plan 60-03) still reference removed symbols. This plan is the deletion core of an atomic removal that completes in wave 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-CONTEXT.md
@.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md
@.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase (across plans 60-01, 60-02, 60-03) creates the following NEW symbols. Drift verification must treat these as newly-introduced, not pre-existing:

- File: `GooseSwift/GooseAppModel+BandFirstSync.swift` (created in 60-02)
- Method: `GooseAppModel.triggerForegroundBLESync()` (60-02)
- Method: `GooseAppModel.handleBGAppRefresh(task:)` (60-02)
- Method: `GooseAppModel.scheduleNextBGAppRefresh()` (60-02)
- Static constant: `GooseAppModel.lastHistorySyncAtKey = "goose.swift.lastHistorySyncAt"` (60-02)
- Static constant: `GooseAppModel.bandFirstSyncCooldown` (60-02)
- Static property: `GooseSwiftApp.sharedModel` (60-02)
- UserDefaults key: `"goose.swift.lastHistorySyncAt"` (60-02)
- Info.plist key: `BGTaskSchedulerPermittedIdentifiers` with value `["com.goose.swift.bg-sync"]` (60-02)
- Info.plist UIBackgroundModes entry: `fetch` (60-02)
- BGTask identifier: `"com.goose.swift.bg-sync"` (60-02)
- ble.record source tag: `"band_first_sync"` with titles `foreground_sync.start`, `foreground_sync.skipped` (60-02)

This plan (60-01) only DELETES symbols; it produces no new symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three overnight extension files</name>
  <files>GooseSwift/GooseAppModel+OvernightRun.swift, GooseSwift/GooseAppModel+OvernightState.swift, GooseSwift/GooseAppModel+OvernightRecovery.swift</files>
  <read_first>
    - GooseSwift/GooseAppModel+OvernightRun.swift (confirm it is the overnight guard runtime; ~815 lines)
    - GooseSwift/GooseAppModel+OvernightState.swift (state helpers; ~404 lines)
    - GooseSwift/GooseAppModel+OvernightRecovery.swift (recovery/export/static helpers; ~685 lines)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md (File-by-File Removal Scope: confirms these three files are deleted entirely per D-01)
  </read_first>
  <action>
    Delete all three files from disk: GooseSwift/GooseAppModel+OvernightRun.swift, GooseSwift/GooseAppModel+OvernightState.swift, GooseSwift/GooseAppModel+OvernightRecovery.swift. Use `rm` for the filesystem removal. Then remove their PBXFileReference, PBXBuildFile, and PBXGroup children entries from GooseSwift.xcodeproj/project.pbxproj so the project no longer references them in the build. Per D-01 the overnight guard feature is deleted entirely, not feature-flagged or deprecated. Per D-04, do NOT delete GooseSwift/OvernightSQLiteMirrorQueue.swift or GooseSwift/OvernightRawNotificationSpool.swift — those are retained. Note that the static methods `overnightGuardDirectoryURL` and `overnightGuardRootDirectoryURL` live in GooseAppModel+ActivityTimeline.swift (NOT in these deleted files) and are cleaned up in plan 60-03 — do not touch ActivityTimeline.swift here.
  </action>
  <verify>
    <automated>test ! -f GooseSwift/GooseAppModel+OvernightRun.swift && test ! -f GooseSwift/GooseAppModel+OvernightState.swift && test ! -f GooseSwift/GooseAppModel+OvernightRecovery.swift && echo DELETED</automated>
  </verify>
  <acceptance_criteria>
    - `GooseSwift/GooseAppModel+OvernightRun.swift` does not exist
    - `GooseSwift/GooseAppModel+OvernightState.swift` does not exist
    - `GooseSwift/GooseAppModel+OvernightRecovery.swift` does not exist
    - `grep -c "OvernightRun.swift\|OvernightState.swift\|OvernightRecovery.swift" GooseSwift.xcodeproj/project.pbxproj` returns 0
    - `GooseSwift/OvernightSQLiteMirrorQueue.swift` still exists (D-04)
  </acceptance_criteria>
  <done>The three overnight extension files are removed from disk and from project.pbxproj; OvernightSQLiteMirrorQueue.swift and OvernightRawNotificationSpool.swift remain.</done>
</task>

<task type="auto">
  <name>Task 2: Strip all overnight state from GooseAppModel.swift</name>
  <files>GooseSwift/GooseAppModel.swift</files>
  <read_first>
    - GooseSwift/GooseAppModel.swift (read in full — current state of all overnight properties, init callbacks, deinit, static constants)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md ("Properties to REMOVE from GooseAppModel.swift", "init callbacks to REMOVE", "deinit cleanup to REMOVE", "init call to REMOVE")
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-PATTERNS.md ("GooseAppModel.swift (MODIFY)" section — exact property/callback list and the KEEP property)
  </read_first>
  <action>
    Remove from GooseSwift/GooseAppModel.swift:
    1. All 25 observable `@Published`/stored `var overnightGuard*` properties (e.g. overnightGuardActive, overnightGuardStatus, overnightGuardReadinessStatus, overnightGuardReadinessSummary, overnightGuardRawNotificationCount, overnightGuardRangePollCount, overnightGuardRangeTelemetryCount, overnightGuardSuccessfulRangePollCount, overnightGuardCommandWriteCount, overnightGuardEventLogCount, overnightGuardTargetSummary, overnightGuardHistoricalOrderSummary, overnightGuardLastPacketSummary, overnightGuardSpoolPath, overnightGuardSpoolSizeSummary, overnightGuardSQLiteMirrorSummary, overnightGuardPowerSummary, overnightGuardWatchdogSummary, overnightGuardWarning, overnightGuardExportStatus, overnightGuardExportInProgress, overnightGuardExportURL, overnightGuardExportManifestURL, overnightGuardExportManifestError, overnightGuardCanExportLastSession).
    2. The `overnightRawSpool` stored property (D-03) and the 20 private `overnightGuard*` stored vars (overnightGuardSession, overnightGuardHeartbeatWorkItem, overnightGuardRangePollWorkItem, overnightGuardFinalSyncDrainWorkItem, overnightGuardFinalSyncPending, overnightGuardCriticalBackgroundTaskID, overnightGuardCriticalBackgroundTaskReason, overnightGuardStartedHealthCapture, overnightGuardTargetCounts, overnightGuardHistoricalOrder, overnightGuardPowerWarning, overnightGuardWatchdogWarning, overnightGuardRawSpoolWarning, overnightGuardBLELogWarning, overnightGuardSQLiteMirrorWarning, overnightGuardWroteInitialRawNotificationStatus, overnightGuardWroteInitialSQLiteMirrorStatus, overnightGuardLastRawStaleWarningAt, overnightGuardLastRangeSuccessWarningAt, overnightGuardLastTargetMissingWarningAt).
    3. All 10 `overnightGuard*` static let constants (overnightGuardDuration, overnightGuardHeartbeatInterval, overnightGuardRangePollInterval, overnightGuardRangeBlockedRetryInterval, overnightGuardRangeFailureRetryInterval, overnightGuardFinalSyncDrainInterval, overnightGuardRawStaleWarningInterval, overnightGuardRangeSuccessWarningDelay, overnightGuardTargetMissingWarningDelay, overnightGuardWarningRepeatInterval).
    4. The three init callback assignments: `ble.onRawNotificationWithContext = {...}` (calls persistOvernightRawNotificationBeforeInterpretation), `ble.onCommandWrite = {...}` (calls persistOvernightCommandWrite), and the `ble.onMessage = {...}` assignment that calls persistOvernightEventLog. Per RESEARCH Open Question 3, simply do not assign these callbacks — the GooseBLEClient properties remain nil optionals (safe no-op). If `ble.onMessage` has non-overnight responsibilities, preserve those and only remove the persistOvernightEventLog call; verify by reading the closure body.
    5. The deinit cleanup lines: overnightGuardHeartbeatWorkItem?.cancel(), overnightGuardRangePollWorkItem?.cancel(), overnightGuardFinalSyncDrainWorkItem?.cancel(), the `if overnightGuardCriticalBackgroundTaskID != .invalid {...}` block, and the `if overnightRawSpool.isActive {...}` block.
    6. The init call `recoverUncleanOvernightGuardSessionIfNeeded()`.
    KEEP the property `let overnightSQLiteMirror = OvernightSQLiteMirrorQueue(databasePath: HealthDataStore.defaultDatabasePath())` (D-04) — it stays as a dormant declaration with no callers and needs no deinit cleanup.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/GooseAppModel.swift | grep -c "overnightGuard"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/GooseAppModel.swift | grep -c "overnightGuard"` returns 0
    - `grep -v '^[[:space:]]*//' GooseSwift/GooseAppModel.swift | grep -c "overnightRawSpool"` returns 0
    - `grep -c "recoverUncleanOvernightGuardSessionIfNeeded" GooseSwift/GooseAppModel.swift` returns 0
    - `grep -c "persistOvernightRawNotificationBeforeInterpretation\|persistOvernightCommandWrite\|persistOvernightEventLog" GooseSwift/GooseAppModel.swift` returns 0
    - `grep -c "let overnightSQLiteMirror" GooseSwift/GooseAppModel.swift` returns 1 (KEEP per D-04)
  </acceptance_criteria>
  <done>GooseAppModel.swift contains zero overnightGuard/overnightRawSpool references (excluding the retained overnightSQLiteMirror property); init no longer wires overnight callbacks or calls recovery; deinit no longer cancels overnight work items.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the five overnight struct types from HealthPacketCaptureTypes.swift</name>
  <files>GooseSwift/HealthPacketCaptureTypes.swift</files>
  <read_first>
    - GooseSwift/HealthPacketCaptureTypes.swift (read the overnight struct definition region, ~lines 99–294)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md ("HealthPacketCaptureTypes.swift" subsection and Pitfall 4 — confirms 5 structs to delete and that OvernightRawSpoolSnapshot/OvernightPowerState live elsewhere)
  </read_first>
  <action>
    Remove these five struct definitions from GooseSwift/HealthPacketCaptureTypes.swift: `OvernightGuardSession`, `OvernightGuardRecoveredSession`, `OvernightGuardTargetCounts`, `OvernightGuardHistoricalOrderEvidence`, `OvernightGuardHistoricalPacketSample`. Do NOT remove `OvernightRawSpoolSnapshot` or `OvernightPowerState` — those are defined in GooseSwift/OvernightRawNotificationSpool.swift (retained per D-04), not in this file. Verify by grep that no other retained file references the five deleted struct names before deleting (the only consumers were the deleted overnight files and the GooseAppModel.swift properties removed in Task 2).
  </action>
  <verify>
    <automated>grep -c "struct OvernightGuardSession\|struct OvernightGuardRecoveredSession\|struct OvernightGuardTargetCounts\|struct OvernightGuardHistoricalOrderEvidence\|struct OvernightGuardHistoricalPacketSample" GooseSwift/HealthPacketCaptureTypes.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct OvernightGuard" GooseSwift/HealthPacketCaptureTypes.swift` returns 0
    - `grep -rl "OvernightGuardSession\|OvernightGuardTargetCounts\|OvernightGuardRecoveredSession\|OvernightGuardHistoricalOrderEvidence\|OvernightGuardHistoricalPacketSample" GooseSwift/ | grep -v -E "(60-)" | wc -l` returns 0 (no retained Swift file references the deleted structs)
  </acceptance_criteria>
  <done>The five overnight struct types are removed from HealthPacketCaptureTypes.swift; no retained source file references them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan only deletes code; no new untrusted-input boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | Leftover dangling overnight symbol after partial deletion | mitigate | Grep gates in acceptance_criteria assert zero overnightGuard references in GooseAppModel.swift and HealthPacketCaptureTypes.swift; the secondary-reference sweep in plan 60-03 plus a full build is the compile-time guarantee |
| T-60-02 | Denial of Service | Retained OvernightSQLiteMirrorQueue with no callers (D-04) | accept | Dormant queue performs no work without enqueue calls; retained intentionally for future use; no runtime cost |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no dependency surface changed |
</threat_model>

<verification>
- Three overnight files deleted from disk and from project.pbxproj.
- GooseAppModel.swift: zero overnightGuard/overnightRawSpool references except retained overnightSQLiteMirror.
- HealthPacketCaptureTypes.swift: zero overnight struct definitions.
- Note: full project build is NOT expected to pass after this plan in isolation — secondary call sites cleaned in plan 60-03 still reference removed symbols. The atomic removal completes in wave 2.
</verification>

<success_criteria>
- `test ! -f GooseSwift/GooseAppModel+OvernightRun.swift` succeeds (and the other two deleted files).
- `grep -v '^[[:space:]]*//' GooseSwift/GooseAppModel.swift | grep -c overnightGuard` returns 0.
- `grep -c "struct OvernightGuard" GooseSwift/HealthPacketCaptureTypes.swift` returns 0.
- `grep -c "let overnightSQLiteMirror" GooseSwift/GooseAppModel.swift` returns 1.
</success_criteria>

<output>
Create `.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-01-SUMMARY.md` when done
</output>
